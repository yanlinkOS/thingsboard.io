---
import { BLOG_CATEGORIES } from '~/data/blog/categories';

export function getStaticPaths() {
	return BLOG_CATEGORIES.map((cat) => ({ params: { cat } }));
}

const { cat } = Astro.params;
return Astro.redirect(`/blog/?category=${cat}`, 301);
---
