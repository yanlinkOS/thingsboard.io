import { Tabs, TabItem, Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '@components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

The Socket Connector runs a TCP or UDP server on the gateway, accepting connections from devices and
forwarding their binary payload data to ThingsBoard.

To enable this connector, add it to the `connectors` list in `tb_gateway.json` — see the
<DocLink product={props.product} path="config/general">General Configuration</DocLink> reference.

<Aside type="note">
  If you are new to the IoT Gateway, use **Basic** configuration mode. If you are familiar with
  configuring the Gateway, use **Advanced** mode.
</Aside>

## Section "General"

General settings for the connector — name, logging, change filtering, and the report strategy.

- **Name** — connector name, used in logs and when saving to persistent storage
- **Enable remote logging** — enables remote logging for the connector
- **Logging level** — `INFO`, `DEBUG`, `WARNING`, `ERROR`, `CRITICAL`, or `NONE`
- **Send data only on change** — sends data only when its value has changed; otherwise data is sent on every check
- **Report strategy** — controls when data is forwarded to ThingsBoard:
  - **On report period** — after every report period
  - **On value change** — when a value changes
  - **On value change or report period** — when a value changes or the report period elapses
  - **On received** — immediately after data is received from the device (default)

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-general-basic-section-1-ce.png',
    alt: 'Socket connector General section settings',
    caption: 'General connector settings.',
  },
]} />

<Aside type="note">
  See <DocLink product={props.product} path="features/report-strategy">Report Strategy</DocLink> for
  a detailed description of each strategy.
</Aside>

## Section "Socket"

Configures the local socket the connector binds to.

<Tabs>
<TabItem label="Basic">

Configure the following fields:

- **Connection type** — `TCP` or `UDP`
- **Address** — IP address to bind to
- **Port** — port to listen on
- **Buffer size** — size of the received data block buffer

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-basic-section-1-ce.png',
    alt: 'Socket section in Basic configuration mode',
    caption: 'Socket settings in Basic mode.',
  },
]} />

</TabItem>
<TabItem label="Advanced">

| Parameter | Default | Description |
|-----------|---------|-------------|
| `name` | `TCP Connector Example` | Connector name |
| `type` | `TCP` | Socket type: `TCP` or `UDP` |
| `address` | `127.0.0.1` | IP address the connector binds to |
| `port` | `50000` | Port the connector listens on |
| `bufferSize` | `1024` | Size of the received data block buffer in bytes |

```json
{
  "type": "TCP",
  "address": "127.0.0.1",
  "port": 50000,
  "bufferSize": 1024
}
```

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-advanced-configuration-section-1-ce.png',
    alt: 'Socket section in Advanced configuration mode',
    caption: 'Socket settings in Advanced mode.',
  },
]} />

</TabItem>
</Tabs>

## Section "Devices"

An array of device configurations. Only devices whose IP address matches `addressFilter` are accepted;
all other connections are rejected.

### Subsection "Device"

<Tabs>
<TabItem label="Basic">

Click the **plus** icon to add a new device, then fill in the following fields:

- **Address filter** — IP address pattern allowed to connect (see examples below)
- **Device name** — device name in ThingsBoard
- **Device profile** — device profile in ThingsBoard (optional)
- **Encoding** — encoding used when writing string data to storage

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-device-subsection-basic-1-ce.png',
    alt: 'Device list with the add button',
    caption: 'Click the plus icon to add a new device.',
  },
  {
    src: '/src/assets/images/gateway/socket-connector/socket-device-subsection-basic-2-ce.png',
    alt: 'New device form',
    caption: 'Fill in the device details.',
  },
]} />

</TabItem>
<TabItem label="Advanced">

| Parameter | Default | Description |
|-----------|---------|-------------|
| `addressFilter` | `*.*` | IP address filter for allowed client connections |
| `deviceName` | `Device Example` | Device name in ThingsBoard |
| `deviceType` | `default` | Device type (profile) in ThingsBoard |
| `encoding` | `utf-8` | Encoding used when writing string data |

```json
{
  "address": "127.0.0.1:50001",
  "deviceName": "Device Example",
  "deviceType": "default",
  "encoding": "utf-8"
}
```

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-subsection-device-advanced-1-ce.png',
    alt: 'Device subsection in Advanced configuration mode',
    caption: 'Device settings in Advanced mode.',
  },
]} />

</TabItem>
</Tabs>

**Address filter examples** — for a device at `192.168.0.120:5001`:

| Address filter | Matches |
|----------------|---------|
| `192.168.0.120:5001` | Only that exact IP address and port |
| `*:5001` | Any IP address on port 5001 |
| `192.168.0.120:*` | That IP address on any port |
| `*:*` | Any IP address on any port |

### Subsections "Time series" and "Attributes"

Both use a binary byte-slicing converter. Values are extracted from the raw incoming payload by
specifying start and end byte positions.

<Tabs>
<TabItem label="Basic">

<Steps>
1. Click the **pencil** icon of the **Attributes** section to add a new attribute key.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-1-ce.png',
       alt: 'Open the Attributes section',
     },
   ]} />

2. Click **Add attribute** in the opened window.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-2-ce.png',
       alt: 'Click Add attribute',
     },
   ]} />

3. Enter the **key name** and **byte positions**, then click **Apply**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-4-ce.png',
       alt: 'Enter key name and byte positions for the attribute',
     },
   ]} />

4. Click the **pencil** icon of the **Time series** section to add a new time series key.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-5-ce.png',
       alt: 'Open the Time series section',
     },
   ]} />

5. Click **Add time series** in the opened window.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-6-ce.png',
       alt: 'Click Add time series',
     },
   ]} />

6. Enter the **key name** and **byte positions**, then click **Apply**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-7-ce.png',
       alt: 'Enter key name and byte positions for the time series key',
     },
   ]} />
</Steps>

You can enable a per-key report strategy. Available strategies:

- **On report period** — after every report period
- **On value change** — when a value changes
- **On value change or report period** — when a value changes or the report period elapses
- **On received** — immediately after data is received (default)

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-adding-device-9-ce.png',
    alt: 'Per-key report strategy settings',
    caption: 'Report strategy settings for a time series or attribute key.',
  },
]} />

<Aside type="note">
  See <DocLink product={props.product} path="features/report-strategy">Report Strategy</DocLink> for
  details on each strategy.
</Aside>

</TabItem>
<TabItem label="Advanced">

Both `telemetry` and `attributes` use the same field structure:

| Parameter | Default | Description |
|-----------|---------|-------------|
| `key` | | Key name in ThingsBoard |
| `byteFrom` | `0` | Start byte index (inclusive) for slicing the received buffer |
| `byteTo` | `-1` | End byte index (exclusive); `-1` means end of buffer |

```json
{
  "address": "127.0.0.1:50001",
  "deviceName": "Device Example",
  "deviceType": "default",
  "encoding": "utf-8",
  "telemetry": [
    { "key": "temp", "byteFrom": 0, "byteTo": -1 },
    { "key": "hum",  "byteFrom": 0, "byteTo": 2  }
  ],
  "attributes": [
    { "key": "name", "byteFrom": 0, "byteTo": -1 },
    { "key": "num",  "byteFrom": 2, "byteTo": 4  }
  ]
}
```

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-subsection-time-series-advanced-1-ce.png',
    alt: 'Time series section in Advanced configuration mode',
    caption: 'Time series settings in Advanced mode.',
  },
  {
    src: '/src/assets/images/gateway/socket-connector/socket-subsection-attributes-advanced-1-ce.png',
    alt: 'Attributes section in Advanced configuration mode',
    caption: 'Attributes settings in Advanced mode.',
  },
]} />

</TabItem>
</Tabs>

### Subsection "Attribute request"

Optional. Lets the device request client-side or shared attributes from ThingsBoard by sending a
specially formatted binary message.

<Tabs>
<TabItem label="Basic">

<Steps>
1. Click the **pencil** icon of the **Attribute requests** section.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-10-ce.png',
       alt: 'Open the Attribute requests section',
     },
   ]} />

2. Click **Add attribute request** in the opened window.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-11-ce.png',
       alt: 'Click Add attribute request',
     },
   ]} />

3. Select the **request type**, enter the **request expression** and **attribute name expression**, then click **Apply**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-12-ce.png',
       alt: 'Configure attribute request type and expressions',
     },
   ]} />
</Steps>

</TabItem>
<TabItem label="Advanced">

| Parameter | Default | Description |
|-----------|---------|-------------|
| `type` | `shared` | Attribute type to request: `shared` or `client` |
| `requestExpression` | `${[0:3]==atr}` | Byte-slice expression that detects whether the incoming message is an attribute request |
| `attributeNameExpression` | `[3:]` | Byte-slice expression that extracts the attribute name from the message |

```json
"attributeRequests": [
  {
    "type": "shared",
    "requestExpression": "${[0:3]==atr}",
    "attributeNameExpression": "[3:]"
  }
]
```

To request multiple attributes in a single message, add multiple slice ranges in `attributeNameExpression`:

```json
"attributeRequests": [
  {
    "type": "shared",
    "requestExpression": "${[0:3]==atr}",
    "attributeNameExpression": "[4:19][20:]"
  }
]
```

The device sends: `atr sharedAttribute sharedAttribute1`

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-subsection-attribute-request-advanced-1-ce.png',
    alt: 'Attribute request in Advanced configuration mode',
    caption: 'Attribute request settings in Advanced mode.',
  },
]} />

</TabItem>
</Tabs>

### Subsection "Attribute update"

Optional. Configures how ThingsBoard shared attribute updates are forwarded to the connected device.

<Tabs>
<TabItem label="Basic">

<Steps>
1. Click the **pencil** icon of the **Attribute updates** section.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-13-ce.png',
       alt: 'Open the Attribute updates section',
     },
   ]} />

2. Click **Add attribute update** in the opened window.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-14-ce.png',
       alt: 'Click Add attribute update',
     },
   ]} />

3. Select **encoding** and fill in the **Attribute on platform** field, then click **Apply**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-15-ce.png',
       alt: 'Configure encoding and attribute name',
     },
   ]} />
</Steps>

</TabItem>
<TabItem label="Advanced">

| Parameter | Default | Description |
|-----------|---------|-------------|
| `encoding` | `utf-16` | Encoding used when writing the attribute value to the device |
| `attributeOnThingsBoard` | `sharedName` | Shared attribute name to subscribe to |

```json
"attributeUpdates": [
  {
    "encoding": "utf-16",
    "attributeOnThingsBoard": "sharedName"
  }
]
```

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-subsection-attribute-updates-advanced-1-ce.png',
    alt: 'Attribute update in Advanced configuration mode',
    caption: 'Attribute update settings in Advanced mode.',
  },
]} />

</TabItem>
</Tabs>

### Subsection "RPC methods"

Optional. Maps ThingsBoard RPC calls to write operations on the connected device.

<Tabs>
<TabItem label="Basic">

<Steps>
1. Click the **pencil** icon of the **RPC methods** section.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-16-ce.png',
       alt: 'Open the RPC methods section',
     },
   ]} />

2. Click **Add method** in the opened window.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-17-ce.png',
       alt: 'Click Add method',
     },
   ]} />

3. Fill in **Method name**, select **encoding**, and set **With Response**, then click **Apply**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/gateway/socket-connector/socket-adding-device-18-ce.png',
       alt: 'Fill in the RPC method name, encoding, and response flag',
     },
   ]} />
</Steps>

</TabItem>
<TabItem label="Advanced">

| Parameter | Default | Description |
|-----------|---------|-------------|
| `methodRPC` | `rpcMethod1` | RPC method name |
| `withResponse` | `true` | Send the device's response back to ThingsBoard |
| `methodProcessing` | `write` | Operation type |
| `encoding` | `utf-8` | Encoding used when writing data to the device |

```json
"serverSideRpc": [
  {
    "methodRPC": "rpcMethod1",
    "withResponse": true,
    "methodProcessing": "write",
    "encoding": "utf-8"
  }
]
```

<ImageGallery images={[
  {
    src: '/src/assets/images/gateway/socket-connector/socket-subsection-rpc-methods-advanced-1-ce.png',
    alt: 'RPC methods in Advanced configuration mode',
    caption: 'RPC method settings in Advanced mode.',
  },
]} />

</TabItem>
</Tabs>

Every telemetry and attribute key also has a built-in `set` RPC method available automatically — no manual
entry required. The required parameters are `withResponse`, `methodProcessing`, and `encoding`. See the
<DocLink product={props.product} path="features/reserved-rpc">Reserved RPC methods</DocLink> guide for details.
