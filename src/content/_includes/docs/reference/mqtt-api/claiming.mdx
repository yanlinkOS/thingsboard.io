import DocLink from '@components/DocLink.astro';
import { Aside } from '@astrojs/starlight/components';

Device claiming lets an end user securely associate a device with their account after it is already deployed and connected. See <DocLink product={props.product} path='TODO'>Claiming</DocLink> for the full workflow — this page covers only the MQTT transport step where the device publishes the claiming request.

## Publish a Claim Request

**Topic:** `v1/devices/me/claim`

```json
{"secretKey": "mySecret", "durationMs": 60000}
```

Both fields are optional:

| Field | Description | Default |
|-------|-------------|---------|
| `secretKey` | Secret the user must supply to complete the claim. | Empty string |
| `durationMs` | Time window in milliseconds during which the claim can be completed. | `SECURITY_CLAIM_DURATION` server setting |

Publishing to this topic initiates the claiming workflow. ThingsBoard marks the device as claimable and waits for a user to complete the claim (via the dashboard **Claim Device** widget or the REST API) within the specified duration.

<Aside type="note">
  The device must already be connected before publishing a claim request. See <DocLink product={props.product} path='reference/mqtt-api/getting-connected'>Getting Connected</DocLink> for connection setup and credential types.
</Aside>

## Example

```bash
mosquitto_pub -d -q 1 -h "$THINGSBOARD_HOST" -p 1883 -t "v1/devices/me/claim" -u "$ACCESS_TOKEN" -m '{"secretKey": "mySecret", "durationMs": 60000}'
```
