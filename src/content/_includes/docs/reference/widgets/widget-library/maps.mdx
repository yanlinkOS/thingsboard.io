import WidgetGrid from '~/components/WidgetGrid.astro';
import BackToWidgetLibrary from '~/components/BackToWidgetLibrary.astro';

<BackToWidgetLibrary product={props.product} />

Map widgets are a group of ThingsBoard widgets that enable visualization of entity locations on a map.
They combine geographic or image-based maps with telemetry, attributes, and events to show where devices, assets, or areas are located — and what is happening with them in real time.

<WidgetGrid bundle="maps" product={props.product} />
