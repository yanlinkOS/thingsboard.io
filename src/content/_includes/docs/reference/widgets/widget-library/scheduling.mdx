import WidgetGrid from '~/components/WidgetGrid.astro';
import BackToWidgetLibrary from '~/components/BackToWidgetLibrary.astro';

<BackToWidgetLibrary product={props.product} />

Scheduling widgets display and manage scheduled events and report generation tasks directly from a dashboard, with list and calendar views and per-row edit and delete actions.

<WidgetGrid bundle="scheduling" product={props.product} />
