import WidgetGrid from '~/components/WidgetGrid.astro';
import BackToWidgetLibrary from '~/components/BackToWidgetLibrary.astro';

<BackToWidgetLibrary product={props.product} />

Tables widgets are a set of widgets that display data in a tabular format.
They provide a compact, user-friendly, and structured way to present information about entities, telemetry, alarms, and RPC requests within the ThingsBoard system.

<WidgetGrid bundle="tables" product={props.product} />
