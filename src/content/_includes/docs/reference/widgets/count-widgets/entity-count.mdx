import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import { Image } from 'astro:assets';
import symbolImg from '~/assets/images/reference/widgets/count-widgets/entity-count.svg';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Entity count widget"
  width={200}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '200px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Entity count** widget displays the total number of entities — devices, assets, customers, or any other entity type — that match specific filter criteria. Use it for KPI dashboards, system overviews, or monitoring entity groups: total connected devices, assets in a region, or active gateways.

</div>
</div>

## Key capabilities

- Counts any entity type (Device, Asset, Customer, etc.) in real time.
- Works with an entity alias and supports additional key filters to narrow the dataset.
- Automatically updates as entities are created, removed, or change attribute values.
- Two layout options: vertical (Column) or horizontal (Row).
- Configurable icon, label, color, and chevron indicator.
- Supports an **On card click** action to navigate to a details view on click.

## Adding the widget

<Steps>
1. Open the dashboard in edit mode. Click **Add widget** in the top toolbar, or click the **Add new widget** icon in the center of an empty dashboard.
2. In the widget bundle selection dialog, find and click **Count widgets**.
3. Select the **Entity count** widget.
4. Configure the datasource, filters, and appearance, then click **Add**.
</Steps>

## Configuration

All settings in this section use **Basic** mode — a quick-start configuration covering the essentials. For finer widget controls like title font, card style, and tooltip, switch to the **Advanced** tab.

### Datasource

Select the entity alias that defines which entities are counted. Use an <DocLink product={props.product} path="user-guide/aliases" bold={false} target="_blank">entity alias</DocLink> to group entities by type, profile, relation, or any other criteria.

Add <DocLink product={props.product} path="user-guide/widgets/#data-filters" bold={false} target="_blank">filters</DocLink> to narrow the counted set — for example, only devices where the `active` attribute equals `true`.

### Appearance

Controls the widget layout, label, icon, and value display.

| Setting | Description |
|---------|-------------|
| **Layout** | **Column** — stacks icon, value, and label vertically. **Row** — places icon and value side by side. |
| **Auto scale** | Automatically adjusts font sizes to fit the widget area when resized. |
| **Label** | Descriptive text shown on the card, like `Active`, `Total`, or `Online`. |
| **Icon** | Optional icon representing the entity category. Configure size and color. |
| **Icon background** | Background color behind the icon. Use to communicate category at a glance, like green for active or red for offline. |
| **Value** | Font and color for the displayed count number. |
| **Chevron** | Adds a chevron (**›**) next to the value to indicate the card is clickable. Pair with an **On card click** action. |

### Card appearance

| Setting | Description |
|---------|-------------|
| **Background** | Card background color, gradient, or transparency. |
| **Show card buttons** | Toggle the **Fullscreen** button in the widget header. |
| **Card border radius** | Roundness of card corners. |

### Actions

The Entity count widget supports the following action sources:

- **On card click** — triggered when the user clicks anywhere on the widget card. Enable **Chevron** in appearance settings to provide a visual click indicator.
- **Widget header button** — adds a custom button to the widget header bar.

## Example

### Total active devices counter

**Goal:** Display the number of currently active devices in the system. Clicking the card navigates to a dashboard state that shows a full list of active devices.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/count-widgets/entity-count/total-active-devices-counter.png', alt: 'Entity count widget showing the total number of active devices', caption: 'Entity count widget displaying the total number of active devices — updates automatically as device states change.', products: [Products.CE] },
]} />

<Aside type="tip">
Download a <a href="/downloads/active_devices_dashboard.json" download="active_devices_dashboard.json">pre-configured dashboard</a> with an active devices counter and import it directly. After importing, verify the entity alias and filter match your environment.
</Aside>

**Prerequisites:** A dashboard with two <DocLink product={props.product} path="user-guide/dashboards/#states" bold={false} target="_blank">states</DocLink> — a default state for the counter card and a second state containing a widget that lists active devices, like an <DocLink product={props.product} path="reference/widgets/tables/entities-table" bold={false} target="_blank">Entities table</DocLink>.

**Step 1. Add the widget.** Open the dashboard in edit mode, click **Add widget**, select **Count widgets**, then choose **Entity count**.

**Step 2. Configure the datasource.** Set **Datasource type** to **Entity alias**. Create or select an alias:
- **Alias name:** `All devices`
- **Filter type:** Entity type → **Device**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/count-widgets/entity-count/example-entity-count-1.png', alt: 'Entity alias configured with Entity type filter for Device', caption: 'Datasource configured — entity alias resolves all devices in the system.', products: [Products.CE] },
]} />

**Step 3. Add a filter.** In the **Filters** section, click **Add key filter** and configure:

| Field | Value |
|-------|-------|
| **Key type** | Attribute |
| **Key name** | `active` |
| **Value type** | Boolean |
| **Operation** | equal |
| **Value** | `True` |

This restricts the count to devices where `active = true`.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/count-widgets/entity-count/example-entity-count-2.png', alt: 'Key filter configured to count only devices with active = true', caption: 'Key filter set to active = true — only active devices are counted.', products: [Products.CE] },
]} />

**Step 4. Configure appearance.** Set **Label** to `Active devices` and enable **Chevron**.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/count-widgets/entity-count/example-entity-count-3.png', alt: 'Appearance configured with label Active devices and chevron enabled', caption: 'Appearance configured — label set to Active devices with chevron enabled to signal the card is clickable.', products: [Products.CE] },
]} />

**Step 5. Configure action.** In the **Actions** section, add an **On card click** action configured to navigate to the dashboard state that contains the active devices list.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/count-widgets/entity-count/example-entity-count-4.png', alt: 'On card click action configured to navigate to the active devices state', caption: 'On card click action — navigates to the second dashboard state showing the full active device list.', products: [Products.CE] },
]} />

**Step 6. Save.** Click **Add**, resize and reposition as needed, then click **Save** on the dashboard toolbar.

**Result:** The card displays the live count of active devices and updates automatically as device states change. Clicking the card navigates to the active devices list.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/count-widgets/entity-count/example-entity-count-5.png', alt: 'Finished Entity count card on the dashboard showing active device count', caption: 'The finished Entity count card — live active device count with a chevron indicating it is clickable.', products: [Products.CE] },
  { src: '/src/assets/images/reference/widgets/count-widgets/entity-count/example-entity-count-6.png', alt: 'Dashboard state opened after clicking the Entity count card, showing active devices list', caption: 'Clicking the card opens the second dashboard state with the full active devices list.', products: [Products.CE] },
]} />

## Troubleshooting

<p style="font-size: 1.5em; font-weight: bold;">Widget shows zero or no count</p>

| Cause | Solution |
|-------|----------|
| Entity alias resolves to no entities | Open **Entity aliases** in the dashboard and verify the alias returns results. |
| Filter excludes all entities | Check all key filters — if **active = true** is set but no device has this attribute, the count is zero. Remove or adjust filters to confirm entities exist. |
| Attribute name mismatch | Verify the filter key name matches the exact attribute name on the device. Attribute names are case-sensitive. |

<p style="font-size: 1.5em; font-weight: bold;">Count does not update automatically</p>

| Cause | Solution |
|-------|----------|
| Dashboard not in Realtime mode | Ensure the dashboard time window is set to **Realtime** so the widget receives live updates. |
| Devices not sending attribute updates | Confirm devices are actively reporting the tracked attribute (like `active`) to ThingsBoard. |

<p style="font-size: 1.5em; font-weight: bold;">Chevron is not visible</p>

| Cause | Solution |
|-------|----------|
| Chevron not enabled | Open widget configuration &#8702; **Appearance** and enable **Chevron**. |

<p style="font-size: 1.5em; font-weight: bold;">Click action does not navigate</p>

| Cause | Solution |
|-------|----------|
| No action configured | Open widget configuration &#8702; **Actions** and add an **On card click** action. |
| Target state does not exist | Verify the destination dashboard state exists and its name matches exactly what is configured in the action. |
