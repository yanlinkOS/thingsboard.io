import { Image } from 'astro:assets';
import DocLink from '@components/DocLink.astro';
import PipeBase from './_pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-long-vertical-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Long vertical pipe SCADA symbol"
  width={66}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '66px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Long vertical pipe** widget represents an extended vertical pipe segment in a SCADA fluid system diagram. It visualizes fluid presence, flow state, direction, and leak conditions based on real-time device data.

</div>
</div>

<PipeBase product={props.product}/>
