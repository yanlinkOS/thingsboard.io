import { Image } from 'astro:assets';
import MeterBase from './_meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-small-right-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Small right meter SCADA symbol"
  width={100}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '100px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Small right meter** widget renders a compact vertical scale gauge with the scale markings and value box aligned to the right side of the symbol.
Use it when the gauge needs to be placed to the right of adjacent elements in the diagram.

</div>
</div>

<MeterBase product={props.product}/>
