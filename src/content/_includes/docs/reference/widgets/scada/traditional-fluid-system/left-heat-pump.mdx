import { Image } from 'astro:assets';
import HeatPump from './_heat-pump.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-left-heat-pump.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Left heat pump SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Left heat pump** widget renders a heat pump with pipe connectors and controls positioned on the left side of the symbol.
It displays the current temperature, animates the fan when the pump is running, and exposes a power button and temperature setpoint controls for direct device interaction from the dashboard.

</div>
</div>

<HeatPump product={props.product}/>
