import { Image } from 'astro:assets';
import BrokenPipeBase from './_broken-pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-horizontal-broken-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Horizontal broken pipe SCADA symbol"
  width={160}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '160px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Horizontal broken pipe** widget represents a damaged horizontal pipe segment in a SCADA fluid system diagram.
Use it to mark a fault or rupture location on a horizontal run.

</div>
</div>

<BrokenPipeBase product={props.product}/>
