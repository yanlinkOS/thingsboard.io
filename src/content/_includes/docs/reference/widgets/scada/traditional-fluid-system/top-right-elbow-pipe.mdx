import { Image } from 'astro:assets';
import DocLink from '@components/DocLink.astro';
import PipeBase from './_pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-top-right-elbow-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Top right elbow pipe SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', height: '159px', objectFit: 'contain' }}
/>

This widget belongs to the **Traditional SCADA fluid system** bundle — a collection of pipe segments, fittings, valves, pumps, and gauges designed for building fluid system diagrams on ThingsBoard dashboards.

The **Top right elbow pipe** widget represents a 90-degree elbow fitting that connects a vertical pipe from the top to a horizontal pipe going rightward. It visualizes fluid presence, flow state, direction, and leak conditions based on real-time device data.

</div>

<PipeBase product={props.product}/>
