import { Image } from 'astro:assets';
import FlowMeterBase from './_flow-meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-top-flow-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Top flow meter SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', height: '199px', objectFit: 'contain' }}
/>

This widget belongs to the **Traditional SCADA fluid system** bundle — a collection of pipe segments, fittings, valves, pumps, and gauges designed for building fluid system diagrams on ThingsBoard dashboards.

The **Top flow meter** widget displays a numeric flow rate value on a square display face positioned above a horizontal pipe segment.
Use it to show live flow measurements at a point where the meter is mounted on top of the pipe run.

</div>

<FlowMeterBase product={props.product}/>
