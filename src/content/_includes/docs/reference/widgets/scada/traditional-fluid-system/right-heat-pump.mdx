import { Image } from 'astro:assets';
import HeatPump from './_heat-pump.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-right-heat-pump.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Right heat pump SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', height: '149px', objectFit: 'contain' }}
/>

This widget belongs to the **Traditional SCADA fluid system** bundle — a collection of pipe segments, fittings, valves, pumps, and gauges designed for building fluid system diagrams on ThingsBoard dashboards.

The **Right heat pump** widget renders a heat pump with pipe connectors and controls positioned on the right side of the symbol.
It displays the current temperature, animates the fan when the pump is running, and exposes a power button and temperature setpoint controls for direct device interaction from the dashboard.

</div>

<HeatPump product={props.product}/>
