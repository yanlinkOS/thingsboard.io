import { Image } from 'astro:assets';
import MeterBase from './_meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-small-left-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Small left meter SCADA symbol"
  width={100}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '100px', height: '199px', objectFit: 'contain' }}
/>

This widget belongs to the **Traditional SCADA fluid system** bundle — a collection of pipe segments, fittings, valves, pumps, and gauges designed for building fluid system diagrams on ThingsBoard dashboards.

The **Small left meter** widget renders a compact vertical scale gauge with the scale markings and value box aligned to the left side of the symbol.
Use it when the gauge needs to be placed to the left of adjacent elements in the diagram.

</div>

<MeterBase product={props.product}/>
