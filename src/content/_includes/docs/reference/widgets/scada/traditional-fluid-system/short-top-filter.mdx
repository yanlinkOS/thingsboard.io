import { Image } from 'astro:assets';
import Filter from './_filter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-short-top-filter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Short top filter SCADA symbol"
  width={100}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '100px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Short top filter** widget renders a compact vertical filter symbol with the pipe connection at the top. Use it to represent a filter unit in fluid system diagrams where vertical space is limited and the top outlet connects to upstream piping — for example, in dense multi-stage filtration layouts or skid-mounted systems that discharge upward.

</div>
</div>

<Filter product={props.product}/>
