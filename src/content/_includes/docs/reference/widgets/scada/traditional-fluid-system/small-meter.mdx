import { Image } from 'astro:assets';
import MeterBase from './_meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-small-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Small meter SCADA symbol"
  width={100}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '100px', height: '199px', objectFit: 'contain' }}
/>

This widget belongs to the **Traditional SCADA fluid system** bundle — a collection of pipe segments, fittings, valves, pumps, and gauges designed for building fluid system diagrams on ThingsBoard dashboards.

The **Small meter** widget renders a compact vertical scale gauge that maps a telemetry value to a configurable range.
Use it where a moderate-width gauge fits the diagram layout while keeping the overall symbol footprint small.

</div>

<MeterBase product={props.product}/>
