import { Image } from 'astro:assets';
import JunctionPipeIntro from './_junction-pipe-intro.mdx';
import JunctionPipeOutro from './_junction-pipe-outro.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-left-tee-pipe.png';
import JunctionPipeBehavior from './_junction-pipe-behavior.mdx';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Left tee pipe SCADA symbol"
  width={160}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '160px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Left tee pipe** widget represents a T-shaped pipe junction with a left branch in a SCADA fluid system diagram. It independently controls fluid presence, flow state, and direction for its three arms: left, top, and bottom.

</div>
</div>

<JunctionPipeIntro product={props.product}/>

## Behavior

Behavior parameters are organized by pipe arm. Each arm is configured independently, allowing different fluid state data sources for each direction. See [Data source configuration](#data-source-configuration) for the full list of available actions.

### Left pipe

| Parameter | Default | Description |
|---|---|---|
| **Fluid presence** | `Absent` | Whether fluid is present in the left arm. See [Data source configuration](#data-source-configuration). |
| **Flow presence** | `Absent` | Whether fluid is flowing in the left arm. See [Data source configuration](#data-source-configuration). |
| **Flow direction** | `Forward` | Direction of the fluid flow animation in the left arm. See [Data source configuration](#data-source-configuration). |
| **Flow animation speed** | `1` | Animation speed. `1` is normal speed, `0` disables animation, `< 1` slower, `> 1` faster. See [Data source configuration](#data-source-configuration). |

### Top pipe

| Parameter | Default | Description |
|---|---|---|
| **Fluid presence** | `Absent` | Whether fluid is present in the top arm. See [Data source configuration](#data-source-configuration). |
| **Flow presence** | `Absent` | Whether fluid is flowing in the top arm. See [Data source configuration](#data-source-configuration). |
| **Flow direction** | `Forward` | Direction of the fluid flow animation in the top arm. See [Data source configuration](#data-source-configuration). |
| **Flow animation speed** | `1` | Animation speed. `1` is normal speed, `0` disables animation, `< 1` slower, `> 1` faster. See [Data source configuration](#data-source-configuration). |

### Bottom pipe

| Parameter | Default | Description |
|---|---|---|
| **Fluid presence** | `Absent` | Whether fluid is present in the bottom arm. See [Data source configuration](#data-source-configuration). |
| **Flow presence** | `Absent` | Whether fluid is flowing in the bottom arm. See [Data source configuration](#data-source-configuration). |
| **Flow direction** | `Forward` | Direction of the fluid flow animation in the bottom arm. See [Data source configuration](#data-source-configuration). |
| **Flow animation speed** | `1` | Animation speed. `1` is normal speed, `0` disables animation, `< 1` slower, `> 1` faster. See [Data source configuration](#data-source-configuration). |

### Leak

| Parameter | Default | Description |
|---|---|---|
| **Leak** | `False` | Whether a leak is present at the junction. See [Data source configuration](#data-source-configuration). |

<JunctionPipeBehavior />

## Appearance

The Appearance section controls the widget title and per-arm fluid colors.

| Setting | Description |
|---|---|
| **Title** | Widget title displayed in the header. Supports `entityName` and `entityLabel` variables. |
| **Card icon** | Icon shown in the widget header. Configure its size and color. |
| **Left fluid color** | Color of the fluid in the left arm. |
| **Top fluid color** | Color of the fluid in the top arm. |
| **Bottom fluid color** | Color of the fluid in the bottom arm. |
| **Pipe color** | Color of the pipe structure itself. |

<JunctionPipeOutro product={props.product}/>
