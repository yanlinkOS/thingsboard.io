import { Image } from 'astro:assets';
import DocLink from '@components/DocLink.astro';
import PipeBase from './_pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-vertical-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Vertical pipe SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', height: '159px', objectFit: 'contain' }}
/>

This widget belongs to the **Traditional SCADA fluid system** bundle — a collection of pipe segments, fittings, valves, pumps, and gauges designed for building fluid system diagrams on ThingsBoard dashboards.

The **Vertical pipe** widget represents a standard-length vertical pipe segment in a SCADA fluid system diagram. It visualizes fluid presence, flow state, direction, and leak conditions based on real-time device data.

</div>

<PipeBase product={props.product}/>
