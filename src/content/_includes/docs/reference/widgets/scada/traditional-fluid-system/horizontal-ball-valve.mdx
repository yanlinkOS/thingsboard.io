import { Image } from 'astro:assets';
import ValveBase from './_valve.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-horizontal-ball-valve.svg';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Horizontal ball valve SCADA symbol"
  width={160}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '160px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Horizontal ball valve** widget represents a quarter-turn ball valve mounted on a horizontal pipe segment in a SCADA fluid system diagram.
It visualizes the open or closed state of the valve and supports click-to-open and click-to-close commands sent directly to the connected device.

</div>
</div>

<ValveBase product={props.product}/>
