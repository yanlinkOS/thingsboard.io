import { Image } from 'astro:assets';
import PipeBase from './_pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-horizontal-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Horizontal pipe SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Horizontal pipe** widget represents a standard-length horizontal pipe segment in a SCADA fluid system diagram.
It visualizes fluid presence, flow state, direction, and leak conditions based on real-time device data.

</div>
</div>

<PipeBase product={props.product}/>
