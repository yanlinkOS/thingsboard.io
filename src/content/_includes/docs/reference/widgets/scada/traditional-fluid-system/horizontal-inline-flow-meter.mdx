import { Image } from 'astro:assets';
import FlowMeterBase from './_flow-meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-horizontal-inline-flow-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Horizontal inline flow meter SCADA symbol"
  width={180}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '180px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Horizontal inline flow meter** widget displays a numeric flow rate value on a rectangular display face embedded directly within a horizontal pipe run.
Use it where the meter is installed in-line with the pipe, with flow passing through the meter body.

</div>
</div>

<FlowMeterBase product={props.product}/>
