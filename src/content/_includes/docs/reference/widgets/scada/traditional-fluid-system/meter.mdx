import { Image } from 'astro:assets';
import MeterBase from './_meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Meter SCADA symbol"
  width={50}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '50px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Meter** widget renders a narrow vertical scale gauge that maps a telemetry value to a configurable range.
Use it where a slim gauge column fits the diagram layout, such as alongside a vertical pipe run.

</div>
</div>

<MeterBase product={props.product}/>
