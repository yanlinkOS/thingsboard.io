import { Image } from 'astro:assets';
import DocLink from '@components/DocLink.astro';
import PipeBase from './_pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-left-bottom-elbow-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Left bottom elbow pipe SCADA symbol"
  width={160}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '160px', objectFit: 'contain' }}
/>
<div style={{ marginTop: 0 }}>

The **Left bottom elbow pipe** widget represents a 90-degree elbow fitting that connects a horizontal pipe from the left to a vertical pipe going downward. It visualizes fluid presence, flow state, direction, and leak conditions based on real-time device data.

</div>
</div>

<PipeBase product={props.product}/>
