import { Image } from 'astro:assets';
import BrokenPipeBase from './_broken-pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-long-horizontal-broken-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Long horizontal broken pipe SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', height: '159px', objectFit: 'contain' }}
/>

This widget belongs to the **Traditional SCADA fluid system** bundle — a collection of pipe segments, fittings, valves, pumps, and gauges designed for building fluid system diagrams on ThingsBoard dashboards.

The **Long horizontal broken pipe** widget represents a damaged longer horizontal pipe segment in a SCADA fluid system diagram.
Use it to mark a fault or rupture location on an extended horizontal run.

</div>

<BrokenPipeBase product={props.product}/>
