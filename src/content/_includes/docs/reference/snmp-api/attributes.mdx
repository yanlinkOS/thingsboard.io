import DocLink from '@components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';

## Client-side attributes

To poll client-side attributes, add a **Client-side attributes querying** communication config. The configuration is identical to telemetry: mappings (OID → attribute key + data type) and querying frequency.

<ImageGallery images={[
  { src: '/src/assets/images/reference/snmp-api/snmp-api-device-profile-configuring-4.1-ce.png', alt: 'SNMP client-side attributes config', caption: 'Client-side attributes querying config with OID mappings.' },
  { src: '/src/assets/images/reference/snmp-api/snmp-api-device-profile-configuring-4.2-ce.png', alt: 'SNMP client-side attributes mappings', caption: 'OID-to-attribute-key mappings for client-side attributes.' },
]} />

## Shared attributes

To push shared attribute updates to an SNMP device, add a **Shared attributes setting** communication config. In this case, the mapping key is the **shared attribute name**, and the OID is the target where the updated value is written via a SET request.

The SNMP variable type used in the SET PDU depends on the configured data type:
- `LONG` → `INTEGER` (signed 32-bit integer)
- All other types → `OCTET STRING`

<ImageGallery images={[
  { src: '/src/assets/images/reference/snmp-api/snmp-api-device-profile-configuring-5-ce.png', alt: 'SNMP shared attributes config', caption: 'Shared attributes setting config: attribute name mapped to a target OID.' },
]} />
