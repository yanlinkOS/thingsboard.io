import DocLink from '@components/DocLink.astro';
import DocImage from '@components/DocImage.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Aside, Steps } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

OTA updates on ThingsBoard Edge follow the same model as on the ThingsBoard server. Firmware and software packages are created on the server, automatically propagated to each connected Edge instance, and then assigned to devices or device profiles on the Edge.

<DocImage src="/src/assets/images/docs/edge/user-guide/ota-updates.svg" alt="OTA update flow: firmware package created on the ThingsBoard server is auto-propagated to the Edge instance, then assigned to a device or device profile to trigger the update" width="800px" />

<Aside type="caution">
OTA update packages can only be created, modified, or deleted on the ThingsBoard server. These actions are not available on the Edge instance.
</Aside>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/ota-update/0-cannot-create-on-edge-ce.png',
      alt: 'OTA Updates section on Edge with no option to add packages',
      caption: 'OTA update packages cannot be created, modified, or deleted on the Edge instance.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/pe/edge/user-guide/ota-update/0-cannot-create-on-edge-pe.png',
      alt: 'OTA Updates section on Edge with no option to add packages',
      caption: 'OTA update packages cannot be created, modified, or deleted on the Edge instance.',
    },
  ]} />
)}

For a general overview of how OTA updates work, see <DocLink product={props.product} path="user-guide/ota-updates" target="_blank">OTA updates</DocLink>.

## Create an OTA update package

<Steps>
1. Log in to your ThingsBoard server and go to **Advanced features → OTA updates**.

2. Click **Add** (+) to open the new package dialog.

   Fill in the following fields:
   - **Title:** The firmware package name.
   - **Version:** The package version number.
   - **Version tag:** Populated automatically. Enter a custom value only if it must match the version string reported by the device.
   - **Device profile:** The target device profile.
   - **Package type:** Select **Firmware** or **Software**.
   - **Source:** Select **Upload binary file** to upload a file from your computer, or **Use external URL** to provide a download URL.

3. Click **Add**.

   Once uploaded, the title, version, device profile, and package type cannot be modified.

4. Confirm the package appears in the list.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/ota-update/1-ota-upd-section-ce.png',
      alt: 'OTA updates section on the ThingsBoard server with the add button',
      caption: 'Step 1–2: Go to Advanced features → OTA updates and click +.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/ota-update/2-add-package-ce.png',
      alt: 'New OTA update package dialog with fields filled in',
      caption: 'Step 2–3: Fill in the package details and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/ota-update/3-verify-on-server-ce.png',
      alt: 'OTA updates list showing the newly uploaded package',
      caption: 'Step 4: The package appears in the list after a successful upload.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/pe/edge/user-guide/ota-update/1-ota-upd-section-pe.png',
      alt: 'OTA updates section on the ThingsBoard server with the add button',
      caption: 'Step 1–2: Go to Advanced features → OTA updates and click +.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/user-guide/ota-update/2-add-package-pe.png',
      alt: 'New OTA update package dialog with fields filled in',
      caption: 'Step 2–3: Fill in the package details and click Add.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/user-guide/ota-update/3-verify-on-server-pe.png',
      alt: 'OTA updates list showing the newly uploaded package',
      caption: 'Step 4: The package appears in the list after a successful upload.',
    },
  ]} />
)}

## Verify the package on Edge

After the package is created on the server, ThingsBoard automatically propagates it to all connected Edge instances.

<Steps>
1. Log in to your ThingsBoard Edge instance and go to **Advanced features → OTA updates**.

2. Confirm the package appears in the list.

   If the package is missing, check the <DocLink product={props.product} path="user-guide/edge-status-events" target="_blank">Cloud Events</DocLink> log on the Edge instance for sync errors.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/ota-update/4-verify-on-edge-ce.png',
      alt: 'OTA updates section on Edge showing the propagated package',
      caption: 'The package propagated from the server appears in the Edge OTA updates list.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/pe/edge/user-guide/ota-update/4-verify-on-edge-pe.png',
      alt: 'OTA updates section on Edge showing the propagated package',
      caption: 'The package propagated from the server appears in the Edge OTA updates list.',
    },
  ]} />
)}

## Deploy the package to devices

After the package is available on Edge, assign it to devices. You can assign it to a single device or to a device profile — any device using that profile, including devices added later, will receive the update automatically.

### By device

{/* TODO: verify tab name ("Firmware"/"Software") and button name ("Apply") against the Edge UI */}

<Steps>
1. Go to **Entities → Devices** and open the target device.

2. Go to the **Firmware** or **Software** tab, depending on the package type.

3. Select the package from the dropdown and click **Apply**.

   ThingsBoard Edge sends the update to the device. Monitor the update progress in the device's **Attributes** — the `fw_state` or `sw_state` attribute changes from `DOWNLOADING` through `UPDATING` to `UPDATED` once the device confirms the new version.
</Steps>

### By device profile

{/* TODO: verify navigation path ("Profiles > Device profiles") against the Edge UI */}

<Steps>
1. Go to **Profiles → Device profiles** and open the target profile.

2. Go to the **OTA updates** tab.

3. Select the firmware and/or software package and click **Save**.

   All current and future devices using this profile receive the update notification.
</Steps>

<Aside type="note">
The device must implement the ThingsBoard OTA update protocol to receive and apply packages — this includes reporting its current firmware version as a device attribute and responding to update notifications over MQTT. For implementation details, see <DocLink product={props.product} path="user-guide/ota-updates" target="_blank">OTA updates</DocLink>.
</Aside>
