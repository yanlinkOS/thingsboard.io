import DocLink from '@components/DocLink.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Aside, Steps } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

Assets are abstract entities that represent real-world objects — factories, vehicles, warehouses, buildings, and similar — and organize other entities beneath them in a relational hierarchy. Devices report telemetry; assets give that telemetry context by grouping the devices that produce it. Dashboard aliases can then use asset relations to dynamically aggregate data from all related devices in a single widget.

For a general introduction to assets, see <DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="user-guide/assets" target="_blank">Assets</DocLink>.

There are two ways to get an asset onto an Edge instance:

- **Create on Edge:** Use this when the asset is new and will be managed primarily on Edge. ThingsBoard pushes it to the server automatically.
- **Assign from server:** Use this when the asset already exists on the server and you want to extend its management to a specific Edge instance.

## Create an asset on Edge

<Aside type="note">
Before creating an asset, make sure the target <DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="user-guide/asset-profiles" target="_blank">asset profile</DocLink> is available on the Edge instance. Asset profiles are created on the server and must be assigned to the Edge instance before they appear in the Edge UI.
</Aside>

<Steps>
1. Go to **Entities → Assets** and click **Add** (+), then select **Add new asset**.

2. Enter the asset **Name** and select an **Asset profile**.

3. Click **Add**.

   The asset appears in the list and is pushed to the server automatically once the Edge connection is established.
</Steps>

{props.product === Products.EDGE_PE && (
  <Aside type="note">
    On ThingsBoard Edge PE, the <strong>Add asset</strong> dialog also includes an <strong>Owner</strong> field — Tenant by default. The asset is added to an auto-created entity group named after the Edge instance.
  </Aside>
)}

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/1-add-new-asset.webp',
      alt: 'Assets section with Add new asset option selected',
      caption: 'Step 1: Click + and select Add new asset.',
    },
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/2-fill-in-required-fields.webp',
      alt: 'Add asset dialog with Name and Asset profile fields',
      caption: 'Step 2: Fill in the asset details and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/3-asset-provisioned-to-cloud.webp',
      alt: 'Assets list showing the newly created asset',
      caption: 'Step 3: The asset is created and pushed to the server automatically.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/1-add-new-asset-pe.webp',
      alt: 'Assets section with Add new asset option selected',
      caption: 'Step 1: Click + and select Add new asset.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/2-fill-in-required-fields-pe.webp',
      alt: 'Add asset dialog with Name, Asset profile, and Owner fields',
      caption: 'Step 2: Fill in the asset details and click Add.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/3-asset-provisioned-to-cloud-pe.webp',
      alt: 'Assets list showing the newly created asset pushed to the server',
      caption: 'Step 3: The asset is pushed to the server and added to the auto-created entity group.',
    },
  ]} />
)}

## Create relations

Relations define the hierarchy between entities. An asset typically acts as the parent that **Manages** one or more devices. Once the relation is set up, you can use it as a filter in dashboard aliases to display data from all related devices dynamically.

The two key parameters are:

| Parameter | Options | Meaning |
|-----------|---------|---------|
| Direction | From (outbound) | The relation originates from this entity |
| Direction | To (inbound) | The relation points to this entity |
| Relation type | Contains | This entity is a part of another |
| Relation type | Manages | This entity is responsible for another |

To add a **Manages** relation from an asset to a device:

<Steps>
1. Go to **Entities → Assets** and open the asset.

2. Go to the **Relations** tab and select **Outbound relations — From**.

3. Click **Add** (+), set **Relation type** to **Manages**, set **To entity** type to **Device**, and select the device from the dropdown.

4. Click **Add**.

   The relation appears in the Relations tab.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/4-relation-tab.webp',
      alt: 'Asset details Relations tab with Outbound relations selected',
      caption: 'Step 2: Open the Relations tab and select Outbound relations — From.',
    },
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/5-add-relation.webp',
      alt: 'Add relation dialog with Manages type and Device entity selected',
      caption: 'Step 3: Set relation type to Manages and select the device.',
    },
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/6-confirm-relations.webp',
      alt: 'Relations tab showing the newly added Manages relation',
      caption: 'Step 4: The relation appears in the list.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/4-relation-tab-pe.webp',
      alt: 'Asset details Relations tab with Outbound relations selected',
      caption: 'Step 2: Open the Relations tab and select Outbound relations — From.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/5-add-relation-pe.webp',
      alt: 'Add relation dialog with Manages type and Device entity selected',
      caption: 'Step 3: Set relation type to Manages and select the device.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/6-confirm-relations-pe.webp',
      alt: 'Relations tab showing the newly added Manages relation',
      caption: 'Step 4: The relation appears in the list.',
    },
  ]} />
)}

To display telemetry from asset-related devices on a dashboard, use a **Relations query** alias with the asset as the root entity. For details, see <DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="user-guide/aliases" target="_blank">Entity aliases</DocLink>.

## Assign an asset from the server

Use this flow when the asset already exists on the server and you want to make it available on a specific Edge instance.

{props.product === Products.EDGE && (
  <Steps>
    <ol>
      <li>Log in to your ThingsBoard server and go to <strong>Edge management → Instances</strong>.</li>
      <li>Open the Edge instance and click <strong>Manage assets</strong>.</li>
      <li>Click <strong>Add</strong> (+), select the asset from the dropdown, and click <strong>Assign</strong>.</li>
      <li>Log in to your Edge instance and go to <strong>Entities → Assets</strong> to confirm the asset was assigned.</li>
    </ol>
  </Steps>
)}

{props.product === Products.EDGE_PE && (
  <Steps>
    <ol>
      <li>Log in to your ThingsBoard server and go to <strong>Edge management → Instances</strong>.</li>
      <li>Open the Edge instance and click <strong>Manage edge asset groups</strong>.</li>
      <li>Click <strong>Add</strong> (+) to assign an existing asset group, or open a group and click <strong>Add</strong> (+) to add a new asset to it.</li>
      <li>Log in to your Edge instance and go to <strong>Entities → Assets</strong> to confirm the asset was assigned.</li>
    </ol>
  </Steps>
)}

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/12-instances-on-cloud.webp',
      alt: 'Edge Instances page with Manage assets button',
      caption: 'Step 2: Open the Edge instance and click Manage assets.',
    },
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/13-edge-assets.webp',
      alt: 'Edge assets dialog with asset selection dropdown',
      caption: 'Step 3: Select the asset and click Assign.',
    },
    {
      src: '/src/assets/images/docs/edge/config/edge-asset-ce/14-confirm.webp',
      alt: 'Assets section on Edge showing the assigned asset',
      caption: 'Step 4: Confirm the asset appears on Edge.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/12-instances-on-cloud.webp',
      alt: 'Edge Instances page with Manage edge asset groups button',
      caption: 'Step 2: Open the Edge instance and click Manage edge asset groups.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/13-asset-groups-page.webp',
      alt: 'Asset groups page with assign and add options',
      caption: 'Step 3: Assign an existing group or open a group to add a new asset.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/14-add-asset.webp',
      alt: 'Asset group page with Add asset dialog',
      caption: 'Step 3: Add a new asset to the group.',
    },
    {
      src: '/src/assets/images/docs/pe/edge/config/edge-asset-pe/15-confirm.webp',
      alt: 'Assets section on Edge showing the assigned asset',
      caption: 'Step 4: Confirm the asset appears on Edge.',
    },
  ]} />
)}

## Unassign an asset from Edge

Unassigning removes the asset from the Edge instance but keeps it on the server. All data associated with the asset is preserved on the server.

<Steps>
1. Log in to your ThingsBoard server and go to **Edge management → Instances**.

2. Open the Edge instance and click **Manage assets**.

3. Find the asset in the list and click **Unassign from edge** (×).

   The asset is removed from the Edge instance immediately and remains fully accessible on the server.
</Steps>
