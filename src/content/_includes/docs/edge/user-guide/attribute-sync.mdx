import DocLink from '@components/DocLink.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import Tabs from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';

When a device publishes client attributes to Edge, Edge forwards them to the server, the server applies a transformation script, and pushes the result back to Edge as shared attributes. The device receives the update by subscribing to the shared attribute topic on Edge.

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/attr-post-processing.svg',
    alt: 'Attribute post-processing flow: device publishes client attributes to Edge, Edge pushes them to the server, the script node transforms them, and the server pushes the result back to Edge as shared attributes',
  },
]} />

For a general introduction to attribute types and how they work, see <DocLink product={props.product === Products.EDGE_PE ? Products.EDGE_PE : Products.EDGE} path="user-guide/digital-twins/attributes" target="_blank">Attributes</DocLink>.

## Prerequisites

Before you begin:

- A ThingsBoard server (ThingsBoard Cloud or self-hosted) is running and reachable from Edge.
- ThingsBoard Edge is installed and connected to the server.
- A device is connected to Edge via MQTT, HTTP, or CoAP.

## Step 1: Route device attributes to the server

Configure the Edge rule chain to forward client attributes from the device to the server using the **push to cloud** node.

<Steps>
  <ol>
    <li>Log in to the server. Go to <strong>Edge management → Rule chain templates</strong> and open the rule chain assigned to your Edge instance. On Edge 4.0 or later, go to <strong>Rule chains</strong> on the Edge instance directly.</li>
    <li>Find the <strong>push to cloud</strong> node in the <strong>Node search</strong> bar and drag it onto the rule chain sheet. In the <strong>Add rule node</strong> dialog, enter a name and select <strong>Client attributes</strong> in the <strong>Entity attributes scope</strong> field. Click <strong>Add</strong>.</li>
    <li>Connect the <strong>save attributes</strong> and <strong>save time series</strong> nodes to the <strong>push to cloud</strong> node with the <strong>Success</strong> label. Click <strong>Apply changes</strong>.</li>
  </ol>
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/1-rule-chain-templates.webp',
      alt: 'Edge management — Rule chain templates section on the server',
      caption: 'Step 1: Open the rule chain assigned to your Edge instance.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/2-add-push-to-cloud.webp',
      alt: 'Push to cloud node dialog with Client attributes selected in Entity attributes scope',
      caption: 'Step 2: Configure the push to cloud node with Client attributes scope.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/3-save.webp',
      alt: 'Rule chain with save attributes and save time series connected to push to cloud',
      caption: 'Step 3: Connect the save nodes to push to cloud and apply changes.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/1-rule-chain-templates-pe.webp',
      alt: 'Edge management — Rule chain templates section on the server (PE)',
      caption: 'Step 1: Open the rule chain assigned to your Edge instance.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/2-add-push-to-cloud-pe.webp',
      alt: 'Push to cloud node dialog with Client attributes selected in Entity attributes scope (PE)',
      caption: 'Step 2: Configure the push to cloud node with Client attributes scope.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/3-save-pe.webp',
      alt: 'Rule chain with save attributes node connected to push to cloud (PE)',
      caption: 'Step 3: Connect the save attributes node to push to cloud and apply changes.',
    },
  ]} />
)}

## Step 2: Process and return attributes to Edge

Configure the server's root rule chain to transform the incoming attributes and push the result back to Edge as shared attributes. The script node sits between the save nodes and the push to edge node:

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/attr-transform.svg',
    alt: 'Rule chain flow: save attributes and save time series connect to a script node, which connects to the push to edge node with Shared attributes scope',
  },
]} />

Use the following post-processing script in the script node — modify the transformation logic as needed:

```js
// Post-processing script — modify as needed
msg = { attribute1_result: "value1_result" };
metadata = {};

return { msg, metadata, msgType };
```

<Steps>
  <ol>
    <li>On the server, go to <strong>Rule chains</strong> and open the root rule chain.</li>
    <li>Find the <strong>script</strong> node in the <strong>Node search</strong> bar and drag it onto the sheet. Enter a name, paste the script above, and click <strong>Add</strong>.</li>
    <li>Find the <strong>push to edge</strong> node and drag it onto the sheet. Enter a name and select <strong>Shared attributes</strong> in the <strong>Entity attributes scope</strong> field. Click <strong>Add</strong>.</li>
    <li>Connect the <strong>save attributes</strong> and <strong>save time series</strong> nodes to the <strong>script</strong> node with the <strong>Success</strong> label. Connect the <strong>script</strong> node to the <strong>push to edge</strong> node with the <strong>Success</strong> label. Click <strong>Apply changes</strong>.</li>
  </ol>
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/4-rule-chain.webp',
      alt: 'Root rule chain section on the server',
      caption: 'Step 1: Open the root rule chain on the server.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/5-add-script.webp',
      alt: 'Script node dialog with post-processing JavaScript entered',
      caption: 'Step 2: Add the script node with the post-processing logic.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/5-add-push-to-edge.webp',
      alt: 'Push to edge node dialog with Shared attributes selected in Entity attributes scope',
      caption: 'Step 3: Configure the push to edge node with Shared attributes scope.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/6-save.webp',
      alt: 'Rule chain with nodes connected to push to edge using Success label',
      caption: 'Step 4: Connect nodes to push to edge and apply changes.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/4-rule-chain-pe.webp',
      alt: 'Root rule chain section on the server (PE)',
      caption: 'Step 1: Open the root rule chain on the server.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/5-add-script-pe.webp',
      alt: 'Script node dialog with post-processing JavaScript entered (PE)',
      caption: 'Step 2: Add the script node with the post-processing logic.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/5-add-push-to-edge-pe.webp',
      alt: 'Push to edge node dialog with Shared attributes selected in Entity attributes scope (PE)',
      caption: 'Step 3: Configure the push to edge node with Shared attributes scope.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/6-save-pe.webp',
      alt: 'Rule chain with nodes connected to push to edge using Success label (PE)',
      caption: 'Step 4: Connect nodes to push to edge and apply changes.',
    },
  ]} />
)}

## Step 3: Subscribe then publish device attributes

Subscribe to shared attribute updates *before* publishing — this ensures the device is listening when the server pushes the processed result back.

Find the device access token in **Entities → Devices**, open the device, and click **Copy access token** on the **Device details** page.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/9-copy-access-token.webp',
      alt: 'Device details page with Copy access token button highlighted',
      caption: 'Copy the access token from the Device details page.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/9-copy-access-token-pe.webp',
      alt: 'Device details page with Copy access token button highlighted (PE)',
      caption: 'Copy the access token from the Device details page.',
    },
  ]} />
)}

Use the protocol your device supports to subscribe to shared attribute updates and publish client attributes:

<Tabs>
<TabItem label="MQTT">

Subscribe to shared attribute updates:

```bash
mosquitto_sub -d -h $THINGSBOARD_HOST_NAME -t "v1/devices/me/attributes" -u "$ACCESS_TOKEN"
```

- `$THINGSBOARD_HOST_NAME` — hostname or IP address of your Edge instance.
- `$ACCESS_TOKEN` — access token copied above.

Publish client attributes:

```bash
mosquitto_pub -d -h $THINGSBOARD_HOST_NAME -t "v1/devices/me/attributes" -u "$ACCESS_TOKEN" -m '{"attribute1": "value1", "attribute2": true}'
```

</TabItem>
<TabItem label="HTTP">

Subscribe to shared attribute updates:

```bash
curl -v -X GET "http://$THINGSBOARD_HOST_NAME/api/v1/$ACCESS_TOKEN/attributes/updates?timeout=20000"
```

- `$THINGSBOARD_HOST_NAME` — hostname or IP address of your Edge instance.
- `$ACCESS_TOKEN` — access token copied above.
- `timeout=20000` — the connection stays open for 20 seconds; if a shared attribute updates in that time, the server responds immediately.

Publish client attributes:

```bash
curl -v -X POST \
  --data '{"attribute1": "value1", "attribute2": true}' \
  "http://$THINGSBOARD_HOST_NAME/api/v1/$ACCESS_TOKEN/attributes" \
  --header "Content-Type: application/json"
```

</TabItem>
<TabItem label="CoAP">

Subscribe to shared attribute updates:

```bash
coap get -o coap://$THINGSBOARD_HOST_NAME/api/v1/$ACCESS_TOKEN/attributes
```

- `$THINGSBOARD_HOST_NAME` — hostname or IP address of your Edge instance.
- `$ACCESS_TOKEN` — access token copied above.

The client receives an update when a shared attribute changes on Edge.

Publish client attributes:

```bash
coap post coap://$THINGSBOARD_HOST_NAME/api/v1/$ACCESS_TOKEN/attributes --payload '{"attribute1": "value1", "attribute2": true}'
```

</TabItem>
</Tabs>

## Step 4: Verify the flow

After publishing from the device, confirm the attributes are received at each point.

<Aside type="note">
Debug events are only visible when debug mode is enabled on the node. To enable it, open the node configuration dialog and toggle **Debug mode** on.
</Aside>

**On the device** — the terminal shows the shared attribute update received after publishing:

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/10-subscribe-post-update-attributes-mqtt.webp',
      alt: 'Terminal output showing shared attribute update received via MQTT subscription',
      caption: 'The device receives the shared attribute update after publishing client attributes.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/10-subscribe-post-update-attributes-mqtt-pe.webp',
      alt: 'Terminal output showing shared attribute update received via MQTT subscription (PE)',
      caption: 'The device receives the shared attribute update after publishing client attributes.',
    },
  ]} />
)}

**Client attributes on Edge** — go to **Entities → Devices**, open the device, select the **Attributes** tab, and choose **Client attributes** in the scope dropdown. The published attributes appear in the list.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/7-confirm-on-device-1.webp',
      alt: 'Device Attributes tab on Edge showing Client attributes received from the device',
      caption: 'Client attributes published by the device are visible on Edge.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/7-confirm-on-device-1-pe.webp',
      alt: 'Device Attributes tab on Edge showing Client attributes received from the device (PE)',
      caption: 'Client attributes published by the device are visible on Edge.',
    },
  ]} />
)}

**On the Edge rule chain** — open the **push to cloud** node and select the **Events** tab. With debug mode enabled, you see **IN** (attributes received from the device) and **OUT** (attributes pushed to the server) records.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/8-confirm-on-edge-node-1.webp',
      alt: 'Edge rule chain with the push to cloud node selected',
      caption: 'Open the push to cloud node on the Edge rule chain.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/8-confirm-on-edge-node-2.webp',
      alt: 'Push to cloud node Events tab with IN and OUT debug records',
      caption: 'IN record: attributes from the device. OUT record: attributes pushed to the server.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/8-confirm-on-edge-node-1-pe.webp',
      alt: 'Edge rule chain with the push to cloud node selected (PE)',
      caption: 'Open the push to cloud node on the Edge rule chain.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/8-confirm-on-edge-node-2-pe.webp',
      alt: 'Push to cloud node Events tab with IN and OUT debug records (PE)',
      caption: 'IN record: attributes from the device. OUT record: attributes pushed to the server.',
    },
  ]} />
)}

**On the server rule chain** — open the **push to edge** node and select the **Events** tab. With debug mode enabled, you see **IN** (attributes received from Edge) and **OUT** (processed attributes pushed back to Edge) records.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/9-confirm-on-cloud-node-1.webp',
      alt: 'Server rule chain with the push to edge node selected',
      caption: 'Open the push to edge node on the server rule chain.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/9-confirm-on-cloud-node-2.webp',
      alt: 'Push to edge node Events tab with IN and OUT debug records',
      caption: 'IN record: attributes from Edge. OUT record: processed attributes pushed back to Edge.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/9-confirm-on-cloud-node-1-pe.webp',
      alt: 'Server rule chain with the push to edge node selected (PE)',
      caption: 'Open the push to edge node on the server rule chain.',
    },
    {
      src: '/src/assets/images/docs/edge/user-guide/attribute-sync/9-confirm-on-cloud-node-2-pe.webp',
      alt: 'Push to edge node Events tab with IN and OUT debug records (PE)',
      caption: 'IN record: attributes from Edge. OUT record: processed attributes pushed back to Edge.',
    },
  ]} />
)}
