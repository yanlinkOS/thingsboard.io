import DocLink from '@components/DocLink.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

Attributes on ThingsBoard Edge are stored and processed locally, and remain available to dashboards and rule chains even when the connection to the server is offline. Changes are not automatically propagated between Edge and server — synchronization requires explicit rule chain configuration on both sides.

For an overview of how attributes work on Edge, see <DocLink product={props.product === Products.EDGE_PE ? Products.EDGE_PE : Products.EDGE} path="key-concepts/attributes" target="_blank">Edge attributes</DocLink>.

## Assigning attributes to entities

Attributes can be added directly on the Edge instance and are stored and processed locally. The steps below use **Server attributes** — the scope for manually assigned metadata that is not reported by the device.

<Steps>
1. Go to the entity section — for example, **Entities → Devices** — and click the entity.

2. On the **Entity details** page, select the **Attributes** tab and choose **Server attributes** in the **Entity attributes scope** dropdown.

3. Click **Add** (+). In the **Add attribute** dialog, enter the attribute name in the **Key** field, select the value type (**String**, **Integer**, **Double**, **Boolean**, or **JSON**), and enter the value. Click **Add**.

   The attribute appears in the list.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/1-devices.webp', alt: 'Devices list on Edge' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/2-add-attribute.webp', alt: 'Add attribute dialog with Key, type, and value fields' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/3-confirm-attribute.webp', alt: 'Attribute added and visible in the attributes list' },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/1-devices-pe.webp', alt: 'Devices list on Edge (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/2-add-attribute-pe.webp', alt: 'Add attribute dialog with Key, type, and value fields (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/3-confirm-attribute-pe.webp', alt: 'Attribute added and visible in the attributes list (PE)' },
  ]} />
)}

If an attribute is assigned to an entity on the server, and that entity is then <DocLink product={props.product === Products.EDGE_PE ? Products.EDGE_PE : Products.EDGE} path="user-guide/edge-management" target="_blank">assigned to an Edge instance</DocLink>, the attribute is pushed to Edge automatically.

## Using attributes in dashboards

Attributes provide context for telemetry data on dashboards — displaying static or dynamic metadata about devices, or acting as configuration parameters to filter or annotate widgets.

<Steps>
1. Open a dashboard and enter edit mode.

2. Add a widget — Cards, input widgets, and entity table widgets are well suited for displaying attributes.

3. In the widget datasource, create an <DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="user-guide/aliases" target="_blank">entity alias</DocLink> pointing to the entity whose attributes you want to display.

4. Select the attribute keys to display and click **Add**.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/4-attributes-dasboard.webp', alt: 'Dashboard displaying device attributes using a card widget' },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/4-attributes-dasboard-pe.webp', alt: 'Dashboard displaying device attributes using a card widget (PE)' },
  ]} />
)}

## Syncing attribute updates

By default, attribute changes are not automatically propagated between Edge and the server. Synchronization requires configuring the rule chain on each side.

### Push attribute changes from the server to Edge

<Steps>
1. Log in to the server. Go to **Rule chains** and open the rule chain.

2. Search for the **push to edge** node in the **Node search** bar. Drag it onto the rule chain sheet. In the **Add rule node** dialog, enter a name and select **Server attributes** in the **Entity attributes scope** field. Click **Add**.

3. Connect the **message type switch** node to the **push to edge** node with the **Attributes Deleted** and **Attributes Updated** link labels. Click **Apply changes**.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/5-rule-chains-section.webp', alt: 'Rule chains section on the server' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/6-push-to-edge.webp', alt: 'Push to edge node configuration dialog' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/7-connect-and-save.webp', alt: 'Message type switch connected to push to edge node with Attributes Updated and Deleted labels' },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/5-rule-chains-section-pe.webp', alt: 'Rule chains section on the server (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/6-push-to-edge-pe.webp', alt: 'Push to edge node configuration dialog (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/7-connect-and-save-pe.webp', alt: 'Message type switch connected to push to edge node with Attributes Updated and Deleted labels (PE)' },
  ]} />
)}

### Push attribute changes from Edge to the server

<Steps>
1. Log in to the server. Go to **Edge management → Rule chain templates** and open the rule chain.

2. Search for the **push to cloud** node in the **Node search** bar. Drag it onto the rule chain sheet. In the **Add rule node** dialog, enter a name and select **Server attributes** in the **Entity attributes scope** field. Click **Add**.

3. Connect the **message type switch** node to the **push to cloud** node with the **Attributes Deleted** and **Attributes Updated** link labels. Click **Apply changes**.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/8-14-19-rule-chain-templates-section.webp', alt: 'Rule chain templates section in Edge management' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/9-push-to-cloud.webp', alt: 'Push to cloud node configuration dialog' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/10-connect-and-save.webp', alt: 'Message type switch connected to push to cloud node with Attributes Updated and Deleted labels' },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/8-14-19-rule-chain-templates-section-pe.webp', alt: 'Rule chain templates section in Edge management (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/9-push-to-cloud-pe.webp', alt: 'Push to cloud node configuration dialog (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/10-connect-and-save-pe.webp', alt: 'Message type switch connected to push to cloud node with Attributes Updated and Deleted labels (PE)' },
  ]} />
)}

<Aside type="note">
Starting with Edge 4.0, you can create and edit rule chains directly on the Edge instance.
</Aside>

## Custom attributes for Edge instances

This section covers attributes assigned to the **Edge instance itself** — introduced in release 3.9. Edge instance attributes act as placeholders in rule chains, letting a single rule chain template serve multiple Edge instances with different configurations at runtime.

### Assign a custom attribute to an Edge instance

<Steps>
1. Log in to the server. Go to **Edge management → Instances** and click the Edge instance.

2. On the **Edge details** page, select the **Attributes** tab. Click **Add** (+) and enter the attribute name and value — for example, `edgeAccessToken`. Click **Add**.

3. Select the **Relations** tab. Click **Add** (+) to create a relation between the Edge and its devices. Click **Add**.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/11-edge-section.webp', alt: 'Edge instances section showing the selected Edge' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/12-edge-token.webp', alt: 'Add attribute dialog with edgeAccessToken key on the Edge details page' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/12-confirm-1.webp', alt: 'Custom attribute confirmed in the attributes list' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/13-edge-manages-devices.webp', alt: 'Add relation dialog linking the Edge to a device' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/13-confirm-1.webp', alt: 'Relation visible in the Relations tab' },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/11-edge-section-pe.webp', alt: 'Edge instances section showing the selected Edge (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/12-edge-token-pe.webp', alt: 'Add attribute dialog with edgeAccessToken key on the Edge details page (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/12-confirm-1-pe.webp', alt: 'Custom attribute confirmed in the attributes list (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/13-edge-manages-devices-pe.webp', alt: 'Add relation dialog linking the Edge to a device (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/13-confirm-1-pe.webp', alt: 'Relation visible in the Relations tab (PE)' },
  ]} />
)}

### Use attributes as rule chain placeholders

<Steps>
1. On the server, go to **Edge management → Rule chain templates** and open the rule chain. If you are running Edge 4.0 or later, you can edit the rule chain directly on the Edge under **Rule chains**.

2. Search for the **related entity data** node in the **Node search** bar. Drag it onto the rule chain sheet. Fill in the fields in the dialog:
   - **Name:** Enter the node name.
   - **Direction:** Select **From originator** or **To originator**.
   - **Relation type:** Select **Contains** or **Manages**.
   - **Entity types:** Add the entity type.

3. In the **Data to fetch** block, select the **Attributes** tab and configure the **Attributes mapping**:
   - **Source attribute key:** Enter the entity attribute key to track.
   - **Target key:** Enter the target key name. Click **Add**.

4. Search for the **rest api call** node. Drag it onto the sheet and configure it:
   - **Name:** Enter the node name.
   - **Headers:** Enter the request header and value. The value can be a static string or a pattern resolved from message metadata. Click **Add**.

5. Connect **message type switch** → **related entity data** with the **Post telemetry** label. Connect **related entity data** → **rest api call** with the **Success** label. Click **Apply changes**.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/8-14-19-rule-chain-templates-section.webp', alt: 'Rule chain templates section' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/15-related-entity-data.webp', alt: 'Related entity data node configuration — direction and relation type fields' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/15-related-entity-data-1.webp', alt: 'Related entity data node — attributes mapping configuration' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/16-rest-api-call.webp', alt: 'REST API call node configuration dialog' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/17-save-rule-chain.webp', alt: 'Rule chain with nodes connected and Apply changes button' },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/8-14-19-rule-chain-templates-section-pe.webp', alt: 'Rule chain templates section (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/15-related-entity-data-pe.webp', alt: 'Related entity data node configuration — direction and relation type fields (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/15-related-entity-data-1-pe.webp', alt: 'Related entity data node — attributes mapping configuration (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/16-rest-api-call-pe.webp', alt: 'REST API call node configuration dialog (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/17-save-rule-chain-pe.webp', alt: 'Rule chain with nodes connected and Apply changes button (PE)' },
  ]} />
)}

## Using attributes in rule chains

This section shows how to use **device or asset attributes** directly in rule chain logic — for example, triggering an alarm whenever a specific device attribute changes.

Before configuring alarm notifications, complete the following:

- Add a device to the Edge instance. For details, see <DocLink product={props.product === Products.EDGE_PE ? Products.EDGE_PE : Products.EDGE} path="key-concepts/entities" target="_blank">Entities and relations</DocLink>.
- Add a custom attribute to the device using the steps in the [Assigning attributes to entities](#assigning-attributes-to-entities) section above.
- Create an asset and set a **Contains** or **Manages** relation between the asset and the device.

<Steps>
1. On the server, go to **Edge management → Rule chain templates** and open the rule chain. If you are running Edge 4.0 or later, edit the rule chain directly on the Edge under **Rule chains**.

2. Search for the **related entity data** node. Drag it onto the sheet and fill in the fields:
   - **Name:** Enter the node name.
   - **Direction:** Select **From originator** or **To originator**.
   - **Relation type:** Select **Contains** or **Manages**.
   - **Entity types:** Add the entity type.

3. In the **Data to fetch** block, select the **Attributes** tab and configure the **Attributes mapping**:
   - **Source attribute key:** Enter the attribute key to track.
   - **Target key:** Enter the target key name. Click **Add**.

4. Search for the **create alarm** node. Drag it onto the sheet, enter a name, and click **Add**.

5. Connect **message type switch** → **related entity data** with the **Attributes Updated** and **Attributes Deleted** labels. Connect **related entity data** → **create alarm** with the **Success** label. Click **Apply changes**.

6. Go to **Entities → Devices**, open the **Attributes** tab on the device details page, and change the attribute you configured in the **related entity data** node. Confirm the alarm notification is received.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/8-14-19-rule-chain-templates-section.webp', alt: 'Rule chain templates section' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/20-related-entity-data-node-1.webp', alt: 'Related entity data node — direction and relation type configuration' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/21-related-entity-data-node-2.webp', alt: 'Related entity data node — attributes mapping configuration' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/22-create-alarm-node.webp', alt: 'Create alarm node configuration dialog' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/23-save.webp', alt: 'Rule chain with all nodes connected and Apply changes clicked' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/24-change-attribute.webp', alt: 'Device attributes tab with the tracked attribute being changed' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/25-alarm-notification.webp', alt: 'Alarm notification received after attribute change' },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/edge/user-guide/attributes/8-14-19-rule-chain-templates-section-pe.webp', alt: 'Rule chain templates section (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/20-related-entity-data-node-1-pe.webp', alt: 'Related entity data node — direction and relation type configuration (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/21-related-entity-data-node-2-pe.webp', alt: 'Related entity data node — attributes mapping configuration (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/22-create-alarm-node-pe.webp', alt: 'Create alarm node configuration dialog (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/23-save-pe.webp', alt: 'Rule chain with all nodes connected and Apply changes clicked (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/24-change-attribute-pe.webp', alt: 'Device attributes tab with the tracked attribute being changed (PE)' },
    { src: '/src/assets/images/docs/edge/user-guide/attributes/25-alarm-notification-pe.webp', alt: 'Alarm notification received after attribute change (PE)' },
  ]} />
)}

<Aside type="note">
Acknowledge and clear the alarm notification after receiving it — either on the server or on the Edge. If an unacknowledged alarm exists, subsequent triggers update it rather than create a new one.
</Aside>
