import DocLink from '@components/DocLink.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';

This recipe demonstrates an in-vehicle IoT monitoring system connected to ThingsBoard Edge.
The device sends readings from 10 sensors — distance, gas consumption, vehicle speed, engine temperature,
ambient temperature, tire temperature, and tire pressure (four tires) — to the Edge.
The Edge root rule chain filters that data and forwards only the **distance** readings to the cloud,
reducing bandwidth while keeping full sensor history available locally.

## Prerequisites

- Completed the <a href="/docs/getting-started/" target="_blank" rel="noopener noreferrer">Getting Started</a> guide.
- Familiar with the <DocLink product={props.product} path="user-guide/rule-chain-templates" target="_blank">Edge Rule Chain Templates</DocLink>.
- ThingsBoard server is running and ThingsBoard Edge is connected to it.

## Create device on Edge

Create a device named **"In-vehicle monitoring system"** on the Edge instance.

<Steps>
1. Log in to your ThingsBoard **Edge** instance and go to the **Devices** page.
2. Click **"+"** in the top-right corner, then click **Add new device**.
3. Enter the name **"In-vehicle monitoring system"** and click **Add**.
4. The device now appears in the devices table.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/provision-devices-item-1.png',
    alt: 'Navigate to the Devices page on the Edge instance',
    caption: 'Log in to your ThingsBoard Edge instance and navigate to the "Devices" page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/provision-devices-item-2.png',
    alt: 'Click the + icon to add a new device',
    caption: 'Click on the "+" icon in the top right corner, then click "Add new device".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/provision-devices-item-3.png',
    alt: 'Enter the device name "In-vehicle monitoring system"',
    caption: 'Enter the name "In-vehicle monitoring system" and click "Add".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/provision-devices-item-4.png',
    alt: 'Device appears in the devices table',
    caption: 'The device "In-vehicle monitoring system" now appears in the devices table.',
  },
]} />

Once the device is created on Edge, verify it has been synchronized to the ThingsBoard server.

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/provision-devices-item-5.png',
    alt: 'Navigate to Devices on the ThingsBoard server',
    caption: 'Log in to your ThingsBoard server instance and navigate to the "Devices" page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/provision-devices-item-6.png',
    alt: 'Device is listed on the server',
    caption: 'Verify that "In-vehicle monitoring system" is listed in the devices table.',
  },
]} />

## Configure Edge rule engine

Update the **Edge Root Rule Chain** on the ThingsBoard server to add a transformation script that filters incoming messages and forwards only the `distance` reading to the cloud.

Here is the final rule chain configuration you are aiming for:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/root-rule-chain.png',
    alt: 'Final Edge Root Rule Chain configuration with transformation script node',
    caption: 'Edge Root Rule Chain with the transformation script node connected between "save timeseries" and "push to cloud".',
  },
]} />

The transformation script creates a new message containing only the `distance` field:

```javascript
var newMsg = {};
newMsg.distance = msg.distance;
return { msg: newMsg, metadata: metadata, msgType: msgType };
```

<Steps>
1. Log in to your ThingsBoard server. Go to **Edge management → Rule chain templates** and open **Edge Root Rule Chain**.
2. Search for nodes containing **"script"**. Drag a **Script** (Transformation) node onto the canvas.
3. Enter the node name **"Transform Incoming Message"** and paste the JavaScript snippet above.
4. Delete the connection between **"save timeseries"** and **"push to cloud"**: select the connection, then click **X**.
5. Drag a new connection from **"save timeseries"** to **"Transform Incoming Message"**. Choose **Success** and click **Add**.
6. Drag a connection from **"Transform Incoming Message"** to **"push to cloud"**. Choose **Success** and click **Add**.
7. Click **Apply Changes** to save.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-1.png',
    alt: 'Open Edge Root Rule Chain in Rule chain templates',
    caption: 'Navigate to Edge management → Rule chain templates and open "Edge Root Rule Chain".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-2.png',
    alt: 'Search for and drag a Script transformation node',
    caption: 'Search for nodes containing "script". Drag the Script node (Transformation) to the rule chain.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-3.png',
    alt: 'Configure the transformation script node',
    caption: 'Enter the node name "Transform Incoming Message" and paste the JavaScript code.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-4.png',
    alt: 'Delete the connection between save timeseries and push to cloud',
    caption: 'Delete the connection between "save timeseries" and "push to cloud" by selecting it and clicking X.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-5.png',
    alt: 'Connect save timeseries to the transformation script node',
    caption: 'Drag a connection from "save timeseries" to the transformation script node.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-6.png',
    alt: 'Choose Success for the new connection',
    caption: 'Choose "Success" from the dropdown list and click "Add".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-8.png',
    alt: 'Connect the transformation node to push to cloud',
    caption: 'Drag a connection from "Transform Incoming Message" to the "push to cloud" node.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-9.png',
    alt: 'Choose Success for the push to cloud connection',
    caption: 'Choose "Success" from the dropdown list and click "Add".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-10.png',
    alt: 'Apply changes to the rule chain',
    caption: 'Click "Apply Changes" to save the updated rule chain.',
  },
]} />

Now verify that the updated rule chain has been synchronized to the Edge instance.

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-11.png',
    alt: 'Open Rule chains on the Edge instance',
    caption: 'Log in to your ThingsBoard Edge instance and navigate to the "Rule chains" page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-12.png',
    alt: 'Open the Edge Root Rule Chain to verify',
    caption: 'Open "Edge Root Rule Chain" to verify the changes.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/update-root-item-13.png',
    alt: 'Rule chain nodes match the server configuration',
    caption: 'The rule chain configuration on Edge matches what was configured on the server.',
  },
]} />

## Connect device and post telemetry

Get the device access token from Edge, then run the telemetry generator script.

<Steps>
1. In the ThingsBoard **Edge** UI, go to **Entities → Devices**.
2. Click on **"In-vehicle monitoring system"** to open device details.
3. Click **Copy access token** — the token is copied to your clipboard.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/copy-access-token-item-1.png',
    alt: 'Open the Devices page on Edge',
    caption: 'Open the Devices page in the ThingsBoard Edge instance.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/copy-access-token-item-2.png',
    alt: 'Open device details for In-vehicle monitoring system',
    caption: 'Click on the "In-vehicle monitoring system" row to open device details.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/copy-access-token-item-3.png',
    alt: 'Copy the device access token',
    caption: 'Click "Copy access token". The token is copied to your clipboard.',
  },
]} />

Install the MQTT Python library:

```bash
pip install paho-mqtt
```

Download the telemetry generator script, update the three placeholders at the top, then run it:

```python download="mqtt-generator.py"
import paho.mqtt.client as mqtt
import json
import random
import time

ACCESS_TOKEN = YOUR_ACCESS_TOKEN # REPLACE with your device access token, e.g. 'edge_vehicle'
THINGSBOARD_EDGE_HOST = YOUR_TB_EDGE_HOST # REPLACE with your ThingsBoard Edge host, e.g. 'localhost'
THINGSBOARD_EDGE_MQTT_PORT = YOUR_TB_EDGE_MQTT_PORT # REPLACE with your Edge MQTT port, e.g. 11883 or 1883
DISTANCE = 0

def on_connect(client, userdata, rc, *extra_params):
    client.subscribe('v1/devices/me/rpc/request/+')
    client.publish('v1/devices/me/telemetry', get_telemetry(), 1)
    print(get_telemetry())

def get_telemetry():
    return json.dumps(device)

while(True):
    device = {
        'distance': DISTANCE,
        'gas_consumption': random.randint(10, 15),
        'vehicle_speed': random.randint(60, 70),
        'engine_temperature': random.randint(195, 220),
        'ambient_temperature': random.randint(65, 80),
        'tire_temperature': random.randint(90, 110),
        'tire_pressure_front_left': random.randint(30, 35),
        'tire_pressure_front_right': random.randint(30, 35),
        'tire_pressure_rear_left': random.randint(30, 35),
        'tire_pressure_rear_right': random.randint(30, 35),
    }
    DISTANCE = DISTANCE + random.randint(0, 100)

    client = mqtt.Client()
    client.on_connect = on_connect
    client.username_pw_set(ACCESS_TOKEN)
    client.connect(THINGSBOARD_EDGE_HOST, THINGSBOARD_EDGE_MQTT_PORT, 60)
    try:
        time.sleep(5)
        client.loop()
    except KeyboardInterrupt:
        client.loop_stop()
        print("Exited!")
```

```bash
python mqtt-generator.py
```

Verify that the device is receiving all 10 sensor readings on Edge:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/verify-device-telemetry-item-1.png',
    alt: 'Navigate to Devices on Edge',
    caption: 'Navigate to the "Devices" page on the Edge instance.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/verify-device-telemetry-item-2.png',
    alt: 'Open device details on Edge',
    caption: 'Click on "In-vehicle monitoring system" to open device details.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/verify-device-telemetry-item-3.png',
    alt: 'All 10 sensor readings visible in Latest telemetry on Edge',
    caption: 'All 10 sensor readings are visible in the Latest telemetry tab on Edge.',
  },
]} />

Verify that only `distance` readings are pushed to the ThingsBoard server:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/verify-device-telemetry-item-4.png',
    alt: 'Navigate to Devices on the ThingsBoard server',
    caption: 'Log in to the ThingsBoard server and open the "Devices" page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/verify-device-telemetry-item-5.png',
    alt: 'Open device details on the server',
    caption: 'Click on "In-vehicle monitoring system" to view its details.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/verify-device-telemetry-item-6.png',
    alt: 'Only distance readings are pushed to the cloud',
    caption: 'The "Latest telemetry" tab on the server shows only distance readings.',
  },
]} />

## Create dashboard

Create a dashboard on the ThingsBoard server to visualize the total distance traveled.

<Steps>
1. Log in to the ThingsBoard server. Navigate to **Dashboards**.
2. Click **"+"** and select **Create new dashboard**. Enter the title **"Edge Vehicle"** and click **Add**.
3. Open the **"Edge Vehicle"** dashboard to begin editing.
4. Click the **Entity aliases** icon in the toolbar. Click **Add alias**.
5. Set **Alias name** to `edge device`, **Filter type** to `Single entity`, **Type** to `Device`, and select **"In-vehicle monitoring system"**. Click **Save**.
6. Click **Add Widget**, find **Digital gauges** in the Widgets Bundle, and select the gauge widget.
7. Set **Type** to `Entity`, **Entity Alias** to `edge device`, and **Data Key** to `distance`. Click **Add**.
8. Open the widget edit panel. On the **Appearance** tab, set **Max value** to `10000` and **Unit title** to `MLS`. Click **Add**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-1.png',
    alt: 'Navigate to Dashboards on the ThingsBoard server',
    caption: 'Log in to the ThingsBoard server and navigate to the "Dashboards" page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-2.png',
    alt: 'Create a new dashboard',
    caption: 'Click "+" and select "Create new dashboard".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-3.png',
    alt: 'Enter the dashboard title Edge Vehicle',
    caption: 'Enter the title "Edge Vehicle" and click "Add".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-4.png',
    alt: 'Open the Edge Vehicle dashboard',
    caption: 'Click on the "Edge Vehicle" dashboard to open it.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-5.png',
    alt: 'Open Entity aliases',
    caption: 'Click the "Entity aliases" icon in the toolbar.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-6.png',
    alt: 'Add alias',
    caption: 'Click "Add alias".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-7.png',
    alt: 'Configure the entity alias for the device',
    caption: 'Set Alias name to "edge device", Filter type to "Single entity", Type to "Device", and select "In-vehicle monitoring system".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-8.png',
    alt: 'Add a widget to the dashboard',
    caption: 'Click "Add Widget".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-9.png',
    alt: 'Select Digital gauges widget bundle',
    caption: 'Find "Digital gauges" in the Widgets Bundle and click to open the widget list.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-10.png',
    alt: 'Select the gauge widget',
    caption: 'Select the gauge widget from the list.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-11.png',
    alt: 'Configure the widget data source',
    caption: 'Set Type to "Entity", Entity Alias to "edge device", and Data Key to "distance". Click "Add".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-12.png',
    alt: 'Open the widget edit panel',
    caption: 'Click the edit icon in the upper right corner of the widget.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-13.png',
    alt: 'Select the Appearance tab',
    caption: 'Select the "Appearance" tab.',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-14.png',
    alt: 'Set max value to 10000',
    caption: 'Set Max value to "10000".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-15.png',
    alt: 'Set unit title to MLS',
    caption: 'Enter "MLS" as the unit title and click "Add".',
  },
  {
    src: '/src/assets/images/edge/use-cases/data-filtering/configure-dashboards-item-16.png',
    alt: 'Completed dashboard with distance gauge widget',
    caption: 'Dashboard widget showing the distance reading from the in-vehicle monitoring system.',
  },
]} />

You have successfully sent telemetry from the device, saved all sensor readings on the Edge, pushed only the filtered distance data to the cloud, and visualized it on the dashboard.
