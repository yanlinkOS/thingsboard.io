import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import Tabs from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';

A warehouse has two devices connected to ThingsBoard Edge: a **DHT22** temperature sensor and an **Air Conditioner**.
When the DHT22 reports a temperature above 50 °C, Edge raises an alarm and immediately sends an RPC command to turn on the Air Conditioner — all without depending on a cloud connection.

## Prerequisites

- Familiar with <DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="user-guide/device-profiles" target="_blank">Device Profiles</DocLink> and alarm rules.
- Familiar with <DocLink product={props.product} path="user-guide/rule-chain-templates" target="_blank">Edge Rule Chain Templates</DocLink>.
- ThingsBoard server is running and ThingsBoard Edge is connected to it.
- Node.js installed on the machine that will simulate the Air Conditioner.

## Configure alarm rules

Create a device profile named **"edge thermostat"** on the ThingsBoard server with an alarm rule that fires when `temperature > 50`.

<Steps>
1. Log in to the ThingsBoard server. Go to **Device profiles** and click **"+"** to add a new profile.
2. Enter the name **"edge thermostat"** and proceed to **Transport configuration** (keep defaults), then to **Alarm rules**.
3. Click **Add alarm rule**. Set the alarm type to **"High temperature"**, then click **"+"** to add a condition.
4. Click **Add key filter**. Set Key type to **Time series**, key name to **temperature**, value type to **Numeric**, and click **Add**.
5. Set the operation to **greater than**, threshold to **50**, and click **Add**.
6. Click **Save**, then **Add** to confirm the profile.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-1.png',
    alt: 'Open Device profiles on the ThingsBoard server',
    caption: 'Log in to the ThingsBoard server and open the Device profiles page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-2.png',
    alt: 'Add a new device profile',
    caption: 'Click "+" to add a new device profile.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-3.png',
    alt: 'Enter profile name edge thermostat',
    caption: 'Enter the name "edge thermostat", then proceed to Transport configuration.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-4.png',
    alt: 'Keep default transport config, proceed to Alarm rules',
    caption: 'Keep the default transport configuration and proceed to Alarm rules.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-5.png',
    alt: 'Add alarm rule',
    caption: 'Click "Add alarm rule".',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-6.png',
    alt: 'Set alarm type to High temperature and add condition',
    caption: 'Set alarm type to "High temperature" and click "+" to add a condition.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-7.png',
    alt: 'Add key filter',
    caption: 'Click "Add key filter".',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-8.png',
    alt: 'Configure key filter for temperature telemetry',
    caption: 'Set Key type to "Time series", key name to "temperature", value type to "Numeric", and click Add.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-9.png',
    alt: 'Set condition threshold greater than 50',
    caption: 'Set operation to "greater than", threshold to 50, and click Add.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-10.png',
    alt: 'Save the alarm condition',
    caption: 'Click Save.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-11.png',
    alt: 'Confirm the alarm rule',
    caption: 'Click Add to confirm.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-12.png',
    alt: 'New edge thermostat profile listed',
    caption: 'The "edge thermostat" profile appears at the top of the list.',
  },
]} />

Verify that the profile has been synchronized to the Edge instance:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-13.png',
    alt: 'Open Device profiles on the Edge instance',
    caption: 'Log in to the Edge instance and open the Device profiles page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/configure-rules-item-14.png',
    alt: 'edge thermostat profile appears on Edge',
    caption: 'Verify that "edge thermostat" has been provisioned to Edge.',
  },
]} />

## Provision devices

Create **DHT22** and **Air Conditioner** devices on the Edge and add a **Manages** relation from DHT22 to Air Conditioner. This relation is used by the rule engine to locate the Air Conditioner when DHT22 reports a critical temperature.

<Steps>
1. Log in to the Edge instance and go to **Entities → Devices**. Click **"+"**.
2. Enter the name **"DHT22"**, select **"edge thermostat"** as the device profile, and click **Add**.
3. Click **"+"** again. Enter the name **"Air Conditioner"** (no specific profile needed) and click **Add**.
4. Open the **DHT22** device, go to the **Relations** tab, and click **"+"**.
5. Set relation type to **Manages**, entity type to **Device**, select **"Air Conditioner"**, and click **Add**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-1.png',
    alt: 'Open Devices on the Edge instance',
    caption: 'Log in to the Edge instance and open the Devices page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-2.png',
    alt: 'Click + to add a new device',
    caption: 'Click "+" in the top right corner.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-3.png',
    alt: 'Add DHT22 with edge thermostat profile',
    caption: 'Enter "DHT22", select "edge thermostat" as the device profile, and click Add.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-4.png',
    alt: 'DHT22 appears in the devices table',
    caption: 'DHT22 appears at the top of the devices table.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-5.png',
    alt: 'Add another device',
    caption: 'Click "+" then "Add new device" to add the Air Conditioner.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-6.png',
    alt: 'Add Air Conditioner device',
    caption: 'Enter "Air Conditioner" and click Add.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-7.png',
    alt: 'Air Conditioner appears in the devices table',
    caption: 'Air Conditioner appears at the top of the devices table.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-8.png',
    alt: 'Add Manages relation from DHT22 to Air Conditioner',
    caption: 'Open DHT22, go to Relations, and click "+" to add a relation.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-9.png',
    alt: 'Configure Manages relation to Air Conditioner',
    caption: 'Set relation type to "Manages", entity type to "Device", select "Air Conditioner", and click Add.',
  },
]} />

Verify that both devices and the relation have been synchronized to the ThingsBoard server:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-10.png',
    alt: 'Open Devices on the ThingsBoard server',
    caption: 'Log in to the ThingsBoard server and open the Devices page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-11.png',
    alt: 'DHT22 and Air Conditioner listed on the server',
    caption: 'Both DHT22 and Air Conditioner appear in the devices list.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/provision-devices-item-12.png',
    alt: 'Manages relation from DHT22 to Air Conditioner synchronized',
    caption: 'The Manages relation from DHT22 to Air Conditioner has been synchronized.',
  },
]} />

## Configure the Edge rule engine

Update the **Edge Root Rule Chain** on the ThingsBoard server to handle **Alarm Created** events from DHT22 and send an RPC command to the Air Conditioner.

Here is the final rule chain configuration:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/root-rule-chain.png',
    alt: 'Final Edge Root Rule Chain configuration',
    caption: 'The updated Edge Root Rule Chain with transformation script, change originator, and RPC Call Request nodes.',
  },
]} />

The transformation script builds the RPC payload to enable the Air Conditioner:

```javascript
var newMsg = {};
newMsg.method = "enabled_air_conditioner";
newMsg.params = {"speed": 1.0};
return { msg: newMsg, metadata: metadata, msgType: msgType };
```

<Steps>
1. Log in to the ThingsBoard server. Go to **Edge management → Rule chain templates** and open **Edge Root Rule Chain**.
2. Search for **"script"** and drag a **Script** (Transformation) node onto the canvas. Enter the name **"Enable Air Conditioner"** and paste the TBEL snippet above. Click **Add**.
3. Drag a connection from **Device Profile Node** to the new script node. Select **Alarm Created** and click **Add**.
4. Click **Apply changes** to save progress.
5. Search for **"change"** and drag a **Change originator** node onto the canvas. Set the name to **"Switch to Air Conditioner"**, originator source to **Related entity**, direction to **Manages**, entity type to **Device**. Click **Add**.
6. Add a **Success** connection from the script node to **Change originator**, and a **Success** connection from **Change originator** to the **RPC Call Request** node. Save changes.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-1.png',
    alt: 'Open Rule chain templates on the server',
    caption: 'Log in to the ThingsBoard server and open Rule chain templates.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-2.png',
    alt: 'Open Edge Root Rule Chain',
    caption: 'Open the default Edge Root Rule Chain.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-3.png',
    alt: 'Add a Script transformation node',
    caption: 'Filter nodes by "script" and drag a Script (Transformation) node to the canvas.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-4.png',
    alt: 'Configure the transformation script',
    caption: 'Enter the node name and paste the TBEL script to build the RPC command. Click Add.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-5.png',
    alt: 'Connect Device Profile Node to the script node',
    caption: 'Drag a connection from Device Profile Node to the script node.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-6.png',
    alt: 'Select Alarm Created connection type',
    caption: 'Select "Alarm Created" and click Add.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-7.png',
    alt: 'Apply changes',
    caption: 'Click Apply changes to save progress.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-8.png',
    alt: 'Add a Change originator node',
    caption: 'Filter nodes by "change" and drag a Change originator node to the canvas.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-9.png',
    alt: 'Configure Change originator to switch to Air Conditioner',
    caption: 'Set name to "Switch to Air Conditioner", source to Related entity, direction to Manages, entity type to Device.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-10.png',
    alt: 'Connect script node to Change originator and then to RPC Call Request',
    caption: 'Add Success connections: script node → Change originator → RPC Call Request. Save changes.',
  },
]} />

Verify that the updated rule chain has been synchronized to the Edge instance:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-11.png',
    alt: 'Open Rule chains on the Edge instance',
    caption: 'Log in to the Edge instance and open the Rule chains page.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/update-root-item-12.png',
    alt: 'Edge Root Rule Chain matches the server configuration',
    caption: 'Open Edge Root Rule Chain to verify the changes have been synchronized.',
  },
]} />

## Subscribe Air Conditioner to RPC commands

Get the Air Conditioner access token from the Edge instance, then run the subscriber script.

<Steps>
1. In the Edge instance, go to **Entities → Devices**.
2. Click on **Air Conditioner** to open device details.
3. Click **Copy access token** and save it.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/copy-access-token-item-1.png',
    alt: 'Open Devices on the Edge instance',
    caption: 'Open the Devices page in the Edge instance.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/copy-access-token-item-2.png',
    alt: 'Open Air Conditioner device details',
    caption: 'Click on "Air Conditioner" to open device details.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/copy-access-token-item-3.png',
    alt: 'Copy the Air Conditioner access token',
    caption: 'Click "Copy access token" and save it.',
  },
]} />

Download the two scripts below, update the placeholders in `mqtt-js.sh`, then run them.

```bash download="mqtt-js.sh"
# Set ThingsBoard Edge host
export TB_EDGE_HOST=YOUR_TB_EDGE_HOST # e.g. localhost
# Set ThingsBoard Edge MQTT port
export TB_EDGE_MQTT_PORT=YOUR_TB_EDGE_MQTT_PORT # e.g. 11883 or 1883
# Set Air Conditioner device token
export ACCESS_TOKEN=YOUR_ACCESS_TOKEN
# Run cooler.js script
node cooler.js
```

```javascript download="cooler.js"
var mqtt = require('mqtt');

console.log('Connecting to: %s:%s using access token: %s',
    process.env.TB_EDGE_HOST, process.env.TB_EDGE_MQTT_PORT, process.env.ACCESS_TOKEN);

var client = mqtt.connect(
    'mqtt://' + process.env.TB_EDGE_HOST + ':' + process.env.TB_EDGE_MQTT_PORT,
    { username: process.env.ACCESS_TOKEN }
);

client.on('connect', function () {
    client.subscribe('v1/devices/me/rpc/request/+');
    console.log('Cooler is connected!');
});

client.on('message', function (topic, message) {
    var tmp = JSON.parse(message.toString());
    console.log('Received RPC command from edge!');
    console.log('Method:', tmp.method);
    console.log('Speed params:', tmp.params.speed);
});

process.on('SIGINT', function () {
    console.log('Disconnecting...');
    client.end();
    process.exit(2);
});
```

Install the mqtt package and start the subscriber:

```bash
npm install mqtt --save
chmod +x mqtt-js.sh
bash mqtt-js.sh
```

You should see:

```
Connecting to: localhost:1883 using access token: <your_token>
Cooler is connected!
```

<Aside type="note">
  Leave this terminal running in the background. Open a new terminal tab for the next step.
</Aside>

## Post telemetry to trigger the alarm

Get the DHT22 access token from the Edge instance.

<Steps>
1. In the Edge instance, go to **Entities → Devices**.
2. Click on **DHT22** to open device details.
3. Click **Copy access token** and save it.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/copy-access-token-item-1.png',
    alt: 'Open Devices on the Edge instance',
    caption: 'Open the Devices page in the Edge instance.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/copy-access-token-item-4.png',
    alt: 'Open DHT22 device details',
    caption: 'Click on "DHT22" to open device details.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/copy-access-token-item-5.png',
    alt: 'Copy the DHT22 access token',
    caption: 'Click "Copy access token" and save it.',
  },
]} />

Publish a temperature reading above the threshold to trigger the **High temperature** alarm. Replace `$ACCESS_TOKEN` with the DHT22 token and `$EDGE_HOST` with your Edge host.

<Tabs>
<TabItem label="HTTP">

```bash
curl -v -X POST \
  -d '{"temperature": 51}' \
  http://$EDGE_HOST:8080/api/v1/$ACCESS_TOKEN/telemetry \
  --header "Content-Type:application/json"
```

</TabItem>
<TabItem label="MQTT">

```bash
mosquitto_pub -d -q 1 \
  -h "$EDGE_HOST" -p 1883 \
  -t "v1/devices/me/telemetry" \
  -u "$ACCESS_TOKEN" \
  -m '{"temperature": 51}'
```

</TabItem>
<TabItem label="CoAP">

```bash
echo -n '{"temperature": 51}' | coap post coap://$EDGE_HOST:5683/api/v1/$ACCESS_TOKEN/telemetry
```

</TabItem>
</Tabs>

The alarm should appear immediately in the DHT22 device alarm tab:

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/copy-access-token-item-4.png',
    alt: 'Click on the DHT22 device row',
    caption: 'Click on DHT22 in the devices table to view device details.',
  },
  {
    src: '/src/assets/images/edge/use-cases/manage-alarms/device-alarm-tab-item-1.png',
    alt: 'High temperature alarm visible in the Alarms tab',
    caption: 'The "High temperature" alarm appears in the Alarms tab.',
  },
]} />

## Verify the RPC command

Switch back to the terminal running `mqtt-js.sh`. You should see the RPC command received by the Air Conditioner:

```
Received RPC command from edge!
Method: enabled_air_conditioner
Speed params: 1
```

The Edge detected the temperature alarm, used the rule chain to build an RPC command, switched the originator to the Air Conditioner via the Manages relation, and delivered the command — without any cloud involvement.
