import UpgradeTable from '~/components/UpgradeTable.astro';

<UpgradeTable product={props.product} />

For upgrade instructions for versions older than 3.0, see the [legacy upgrade guide](/docs/installation/upgrade-instructions/).
