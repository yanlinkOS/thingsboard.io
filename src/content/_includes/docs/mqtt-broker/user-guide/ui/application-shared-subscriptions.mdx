import { Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

The Application Shared Subscription entity enables the <DocLink product={props.product} path="user-guide/shared-subscriptions">Shared Subscriptions</DocLink> feature for **APPLICATION** clients,
allowing multiple clients to subscribe and receive messages from a shared subscription.

## Usage notes

- Create an Application Shared Subscription entity when you plan to use shared subscriptions with <DocLink product={props.product} path="user-guide/mqtt-client-type/#application-client">APPLICATION clients</DocLink>.
- After creation, the **Topic filter** and **Partitions** fields **cannot be changed**.
- Shared subscriptions work with **MQTT 5 and earlier versions**.

Administrators can manage shared subscriptions via the Web UI or <DocLink product={props.product} path="application-shared-subscription">REST API</DocLink>.

## Add shared subscription

1. Open the **Shared Subscriptions** page and click the **plus icon**.
2. Fill in the following fields:
   - **Name** — any display name, e.g., "Application Shared Subscription".
   - **Topic filter** — the actual MQTT topic filter (wildcards `#` and `+` are supported). If your full shared subscription topic filter is `$share/group1/city/+/home/#`, set this field to `city/+/home/#`. **Cannot be changed after creation and must be unique.**
   - **Partitions** — set to equal or a multiple of the expected number of subscriber clients for even load distribution. For example, if 5 clients will subscribe, use 5, 10, or 15. **Cannot be changed after creation.**
3. Click **Add**.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/add-shared-subscriptions-1.png', alt: 'Open Shared Subscriptions page and click the plus icon', caption: 'Open Shared Subscriptions page and click on the "plus" icon.' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/add-shared-subscriptions-2.png', alt: 'Fill in Name, Topic filter, and Partitions fields', caption: 'Fill in the Name, Topic filter and Partitions fields.' },
]} />

After creation, a dedicated Kafka topic is created using the following naming convention:

| MQTT topic filter | Kafka topic |
|---|---|
| `test/topic` | `tbmq.msg.app.shared.test.topic` |
| `test/#` | `tbmq.msg.app.shared.test.mlw` |
| `test/+` | `tbmq.msg.app.shared.test.slw` |

Where `/` → `.`, `#` → `mlw` (multi-level wildcard), `+` → `slw` (single-level wildcard).

If the topic filter contains special characters beyond alphanumerics, a hash is used instead:

```
tbmq.msg.app.shared.$TOPIC_FILTER_HASH
```

This behavior is controlled by `TB_APP_PERSISTED_MSG_SHARED_TOPIC_VALIDATION` (enabled by default). When disabled, Kafka topics are not created for topic filters with special characters.

## Edit shared subscription

Only the **Name** field can be edited after creation.

1. Click on the shared subscription row.
2. Click the **Toggle edit mode** button (pencil icon).
3. Modify the name and click **Apply changes**.

## Delete shared subscription

1. **Delete single** — click the **Delete** icon in the corresponding row, or open the entity and click the **Delete** button.
2. **Delete multiple** — select multiple items using checkboxes, then click the **Delete** icon in the top right corner.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/delete-shared-subscriptions-1.png', alt: 'Click Delete icon in the subscription row to delete single', caption: 'Delete single. Click on the icon "Delete".' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/delete-shared-subscriptions-2.png', alt: 'Select multiple subscriptions and click Delete in the top right corner', caption: 'Delete multiple. Select subscriptions you want to delete and click on the "Delete" icon in the top right corner.' },
]} />

<Aside type="caution">
  When a shared subscription is deleted, the corresponding Kafka topic is also deleted. This requires <code>TB_KAFKA_ENABLE_TOPIC_DELETION=true</code>.
</Aside>
