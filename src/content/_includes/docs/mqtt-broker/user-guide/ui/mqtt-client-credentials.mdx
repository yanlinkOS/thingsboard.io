import { Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

TBMQ provides multiple options for managing MQTT client credentials via the Web UI or <DocLink product={props.product} path="mqtt-client-credentials-management">REST API</DocLink>.

TBMQ supports the following credential types for authenticating MQTT client connections:

- **Basic** — authentication based on combinations of client ID, username, and password.
  - _Advantages:_ Quick to implement, widely supported, low network overhead.
  - _Disadvantages:_ Limited security.
- **X.509 Certificate Chain** — advanced security using an X.509 certificate chain to verify client identity.
  - _Advantages:_ Mutual authentication between client and broker; more granular access control based on certificate subject attributes.
  - _Disadvantages:_ More complex to set up, requires more expertise, higher compute cost for encryption/decryption.
- **SCRAM** — Salted Challenge Response Authentication Mechanism; secure, password-based authentication using the MQTT 5.0 enhanced authentication feature.
  - _Advantages:_ Higher security than basic auth — uses a challenge-response process with hashed credentials so the password is never sent in plain text.
  - _Disadvantages:_ Additional computational resources required for salted hash generation and validation.

Before using any of these credential types, ensure the appropriate authentication method is <DocLink product={props.product} path="security/authentication/basic">enabled</DocLink>.
For a complete security overview, see the <DocLink product={props.product} path="security/overview">Security guide</DocLink>.

## Add client credentials

1. Go to **Authentication → Credentials** and click the **Add Client Credentials** button (plus icon).
2. Fill in the **Name** field (does not need to be unique).
3. Select the **Client Type**:
   - **Device** — for clients that publish frequently but subscribe to few topics with low message rate (IoT devices).
   - **Application** — for clients that subscribe to high message rate topics and require offline persistence (e.g., ThingsBoard, AWS IoT Core).

   See <DocLink product={props.product} path="user-guide/mqtt-client-type">Client types</DocLink> for details.
4. Select the **Credentials Type** and configure authentication and authorization settings.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/add-client-credentials-1.png', alt: 'Go to Credentials page and click Add Client Credentials', caption: 'Go to the Credentials page and click the Add Client Credentials button, represented by a "plus" icon.' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/add-client-credentials-2.png', alt: 'Fill in Name and Credentials Type', caption: 'Fill in the Name, Credentials Type.' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/add-client-credentials-3.png', alt: 'Fill in Authentication settings', caption: 'Fill in the Authentication settings.' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/add-client-credentials-4.png', alt: 'Set Authorization rules', caption: 'Set Authorization rules.' },
]} />

### MQTT Basic credentials

#### Authentication

Basic authentication is based on combinations of client ID, username, and/or password:

| Option | Verification |
|---|---|
| **Client ID** | Verifies the connecting client has the specified client ID. |
| **Username** | Verifies the connecting client has the specified username. |
| **Client ID and username** | Verifies both client ID and username. |
| **Username and password** | Verifies username and password. |
| **Client ID and password** | Verifies client ID and password. |
| **Client ID, username, and password** | Verifies all three. |

#### Authorization rules

Authorization rule patterns control which topics clients can publish or subscribe to, using **regular expression syntax**:

- `country/.*` — allows clients to publish/subscribe only to topics starting with `country/`.
- `.*` (default) — allows any topic.
- Empty — forbids all topics.

#### Change password

To change the password for Basic credentials:

1. Go to **Authentication → Client Credentials** and click on the credentials row.
2. Click the **Change password** button.
3. Enter the current password, set a new one, and confirm.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/change-password-1.png', alt: 'Go to Credentials page, click on the row and click Edit', caption: 'Go to the Credentials page, click on the corresponding row and click the Edit button.' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/change-password-2.png', alt: 'Click the Change password button', caption: 'Click the Change password button.' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/change-password-3.png', alt: 'Enter current password, set a new one and click Change password', caption: 'Input your current password and set a new one. Click the Change password button.' },
]} />

### X.509 Certificate Chain credentials

**X.509 Certificate Chain** is a mutual TLS authentication method using a chain of public-key certificates.

#### Authentication

The behavior depends on the **Use certificate CN regex** parameter:

- **Disabled (exact match)** — the Certificate Common Name (CN) must exactly match the client's or one of the parent's certificate CN.
- **Enabled (regex match)** — the CN regex is matched against the client's certificate CN or parent CAs. Provides flexibility for batch client verification.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/features/security-authentication-tls-2.png', alt: 'TLS authentication with Use certificate CN regex disabled — exact CN match', caption: 'TLS authentication with "Use certificate CN regex" disabled to match certificate common name by exact match.' },
  { src: '/src/assets/images/docs/mqtt-broker/features/security-authentication-tls-3.png', alt: 'TLS authentication with Use certificate CN regex enabled — regex CN match', caption: 'TLS authentication with "Use certificate CN regex" enabled to match certificate common name using a regex pattern.' },
]} />

#### Authorization rules

Authorization rules control topics based on the **Client certificate CN matcher regex** combined with publish/subscribe rule patterns:

- If the client certificate has CN `cn_example_1` — set the CN matcher regex to `cn_example_1`, `example_1`, or `.*example.*`.
- If Publish authorization rule patterns are set to `pub_topic/one/.*, pub_topic/two/.*` — the client can only publish to topics starting with `pub_topic/one/` or `pub_topic/two/`.
- If Subscribe authorization rule patterns are set to `.*` (default) — the client can subscribe to any topic.
- If Publish/Subscribe authorization rules are empty — the client is forbidden from publishing/subscribing to any topic.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ssl-credentials-authorization.png', alt: 'X.509 credentials authorization rules configuration', caption: 'Examples of rule patterns for allowing all or forbidding all.' },
]} />

### SCRAM

**SCRAM** (Salted Challenge Response Authentication Mechanism) is an enhanced MQTT 5 authentication method based on username and password. The password is salted and hashed before storage — never transmitted in plain text.

- **Username** — unique identifier for the client.
- **Password** — secret string; salted and hashed before storage.

#### Authorization rules

Authorization rule patterns control which topics clients can publish or subscribe to, using **regular expression syntax**:

- `country/.*` — allows clients to publish/subscribe only to topics starting with `country/`.
- `.*` (default) — allows any topic.
- Empty — forbids all topics.

## Delete client credentials

1. **Delete single** — click the **Delete** icon in the credentials row, or open the credentials and click **Delete Client Credentials**.
2. **Delete multiple** — select multiple credentials using checkboxes, then click the **Delete** icon in the top right corner.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/delete-credentials-1.png', alt: 'Click Delete icon in the credentials row to delete single credentials', caption: 'Delete single. Click on the icon "Delete".' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/delete-credentials-2.png', alt: 'Select multiple credentials and click Delete in the top right corner', caption: 'Delete multiple. Click on the checkboxes and confirm action by clicking on the button "Delete" in the top right corner.' },
]} />

## Check connectivity

**Check Connectivity** auto-generates `mosquitto_sub` and `mosquitto_pub` commands using the broker's host, port, and the selected client credentials. Available for **Basic** credentials only.

1. Click the **Check connectivity** button.
2. Select your operating system.
3. Install the required client tools using the command shown.
4. Copy and run the generated commands.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/check-connectivity-1.png', alt: 'Click Check connectivity button on Basic client credentials', caption: 'Click on the button "Check connectivity" of the Basic client credentials.' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/check-connectivity-2.png', alt: 'Select OS, install tools, and copy the generated commands', caption: 'Select your operating system, install required tools, and copy commands.' },
]} />

If the credentials include a password, replace `$YOUR_PASSWORD` with the actual password:

```bash
mosquitto_sub -d -q 1 -h localhost -p 1883 -t tbmq/demo/+ -i "tbmq_eJzCIh6r" -u "tbmq_un_VxUVPaF8" -P "$YOUR_PASSWORD" -v
mosquitto_pub -d -q 1 -h localhost -p 1883 -t tbmq/demo/topic -i "tbmq_eJzCIh6r" -u "tbmq_un_VxUVPaF8" -P "$YOUR_PASSWORD" -m 'Hello World'
```

If the client has configured publish/subscribe authorization rule patterns different from the default `.*`, replace `$YOUR_TOPIC` with a topic the client is permitted to use.
