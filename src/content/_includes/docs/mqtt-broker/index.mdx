import { CardGrid } from '@astrojs/starlight/components'
import Card from '~/components/Landing/Card.astro'
import ListCard from '~/components/Landing/ListCard.astro'
import SplitCard from '~/components/Landing/SplitCard.astro'
import DocLink from '@components/DocLink.astro';
import Button from '~/components/Button.astro';

<CardGrid>
  <SplitCard title="Connect in minutes" icon="rocket">
    <div>
      Use the free public MQTT broker — no installation required.

      **Host:** `demo.tbmq.io`<br />
      **TCP Port:** `1883`<br />
      **TLS Port:** `8883`<br />
      **Username:** `demo`<br />
      **Password:** *(leave empty)*

      <Button link="https://demo.tbmq.io/signup" variant="secondary" target="_blank">Sign up for free</Button>
    </div>

    <div>
      ```bash
      # Subscribe to a topic
      mosquitto_sub -d -h demo.tbmq.io -p 1883 -q 1 \
        -t sensors/temperature -u demo
      ```

      ```bash
      # Publish a message
      mosquitto_pub -d -h demo.tbmq.io -p 1883 -q 1 \
        -t sensors/temperature -m 25 -u demo
      ```
    </div>
  </SplitCard>

  <ListCard title="Architecture" icon="open-book">
    <DocLink product={props.product} path="architecture" bold={false}>TBMQ architecture</DocLink> — distributed cluster design built for scale and resilience.

    - <DocLink product={props.product} path="reference/1m-throughput-p2p-performance-test" bold={false}>Point-to-point: 1M Msg/sec throughput</DocLink>
    - <DocLink product={props.product} path="reference/3m-throughput-single-node-performance-test" bold={false}>Fan-out: 3M Msg/sec throughput</DocLink>
    - <DocLink product={props.product} path="reference/100m-connections-performance-test" bold={false}>Fan-in: 100M MQTT connections</DocLink>
  </ListCard>

  <ListCard title="Install TBMQ" icon="laptop">
    - <DocLink product={props.product} path="install/installation-options" bold={false}>All installation options</DocLink>
    - <DocLink product={props.product} path="install/cluster/docker-compose-setup" bold={false}>Docker Compose cluster</DocLink>
    - <DocLink product={props.product} path="install/installation-options/?installationType=cloud" bold={false}>Cloud (AWS, Azure, GCP)</DocLink>
    - <DocLink product={props.product} path="install/installation-options/?installationType=helm" bold={false}>Kubernetes with Helm</DocLink>
    - <DocLink product={props.product} path="install/config" bold={false}>Configuration reference</DocLink>
  </ListCard>

  <ListCard title="Security" icon="tabler:shield-lock">
    - <DocLink product={props.product} path="security/authentication/basic" bold={false}>Basic authentication</DocLink>
    - <DocLink product={props.product} path="security/authentication/x509" bold={false}>X.509 certificate authentication</DocLink>
    - <DocLink product={props.product} path="security/authentication/jwt" bold={false}>JWT authentication</DocLink>
    - <DocLink product={props.product} path="security/authentication/http" bold={false}>HTTP authentication</DocLink>
    - <DocLink product={props.product} path="security/overview#authorization" bold={false}>Client authorization</DocLink>
  </ListCard>

  <ListCard title="Client management" icon="puzzle">
    - <DocLink product={props.product} path="user-guide/ui/sessions" bold={false}>Sessions</DocLink>
    - <DocLink product={props.product} path="user-guide/ui/subscriptions" bold={false}>Subscriptions</DocLink>
    - <DocLink product={props.product} path="user-guide/ui/mqtt-client-credentials" bold={false}>Client credentials</DocLink>
    - <DocLink product={props.product} path="user-guide/ui/monitoring" bold={false}>Broker monitoring</DocLink>
    - <DocLink product={props.product} path="user-guide/ui/websocket-client" bold={false}>WebSocket client</DocLink>
  </ListCard>

</CardGrid>
