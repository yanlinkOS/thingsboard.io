import { Aside, Steps, Code } from '@astrojs/starlight/components';
import Tabs from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';
import DocLink from '@components/DocLink.astro';
import ConditionalHeading from '~/components/ConditionalHeading.astro';
import { Products } from '~/models/site.models';
import { TBMQ_VER, TBMQ_PE_VER, TBMQ_BRANCH } from '~/data/versions';

export const pullCmdCe = `docker pull thingsboard/tbmq-node:${TBMQ_VER}
docker pull thingsboard/tbmq-integration-executor:${TBMQ_VER}`;

export const pullCmdPe = `docker pull thingsboard/tbmq-pe-node:${TBMQ_PE_VER}
docker pull thingsboard/tbmq-pe-integration-executor:${TBMQ_PE_VER}`;

export const cloneCmdCe = `git clone -b ${TBMQ_BRANCH} https://github.com/thingsboard/tbmq.git
cd tbmq/docker`;

export const cloneCmdPe = `git clone -b ${TBMQ_BRANCH} https://github.com/thingsboard/tbmq-pe-docker-compose.git
cd tbmq-pe-docker-compose/cluster`;

export const gitPullCmd = `git pull origin ${TBMQ_BRANCH}`;
export const gitPullXTheirsCmd = `git pull origin ${TBMQ_BRANCH} -X theirs`;

export const upgradeWithoutFromVersion = `./scripts/docker-stop-services.sh
./scripts/docker-upgrade-tbmq.sh
./scripts/docker-start-services.sh`;

export const upgradeWithFromVersion = `./scripts/docker-stop-services.sh
./scripts/docker-upgrade-tbmq.sh --fromVersion=FROM_VERSION
./scripts/docker-start-services.sh`;

This guide covers setting up TBMQ in cluster mode using Docker Compose.

## Prerequisites

- [Docker](https://docs.docker.com/engine/installation/) installed.

<Aside type="caution">
  Add your Linux user to the `docker` group before running Docker commands.
  See [Manage Docker as a non-root user](https://docs.docker.com/engine/install/linux-postinstall/#manage-docker-as-a-non-root-user).
</Aside>

## Pull TBMQ Image

Make sure you have [logged in](https://docs.docker.com/engine/reference/commandline/login/) to Docker Hub from the command line.

{props.product !== Products.TBMQ_PE && <Code code={pullCmdCe} lang="bash" />}
{props.product === Products.TBMQ_PE && <Code code={pullCmdPe} lang="bash" />}

{props.product !== Products.TBMQ_PE && (
  <>
    <ConditionalHeading level={2} id="clone-tbmq-repository">Clone TBMQ repository</ConditionalHeading>
    <Code code={cloneCmdCe} lang="bash" />
  </>
)}
{props.product === Products.TBMQ_PE && (
  <>
    <ConditionalHeading level={2} id="clone-tbmq-pe-repository">Clone TBMQ PE Docker Compose repository</ConditionalHeading>
    <Code code={cloneCmdPe} lang="bash" />
  </>
)}

{props.product === Products.TBMQ_PE && (
  <>
    <ConditionalHeading level={2} id="get-the-license-key">Get the license key</ConditionalHeading>
    <p>Before proceeding, select a subscription plan or purchase a perpetual license on the{' '}
    <a href="https://thingsboard.io/pricing/?section=tbmq-options" target="_blank" rel="noopener noreferrer">Pricing page</a>.</p>

    <ConditionalHeading level={2} id="configure-the-license-key">Configure the license key</ConditionalHeading>
    <p>Open the <code>tbmq.env</code> file:</p>
    <Code code="nano tbmq.env" lang="bash" />
    <p>Replace <strong>YOUR_LICENSE_KEY_HERE</strong> with your actual license key:</p>
    <Code code={`# License info\nTBMQ_LICENSE_SECRET=YOUR_LICENSE_KEY_HERE`} lang="text" />
    <Aside type="caution">The broker will not start without a valid license key.</Aside>
  </>
)}

## Installation

Create the necessary Docker volumes and update the HAProxy config in the created volume:

```bash
./scripts/docker-create-volumes.sh
```

Run the installation:

```bash
./scripts/docker-install-tbmq.sh
```

## Running

Start all services:

```bash
./scripts/docker-start-services.sh
```

Once all services have started, open `http://{your-host-ip}` in your browser (e.g. **http://localhost**) and connect MQTT clients on port 1883.

You should see the TBMQ login page. Use the default **System Administrator** credentials:

- **Username:** `sysadmin@thingsboard.org`
- **Password:** `sysadmin`

On first login you will be prompted to change the default password.

## Logs, stop and start commands

To view TBMQ node logs:

```bash
docker compose logs -f tbmq1
```

To view the state of all containers:

```bash
docker compose ps
```

To inspect logs of all running services:

```bash
docker compose logs -f
```

See [docker compose logs](https://docs.docker.com/compose/reference/logs/) reference for more details.

To stop services:

```bash
./scripts/docker-stop-services.sh
```

To stop and completely remove deployed containers:

```bash
./scripts/docker-remove-services.sh
```

To remove Docker volumes for all containers (**warning: deletes all data**):

```bash
./scripts/docker-remove-volumes.sh
```

To apply config changes at runtime (e.g. edits to `haproxy.cfg` or `logback.xml`):

```bash
./scripts/docker-refresh-config.sh
```

To reload HAProxy configuration without restarting the container, send a HUP signal:

```bash
docker exec -it haproxy-certbot sh -c "kill -HUP 1"
```

## Upgrading

Review the <DocLink product={props.product} path="releases">release notes</DocLink> and{' '}
<DocLink product={props.product} path="install/upgrade-instructions">upgrade instructions</DocLink> for details on the latest changes.

{props.product !== Products.TBMQ_PE && (
  <p>If there are no <strong>Upgrade to x.x.x</strong> notes for your version, proceed directly with the general <a href="#run-upgrade">upgrade instructions</a>.</p>
)}

If your specific upgrade path is not covered, <DocLink product={props.product} path="help">contact us</DocLink> for further guidance.

### Backup and restore (Optional)

While backing up your PostgreSQL database before upgrading is recommended, it is optional.
For guidance, follow the{' '}
<a
  href={props.product !== Products.TBMQ_PE
    ? 'https://github.com/thingsboard/tbmq/blob/main/docker/backup-restore/README.md'
    : 'https://github.com/thingsboard/tbmq-pe-docker-compose/blob/master/cluster/backup-restore/README.md'}
  target="_blank"
  rel="noopener noreferrer"
>next instructions</a>.

{props.product !== Products.TBMQ_PE && (
  <>
    <ConditionalHeading level={3} id="upgrade-to-2-2-0">Upgrade to 2.2.0</ConditionalHeading>
    <p>
      This release migrates the MQTT authentication mechanism from YAML/env configuration into the database.
      During the upgrade, TBMQ needs to know which authentication providers are enabled in your deployment — these
      are passed as environment variables to the upgrade container.
    </p>
    <p>
      The upgrade script reads from the <strong>tb-mqtt-broker.env</strong> file. Environment variables in{' '}
      <code>docker-compose.yml</code> are <strong>not</strong> applied during the upgrade — only the values in{' '}
      <code>tb-mqtt-broker.env</code> are used. Make sure this file reflects your active configuration.
    </p>
    <Aside>
      <ul>
        <li>If you use only Basic authentication, set <code>SECURITY_MQTT_SSL_ENABLED=false</code>.</li>
        <li>If you use only X.509 authentication, set <code>SECURITY_MQTT_BASIC_ENABLED=false</code> and <code>SECURITY_MQTT_SSL_ENABLED=true</code>.</li>
      </ul>
    </Aside>
    <p><strong>Supported variables in tb-mqtt-broker.env</strong></p>
    <ul>
      <li><code>SECURITY_MQTT_BASIC_ENABLED</code> (<code>true|false</code>)</li>
      <li><code>SECURITY_MQTT_SSL_ENABLED</code> (<code>true|false</code>)</li>
      <li><code>SECURITY_MQTT_SSL_SKIP_VALIDITY_CHECK_FOR_CLIENT_CERT</code> (<code>true|false</code>) — usually <code>false</code></li>
    </ul>
    <p>Once the file is verified, proceed with the <a href="#run-upgrade">upgrade process</a>.</p>

    <ConditionalHeading level={3} id="upgrade-to-2-1-0">Upgrade to 2.1.0</ConditionalHeading>
    <p>TBMQ v2.1.0 introduces the Integration Executor microservice and bumps versions of third-party dependencies.</p>
    <ConditionalHeading level={4} id="add-integration-executor">Add Integration Executor microservice</ConditionalHeading>
    <p>
      This release adds support for external integrations via the new{' '}
      <a href="/docs/mqtt-broker/integrations/">Integration Executor</a> microservice.
    </p>
    <p>
      For the complete updated <code>docker-compose.yml</code>, see the{' '}
      <a href="https://github.com/thingsboard/tbmq/blob/release-2.1.0/docker/docker-compose.yml" target="_blank" rel="noopener noreferrer">official example</a>.
    </p>
    <p>
      To retrieve the latest configuration files including Integration Executor configs, pull the updates from the release
      branch by following the <a href="#run-upgrade">run upgrade steps</a> up to (but not including) the <code>.sh</code> commands.
      Then create the required Docker volumes:
    </p>
    <Code code="./scripts/docker-create-volumes.sh" lang="bash" />
    <p>You should see output similar to:</p>
    <Code code={`user@user:~/tbmq/docker$ ./scripts/docker-create-volumes.sh\n\ntbmq-ie1-logs\ntbmq-ie2-logs\ntbmq-ie-config`} lang="text" />
    <ConditionalHeading level={4} id="update-third-party-services">Update third-party services</ConditionalHeading>
    <p>
      TBMQ v2.1.0 updates the following third-party dependencies. See the{' '}
      <a href="https://github.com/thingsboard/tbmq/pull/218" target="_blank" rel="noopener noreferrer">pull request</a> for details.
    </p>
    <table>
      <thead><tr><th>Service</th><th>Previous version</th><th>Updated version</th></tr></thead>
      <tbody>
        <tr><td><strong>Redis</strong></td><td>7.0</td><td>7.2.5</td></tr>
        <tr><td><strong>PostgreSQL</strong></td><td>15.x</td><td>16.x</td></tr>
        <tr><td><strong>Kafka</strong></td><td>3.5.1</td><td>3.7.0</td></tr>
      </tbody>
    </table>
    <Aside>
      Aligning with the updated third-party versions is recommended for full compatibility.
      Simply changing the image tag is not sufficient — consult the official documentation of each service.
      If you skip updating third-party versions, check whether the merge process changed any of their versions and revert if needed.
    </Aside>
    <p>After addressing third-party service versions, continue with the remaining <a href="#run-upgrade">upgrade steps</a>.</p>
  </>
)}

{props.product === Products.TBMQ_PE && (
  <>
    <ConditionalHeading level={3} id="upgrade-from-ce-to-pe">Upgrade from TBMQ to TBMQ PE (v2.2.0)</ConditionalHeading>
    <p>
      To upgrade an existing TBMQ installation to TBMQ PE, ensure you are running the latest TBMQ{' '}
      <strong>{TBMQ_VER}</strong> version before starting. Merge your current configuration with the latest{' '}
      <a href={`https://github.com/thingsboard/tbmq-pe-docker-compose/tree/${TBMQ_BRANCH}`} target="_blank" rel="noopener noreferrer">TBMQ PE Docker Compose scripts</a>.
      Don't forget to <a href="#configure-the-license-key">configure the license key</a>.
    </p>
    <p>Run the following commands to stop services, migrate the database, and restart:</p>
    <Code code={`./scripts/docker-stop-services.sh\n./scripts/docker-upgrade-tbmq.sh --fromVersion=ce\n./scripts/docker-start-services.sh`} lang="bash" />
  </>
)}

{props.product !== Products.TBMQ_PE && (
  <>
    <ConditionalHeading level={3} id="run-upgrade">Run upgrade</ConditionalHeading>
    <p>Pull the latest changes from the release branch:</p>
    <Code code={gitPullCmd} lang="bash" />
    <Aside>
      To upgrade to a specific version (e.g. v2.0.0), replace the branch with the target branch name, e.g. <code>release-2.0.0</code>.
    </Aside>
    <p>
      <strong>Note:</strong> Make sure any custom changes are not lost during the merge. Also ensure{' '}
      <code>TBMQ_VERSION</code> in the <code>.env</code> file is set to the target version (e.g. <strong>{TBMQ_VER}</strong> for the latest).
    </p>
    <Aside>
      <p>If you encounter conflicts during the merge that are unrelated to your changes, accept all remote changes:</p>
      <ol>
        <li>Abort the current merge: <code>git merge --abort</code></li>
        <li>Re-pull accepting remote changes: <code>git pull origin {TBMQ_BRANCH} -X theirs</code></li>
      </ol>
      <p>Merge strategy options:</p>
      <ul>
        <li><code>-X ours</code> — auto-resolve conflicts by favoring your local version</li>
        <li><code>-X theirs</code> — auto-resolve conflicts by favoring the remote version</li>
      </ul>
    </Aside>
    <p>After pulling, execute the upgrade commands:</p>
  </>
)}

{props.product !== Products.TBMQ_PE && (
  <Tabs>
    <TabItem label="Since v2.1.0">
      <Code code={upgradeWithoutFromVersion} lang="bash" />
    </TabItem>
    <TabItem label="Before v2.1.0">
      <Code code={upgradeWithFromVersion} lang="bash" />
      <p>Where <code>FROM_VERSION</code> is the version you are upgrading from.
      See <DocLink product={props.product} path="install/upgrade-instructions">Upgrade Instructions</DocLink> for valid values.</p>
    </TabItem>
  </Tabs>
)}

## Generate certificate for HTTPS

HAProxy handles traffic proxying and serves the web UI on ports 80 and 443 by default.
To enable HTTPS with a valid certificate, run:

```bash
docker exec haproxy-certbot certbot-certonly --domain your_domain --email your_email
docker exec haproxy-certbot haproxy-refresh
```

<Aside>
  The valid certificate is only used when accessing the web UI via a domain name URL.
</Aside>

## Enable MQTTS (MQTT over SSL/TLS)

MQTTS allows clients to connect over an encrypted TLS/SSL channel. There are two common approaches:

- **Two-way MQTTS (Mutual TLS)** — TBMQ terminates TLS; clients must present valid certificates.
- **One-way MQTTS (TLS termination at load balancer)** — HAProxy handles TLS termination and forwards plain MQTT to TBMQ over the internal network.

Both approaches encrypt external connections. Two-way MQTTS adds client certificate verification for higher security; one-way MQTTS simplifies broker configuration and can reuse existing HTTPS certificates.

### Two-way MQTTS

TBMQ handles TLS termination and optional client certificate authentication directly.
For supported certificate formats, see the <DocLink product={props.product} path="security/mqtts">MQTT over SSL</DocLink> guide.

**Provide SSL certificates**

Obtain a valid SSL certificate and private key:

- `mqttserver.pem` — public certificate (may include the full chain)
- `mqttserver_key.pem` — private key

<Aside>
  Self-signed certificates are supported for testing. Use certificates from a trusted Certificate Authority in production.
</Aside>

**Mount certificates into the container**

In `docker-compose.yml`, uncomment and update the volume mount:

```yaml
volumes:
  - PATH_TO_CERTS:/config/certificates
```

Replace `PATH_TO_CERTS` with the path to your certificate files. Ensure TBMQ has read access.

**Configure environment variables**

Edit the <code>{props.product !== Products.TBMQ_PE ? 'tb-mqtt-broker.env' : 'tbmq.env'}</code> file and uncomment/configure the following lines:

```text
LISTENER_SSL_ENABLED=true
LISTENER_SSL_PEM_CERT=/config/certificates/mqttserver.pem
LISTENER_SSL_PEM_KEY=/config/certificates/mqttserver_key.pem
LISTENER_SSL_PEM_KEY_PASSWORD=server_key_password
```

Leave `LISTENER_SSL_PEM_KEY_PASSWORD` empty if your private key has no password.

**Restart services**

```bash
./scripts/docker-start-services.sh
```

Once restarted, MQTT clients can connect securely on **port 8883**.

### One-way MQTTS

TLS is terminated at HAProxy. Clients connect to HAProxy on port 8883, which forwards plain MQTT to TBMQ on port 1883.
You can reuse the same certificate used for HTTPS.

Point HAProxy to your certificate bundle (PEM with full chain + private key) by updating `haproxy.cfg`:

```text
 listen mqtts-in
  bind *:${MQTTS_PORT} ssl crt /usr/local/etc/haproxy/certs.d/fullchain-and-key.pem
  mode tcp
  option clitcpka # For TCP keep-alive
  timeout client 3h
  timeout server 3h
  option tcplog
  balance leastconn
  server tbMqtt1 tbmq1:1883 check inter 5s resolvers docker_resolver resolve-prefer ipv4
  server tbMqtt2 tbmq2:1883 check inter 5s resolvers docker_resolver resolve-prefer ipv4
```

Replace `fullchain-and-key.pem` with the actual filename of your certificate bundle.
