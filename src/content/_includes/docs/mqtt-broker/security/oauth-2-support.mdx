import { Aside, Steps } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

TBMQ allows you to provide Single Sign-On (SSO) functionality for your users and automatically create Administrators or Viewers using external user management platforms that support the OAuth 2.0 protocol.
Examples of platforms that support OAuth 2.0 include: [Google](#login-with-google), [Auth0](#login-with-auth0), [Keycloak](#login-with-keycloak), <a href="https://www.okta.com/" target="_blank" rel="noopener noreferrer">Okta</a>, <a href="https://portal.azure.com/" target="_blank" rel="noopener noreferrer">Azure</a>, etc.

## OAuth 2.0 authentication flow

TBMQ supports the Authorization Code grant type to exchange an authorization code for an access token.
Once the user is redirected back to the TBMQ client, the platform retrieves the authorization code from the URL and uses it to request an access token from the external user management platform.
Using the [basic mapper](#basic-mapper) or [custom mapper](#custom-mapper), the external user info object is converted from the external platform into a TBMQ internal OAuth 2.0 user.
After this, the regular TBMQ authorization flow takes place.

## Setting up authentication via an external provider

OAuth 2.0 clients are configured separately from domains, allowing reuse of the configured client and making the settings clearer.
To use authentication through an external provider, first configure an OAuth 2.0 client with the necessary credentials.
Then, either add a new domain or use an existing one, and update the OAuth 2.0 client list with the new client.

### Operations with domain

**Adding a domain**

<Steps>
1. On the **Domains** tab of the **OAuth 2.0** page, click the **plus** icon.
2. Provide your domain name and OAuth 2.0 client.
3. Click **Add** to finalize.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/adding-domain-1.png',
    alt: 'Add a new domain on the OAuth 2.0 Domains tab',
    caption: 'On the "Domains" tab of the "OAuth 2.0" page, click the "plus" icon to add a new domain. Provide your domain name and OAuth 2.0 client. Then, click "Add".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/adding-domain-2.png',
    alt: 'Domain added successfully',
    caption: 'Domain added.',
  },
]} />

**Editing a domain**

<Steps>
1. Click on the domain to view its details.
2. Switch to editing mode by clicking the large orange button.
3. Make the required modifications.
4. Save your changes by clicking **Apply changes**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/managing-domain-1.png',
    alt: 'View domain details and switch to editing mode',
    caption: 'Click on the domain to view its details. Switch to editing mode by clicking the large orange button.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/managing-domain-2.png',
    alt: 'Edit domain and save changes',
    caption: 'Make the required modifications. Then confirm and save the changes by clicking the "Apply changes" button.',
  },
]} />

**Deleting a domain**

<Steps>
1. Click the **trash** icon in the row of the domain you wish to remove.
2. Confirm the deletion by clicking **Yes**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/deleting-domain-1.png',
    alt: 'Click the trash icon to delete the domain',
    caption: "Click the \"trash\" icon in the domain's row you wish to remove.",
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/deleting-domain-2.png',
    alt: 'Confirm domain deletion',
    caption: 'Confirm the deletion by clicking "Yes".',
  },
]} />

### Operations with OAuth 2.0 client

**Adding an OAuth 2.0 client**

<Steps>
1. Navigate to the **OAuth 2.0 clients** tab on the **OAuth 2.0** page, and click the **plus** icon.
2. Enter a descriptive title for the client.
3. Select the authentication provider from the dropdown menu.
4. Provide the Client ID and Client Secret obtained from your authentication provider.
5. Configure advanced settings as necessary.
6. Click **Add** to finalize.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/adding-oauth2-client-1.png',
    alt: 'Navigate to OAuth 2.0 clients tab and click plus icon',
    caption: 'Navigate to the "OAuth 2.0 clients" tab on the "OAuth 2.0" page. Click the "plus" icon to add a new OAuth 2.0 client.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/adding-oauth2-client-2.png',
    alt: 'Configure OAuth 2.0 client title, provider, and credentials',
    caption: 'Enter a descriptive title for the client, and select the provider from the dropdown menu. Provide the Client ID and Client Secret. Configure advanced settings as necessary. Then, click "Add".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/adding-oauth2-client-3.png',
    alt: 'New OAuth 2.0 client added successfully',
    caption: 'New OAuth 2.0 client added.',
  },
]} />

**Editing an OAuth 2.0 client**

<Steps>
1. Click on the client to view its details.
2. Switch to editing mode by clicking the large orange button.
3. Make the required modifications.
4. Save your changes by clicking **Apply changes**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/managing-oauth2-client-1.png',
    alt: 'View OAuth 2.0 client details and switch to editing mode',
    caption: 'Click on the OAuth 2.0 client to view its details. Switch to editing mode by clicking the large orange button.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/managing-oauth2-client-2.png',
    alt: 'Edit OAuth 2.0 client and save changes',
    caption: 'Make the required modifications. Then confirm and save the changes by clicking the "Apply changes" button.',
  },
]} />

**Deleting an OAuth 2.0 client**

<Steps>
1. Click the **trash** icon in the row of the client you wish to remove.
2. Confirm the deletion by clicking **Yes**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/deleting-oauth2-client-1.png',
    alt: 'Click the trash icon to delete the OAuth 2.0 client',
    caption: "Click the \"trash\" icon in the client's row you wish to remove.",
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/deleting-oauth2-client-2.png',
    alt: 'Confirm OAuth 2.0 client deletion',
    caption: 'Confirm the deletion by clicking "Yes".',
  },
]} />

## Login with Google

This example uses <a href="https://developers.google.com/identity/protocols/oauth2/openid-connect" target="_blank" rel="noopener noreferrer">Google</a> for authentication.

To map external user information from Google to TBMQ, use the built-in [basic mapper](#basic-mapper).
If the basic mapper does not fit your business needs, you can configure a [custom mapper](#custom-mapper) for more flexibility.

### Preparations

To use Google OAuth 2.0 authentication, you must set up a project in the <a href="https://console.developers.google.com/" target="_blank" rel="noopener noreferrer">Google API Console</a> to obtain OAuth 2.0 credentials.

<Steps>
1. Go to the **Credentials** page in the left menu and select **OAuth client ID** from the **Create credentials** dropdown.
2. Enter an OAuth client name and add the TBMQ redirect URI in the **Authorized Redirect URIs** section using the format:

   ```
   http(s)://domain:port/login/oauth2/code/
   ```

   Replace `domain` with your TBMQ domain and specify the port used for HTTP access. For example:

   ```
   https://my.tbmq.org/login/oauth2/code/
   ```

3. Click **Create**. You now have a *Client ID* and a *Client Secret*.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-credentials-for-oauth-1.png',
    alt: 'Create OAuth client ID in Google API Console',
    caption: 'Go to the "Credentials" page in the left menu and select "OAuth client ID" from the "Create credentials" dropdown menu.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-credentials-for-oauth-2.png',
    alt: 'Enter OAuth client name and add TBMQ redirect URI',
    caption: 'Enter a OAuth client name, and add the TBMQ redirect URI, to the "Authorized Redirect URIs" section. Then, click "Create".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-credentials-for-oauth-3.png',
    alt: 'OAuth client created with Client ID and Client Secret',
    caption: 'OAuth client created. You now have credentials consisting of a Client ID and a Client secret.',
  },
]} />

### Configuring Google as an OAuth 2.0 provider in TBMQ

<Steps>
1. Log in to your TBMQ instance. Go to the **OAuth 2.0** page in the **Security** section. On the **Domains** tab, click the **plus** icon. Enter your domain name or IP address and click **Create new** in the **OAuth 2.0 clients** section.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-1.png',
       alt: 'Add new domain in TBMQ OAuth 2.0 page',
       caption: 'Log in to your TBMQ instance. Go to the "OAuth 2.0" page of the "Security" section. While on the "Domains" tab, click the "plus" icon.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-2.png',
       alt: 'Enter domain name and create new OAuth 2.0 client',
       caption: 'Enter your domain name or IP address of your TBMQ instance. Click "Create new" in the "OAuth 2.0 clients" section to add a new one.',
     },
   ]} />

2. Configure the new OAuth 2.0 client: enter **Google** as the title, set the provider to **Google**, and enter the Client ID and Client Secret from the <a href="https://console.developers.google.com/" target="_blank" rel="noopener noreferrer">Google API Console</a>.

   Then expand **Advanced settings → General**:
   - Refer to Google's <a href="https://developers.google.com/identity/protocols/oauth2/openid-connect#discovery" target="_blank" rel="noopener noreferrer">OpenID Connect discovery</a> for the latest **Access Token URI** and **Authorization URI**.
   - Select **POST** as the client authentication method.
   - Enable **Allow user creation**.
   - Add the following scopes: `email`, `openid`, `profile`.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-3.png',
       alt: 'Configure Google OAuth 2.0 client settings in TBMQ',
       caption: 'Enter "Google" as the title. The provider should be set to "Google". Enter the Client ID and Client secret. Expand Advanced settings and configure the General block.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-4.png',
       alt: 'Configure General block settings for Google OAuth 2.0 client',
       caption: 'Select "POST" as the client authentication method. Turn on the "Allow user creation" option. Add to the scope field: "email", "openid", and "profile".',
     },
   ]} />

3. Go to the **Mapper** block. Keep the mapper type as **BASIC**. Configure the role assignment settings, then click **Add**.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-5.png',
       alt: 'Configure mapper block for Google OAuth 2.0 client',
       caption: 'Go to the "Mapper" block. Leave the mapper type "BASIC". If necessary, change the user role or leave as it is. Click "Add".',
     },
   ]} />

4. The OAuth client has been added successfully. Click **Add** again to confirm the domain.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-6.png',
       alt: 'OAuth client added, confirm domain',
       caption: 'The OAuth client is added successfully. Click "Add" again to confirm the addition of the domain.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-7.png',
       alt: 'New domain with Google OAuth 2.0 added',
       caption: 'A new domain has been added.',
     },
   ]} />
</Steps>

### Sign in

Go to the TBMQ login screen. You will see a new **Login with Google** option.
Select one of your Google accounts — you will be logged into TBMQ using your Google email.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/login-with-google-1.png',
    alt: 'Login with Google on the TBMQ login screen',
    caption: 'Navigate to the TBMQ login screen. You will see an additional "Login with Google" option. Click this button and log in using your Google account.',
  },
]} />

Go to the **Users** page to find the newly created user.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/login-oauth-user.png',
    alt: 'New user created via Google OAuth 2.0 login',
    caption: 'Go to the "Users" page. There you should find the new user that logged in.',
  },
]} />

## Login with Auth0

This example configures OAuth authentication using <a href="https://auth0.com/" target="_blank" rel="noopener noreferrer">Auth0</a> as an external provider.

### Preparations

<Steps>
1. Go to the <a href="https://manage.auth0.com/" target="_blank" rel="noopener noreferrer">Auth0 management console</a>. Open the **Applications** page, and click **+ Create Application**. Name your application **TBMQ** and choose the type **Regular Web Applications**. Next, choose **Java Spring Boot** as the technology.

2. Once your application is created, navigate to the **Settings** tab to find the *Client ID* and *Client Secret*.

3. In the **Allowed Callback URLs** field, add the redirect URI using the format:

   ```
   http(s)://domain:port/login/oauth2/code/
   ```

   For example, if your domain is `my.tbmq.org`:

   ```
   https://my.tbmq.org/login/oauth2/code/
   ```

   <Aside type="note">
     It is not necessary to update the Application Login URI.
   </Aside>

4. In the **Advanced Settings** section, find all the necessary endpoint URLs required for OAuth 2.0 configuration. Click **Save Changes**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-credentials-1.png',
    alt: 'Create Application in Auth0 Management Console',
    caption: 'Go to the Auth0 Management Console. Open the "Applications" page, and click "+ Create Application" button.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-credentials-2.png',
    alt: 'Name application TBMQ and choose Regular Web Applications',
    caption: 'Name your application "TBMQ", and choose the application type - "Regular Web Applications".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-credentials-3.png',
    alt: 'Choose Java Spring Boot as the technology',
    caption: 'Choose the "Java Spring Boot" technology.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-credentials-4.png',
    alt: 'Find Client ID and Client Secret on the Settings tab',
    caption: 'Navigate to the "Settings" tab to find the Client ID and Client Secret.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-credentials-5.png',
    alt: 'Update Allowed Callback URLs with TBMQ redirect URI',
    caption: 'Update your allowed Callback URLs.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-credentials-6.png',
    alt: 'Find endpoint URLs in Advanced Settings and save changes',
    caption: 'In the "Advanced Settings" section you will find all the required URLs for OAuth 2.0 configuration. Click "Save Changes".',
  },
]} />

### Configuring Auth0 as an OAuth 2.0 provider in TBMQ

<Steps>
1. Log in to your TBMQ instance. Go to the **OAuth 2.0** page in the **Security** section. On the **Domains** tab, click the **plus** icon. Enter your domain name or IP address and click **Create new**.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-1.png',
       alt: 'Add new domain in TBMQ OAuth 2.0 page',
       caption: 'Log in to your TBMQ instance. Go to the "OAuth 2.0" page of the "Security" section. While on the "Domains" tab, click the "plus" icon.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/google-configuration-of-thingsboard-2.png',
       alt: 'Enter domain name and create new OAuth 2.0 client',
       caption: 'Enter your domain name. Click "Create new" in the "OAuth 2.0 clients" section to add a new one.',
     },
   ]} />

2. Configure the new OAuth 2.0 client: enter **Auth0** as the title, select **Custom** as the provider, and enter the *Client ID* and *Client Secret* from Auth0.

   In **Advanced settings → General**: fill in all required URLs from Auth0, select **POST** as the authentication method, enter **Auth0** as the provider label, and add scopes: `openid`, `email`, `profile`.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-configuration-of-thingsboard-3.png',
       alt: 'Configure Auth0 OAuth 2.0 client with URLs and scopes',
       caption: 'Enter "Auth0" as the title. Select "Custom" as the provider. Enter the Client ID and Client secret. Fill in all necessary URLs. Set authentication method to POST. Add scopes: "openid", "email", "profile".',
     },
   ]} />

3. Go to the **Mapper** block. Leave the mapper type as **BASIC**. Configure the role assignment settings. Click **Add**.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-configuration-of-thingsboard-4.png',
       alt: 'Configure mapper block for Auth0 OAuth 2.0 client',
       caption: 'Proceed to the "Mapper" block. Leave the mapper type "BASIC". Configure role assignment settings. Click "Add" to confirm.',
     },
   ]} />

4. The Auth0 client has been successfully added. Click **Add** again to confirm the domain.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-configuration-of-thingsboard-5.png',
       alt: 'Auth0 client added, confirm domain',
       caption: 'The Auth0 client has been successfully added. Click "Add" again to confirm the addition of the domain.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/oauth0-configuration-of-thingsboard-6.png',
       alt: 'New domain with Auth0 OAuth 2.0 added',
       caption: 'A new domain has been added.',
     },
   ]} />
</Steps>

### Sign in

Navigate to the login screen. You will find the **Login with Auth0** option. Click it and use your Auth0 credentials to log in.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/login-with-oauth0-1.png',
    alt: 'Login with Auth0 on the TBMQ login screen',
    caption: 'Navigate to the login screen. You will find Auth0 login method. Click on the "Login with Auth0" button and use your Auth0 credentials to log in.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/login-oauth-user.png',
    alt: 'New user created via Auth0 OAuth 2.0 login',
    caption: 'Go to the "Users" page. There you should find the new user that logged in.',
  },
]} />

## Login with Keycloak

This example uses <a href="https://www.keycloak.org/" target="_blank" rel="noopener noreferrer">Keycloak</a> for authentication.

### Preparations

**Start Keycloak**

Make sure you have <a href="https://docs.docker.com/compose/install/" target="_blank" rel="noopener noreferrer">Docker</a> installed, then run:

```bash
docker run -p 8081:8080 -e KC_BOOTSTRAP_ADMIN_USERNAME=admin -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:26.0.5 start-dev
```

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/terminal-start-keycloak-1.png',
    alt: 'Start Keycloak with Docker command',
    caption: 'Run this command to start Keycloak on local port 8081 and create an initial admin user.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/terminal-start-keycloak-2.png',
    alt: 'Keycloak starting up',
    caption: '',
  },
]} />

**Log in to the admin console**

Log in to the <a href="http://localhost:8081/admin" target="_blank" rel="noopener noreferrer">Keycloak Admin Console</a> using **admin** as both username and password.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/log-in-to-admin-console-1.png',
    alt: 'Log in to Keycloak Admin Console',
    caption: 'Log in to the Keycloak Admin Console using your username and password.',
  },
]} />

**Create a realm**

<Steps>
1. Click **Keycloak** next to the master realm, then click **Create realm**.
2. Enter **ThingsBoard** in the realm name field, and click **Create**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-new-realm-1.png',
    alt: 'Click Create realm in Keycloak',
    caption: 'Click "Keycloak" next to the master realm, then click "Create realm" button.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-new-realm-2.png',
    alt: 'Enter ThingsBoard as the realm name',
    caption: 'Enter "ThingsBoard" in the realm name field, and click "Create" button.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-new-realm-3.png',
    alt: 'New realm created in Keycloak',
    caption: 'The new realm has been created.',
  },
]} />

**Create a new client**

<Steps>
1. Go to the **Clients** page in the left-hand menu, and click **Create client**.
2. Enter **thingsboard** as the client ID. Leave the type as **OpenID Connect**. Click **Next**.
3. Enable **Client authentication**. Confirm that **Standard flow** is enabled. Click **Next**.
4. In the **Login settings** section, add the TBMQ redirect URI to **Authorized Redirect URIs**:

   ```
   http(s)://domain:port/login/oauth2/code/
   ```

   For example: `https://my.thingsboard.instance/login/oauth2/code/`

5. Click **Save**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-client-1.png',
    alt: 'Go to Clients page and click Create client',
    caption: 'Go to the "Clients" page in the left-hand menu, and click the "Create client" button.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-client-2.png',
    alt: 'Enter thingsboard as client ID',
    caption: 'Enter "thingsboard" as the client ID. Leave the client type as "OpenID Connect". Click "Next".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-client-3.png',
    alt: 'Enable Client authentication and Standard flow',
    caption: 'Turn on "Client authentication" option. Confirm that "Standard flow" is enabled. Click "Next".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-client-4.png',
    alt: 'Add TBMQ redirect URI to Authorized Redirect URIs',
    caption: 'Add the TBMQ redirect URI to the "Authorized Redirect URIs" section. Then, click "Save".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-client-5.png',
    alt: 'Keycloak client created successfully',
    caption: 'Client created successfully.',
  },
]} />

The Client ID is on the **Settings** tab; the Client Secret is on the **Credentials** tab.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/client-id-and-secret-1.png',
    alt: 'Find Client ID on the Settings tab',
    caption: 'You can find the "Client ID" on the "Settings" tab.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/client-id-and-secret-2.png',
    alt: 'Find Client Secret on the Credentials tab',
    caption: 'The "Client Secret" is located on the "Credentials" tab.',
  },
]} />

**Get endpoint URLs**

Go to the **Realm settings** page, scroll down to locate the **OpenID Endpoint Configuration** link, and click it.
A new window will open with the configuration — check **Pretty-print** for easier reading.
Here you can find the **Access Token URI**, **Authorization URI**, **JSON Web Key URI**, and **User Info URI**.
A description of all available endpoints is provided in the <a href="https://www.keycloak.org/securing-apps/oidc-layers" target="_blank" rel="noopener noreferrer">Keycloak OIDC layers</a> documentation.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/openid-endpoint-configuration-1.png',
    alt: 'Open OpenID Endpoint Configuration from Realm settings',
    caption: 'Go to the "Realm settings" page. Scroll down and locate the link to "OpenID Endpoint Configuration", then click on it.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/openid-endpoint-configuration-2.png',
    alt: 'OpenID Endpoint Configuration showing all required URLs',
    caption: 'A new window with OpenID Endpoint Configuration will open. Check "Pretty-print" option. Here you find Access token URI, Authorization URI, JSON Web Key URI, and User info URI.',
  },
]} />

**Create a user**

Only users added to Keycloak will be able to authenticate via Keycloak.

<Steps>
1. Go to the **Users** page in the left-hand menu. Click **Create new user**.
2. Enter the username and email address. Click **Create**.
3. Navigate to the **Credentials** tab and click **Set password**. Fill in the password form. Toggle **Temporary** to **Off**. Click **Save password**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-user-1.png',
    alt: 'Go to Users page and click Create new user',
    caption: 'Go to the "Users" page in the left-hand menu. Click "Create new user".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-user-2.png',
    alt: 'Enter username and email address',
    caption: 'Enter the username and email address in the form. First name and last name are optional. Then, click "Create".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-user-3.png',
    alt: 'User created in Keycloak',
    caption: 'The user has been created.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-user-4.png',
    alt: 'Navigate to Credentials tab and click Set password',
    caption: 'Navigate to the "Credentials" tab. Click "Set password".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-user-5.png',
    alt: 'Fill in password form and toggle Temporary to Off',
    caption: 'Fill in the "Set password" form with a password. Toggle "Temporary" to "Off". Click "Save".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-user-6.png',
    alt: 'Confirm password by clicking Save password',
    caption: 'Confirm the set password by clicking "Save password".',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak/create-user-7.png',
    alt: 'Password set successfully',
    caption: 'The password has been set successfully.',
  },
]} />

### Configuring Keycloak as an OAuth 2.0 provider in TBMQ

<Steps>
1. Log in to your TBMQ instance. Go to the **OAuth 2.0** page in the **Security** section. Navigate to the **OAuth 2.0 clients** tab, and click the **plus** icon.

   Enter **Keycloak** as the title. Select **Custom** as the provider. Enter the *Client ID* and *Client Secret* from Keycloak.

   In **Advanced settings → General**: fill in the endpoint URLs, set authentication method to **POST**, enter **Keycloak** as the provider label, and add scopes: `email`, `openid`, `profile`.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/adding-oauth2-client-1.png',
       alt: 'Navigate to OAuth 2.0 clients and click plus icon',
       caption: 'Log in to your TBMQ instance. Navigate to the "OAuth 2.0" in the "Security" menu section, open "OAuth 2.0 clients" tab, and click "plus" icon.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak-configuration-of-thingsboard-2.png',
       alt: 'Configure Keycloak OAuth 2.0 client settings',
       caption: 'Enter "Keycloak" as the title. Select "Custom" as the provider. Enter the Client ID and Client secret. Fill in all necessary URLs. Set authentication method to POST. Add scopes: "email", "openid", "profile".',
     },
   ]} />

2. Go to the **Mapper** block. Leave the mapper type as **BASIC**. Configure the role assignment settings. Click **Add** to confirm.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak-configuration-of-thingsboard-3.png',
       alt: 'Configure mapper block for Keycloak OAuth 2.0 client',
       caption: 'Go to the "Mapper" block. Leave the mapper type "BASIC". Configure role assignment settings. Click "Add" to confirm.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak-configuration-of-thingsboard-4.png',
       alt: 'New Keycloak OAuth 2.0 client added',
       caption: 'A new OAuth 2.0 client has been added.',
     },
   ]} />

3. Now add a new domain: go to the **Domains** tab, click the **plus** icon, enter your domain name or IP address, specify **Keycloak** as the OAuth 2.0 client, and click **Add**.

   <ImageGallery product={props.product} images={[
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak-adding-domain-1.png',
       alt: 'Add new domain with Keycloak OAuth 2.0 client',
       caption: 'Navigate to the "Domains" tab, and click "plus" icon. Enter your domain name or IP address. Specify "Keycloak" as the OAuth 2.0 client. Click "Add" to confirm.',
     },
     {
       src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/keycloak-adding-domain-2.png',
       alt: 'New domain with Keycloak added',
       caption: 'A new domain has been added.',
     },
   ]} />
</Steps>

### Sign in

Go to the TBMQ login screen. You will see the **Login with Keycloak** option.
Click it, enter your Keycloak credentials, and click **Sign In**.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/login-with-keycloak-1.png',
    alt: 'Login with Keycloak on the TBMQ login screen',
    caption: 'Go to the TBMQ login screen. You will see an additional option, "Login with Keycloak". Click this button and login using your Keycloak credentials.',
  },
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/login-oauth-user.png',
    alt: 'New user created via Keycloak OAuth 2.0 login',
    caption: 'Go to the "Users" page. There you should find the new user that logged in.',
  },
]} />

## Mapping external user into TBMQ

Mapping an external user info object into a TBMQ user can be achieved using the **Basic**, **Custom**, GitHub, and Apple mappers.

### Basic mapper

The basic mapper merges an external OAuth 2.0 user info object into the TBMQ OAuth 2.0 user with a predefined set of rules.

To use the basic mapper, set the mapper type to **Basic**.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/mapper-basic-1.png',
    alt: 'Set mapper type to Basic',
    caption: 'To use a basic mapper, set mapper type "Basic".',
  },
]} />

Available properties:

- **Allow user creation** — If enabled and the user account does not exist in TBMQ, it will be created automatically. If disabled, the user will receive an *access denied* error if no corresponding TBMQ user exists.
- **Email attribute key** — The attribute key from the external OAuth 2.0 user info used for the TBMQ user email.
- **First name attribute key** — The attribute key used for the TBMQ user first name.
- **Last name attribute key** — The attribute key used for the TBMQ user last name.
- **Role assignment settings** — Select a fixed role or map roles dynamically from user attributes:
  - **Static** — This role will be assigned to all users authenticated through this provider.
  - **Dynamic** — Roles are assigned based on the value of a specific attribute in the user info object.
    - **Role attribute key** — User info attribute (e.g., `roles` or `groups`) for mapping. If both match, Administrator is granted over Viewer.
    - **Administrator role values** — List of attribute values that will grant the Administrator role.
    - **Viewer role values** — List of attribute values that will grant the Viewer role.

<Aside type="note">
  If a user signs in using OAuth 2.0 while **Allow user creation** is enabled, the user account is created without a password.
  These users can continue using SSO to log in. If the user wants to log in using a regular username/password flow,
  they should go to **Account → Security** and <DocLink product={props.product} path="user-guide/ui/settings/#change-password">set a new password</DocLink> (leave the **Current password** field empty).
</Aside>

### Custom mapper

If the basic mapper does not meet your business needs, you can configure a custom mapper to implement logic that fits your specific goals.

A custom mapper is designed as a separate microservice running alongside the TBMQ microservice.
TBMQ forwards all mapping requests to this microservice and expects a TBMQ OAuth 2.0 user object in response.

Refer to the <a href="https://github.com/thingsboard/tbmq-custom-oauth2-mapper" target="_blank" rel="noopener noreferrer">base implementation</a> as a starting point for your custom mapper.

To use the custom mapper, set the mapper type to **Custom**.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/user-guide/oauth-2-support/mapper-custom-1.png',
    alt: 'Set mapper type to Custom',
    caption: 'To use the custom mapper, set mapper type "Custom".',
  },
]} />

Available properties:

- **URL** — The URL of the custom mapper endpoint.
- **username** — If the custom mapper endpoint uses basic authentication, specify the username here.
- **password** — If the custom mapper endpoint uses basic authentication, specify the password here.

## HAProxy configuration

If TBMQ is running behind a load balancer such as HAProxy, configure the balancing algorithm to ensure the correct session is maintained on the TBMQ instance:

```bash
backend tbmq-api-backend
  ...
  balance source # balance must be set to 'source'
  ...
```

Also configure ACL mapping for HTTP and HTTPS requests to include the OAuth 2.0 paths:

```bash
frontend http-in
  ...
  acl tbmq_api_acl path_beg /api/ /swagger /webjars /v2/ /oauth2/ /login/oauth2/ # '/oauth2/ /login/oauth2/' added
  ...
```

```bash
frontend https_in
  ...
  acl tbmq_api_acl path_beg /api/ /swagger /webjars /v2/ /oauth2/ /login/oauth2/ # '/oauth2/ /login/oauth2/' added
  ...
```
