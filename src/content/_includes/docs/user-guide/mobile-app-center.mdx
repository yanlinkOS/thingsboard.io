import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import ImageGallery from '~/components/ImageGallery.astro';
import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import ConditionalHeading from '~/components/ConditionalHeading.astro';

The **Mobile App Center** is a hub for building, configuring, and managing mobile apps in ThingsBoard. Whether you are starting from scratch or preparing to publish the ThingsBoard Mobile Application to the Google Play Store or App Store, this tool guides you through every step.

The Mobile App Center is divided into tabs:

- **[Bundle](#bundle)** — manage settings for mobile applications included in the bundle: authentication methods, layout{props.product !== Products.CE && ', and self-registration'}.
- <DocLink product={props.product} path="user-guide/mobile-app-center/applications">Applications</DocLink> — add, edit, or remove mobile applications.
- <DocLink product={props.product} path="user-guide/mobile-app-center/qr-code-widget">QR code widget</DocLink> — set up a QR code widget on the Home page for easy mobile app access.

## Bundle

A **Bundle** groups together configurations like OAuth 2.0 settings, layout design{props.product !== Products.CE && ', and self-registration'} for one or more mobile apps — allowing you to manage Android and iOS versions in one place.

The bundle wizard includes the following steps:

<ul>
  <li><strong>Basic settings</strong> — define the bundle name and link your apps. You can also create a new app directly.</li>
  <li><strong>OAuth 2.0</strong> — enable Single Sign-On (SSO) via third-party identity providers that support OAuth 2.0.</li>
  <li><strong>Layout</strong> — customize the app's navigation menu to match your use cases.</li>
  {props.product !== Products.CE && <li><strong>Self-registration</strong> — set up a registration form for new mobile users.</li>}
</ul>

### Adding a new bundle

<Steps>
1. Go to the **Mobile center** page.
2. On the **Bundle** tab, click **+ Add bundle** in the upper-right corner.
</Steps>

A new settings dialog will open.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/add-new-bundle-1.png', alt: 'Mobile center Bundle tab with + Add bundle button highlighted', caption: 'Navigate to Mobile center → Bundle tab and click + Add bundle.' },
]} />

### Basic settings

Enter a name for your bundle and select existing Android/iOS apps, or [create new ones](#creating-an-application) directly from this window.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/basic-settings-1.png', alt: 'Bundle basic settings with bundle name field and Android/iOS application selectors', caption: 'Enter the bundle name and select or create Android and iOS applications.' },
]} />

<Aside type="tip">
  You can add applications even after the bundle is created.
</Aside>

#### Creating an application

<Steps>
1. Click **Create new** in the corresponding section (Android Application or iOS Application).
2. Enter the **application package** name.
3. An **Application Secret** will be generated automatically. Save it or replace with your own.
4. Set the **application status**:
   - **Draft** — for applications still in development. Store information can be added later.
   - **Published** — for applications ready to be launched and used by end users.
   - **Deprecated** — for older applications that are no longer actively supported but still available.
   - **Suspended** — for applications that are temporarily disabled or restricted.
5. (Optional) Specify minimum/latest app versions and release notes.
6. Add **store information**:
   - **Android**: Google Play Store link and SHA256 certificate fingerprints.
   - **iOS**: App Store link and App ID.
7. Click **Add** to save the application.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/add-application-1.png', alt: 'Create new application button in the Basic settings step', caption: 'Click Create new to add a mobile application directly from the bundle wizard.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/android-application-2.png', alt: 'Android application form showing package name, secret, status, versions, and store info fields', caption: 'Configure the Android application: package name, secret, status, versions, and Google Play store info.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/ios-application-2.png', alt: 'iOS application form showing package name, secret, status, App Store URL, and App ID fields', caption: 'Configure the iOS application: package name, secret, status, and App Store info.' },
]} />

Then, click **Next**.

### OAuth 2.0

Add authentication for your users through an external provider that supports the <DocLink product={props.product} path="user-guide/security/oauth-2-support" target="_blank">OAuth 2.0</DocLink> protocol.
Specify an already configured OAuth 2.0 client or <DocLink product={props.product} path="user-guide/security/oauth-2-support/#oauth-20-client" target="_blank">configure a new one</DocLink>.

You can also disable OAuth 2.0 for mobile devices.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/specify-oauth-clients-1.png', alt: 'OAuth 2.0 step showing an empty client selector and disable OAuth toggle', caption: 'Select an OAuth 2.0 client or disable OAuth 2.0 for mobile devices.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/specify-oauth-clients-2.png', alt: 'OAuth 2.0 step with a client configured and selected', caption: 'OAuth 2.0 client configured and selected for the bundle.' },
]} />

After completing the OAuth 2.0 configuration, click **Next**.

### Layout

Set up quick access to the features and dashboards you use most frequently. Add menu items, change icons and item names, and reorganize the navigation menu.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/mobile-layout-1.png', alt: 'Layout step showing the default navigation menu items list', caption: 'The Layout step shows the default navigation menu items.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/mobile-layout-2.png', alt: 'Layout step with menu items and + Add specific page buttons visible', caption: 'Reorder items or add new pages using the + Add specific page buttons.' },
]} />

To add a new menu item:

<Steps>
1. Click **+ Add specific page** at the bottom of the page or between existing menu items.
2. Enter the **name** for the new menu page.
3. Select the **page type**:
   - **Dashboard** — link to a ThingsBoard dashboard.
   - **Webview** — link to an external webpage.
   - **Custom** — link to built-in ThingsBoard pages.
4. Depending on the selected page type, specify a dashboard, external URL, or ThingsBoard menu item.
5. Click **Add**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/custom-page-type-1.png', alt: '+ Add specific page button highlighted in the layout menu', caption: 'Click + Add specific page to insert a new menu item.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/custom-page-type-2.png', alt: 'Page type selection dialog with Dashboard, Webview, and Custom options', caption: 'Select the page type: Dashboard, Webview, or Custom.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/custom-page-type-3.png', alt: 'Layout list with the new menu item added', caption: 'The new menu item appears in the layout list.' },
]} />

The following routes are available for the **Custom** page type:

| Page name | Route | Description |
|---|---|---|
| Home | `/home` | Displays the Home page |
| Assets | `/assets` | Displays the Assets page |
| Device profiles | `/devices` | Displays the Device profiles page |
| All devices | `/deviceList` | Displays the list of all devices |
| Alarms | `/alarms` | Displays the list of alarms |
| Dashboards | `/dashboards` | Displays the list of dashboards |
| Specific dashboard | `/dashboard/:id` | Displays the specified dashboard |
| Audit logs | `/auditLogs` | Displays the Audit logs page |
| Customers | `/customers` | Displays the list of customers |
| Specific customer | `/customer/:id` | Displays the specified customer |
| Notifications | `/notifications` | Displays the list of notifications |
| Custom URL link | `/url/:link` | Displays an external URL |

{props.product !== Products.CE && (
<Aside type="tip">
  The menu configuration algorithm is similar to menu customization in the desktop ThingsBoard. Refer to the <DocLink product={props.product} path="user-guide/white-labeling-menu" target="_blank">custom menu</DocLink> documentation for details.
</Aside>
)}

#### "More" page

The number of menu items displayed in the bottom navigation menu depends on the screen size of the mobile device. Items that do not fit will be available under the **More** page.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/mobile-layout-3.png', alt: 'Mobile device showing the bottom navigation bar with a More tab', caption: 'Menu items that do not fit the navigation bar are grouped under More.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/mobile-layout-4.png', alt: 'Mobile device showing the More page with additional menu items listed', caption: 'The More page lists all menu items that overflow the bottom navigation bar.' },
]} />

{props.product !== Products.CE && (<>

<ConditionalHeading level={3} id="self-registration" showFor="pe, paas, paas/eu">Self-registration</ConditionalHeading>

<p>The <strong>self-registration</strong> feature allows tenants to configure a sign-up page for their clients, enabling them to sign up and log in to ThingsBoard with predefined permission configurations.</p>

<p>Configure the self-registration form:</p>

<p><strong>Basic settings:</strong></p>

<ul>
  <li><strong>Page title</strong> — the header displayed at the top of the registration page.</li>
  <li><strong>Notification recipient</strong> — recipients that will receive notifications about new user registrations. Learn more about <DocLink product={props.product} path="user-guide/notifications" target="_blank">notifications</DocLink>.</li>
  <li><strong>Redirect settings</strong>:
    <ul>
      <li><strong>Application URL scheme</strong> — the custom URL scheme for redirecting users after registration (e.g., <code>tbscheme</code>).</li>
      <li><strong>Application URL hostname</strong> — the hostname used in redirection (e.g., <code>app.pe.thingsboard.org</code>).</li>
    </ul>
  </li>
</ul>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/self-registration-1-pe.png', alt: 'Self-registration basic settings showing page title, notification recipient, and redirect URL fields', caption: 'Configure the page title, notification recipient, and redirect settings for self-registration.' },
]} />

<p><strong>Registration form fields:</strong></p>

<p>The mandatory fields are email, password, and repeat password. Additional fields can be specified as needed.</p>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/self-registration-2-pe.png', alt: 'Registration form fields table with Required toggle, Field type, and Label columns', caption: 'Define mandatory and optional fields for the registration form.' },
]} />

<p><strong>Captcha properties:</strong></p>

<p>Captcha prevents automated creation of fake accounts.</p>

<p>To generate Captcha keys, navigate to the <a href="https://www.google.com/recaptcha/intro/v3.html" target="_blank">Google reCAPTCHA admin console</a>:</p>

<Steps>
  <ol>
    <li>Select the reCAPTCHA version (v2, v3, or Enterprise).</li>
    <li>Specify the domain name of your ThingsBoard instance.</li>
    <li>Copy and securely store the site key and secret key.</li>
  </ol>
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/captcha-1.png', alt: 'Google reCAPTCHA homepage with v3 Admin Console and Get Started links', caption: 'Open the Google reCAPTCHA admin console to generate keys.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/captcha-2.png', alt: 'Register a new site form with label, reCAPTCHA type, and domain fields highlighted', caption: 'Enter a label, select the reCAPTCHA type, and specify your ThingsBoard domain.' },
  { src: '/src/assets/images/user-guide/mobile-app-center/captcha-3.png', alt: 'reCAPTCHA success page showing Copy Site Key and Copy Secret Key buttons', caption: 'Copy the generated site key and secret key for use in ThingsBoard.' },
]} />

<p>In ThingsBoard:</p>

<table>
  <thead><tr><th>Parameter</th><th>Description</th></tr></thead>
  <tbody>
    <tr><td><strong>Captcha version</strong></td><td>The reCAPTCHA version to use.</td></tr>
    <tr><td><strong>Captcha site key</strong></td><td>The public key used to render the captcha on the site.</td></tr>
    <tr><td><strong>Captcha secret key</strong></td><td>The private key used to verify captcha responses on the server.</td></tr>
    <tr><td><strong>Captcha log action name</strong></td><td>The action name associated with the captcha, used for logging.</td></tr>
  </tbody>
</table>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/self-registration-3-pe.png', alt: 'Captcha settings in ThingsBoard showing version, site key, secret key, and action name fields', caption: 'Enter the captcha version, site key, secret key, and action name in ThingsBoard.' },
]} />

<p><strong>Additional settings:</strong></p>

<ul>
  <li><strong>Show Privacy Policy</strong> — enable or disable the Privacy Policy link on the registration page.</li>
  <li><strong>Show Terms of Use</strong> — enable or disable the Terms of Use link on the registration page.</li>
  <li><strong>User group roles</strong> — roles assigned to the user upon successful registration, determining their permissions and access level.</li>
</ul>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/self-registration-4-pe.png', alt: 'Additional settings showing Privacy Policy, Terms of Use toggles, and User group roles selector', caption: 'Enable Privacy Policy and Terms of Use links, and assign user group roles.' },
]} />

<p><strong>Advanced settings:</strong></p>

<table>
  <thead><tr><th>Parameter</th><th>Description</th></tr></thead>
  <tbody>
    <tr><td><strong>Customer group</strong></td><td>The customer group to which the new user will be added.</td></tr>
    <tr><td><strong>Customer title prefix</strong></td><td>A prefix added to the customer's title for identification.</td></tr>
    <tr><td><strong>Custom menu</strong></td><td>A custom navigation menu applied for the new user.</td></tr>
    <tr><td><strong>Default dashboard</strong></td><td>The dashboard displayed when the user logs in for the first time.</td></tr>
  </tbody>
</table>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/self-registration-5-pe.png', alt: 'Advanced settings showing customer group, title prefix, custom menu, and default dashboard fields', caption: 'Configure the customer group, title prefix, custom menu, and default dashboard.' },
]} />

<p><strong>Home dashboard:</strong></p>

<p>The main dashboard displayed when the user navigates to the Home menu item.</p>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/self-registration-6-pe.png', alt: 'Home dashboard field with dashboard selector and Hide toolbar option, and Add button at bottom', caption: 'Select the home dashboard and set toolbar visibility, then click Add to finish.' },
]} />

</>)}

### Configuration dialog

After creating the bundle, a configuration dialog opens with basic configuration settings for the ThingsBoard Mobile Application.
Continue configuring your new mobile app using the provided instructions, or skip this step if you already have a mobile app configured. Click **Close**.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/add-new-bundle-2.png', alt: 'Configuration dialog showing SDK setup instructions and a Close button', caption: 'The configuration dialog provides setup instructions for the ThingsBoard Mobile Application SDK.' },
]} />

<Aside type="tip">
  For more information on launching your new app, refer to the **Getting started with the ThingsBoard Mobile Application** documentation.
</Aside>

The bundle has been added. The applications specified in the bundle now use the defined settings.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/mobile-app-center/add-new-bundle-3.png', alt: 'Bundle list showing the newly created bundle entry', caption: 'The new bundle appears in the Bundle list and its applications are now active.' },
]} />
