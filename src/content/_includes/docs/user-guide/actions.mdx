import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import DocImage from '~/components/DocImage.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import { Steps, Aside } from '@astrojs/starlight/components';

**Widget actions** allow users to trigger predefined operations from a widget through interactions such as clicking a button, a row, or the widget title.

Actions are configured per widget and triggered by specific interactions called action sources. Available sources depend on the widget type, while action types are the same for all widgets.

Actions can be used to navigate between <DocLink product={props.product} path="user-guide/dashboards">dashboards</DocLink>, interact with devices, update entity data, or execute custom logic.

---

## Add action

ThingsBoard widgets have two edit modes, and the steps for adding an action differ slightly between them:

- **Basic** — a simplified settings panel that opens when you click the pencil icon on the widget. Actions are in a dedicated **Actions** section within the panel, managed through a modal list.
- **Advanced** — a full-screen tabbed editor opened from the basic panel. Actions have their own **Actions** tab with a flat list and a **+** button — no intermediate modal.

Both modes use the same **Add action** dialog to configure source, name, icon, and type.

<h4>Basic widget configuration</h4>

<Steps>
1. Click the pencil icon on the widget to open its settings panel.
2. Scroll to the **Actions** section and click **Add action** to open the Actions list. After the first action is saved, the button is replaced by a **+** icon.
3. Click **+** in the top-right corner of the Actions list to open the **Add action** dialog. Set the action source, name, icon, and type, then click **Add**.
4. Review the action in the list and click **Save** to close the list.
5. Click **Apply** to save the widget settings, then click **Save** in the dashboard toolbar.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/add-action-2.png', alt: 'Open widget settings panel', caption: 'Click the pencil icon on the Entities table widget to open its settings panel.' },
  { src: '/src/assets/images/user-guide/actions/add-action-3.png', alt: 'Actions section with Add action button', caption: 'Scroll to the Actions section and click "Add action" to open the Actions list.' },
  { src: '/src/assets/images/user-guide/actions/add-action-4.png', alt: 'Empty Actions list', caption: 'The Actions list is empty — click "+" in the top-right corner to open the Add action dialog.' },
  { src: '/src/assets/images/user-guide/actions/add-action-5.png', alt: 'Add action dialog configured', caption: 'Add action dialog — set source to "Action cell button", enter name "Temperature details", select type "Navigate to new dashboard state", choose target state "temperature_sensors", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/add-action-6.png', alt: 'Action in list, click Save', caption: 'The action "Temperature details" appears in the Actions list — click "Save" to close the list.' },
  { src: '/src/assets/images/user-guide/actions/add-action-7.png', alt: 'Click Apply to save widget settings', caption: 'Click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/add-action-8.png', alt: 'Save dashboard', caption: 'Dashboard in edit mode — action buttons (···) appear next to each entity row — click "Save" to save the dashboard.' },
]} />

<h4>Advanced widget configuration</h4>

<Steps>
1. Click the pencil icon on the widget to open its settings panel, then switch to advanced mode to open the full widget editor.
2. Navigate to the **Actions** tab and click **+** to open the **Add action** dialog.
3. Set the action source, name, icon, and type, then click **Add**.
4. The action appears in the **Actions** tab — click **Apply** to save the widget settings, then click **Save** in the dashboard toolbar.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/add-action-9.png', alt: 'Actions tab in advanced widget editor', caption: 'Advanced widget editor — the Actions tab is empty. Click "+" to open the Add action dialog.' },
  { src: '/src/assets/images/user-guide/actions/add-action-10.png', alt: 'Add action dialog in advanced mode', caption: 'Add action dialog — set source to "Action cell button", enter name "Temperature details", select type "Navigate to new dashboard state", choose target state "temperature_sensors", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/add-action-11.png', alt: 'Action in Actions tab', caption: 'The action "Temperature details" appears in the Actions tab — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/add-action-12.png', alt: 'Save dashboard', caption: 'Dashboard in edit mode — action buttons (···) appear next to each entity row — click "Save" to save the dashboard.' },
]} />

---

## Action types

| Action type | Description |
|-------------|-------------|
| [Navigate to new dashboard state](#navigate-to-new-dashboard-state) | Navigates to a specified state within the current dashboard. |
| [Update current dashboard state](#update-current-dashboard-state) | Updates the entity context of the current dashboard without navigation. |
| [Navigate to other dashboard](#navigate-to-other-dashboard) | Navigates to a different dashboard, optionally to a specific state within it. |
| [Custom action](#custom-action) | Executes a custom JavaScript function (e.g. deleting a device or asset). |
| [Custom action (with HTML template)](#custom-action-with-html-template) | Executes a custom JavaScript function rendered inside an HTML template (e.g. a create/edit dialog). |
| [Mobile action](#mobile-action) | Triggers mobile device functions: camera, QR code scan, location, phone call. |
| [Open URL](#open-url) | Opens any URL, optionally in a new browser tab. |
| [Place map item](#place-map-item) | Creates a new entity and places it directly on the map. Available for <DocLink product={props.product} path="reference/widgets/map-widgets">Map widgets</DocLink> only. |

### Navigate to new dashboard state

Navigates to a <DocLink product={props.product} path="user-guide/dashboards">dashboard state</DocLink> you select. By default the state replaces the current view, but you can instead open it in an overlay dialog or a popover anchored to the trigger element.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select an action source (e.g., **Action cell button**).
3. Enter an action name and choose an icon.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Save** and **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-1.png', alt: 'Configure Navigate to new dashboard state action', caption: 'Add action dialog — set source to "Action cell button", enter name "Device details", select type "Navigate to new dashboard state", choose target state "device_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-2.png', alt: 'Action in list', caption: 'The action "Device details" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-3.png', alt: 'Action chip in widget settings', caption: 'The action chip "Device details" appears in the Actions section — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-4.png', alt: 'Dashboard with action cell buttons', caption: 'Dashboard in edit mode — an action button appears next to each entity row — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-5.png', alt: 'Click action button in view mode', caption: 'Dashboard in view mode — click the action button next to any entity to navigate to the "device_details" state.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-6.png', alt: 'Target state opened', caption: 'The "Device details" state opens, showing the selected entity\'s temperature card, time series chart, and map.' },
]} />

<h4>Open in a separate dialog or popover</h4>

Instead of replacing the current view, you can open the target state as an overlay. Use the **Dashboard state display option** field in the action settings to choose one of three modes:

- **Normal** — navigates to the state, replacing the current view (default).
- **Open in separate dialog** — opens the state in a modal dialog overlay. You can configure the title, toolbar visibility, and dialog dimensions.
- **Open in popover** — opens the state in a popup anchored near the trigger element. You can set placement, width, and height.

<Steps>
1. In the action settings, open the **Dashboard state display option** dropdown and select **Open in separate dialog**.
2. Optionally uncheck **Hide dashboard toolbar in dialog** to show the time window, export, and fullscreen controls inside the dialog.
3. Optionally set the dialog width and height (as percentages of the viewport).
4. Save all changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-7.png', alt: 'Dashboard state display option dropdown', caption: 'In the action settings, open the "Dashboard state display option" dropdown — choose "Normal", "Open in separate dialog", or "Open in popover".' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-8.png', alt: 'Configure separate dialog options', caption: 'Select "Open in separate dialog" — configure the dialog title, toolbar visibility, and dimensions, then save.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-9.png', alt: 'Click action button to open dialog', caption: 'Dashboard in view mode — click the action button to open the target state as a dialog overlay.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-10.png', alt: 'State opened as dialog overlay', caption: '"Device details" opens as a dialog overlay on the current dashboard, showing temperature card, chart, and map.' },
]} />

### Update current dashboard state

Updates the entity context of the current dashboard state without navigating away. Commonly used to show details for a selected entity in a companion chart or map widget on the same dashboard.

For this example, add a **Timeseries Line Chart** widget and configure its datasource to use the <DocLink product={props.product} path="user-guide/aliases">"Entity from dashboard state"</DocLink> alias, so the chart reflects whichever entity is currently selected:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-1.png', alt: 'Add Timeseries Line Chart widget', caption: 'Enter edit mode and click "+ Add widget" to begin adding a Time series chart.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-2.png', alt: 'Select Charts bundle', caption: 'Select the "Charts" widget bundle.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-3.png', alt: 'Select Time series chart widget', caption: 'Select the "Time series chart" widget.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-4.png', alt: 'Create entity alias', caption: 'Switch to the Entity alias tab, enter "Chart-temperature" as the alias name, and click "Create a new one".' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-5.png', alt: 'Set filter type to Entity from dashboard state', caption: 'In the Add alias dialog, set Filter type to "Entity from dashboard state" and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-6.png', alt: 'Update series label', caption: 'The alias is created — update the series label to "${entityName}" and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-7.png', alt: 'Position the chart widget', caption: 'Position and resize the "Time series chart" widget on the dashboard.' },
]} />

Now add an **Update current dashboard state** action to the Entities table widget:

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **Action cell button** as the action source, enter a name (e.g. *Chart temperature*), and choose an icon.
3. Select **Update current dashboard state** as the action type.
4. Click **Add**, then **Save** and **Apply** to save the widget settings.
5. Click **Save** in the dashboard toolbar.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-8.png', alt: 'Add action dialog', caption: 'Add action dialog — set source to "Action cell button", enter name "Chart temperature", select type "Update current dashboard state", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-9.png', alt: 'Action in list', caption: 'The action "Chart temperature" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-10.png', alt: 'Action chip in Actions section', caption: 'The action chip "Chart temperature" appears in the Actions section — click "Apply".' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-11.png', alt: 'Dashboard in edit mode with action icons', caption: 'Dashboard in edit mode — thermometer action icons appear next to each entity — click "Save".' },
]} />

Clicking the action icon next to any entity updates the Timeseries Line Chart to show that entity's data.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-12.png', alt: 'Click action icon in view mode', caption: 'Dashboard in view mode — click the thermometer icon next to any entity to update the chart.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-13.png', alt: 'Chart updated for selected entity', caption: 'After clicking the icon for "Compressor NM-56", the Time series chart updates to show that entity\'s data.' },
]} />

### Navigate to other dashboard

Navigates to a previously created dashboard, optionally to a specific state within it.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select the action source (e.g. **Widget header button**), enter a name, and choose an icon.
3. Select **Navigate to other dashboard** as the action type, then choose the target dashboard. You can also select a specific state within that dashboard.
4. Click **Add**, then **Save** and **Apply** to save the widget settings.
5. Click **Save** in the dashboard toolbar.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-1.png', alt: 'Configure Navigate to other dashboard action', caption: 'Add action dialog — set source to "Widget header button", enter name, select type "Navigate to other dashboard", choose target dashboard "Temperature & Humidity", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-2.png', alt: 'Action in list', caption: 'The action appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-3.png', alt: 'Action chip in Actions section', caption: 'The action chip appears in the Actions section — click "Apply" to save widget settings.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-4.png', alt: 'Dashboard in edit mode', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-5.png', alt: 'Click action button in widget header', caption: 'Dashboard in view mode — click the action button in the widget header to navigate to the target dashboard.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-6.png', alt: 'Target dashboard opens', caption: 'The "Temperature & Humidity" dashboard opens with the Sensors table, alarms, and map.' },
]} />

### Custom action

Executes a custom JavaScript function when triggered. Useful for operations like deleting a device or asset directly from a table widget. Example — device deletion:

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **Action cell button** as the action source, enter a name (e.g. *Delete device*), and choose an icon.
3. Select **Custom action** as the action type. A JavaScript function editor appears.
4. Paste your custom function (example below), then click **Add**, **Save**, **Apply**, and **Save** the dashboard.
</Steps>

An example of the function for a device deletion:

```js maxLines=20 collapsible
let $injector = widgetContext.$scope.$injector;
let dialogs = $injector.get(widgetContext.servicesMap.get('dialogs'));
let deviceService = $injector.get(widgetContext.servicesMap.get('deviceService'));

openDeleteDeviceDialog();

function openDeleteDeviceDialog() {
    let title = "Are you sure you want to delete the device " + entityName + "?";
    let content = "Be careful, after the confirmation, the device and all related data will become unrecoverable!";
    dialogs.confirm(title, content, 'Cancel', 'Delete').subscribe(
        function(result) {
            if (result) {
                deleteDevice();
            }
        }
    );
}

function deleteDevice() {
    deviceService.deleteDevice(entityId.id).subscribe(
        function() {
            widgetContext.updateAliases();
        }
    );
}
```

Click **Add**, then **Save** and **Apply** to save the widget settings, and click **Save** in the dashboard toolbar.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-1.png', alt: 'Configure custom action with delete function', caption: 'Add action dialog — set source to "Action cell button", enter name "Delete device", select type "Custom action", paste the function, and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-2.png', alt: 'Select custom action type', caption: 'The action "Delete device" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-3.png', alt: 'Click Add', caption: 'The action chip "Delete device" appears in the Actions section — click "Apply" to save widget settings.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-4.png', alt: 'Review and save', caption: 'Dashboard in edit mode — trash action icons appear next to each entity row — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-5.png', alt: 'Save dashboard', caption: 'Dashboard in view mode — click the trash icon next to any entity to trigger the delete action.' },
]} />

Clicking the trash icon next to an entity opens a confirmation dialog.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-6.png', alt: 'Delete confirmation dialog', caption: 'A confirmation dialog appears — click "Delete" to confirm the deletion of the selected device.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-7.png', alt: 'Device removed from list', caption: 'The device "Compressor AQ-34" is removed from the list.' },
]} />

### Custom action (with HTML template)

Like **Custom action**, but the JavaScript function is rendered inside an HTML template — useful for building create/edit dialogs for entities. Four tabs are available:

- **Resources** — external JavaScript/CSS resources;
- **CSS** — custom styles;
- **HTML** — dialog markup;
- **JavaScript** — action logic.

This example adds two action buttons: one in the widget header to create entities, and one per row to edit them. Starting with the **add** action:

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **Widget header button** as the action source, enter a name (e.g. *Add device*), and choose an icon.
3. Select **Custom action (with HTML template)** as the action type.
4. In the **JavaScript** tab, paste the function for adding a new entity (example below).
5. In the **HTML** tab, paste the dialog markup (example below).
6. Click **Add**, then **Save** and **Apply** to save the widget settings.
</Steps>

Example of a JavaScript function that adds the ability to add a device or an asset:

```js maxLines=20 collapsible
let $injector = widgetContext.$scope.$injector;
let customDialog = $injector.get(widgetContext.servicesMap.get('customDialog'));
let assetService = $injector.get(widgetContext.servicesMap.get('assetService'));
let deviceService = $injector.get(widgetContext.servicesMap.get('deviceService'));
let attributeService = $injector.get(widgetContext.servicesMap.get('attributeService'));
let entityRelationService = $injector.get(widgetContext.servicesMap.get('entityRelationService'));

openAddEntityDialog();

function openAddEntityDialog() {
customDialog.customDialog(htmlTemplate, AddEntityDialogController).subscribe();
}

function AddEntityDialogController(instance) {
let vm = instance;

    vm.allowedEntityTypes = ['ASSET', 'DEVICE'];
    vm.entitySearchDirection = {
        from: "FROM",
        to: "TO"
    }

    vm.addEntityFormGroup = vm.fb.group({
     entityName: ['', [vm.validators.required]],
     entityType: ['DEVICE'],
     entityLabel: [null],
     type: ['', [vm.validators.required]],
     attributes: vm.fb.group({
         latitude: [null],
         longitude: [null],
         address: [null],
         owner: [null],
         number: [null, [vm.validators.pattern(/^-?[0-9]+$/)]],
         booleanValue: [null]
     }),
     relations: vm.fb.array([])
    });

    vm.cancel = function() {
        vm.dialogRef.close(null);
    };

    vm.relations = function() {
        return vm.addEntityFormGroup.get('relations');
    };

    vm.addRelation = function() {
        vm.relations().push(vm.fb.group({
         relatedEntity: [null, [vm.validators.required]],
         relationType: [null, [vm.validators.required]],
         direction: [null, [vm.validators.required]]
        }));
    };

    vm.removeRelation = function(index) {
        vm.relations().removeAt(index);
        vm.relations().markAsDirty();
    };

    vm.save = function() {
        vm.addEntityFormGroup.markAsPristine();
        saveEntityObservable().subscribe(
            function (entity) {
                widgetContext.rxjs.forkJoin([
                    saveAttributes(entity.id),
                    saveRelations(entity.id)
                ]).subscribe(
                    function () {
                        widgetContext.updateAliases();
                        vm.dialogRef.close(null);
                    }
                );
            }
        );
    };

    function saveEntityObservable() {
        const formValues = vm.addEntityFormGroup.value;
        let entity = {
            name: formValues.entityName,
            type: formValues.type,
            label: formValues.entityLabel
        };
        if (formValues.entityType == 'ASSET') {
            return assetService.saveAsset(entity);
        } else if (formValues.entityType == 'DEVICE') {
            return deviceService.saveDevice(entity);
        }
    }

    function saveAttributes(entityId) {
        let attributes = vm.addEntityFormGroup.get('attributes').value;
        let attributesArray = [];
        for (let key in attributes) {
            if(attributes[key] !== null) {
                attributesArray.push({key: key, value: attributes[key]});
            }
        }
        if (attributesArray.length > 0) {
            return attributeService.saveEntityAttributes(entityId, "SERVER_SCOPE", attributesArray);
        }
        return widgetContext.rxjs.of([]);
    }

    function saveRelations(entityId) {
        let relations = vm.addEntityFormGroup.get('relations').value;
        let tasks = [];
        for(let i=0; i < relations.length; i++) {
            let relation = {
                type: relations[i].relationType,
                typeGroup: 'COMMON'
            };
            if (relations[i].direction == 'FROM') {
                relation.to = relations[i].relatedEntity;
                relation.from = entityId;
            } else {
                relation.to = entityId;
                relation.from = relations[i].relatedEntity;
            }
            tasks.push(entityRelationService.saveRelation(relation));
        }
        if (tasks.length > 0) {
            return widgetContext.rxjs.forkJoin(tasks);
        }
        return widgetContext.rxjs.of([]);
    }
}
```

Switch to the **HTML** tab and enter the HTML template for the dialog:

Example of HTML code that adds the ability to add a device or an asset:

```html maxLines=20 collapsible
<form #addEntityForm="ngForm" [formGroup]="addEntityFormGroup"
      (ngSubmit)="save()" class="add-entity-form">
    <mat-toolbar fxLayout="row" color="primary">
        <h2>Add entity</h2>
        <span fxFlex></span>
        <button mat-icon-button (click)="cancel()" type="button">
            <mat-icon class="material-icons">close</mat-icon>
        </button>
    </mat-toolbar>
    <mat-progress-bar color="warn" mode="indeterminate" *ngIf="isLoading$ | async">
    </mat-progress-bar>
    <div style="height: 4px;" *ngIf="!(isLoading$ | async)"></div>
    <div mat-dialog-content fxLayout="column">
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Name</mat-label>
                <input matInput formControlName="entityName" required>
                <mat-error *ngIf="addEntityFormGroup.get('entityName').hasError('required')">
                    Entity name is required.
                </mat-error>
            </mat-form-field>
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Label</mat-label>
                <input matInput formControlName="entityLabel" >
            </mat-form-field>
        </div>
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <tb-entity-type-select
                    class="mat-block"
                    formControlName="entityType"
                    [showLabel]="true"
                    [allowedEntityTypes]="allowedEntityTypes"
            ></tb-entity-type-select>
            <tb-entity-subtype-autocomplete
                    fxFlex *ngIf="addEntityFormGroup.get('entityType').value == 'ASSET'"
                    class="mat-block"
                    formControlName="type"
                    [required]="true"
                    [entityType]="'ASSET'"
            ></tb-entity-subtype-autocomplete>
            <tb-entity-subtype-autocomplete
                    fxFlex *ngIf="addEntityFormGroup.get('entityType').value != 'ASSET'"
                    class="mat-block"
                    formControlName="type"
                    [required]="true"
                    [entityType]="'DEVICE'"
            ></tb-entity-subtype-autocomplete>
        </div>
        <div formGroupName="attributes" fxLayout="column">
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Latitude</mat-label>
                    <input type="number" step="any" matInput formControlName="latitude">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Longitude</mat-label>
                    <input type="number" step="any" matInput formControlName="longitude">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Address</mat-label>
                    <input matInput formControlName="address">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Owner</mat-label>
                    <input matInput formControlName="owner">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Integer Value</mat-label>
                    <input type="number" step="1" matInput formControlName="number">
                    <mat-error *ngIf="addEntityFormGroup.get('attributes.number').hasError('pattern')">
                        Invalid integer value.
                    </mat-error>
                </mat-form-field>
                <div class="boolean-value-input" fxLayout="column" fxLayoutAlign="center start" fxFlex>
                    <label class="checkbox-label">Boolean Value</label>
                    <mat-checkbox formControlName="booleanValue" style="margin-bottom: 40px;">
                        {{ (addEntityFormGroup.get('attributes.booleanValue').value ? "value.true" : "value.false") | translate }}
                    </mat-checkbox>
                </div>
            </div>
        </div>
        <div class="relations-list">
            <div class="mat-body-1" style="padding-bottom: 10px; color: rgba(0,0,0,0.57);">Relations</div>
            <div class="body" [fxShow]="relations().length">
                <div class="row" fxLayout="row" fxLayoutAlign="start center" formArrayName="relations" *ngFor="let relation of relations().controls; let i = index;">
                    <div [formGroupName]="i" class="mat-elevation-z2" fxFlex fxLayout="row" style="padding: 5px 0 5px 5px;">
                        <div fxFlex fxLayout="column">
                            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                                <mat-form-field class="mat-block" style="min-width: 100px;">
                                    <mat-label>Direction</mat-label>
                                    <mat-select formControlName="direction" name="direction">
                                        <mat-option *ngFor="let direction of entitySearchDirection | keyvalue" [value]="direction.value">
                                            {{ ("relation.search-direction." + direction.value) | translate}}
                                        </mat-option>
                                    </mat-select>
                                    <mat-error *ngIf="relation.get('direction').hasError('required')">
                                        Relation direction is required.
                                    </mat-error>
                                </mat-form-field>
                                <tb-relation-type-autocomplete
                                        fxFlex class="mat-block"
                                        formControlName="relationType"
                                        [required]="true">
                                </tb-relation-type-autocomplete>
                            </div>
                            <div fxLayout="row" fxLayout.xs="column">
                                <tb-entity-select
                                        fxFlex class="mat-block"
                                        [required]="true"
                                        formControlName="relatedEntity">
                                </tb-entity-select>
                            </div>
                        </div>
                        <div fxLayout="column" fxLayoutAlign="center center">
                            <button mat-icon-button color="primary"
                                    aria-label="Remove"
                                    type="button"
                                    (click)="removeRelation(i)"
                                    matTooltip="Remove relation"
                                    matTooltipPosition="above">
                                <mat-icon>close</mat-icon>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <div>
                <button mat-raised-button color="primary"
                        type="button"
                        (click)="addRelation()"
                        matTooltip="Add Relation"
                        matTooltipPosition="above">
                    Add
                </button>
            </div>
        </div>
    </div>
    <div mat-dialog-actions fxLayout="row" fxLayoutAlign="end center">
        <button mat-button color="primary"
                type="button"
                [disabled]="(isLoading$ | async)"
                (click)="cancel()" cdkFocusInitial>
            Cancel
        </button>
        <button mat-button mat-raised-button color="primary"
                type="submit"
                [disabled]="(isLoading$ | async) || addEntityForm.invalid || !addEntityForm.dirty">
            Create
        </button>
    </div>
</form>
```

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-1.png', alt: 'Configure custom action with HTML template', caption: 'Add action dialog — set source to "Widget header button", enter name "Add device", select type "Custom action (with HTML template)", and paste the JavaScript function.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-2.png', alt: 'Enter HTML code', caption: 'JS tab — the complete JavaScript function for the "Add device" action.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-3.png', alt: 'Click Add', caption: 'Switch to the HTML tab, enter the HTML template for the form, and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-4.png', alt: 'Add device action saved in list', caption: 'The "Add device" action is saved in the list — click "+" to add the second action.' },
]} />

Now add a second action — a per-row **Edit** button. Open a new Add action dialog (click **+** in the Actions list), select **Action cell button** as the source, enter a name (e.g. *Edit device*), select **Custom action (with HTML template)**, and follow the same steps: paste the JavaScript function and the HTML template below.

Example of a JavaScript function that adds a possibility to edit a device or an asset:

```js maxLines=20 collapsible
let $injector = widgetContext.$scope.$injector;
let customDialog = $injector.get(widgetContext.servicesMap.get('customDialog'));
let entityService = $injector.get(widgetContext.servicesMap.get('entityService'));
let assetService = $injector.get(widgetContext.servicesMap.get('assetService'));
let deviceService = $injector.get(widgetContext.servicesMap.get('deviceService'));
let attributeService = $injector.get(widgetContext.servicesMap.get('attributeService'));
let entityRelationService = $injector.get(widgetContext.servicesMap.get('entityRelationService'));

openEditEntityDialog();

function openEditEntityDialog() {
customDialog.customDialog(htmlTemplate, EditEntityDialogController).subscribe();
}

function EditEntityDialogController(instance) {
let vm = instance;

    vm.entityName = entityName;
    vm.entityType = entityId.entityType;
    vm.entitySearchDirection = {
        from: "FROM",
        to: "TO"
    };
    vm.attributes = {};
    vm.oldRelationsData = [];
    vm.relationsToDelete = [];
    vm.entity = {};

    vm.editEntityFormGroup = vm.fb.group({
        entityName: ['', [vm.validators.required]],
        entityType: [null],
        entityLabel: [null],
        type: ['', [vm.validators.required]],
        attributes: vm.fb.group({
            latitude: [null],
            longitude: [null],
            address: [null],
            owner: [null],
            number: [null, [vm.validators.pattern(/^-?[0-9]+$/)]],
            booleanValue: [false]
        }),
        oldRelations: vm.fb.array([]),
        relations: vm.fb.array([])
    });

    getEntityInfo();

    vm.cancel = function() {
        vm.dialogRef.close(null);
    };

    vm.relations = function() {
        return vm.editEntityFormGroup.get('relations');
    };

    vm.oldRelations = function() {
        return vm.editEntityFormGroup.get('oldRelations');
    };

    vm.addRelation = function() {
        vm.relations().push(vm.fb.group({
            relatedEntity: [null, [vm.validators.required]],
            relationType: [null, [vm.validators.required]],
            direction: [null, [vm.validators.required]]
        }));
    };

    function addOldRelation() {
        vm.oldRelations().push(vm.fb.group({
            relatedEntity: [{value: null, disabled: true}, [vm.validators.required]],
            relationType: [{value: null, disabled: true}, [vm.validators.required]],
            direction: [{value: null, disabled: true}, [vm.validators.required]]
        }));
    }

    vm.removeRelation = function(index) {
        vm.relations().removeAt(index);
        vm.relations().markAsDirty();
    };

    vm.removeOldRelation = function(index) {
        vm.oldRelations().removeAt(index);
        vm.relationsToDelete.push(vm.oldRelationsData[index]);
        vm.oldRelations().markAsDirty();
    };

    vm.save = function() {
        vm.editEntityFormGroup.markAsPristine();
        widgetContext.rxjs.forkJoin([
            saveAttributes(entityId),
            saveRelations(entityId),
            saveEntity()
        ]).subscribe(
            function () {
                widgetContext.updateAliases();
                vm.dialogRef.close(null);
            }
        );
    };

    function getEntityAttributes(attributes) {
        for (var i = 0; i < attributes.length; i++) {
            vm.attributes[attributes[i].key] = attributes[i].value;
        }
    }

    function getEntityRelations(relations) {
        let relationsFrom = relations[0];
        let relationsTo = relations[1];
        for (let i=0; i < relationsFrom.length; i++) {
            let relation = {
                direction: 'FROM',
                relationType: relationsFrom[i].type,
                relatedEntity: relationsFrom[i].to
            };
            vm.oldRelationsData.push(relation);
            addOldRelation();
        }
        for (let i=0; i < relationsTo.length; i++) {
            let relation = {
                direction: 'TO',
                relationType: relationsTo[i].type,
                relatedEntity: relationsTo[i].from
            };
            vm.oldRelationsData.push(relation);
            addOldRelation();
        }
    }

    function getEntityInfo() {
        widgetContext.rxjs.forkJoin([
            entityRelationService.findInfoByFrom(entityId),
            entityRelationService.findInfoByTo(entityId),
            attributeService.getEntityAttributes(entityId, 'SERVER_SCOPE'),
            entityService.getEntity(entityId.entityType, entityId.id)
        ]).subscribe(
            function (data) {
                getEntityRelations(data.slice(0,2));
                getEntityAttributes(data[2]);
                vm.entity = data[3];
                vm.editEntityFormGroup.patchValue({
                    entityName: vm.entity.name,
                    entityType: vm.entityType,
                    entityLabel: vm.entity.label,
                    type: vm.entity.type,
                    attributes: vm.attributes,
                    oldRelations: vm.oldRelationsData
                }, {emitEvent: false});
            }
        );
    }

    function saveEntity() {
        const formValues = vm.editEntityFormGroup.value;
        if (vm.entity.label !== formValues.entityLabel){
            vm.entity.label = formValues.entityLabel;
            if (formValues.entityType == 'ASSET') {
                return assetService.saveAsset(vm.entity);
            } else if (formValues.entityType == 'DEVICE') {
                return deviceService.saveDevice(vm.entity);
            }
        }
        return widgetContext.rxjs.of([]);
    }

    function saveAttributes(entityId) {
        let attributes = vm.editEntityFormGroup.get('attributes').value;
        let attributesArray = [];
        for (let key in attributes) {
            if (attributes[key] !== vm.attributes[key]) {
                attributesArray.push({key: key, value: attributes[key]});
            }
        }
        if (attributesArray.length > 0) {
            return attributeService.saveEntityAttributes(entityId, "SERVER_SCOPE", attributesArray);
        }
        return widgetContext.rxjs.of([]);
    }

    function saveRelations(entityId) {
        let relations = vm.editEntityFormGroup.get('relations').value;
        let tasks = [];
        for(let i=0; i < relations.length; i++) {
            let relation = {
                type: relations[i].relationType,
                typeGroup: 'COMMON'
            };
            if (relations[i].direction == 'FROM') {
                relation.to = relations[i].relatedEntity;
                relation.from = entityId;
            } else {
                relation.to = entityId;
                relation.from = relations[i].relatedEntity;
            }
            tasks.push(entityRelationService.saveRelation(relation));
        }
        for (let i=0; i < vm.relationsToDelete.length; i++) {
            let relation = {
                type: vm.relationsToDelete[i].relationType
            };
            if (vm.relationsToDelete[i].direction == 'FROM') {
                relation.to = vm.relationsToDelete[i].relatedEntity;
                relation.from = entityId;
            } else {
                relation.to = entityId;
                relation.from = vm.relationsToDelete[i].relatedEntity;
            }
            tasks.push(entityRelationService.deleteRelation(relation.from, relation.type, relation.to));
        }
        if (tasks.length > 0) {
            return widgetContext.rxjs.forkJoin(tasks);
        }
        return widgetContext.rxjs.of([]);
    }
}
```

Switch to the **HTML** tab and enter the HTML template for the edit dialog:

Example of HTML code that adds a possibility to edit a device or an asset:

```html maxLines=20 collapsible
<form #editEntityForm="ngForm" [formGroup]="editEntityFormGroup"
      (ngSubmit)="save()"  class="edit-entity-form">
    <mat-toolbar fxLayout="row" color="primary">
        <h2>Edit {{entityType.toLowerCase()}} {{entityName}}</h2>
        <span fxFlex></span>
        <button mat-icon-button (click)="cancel()" type="button">
            <mat-icon class="material-icons">close</mat-icon>
        </button>
    </mat-toolbar>
    <mat-progress-bar color="warn" mode="indeterminate" *ngIf="isLoading$ | async">
    </mat-progress-bar>
    <div style="height: 4px;" *ngIf="!(isLoading$ | async)"></div>
    <div mat-dialog-content fxLayout="column">
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Name</mat-label>
                <input matInput formControlName="entityName" required readonly="">
            </mat-form-field>
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Label</mat-label>
                <input matInput formControlName="entityLabel">
            </mat-form-field>
        </div>
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Type</mat-label>
                <input matInput formControlName="entityType" readonly>
            </mat-form-field>
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Type</mat-label>
                <input matInput formControlName="type" readonly>
            </mat-form-field>
        </div>
        <div formGroupName="attributes" fxLayout="column">
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Latitude</mat-label>
                    <input type="number" step="any" matInput formControlName="latitude">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Longitude</mat-label>
                    <input type="number" step="any" matInput formControlName="longitude">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Address</mat-label>
                    <input matInput formControlName="address">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Owner</mat-label>
                    <input matInput formControlName="owner">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Integer Value</mat-label>
                    <input type="number" step="1" matInput formControlName="number">
                    <mat-error *ngIf="editEntityFormGroup.get('attributes.number').hasError('pattern')">
                        Invalid integer value.
                    </mat-error>
                </mat-form-field>
                <div class="boolean-value-input" fxLayout="column" fxLayoutAlign="center start" fxFlex>
                    <label class="checkbox-label">Boolean Value</label>
                    <mat-checkbox formControlName="booleanValue" style="margin-bottom: 40px;">
                        {{ (editEntityFormGroup.get('attributes.booleanValue').value ? "value.true" : "value.false") | translate }}
                    </mat-checkbox>
                </div>
            </div>
        </div>
        <div class="relations-list old-relations">
            <div class="mat-body-1" style="padding-bottom: 10px; color: rgba(0,0,0,0.57);">Relations</div>
            <div class="body" [fxShow]="oldRelations().length">
                <div class="row" fxLayout="row" fxLayoutAlign="start center" formArrayName="oldRelations"
                     *ngFor="let relation of oldRelations().controls; let i = index;">
                    <div [formGroupName]="i" class="mat-elevation-z2" fxFlex fxLayout="row" style="padding: 5px 0 5px 5px;">
                        <div fxFlex fxLayout="column">
                            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                                <mat-form-field class="mat-block" style="min-width: 100px;">
                                    <mat-label>Direction</mat-label>
                                    <mat-select formControlName="direction" name="direction">
                                        <mat-option *ngFor="let direction of entitySearchDirection | keyvalue" [value]="direction.value">
                                            {{ ("relation.search-direction." + direction.value) | translate}}
                                        </mat-option>
                                    </mat-select>
                                    <mat-error *ngIf="relation.get('direction').hasError('required')">
                                        Relation direction is required.
                                    </mat-error>
                                </mat-form-field>
                                <tb-relation-type-autocomplete
                                        fxFlex class="mat-block"
                                        formControlName="relationType"
                                        required="true">
                                </tb-relation-type-autocomplete>
                            </div>
                            <div fxLayout="row" fxLayout.xs="column">
                                <tb-entity-select
                                        fxFlex class="mat-block"
                                        required="true"
                                        formControlName="relatedEntity">
                                </tb-entity-select>
                            </div>
                        </div>
                        <div fxLayout="column" fxLayoutAlign="center center">
                            <button mat-icon-button color="primary"
                                    aria-label="Remove"
                                    type="button"
                                    (click)="removeOldRelation(i)"
                                    matTooltip="Remove relation"
                                    matTooltipPosition="above">
                                <mat-icon>close</mat-icon>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="relations-list">
            <div class="mat-body-1" style="padding-bottom: 10px; color: rgba(0,0,0,0.57);">New Relations</div>
            <div class="body" [fxShow]="relations().length">
                <div class="row" fxLayout="row" fxLayoutAlign="start center" formArrayName="relations" *ngFor="let relation of relations().controls; let i = index;">
                    <div [formGroupName]="i" class="mat-elevation-z2" fxFlex fxLayout="row" style="padding: 5px 0 5px 5px;">
                        <div fxFlex fxLayout="column">
                            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                                <mat-form-field class="mat-block" style="min-width: 100px;">
                                    <mat-label>Direction</mat-label>
                                    <mat-select formControlName="direction" name="direction">
                                        <mat-option *ngFor="let direction of entitySearchDirection | keyvalue" [value]="direction.value">
                                            {{ ("relation.search-direction." + direction.value) | translate}}
                                        </mat-option>
                                    </mat-select>
                                    <mat-error *ngIf="relation.get('direction').hasError('required')">
                                        Relation direction is required.
                                    </mat-error>
                                </mat-form-field>
                                <tb-relation-type-autocomplete
                                        fxFlex class="mat-block"
                                        formControlName="relationType"
                                        [required]="true">
                                </tb-relation-type-autocomplete>
                            </div>
                            <div fxLayout="row" fxLayout.xs="column">
                                <tb-entity-select
                                        fxFlex class="mat-block"
                                        [required]="true"
                                        formControlName="relatedEntity">
                                </tb-entity-select>
                            </div>
                        </div>
                        <div fxLayout="column" fxLayoutAlign="center center">
                            <button mat-icon-button color="primary"
                                    aria-label="Remove"
                                    type="button"
                                    (click)="removeRelation(i)"
                                    matTooltip="Remove relation"
                                    matTooltipPosition="above">
                                <mat-icon>close</mat-icon>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <div>
                <button mat-raised-button color="primary"
                        type="button"
                        (click)="addRelation()"
                        matTooltip="Add Relation"
                        matTooltipPosition="above">
                    Add
                </button>
            </div>
        </div>
    </div>
    <div mat-dialog-actions fxLayout="row" fxLayoutAlign="end center">
        <button mat-button color="primary"
                type="button"
                [disabled]="(isLoading$ | async)"
                (click)="cancel()" cdkFocusInitial>
            Cancel
        </button>
        <button mat-button mat-raised-button color="primary"
                type="submit"
                [disabled]="(isLoading$ | async) || editEntityForm.invalid || !editEntityForm.dirty">
            Save
        </button>
    </div>
</form>
```

Click **Add**, then **Save** and **Apply** to save the widget settings, and click **Save** in the dashboard toolbar.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-5.png', alt: 'Actions list before adding Edit device', caption: 'Actions list with "Add device" — click "+" to open a new Add action dialog for the "Edit device" action.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-6.png', alt: 'JavaScript tab for Edit device action', caption: 'JS tab — the complete JavaScript function for the "Edit device" action.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-7.png', alt: 'HTML tab for Edit device action', caption: 'Switch to the HTML tab, enter the HTML template for the edit form, and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-8.png', alt: 'Both actions in list', caption: 'Both actions — "Edit device" and "Add device" — appear in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-9.png', alt: 'Apply and save dashboard', caption: 'The action chips appear in the Actions section — click "Apply", then "Save" to save the dashboard.' },
]} />

Click the **Add device** button in the widget header to open the create dialog. Select entity type "Device", fill in the required fields, and click **Create**.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-10.png', alt: 'Click Add device button', caption: 'Dashboard in view mode — click the "Add device" button in the widget header to open the create dialog.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-11.png', alt: 'Fill in entity details', caption: 'In the "Add entity" dialog, enter entity name, select type "Device", choose device profile, and click "Create".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-12.png', alt: 'Click Create', caption: 'The new entity "Compressor GF-11" is added to the table — pencil icons are available to edit each entity.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-13.png', alt: 'New entity in table', caption: 'Click the pencil icon next to any entity to open the edit dialog.' },
]} />

Click the pencil icon next to any entity to open the edit dialog. Make the necessary changes and click **Save**.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-14.png', alt: 'Click edit action button', caption: 'Dashboard in view mode — click the pencil icon next to the entity you want to edit.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-15.png', alt: 'Edit entity dialog', caption: 'The "Edit" dialog opens pre-filled with the entity\'s current data.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-16.png', alt: 'Make changes and save', caption: 'Make the necessary changes and click "Save".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-17.png', alt: 'Entity updated', caption: 'The entity is updated — the "address" column now shows "Kyiv" for "Compressor AQ-34".' },
]} />

### Mobile action

Triggers a native mobile capability on the user's device when the action button is tapped in the ThingsBoard mobile app. Available mobile action types:

- **Scan QR code** — opens the camera to scan a QR code and processes the result in a custom JavaScript function.
- **Take photo** — opens the camera to capture a photo, which is then handled by a custom function (e.g. uploaded as an attribute).
- **Get location** — fetches the device's current GPS coordinates and passes them to a custom function.
- **Make phone call** — dials a phone number provided by a custom function or taken from an entity attribute.
- **Open URL** — opens a URL in the mobile app's in-app browser.

Mobile actions are only active inside the ThingsBoard mobile app — on the web dashboard they are not triggered. See {[Products.PE, Products.PAAS, Products.PAAS_EU].includes(props.product) ? <a href="/docs/mobile/pe/mobile-actions/" target="_blank"><b>mobile actions</b></a> : <a href="/docs/mobile/mobile-actions/" target="_blank"><b>mobile actions</b></a>} for the full configuration guide.

### Open URL

Opens any URL resource — a web page, document, image, or an internal ThingsBoard page. Can optionally open in a new browser tab. The URL field supports template variables such as `${entityName}` and `${entityId}` to build dynamic links based on the entity context.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select the action source (e.g. **Widget header button**), enter a name, and choose an icon.
3. Select **Open URL** as the action type. Enter the target URL. Optionally enable **Open in a new browser tab**.
4. Click **Add**, then **Save** and **Apply** to save the widget settings.
5. Click **Save** in the dashboard toolbar.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/open-url-1.png', alt: 'Configure Open URL action', caption: 'Add action dialog — set source to "Widget header button", enter name "Working with widgets", select type "Open URL", paste the target URL, enable "Open in a new browser tab", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/open-url-2.png', alt: 'Action in list', caption: 'The action "Working with widgets" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/open-url-3.png', alt: 'Action chip in Actions section', caption: 'The action chip "Working with widgets" appears in the Actions section — click "Apply" to save widget settings.' },
  { src: '/src/assets/images/user-guide/actions/open-url-4.png', alt: 'Dashboard in edit mode', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/open-url-5.png', alt: 'Click action button in widget header', caption: 'Dashboard in view mode — click the action button in the widget header to open the URL.' },
  { src: '/src/assets/images/user-guide/actions/open-url-6.png', alt: 'Target URL opens in new tab', caption: 'The "Working with Widgets" documentation page opens in a new browser tab.' },
]} />

### Place map item

<table style={{width:'auto'}}><thead><tr><td style={{textAlign:'center'}}><strong><em>Only for Map widgets</em></strong></td></tr></thead></table>

The **Place map item** action adds a button to the map widget that lets users create a new entity and immediately position it on the map. After clicking the button, the user clicks a location on the map — a "Create entity" dialog opens, and once confirmed, the entity is saved to ThingsBoard and a new item appears at the chosen coordinates.

Four item types are supported:

| Item type | Description |
|-----------|-------------|
| **Marker** | A point marker placed at the clicked location. |
| **Circle** | A circle centered on the clicked point, with a configurable radius. |
| **Polygon** | A polygon drawn by clicking multiple vertices, closed on the final click. |
| **Polyline** | A line path drawn by clicking multiple points. |

The action is pre-filled with a default JavaScript function that opens a standard create dialog. You can replace it with a custom function — using the same JS + HTML template approach as [Custom action (with HTML template)](#custom-action-with-html-template) — to control which fields appear in the dialog.

**Example:** add an "Add EV station" button that places new charging station assets directly on the map.

<h4>Configure the datasource</h4>

For newly created entities to appear on the map immediately, the widget's datasource alias must resolve to the entity type you will be creating. Configure this before adding the action:

<Steps>
1. In the widget settings, open the **Overlays** section and select the **Marker** tab.
2. Create a new entity alias — name it "EV stations", set the filter type to **Asset type**, and specify the type as "EV station".
3. Click **Apply**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/data-source-for-action-1.png', alt: 'Create EV stations alias in Overlays section', caption: 'Map widget settings — Overlays > Marker tab. Enter alias name "EV stations" and click "Create a new one!".' },
  { src: '/src/assets/images/user-guide/actions/data-source-for-action-2.png', alt: 'Configure alias filter type', caption: '"Add alias" dialog — set filter type to "Asset type", specify type "EV station", click "Add", then click "Apply".' },
]} />

<h4>Configure the action</h4>

<Steps>
1. Open the widget in edit mode, scroll to the **Actions** section, and click **Add action**, then click **+**.
2. Select **Widget header button** as the action source. Enter a name (e.g. *Add EV station*), then configure the button appearance — type, icon, and optional background color.
3. From the **Action** dropdown, select **Place map item** and choose the item type — in this example, **Marker**.
4. The JavaScript function field is pre-filled with the default create dialog — leave it unchanged or replace it with a custom function.
5. Click **Add**, then **Save**, **Apply**, and **Save** the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-1.png', alt: 'Actions section with Add action button', caption: 'Map widget settings — scroll to the Actions section and click "Add action".' },
  { src: '/src/assets/images/user-guide/actions/action-2.png', alt: 'Empty Actions list', caption: 'The Actions list is empty — click "+" to open the Add action dialog.' },
  { src: '/src/assets/images/user-guide/actions/action-3.png', alt: 'Configure action source and name', caption: 'Add action dialog — set source to "Widget header button" and enter name "Add EV station".' },
  { src: '/src/assets/images/user-guide/actions/action-4.png', alt: 'Configure button appearance', caption: 'Configure button appearance — set type to "Raised", enable the icon, and optionally set a custom background color.' },
  { src: '/src/assets/images/user-guide/actions/action-5.png', alt: 'Select Place map item and Marker type', caption: 'Select "Place map item" as the action type, choose "Marker" as the item type, and review the pre-filled default function.' },
  { src: '/src/assets/images/user-guide/actions/action-6.png', alt: 'Action in list, click Save', caption: 'The "Add EV station" action appears in the Actions list — click "Save" to close the list.' },
  { src: '/src/assets/images/user-guide/actions/action-7.png', alt: 'Dashboard saved with Add EV station button', caption: 'The dashboard is saved — the "Add EV station" button is now visible in the map widget header.' },
]} />

<h4>Place an item</h4>

Click **Add EV station** in the widget header to activate placement mode, then click the desired location on the map. An **"Add entity"** dialog opens — enter a name, select the entity and asset type, and click **Create**. The new marker appears on the map immediately.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-place-marker-1.png', alt: 'Placement mode activated', caption: 'Click the "Add EV station" button in the widget header (①) — the map enters placement mode. Find the desired location and click on it (②).' },
  { src: '/src/assets/images/user-guide/actions/action-place-marker-2.png', alt: 'Add entity dialog', caption: 'The "Add entity" dialog opens — enter a name (e.g. "EV station 1"), set Entity Name to "Asset" and Asset type to "EV station", then click "Create".' },
  { src: '/src/assets/images/user-guide/actions/action-place-marker-3.png', alt: 'New marker on map', caption: 'The new "EV station 1" marker appears on the map immediately after creation.' },
]} />

Navigate to **Assets** to confirm the new asset was saved.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-created-asset-1.png', alt: 'New asset on Assets page', caption: 'The newly created "EV station 1" asset appears on the Assets page.' },
]} />

## Action sources

An action source defines what the user does to trigger an action — clicking a button, clicking a row, double-clicking, selecting a map marker, and so on. Available sources depend on the widget type. The examples below use the **Entities table** widget with the **Navigate to new dashboard state** action type.

### Widget header button

Adds an action button to the widget header — applies to the whole widget, not to individual entities. Available in all widget types and is the most commonly used action source.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **Widget header button** as the action source.
3. Enter an action name and choose an icon for the button.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Save** and **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/widget-header-button-1.png', alt: 'Configure widget header button action', caption: 'Add action dialog — set source to "Widget header button", enter name "Map", select type "Navigate to new dashboard state", choose target state "map", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-2.png', alt: 'Widget header button action in list', caption: 'The action "Map" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-3.png', alt: 'Widget header button action chip', caption: 'The action chip "Map" appears in the Actions section — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-4.png', alt: 'Dashboard with widget header button', caption: 'Dashboard in edit mode — the action button appears in the widget header — click "Save" to save the dashboard.' },
]} />

**Result**

<MultiProductImageGallery product={props.product} images={[

  { src: '/src/assets/images/user-guide/actions/widget-header-button-5.png', alt: 'Click widget header button', caption: 'Dashboard in view mode — click the action button in the widget header to execute the configured action.' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-6.png', alt: 'Navigated to target state', caption: 'The "Map" state opens with an OpenStreetMap widget.' },
]} />

### Action cell button

Adds an icon button to each entity row. Clicking it executes the configured action for that specific entity — useful for navigating to details, editing, or deleting individual items.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **Action cell button** as the action source.
3. Enter an action name and choose an icon for the button.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Save** and **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-cell-button-1.png', alt: 'Configure action cell button', caption: 'Add action dialog — set source to "Action cell button", enter name "Temperature details", select type "Navigate to new dashboard state", choose target state "temperature_sensors", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-2.png', alt: 'Action cell button in list', caption: 'The action "Temperature details" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-3.png', alt: 'Action cell button chip', caption: 'The action chip "Temperature details" appears in the Actions section — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-4.png', alt: 'Dashboard with action cell buttons', caption: 'Dashboard in edit mode — an action button appears next to each entity row — click "Save" to save the dashboard.' },
]} />

**Result**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-cell-button-5.png', alt: 'Click action cell button', caption: 'Dashboard in view mode — click the action button next to any entity to execute the configured action.' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-6.png', alt: 'Navigated to target state', caption: 'The configured state opens, showing details for the selected entity.' },
]} />


### On row click

Triggers when the user clicks anywhere on an entity row — the entire row is the clickable target, with no dedicated button shown. Can be combined with **On row double click** on the same widget to handle single and double clicks with different actions.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **On row click** as the action source.
3. Enter an action name. Optionally, choose an icon.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Save** and **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-click-1.png', alt: 'Configure On row click action', caption: 'Add action dialog — set source to "On row click", enter name "Device details", select type "Navigate to new dashboard state", choose target state "device_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-2.png', alt: 'On row click action in list', caption: 'The action "Device details" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-3.png', alt: 'On row click action chip', caption: 'The action chip "Device details" appears in the Actions section — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-4.png', alt: 'Dashboard ready for row click', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
]} />

**Result**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-click-5.png', alt: 'Click entity row', caption: 'Dashboard in view mode — click any entity row to navigate to the "device_details" state.' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-6.png', alt: 'Device details state', caption: 'The "Device details" state opens, showing detailed information for the selected device.' },
]} />

### On row double click

Triggers when the user double-clicks an entity row. Can be combined with **On row click** on the same widget to handle single and double clicks with different actions.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **On row double click** as the action source.
3. Enter an action name. Optionally, choose an icon.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Save** and **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-1.png', alt: 'Configure On row double click action', caption: 'Add action dialog — set source to "On row double click", enter name "Device details", select type "Navigate to new dashboard state", choose target state "device_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-2.png', alt: 'On row double click action in list', caption: 'The action "Device details" appears in the list — click "Save".' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-3.png', alt: 'On row double click action chip', caption: 'The action chip "Device details" appears in the Actions section — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-4.png', alt: 'Dashboard ready for row double click', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
]} />

**Result**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-5.png', alt: 'Double-click entity row', caption: 'Dashboard in view mode — double-click any entity row to navigate to the "device_details" state.' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-6.png', alt: 'Device details state', caption: 'The "Device details" state opens, showing temperature card, time series chart, and map for the selected device.' },
]} />

### On cell click

<table style={{width:'auto'}}><thead><tr><td style={{textAlign:'center'}}><strong><em>Only for Entities table, Alarms table, Device admin table, and Asset admin table widgets</em></strong></td></tr></thead></table>

Triggers when a specific table cell is clicked. Each column can have its own independent action, configured by column index. Unlike **On row click**, this lets you assign different behaviors to different columns — for example, clicking the temperature cell opens a temperature popover, while clicking the humidity cell opens a humidity popover.

**Configure the temperature column action:**

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **On cell click** as the action source.
3. From the **Column index** dropdown, select the **temperature** column.
4. Enter an action name, select **Navigate to new dashboard state**, and choose the target state.
5. In **Dashboard state display options**, select **Open in popover**, set placement to **Right**, and specify width/height.
6. Click **Add**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-cell-click-1.png', alt: 'Select On cell click source and column', caption: 'Add action dialog — set source to "On cell click" and select column "1 (temperature)" from the Column index dropdown.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-2-1.png', alt: 'Configure temperature action name and target', caption: 'Enter name "Temperature value from the device", select type "Navigate to new dashboard state", and choose target state "device_temperature".' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-2-2.png', alt: 'Configure temperature popover settings', caption: 'Set "Dashboard state display option" to "Open in popover", specify placement "Right", width 45 vw, height 28 vh, and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-3.png', alt: 'Temperature action in list', caption: 'The temperature action "Temperature value from the device" appears in the actions list.' },
]} />

**Configure the humidity column action:**

<Steps>
1. Click **+** to add another action.
2. Select **On cell click** as the action source.
3. From the **Column index** dropdown, select the **humidity** column.
4. Enter an action name, select **Navigate to new dashboard state**, and choose the target state.
5. In **Dashboard state display options**, select **Open in popover**, set placement to **Bottom**, and specify width/height.
6. Click **Add**, then **Save** and **Apply** to save the widget settings.
7. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-cell-click-4.png', alt: 'Actions list with temperature action', caption: 'Actions list showing the temperature action — click "+" to add the humidity column action.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-5.png', alt: 'Add humidity action dialog source', caption: 'Add action dialog — set source to "On cell click" and select column "2 (humidity)" from the Column index dropdown.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-6-1.png', alt: 'Configure humidity action name and target', caption: 'Enter name "Humidity value from the device", select type "Navigate to new dashboard state", and choose target state "device_humidity".' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-6-2.png', alt: 'Configure humidity popover settings', caption: 'Set "Dashboard state display option" to "Open in popover", specify placement "Bottom", width 40 vw, height 40 vh, and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-7.png', alt: 'Both actions in list', caption: 'Both actions appear in the list — click "Save" to close the actions dialog.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-8.png', alt: 'Click Apply to save widget settings', caption: 'Widget settings panel — click "Apply" to save the widget configuration.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-9.png', alt: 'Save the dashboard', caption: 'Dashboard in edit mode — click "Save" in the toolbar to save the dashboard.' },
]} />

Both actions are now configured. Clicking a temperature or humidity cell opens the corresponding details popover for the selected device.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-cell-click-10.png', alt: 'Click temperature column cell', caption: 'Dashboard in view mode — click any cell in the "temperature" column to trigger the action.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-11.png', alt: 'Temperature details popover open', caption: 'Clicking a temperature cell opens the temperature details popover for the selected device (e.g., Compressor AQ-34 at 55 °C).' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-12.png', alt: 'Click humidity column cell', caption: 'Click any cell in the "humidity" column to trigger the humidity action.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-13.png', alt: 'Humidity details popover open', caption: 'Clicking a humidity cell opens the humidity details popover for the selected device (e.g., Compressor OI-73 at 52 %).' },
]} />

### On node selected

<table style={{width:'auto'}}><thead><tr><td style={{textAlign:'center'}}><strong><em>Only for Entities Hierarchy widget</em></strong></td></tr></thead></table>

Triggers when the user selects a node in the <DocLink product={props.product} path="user-guide/widgets/entity-widgets/entities-hierarchy">Entities Hierarchy</DocLink> widget, which displays entities and their relations as a tree. Clicking any node executes the configured action for that entity.


<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-node-selected-1.png', alt: 'Add Entity hierarchy widget', caption: 'Dashboard in edit mode — click "+ Add widget" to start adding the Entity hierarchy widget.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-2.png', alt: 'Configure widget data source', caption: 'Widget bundle selection — select the "Entity widgets" bundle.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-3.png', alt: 'Open Add action window', caption: 'Entity widgets — select the "Entities hierarchy" widget.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-4.png', alt: 'Select On node selected', caption: 'In widget configuration, navigate to the "Actions" tab and click the "+" icon to add a new action.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-5.png', alt: 'Configure action', caption: 'Add action dialog — set source to "On node selected", enter name "Node", select type "Navigate to new dashboard state", choose target state "device_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-6.png', alt: 'Click Add', caption: 'The action "Node" appears in the list — click "Add" to add the widget to the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-7.png', alt: 'Save action and dashboard', caption: 'Dashboard in edit mode — the "Entities hierarchy" widget shows the hierarchy tree — click "Save" to save the dashboard.' },
]} />

To perform an action you should click any of the nodes in the Entities hierarchy. In our example, clicking a node will navigate to a state that displays the details of the selected node.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-node-selected-8.png', alt: 'Click node in hierarchy', caption: 'Click any node in the Entities hierarchy.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-9.png', alt: 'Node details state', caption: 'The "Device details" state opens, showing temperature card, time series chart, and map for the selected node.' },
]} />

### On HTML element click

<table style={{width:'auto'}}><thead><tr><td style={{textAlign:'center'}}><strong><em>Only for HTML widgets</em></strong></td></tr></thead></table>

**On HTML element click** allows you to trigger an action (navigate to a state, open a link, execute a custom action, etc.) when a user clicks on an element in the HTML layout of the widget.

**How it works**

When configuring **On HTML element click**, the `id` attribute is the link between the **HTML code** of the widget and the **action** defined in the widget's **Actions** tab.

You can assign the `id` to any HTML element (e.g., `<div>`, `<span>`, `<p>`, `<h1>`, `<button>`). Typically, the `id` is applied to a container element (like `<p>` or `<div>`), so the entire block becomes clickable.

```html
<p id="details" class="blue-box">
    Current temperature: <span>22.5 °C</span>
</p>
```

**Tips**
- Works **only with HTML widgets**; for tables, charts, and others use their specific action sources ([On row click](#on-row-click), [On cell click](#on-cell-click), [On node selected](#on-node-selected), etc.).
- `ID` must match **Name** exactly (case-sensitive).
- For more advanced scenarios (dialogs, forms), use [Custom action with HTML template](#custom-action-with-html-template) / *widgetContext.dialogs* / *widgetContext.customDialog* in widgets that support it.

**Configuration step by step**

<Steps>
1. Enter **widget edit mode** -> go to the **Actions** tab -> click **+** (**Add action**).
2. In Action source, select **On HTML element click**.
3. Set a **Name** (this will also serve as the **ID** for the HTML element).
4. In **Action**, choose the action type (e.g., [Navigate to new dashboard state](#navigate-to-new-dashboard-state)) and specify the target state.
5. Save the action (**Add**).
6. Go to the **Appearance** tab -> **HTML** section, and add an element with an `id` that **exactly matches** the action's Name.
7. Apply changes (**Apply**) and **save the dashboard**.
</Steps>

After saving, ThingsBoard "listens" for clicks on the element with an id that matches the Name of the created action for that widget. If found, the chosen action type (navigate, update state, custom action, etc.) is executed.

**Example: Markdown/HTML Card widget -- Click the blue area**

**Preparation:**
1. Add a <DocLink product={props.product} path="reference/widgets/markdown-html-card">Markdown/HTML Card</DocLink> widget to your dashboard using the HTML and CSS provided below.
2. <DocLink product={props.product} path="user-guide/dashboards">Create a new dashboard state</DocLink> with device **temperature details**.

**Alternative:** You can <a href="/resources/smart_device_details.json" download="smart_device_details.json">download a pre-configured dashboard</a> with the Markdown/HTML Card widget and the details state already set up, then <DocLink product={props.product} path="user-guide/dashboards">import</DocLink> it into your ThingsBoard instance.

<Aside type="note">
**Important:** After importing, update the **target entity** and **data key** in the widget's datasource.
</Aside>

**Add the Markdown/HTML Card**

Markdown/HTML pattern:

```js
const entity = data[0];
const color = entity.temperature > 25 ? "red" : entity.temperature > 20 ? 'green' : 'blue'
const entityName = `### Temperature value card\n - Current entity: <span >${entity.entityName}</span>\n `
const temp = `- Current temperature: <span style="color:${color};">${entity.temperature.toFixed(1)} °C</span>\n `
return entityName + temp;
```

Markdown/HTML CSS:

```css maxLines=20 collapsible
.office-card {
  box-sizing: border-box;
  padding: 16px;
  margin: 6px 0;
  text-align: center;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, "Noto Sans";
}

.office-card h3 {
  margin: 0 0 12px;
  font-size: 24px;
}

.blue-box {
  background: #2196F31A;
  border: 2px solid #2196F3;
  border-radius: 10px;
  padding: 10px;
  margin: 8px 0;
  font-weight: 700;
}

.green-box {
  background: #4CAF501A;
  border: 2px solid #4CAF50;
  border-radius: 10px;
  padding: 10px;
  margin: 6px 0;
  font-weight: 700;
}

.temp-value,
.hum-value {
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}
```

**Add the action**

<Steps>
1. Enter **widget edit mode** -> go to the **Actions** tab -> click **+** (**Add action**).
2. Choose **Action source**: **On HTML element click**.
3. Enter **details** as the **action name**.
4. In **Action**, select **Navigate to new dashboard state**.
5. In **Target dashboard state**, select the state you <DocLink product={props.product} path="user-guide/dashboards">created earlier</DocLink>.
6. Click **Add** to save the action.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-1.png', alt: 'Configure On HTML element click action', caption: 'Add action dialog — set source to "On HTML element click", enter name "details", select type "Navigate to new dashboard state", choose target state "temperature_history", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-2.png', alt: 'Select target state', caption: 'In the Appearance tab, add id="details" to the blue-box paragraph element, then click "Apply".' },
]} />

**Add id to the HTML**

<Steps>
1. Go to the **Appearance** tab -> **HTML** section.
2. Add an element with the `id=details` to the `<p class="blue-box">` line. Replace it with:

   ```text
   <p id="details" class="blue-box">
   ```

   This makes the entire blue-box block clickable and tied to the **details** action.

3. Apply changes (**Apply**) and **save the dashboard**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-3.png', alt: 'Add id to HTML element', caption: 'Dashboard in view mode — click the blue block with the current temperature value to execute the action.' },
]} />

**Result**

Clicking the **blue block with the current temperature** will execute the configured action and navigate to your target state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-4.png', alt: 'Click blue block to navigate', caption: 'The "Temperature history" state opens with a full temperature chart.' },
]} />

### Map widget action sources

Map widgets expose additional action sources tied to their interactive elements — markers, circles, polygons, polylines, and tooltip links. The examples below assume you already have a <DocLink product={props.product} path="reference/widgets/map-widgets">map widget</DocLink> and a target <DocLink product={props.product} path="user-guide/dashboards">dashboard state</DocLink> set up.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/map-widget-action-sources-1.png', alt: 'Map widget on dashboard', caption: 'OpenStreetMap widget added to the dashboard.' },
]} />

#### On circle click

Triggers when the user clicks a circle geo-zone on the map. A circle is defined by a JSON attribute on the entity: `{"latitude": ..., "longitude": ..., "radius": ...}` (radius in meters). The clicked entity is automatically passed as context to the target state. See <DocLink product={props.product} path="reference/widgets/map-widgets">Map widget configuration</DocLink> for setup details.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-circle-click-1.png', alt: 'Circle on map widget', caption: 'A circle geo-zone displayed on the map widget.' },
]} />

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **On circle click** as the action source.
3. Enter an action name. Optionally, choose an icon.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-circle-click-2.png', alt: 'Configure On circle click action', caption: 'Add action dialog — set source to "On circle click", enter name "Compressor NM-56 details", select type "Navigate to new dashboard state", choose target state "device_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-circle-click-3.png', alt: 'On circle click action in list', caption: 'The action "Compressor NM-56 details" appears in the list — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/on-circle-click-4.png', alt: 'Dashboard ready for circle click', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/on-circle-click-5.png', alt: 'Click circle on map', caption: 'Dashboard in view mode — click the circle on the map to execute the configured action.' },
  { src: '/src/assets/images/user-guide/actions/on-circle-click-6.png', alt: 'Device details state', caption: 'The "Device details" state opens, showing temperature card, time series chart, and map for the selected device.' },
]} />

#### On marker click

Triggers when the user clicks an entity marker on the map. Markers appear automatically for every entity that has latitude and longitude coordinate attributes. The clicked entity is automatically passed as context to the target state.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **On marker click** as the action source.
3. Enter an action name. Optionally, choose an icon.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-marker-click-1.png', alt: 'Configure On marker click action', caption: 'Add action dialog — set source to "On marker click", enter name "Building details", select type "Navigate to new dashboard state", choose target state "building_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-marker-click-2.png', alt: 'On marker click action in list', caption: 'The action "Building details" appears in the list — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/on-marker-click-3.png', alt: 'Dashboard ready for marker click', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/on-marker-click-4.png', alt: 'Click marker on map', caption: 'Dashboard in view mode — click a marker on the map to execute the configured action.' },
  { src: '/src/assets/images/user-guide/actions/on-marker-click-5.png', alt: 'Building details state', caption: 'The "Building details" state opens, showing the contact info card for the selected building.' },
]} />

#### On polygon click

Triggers when the user clicks anywhere inside a polygon geo-zone on the map. A polygon is defined by a JSON array of coordinate pairs `[[lat, lng], [lat, lng], ...]` stored in an entity attribute and can represent buildings, districts, or any bounded area. The clicked entity is automatically passed as context to the target state. See <DocLink product={props.product} path="reference/widgets/map-widgets">Map widget configuration</DocLink> for setup details.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-1.png', alt: 'Polygon on map widget', caption: 'A polygon geo-zone displayed on the map widget.' },
]} />

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **On polygon click** as the action source.
3. Enter an action name. Optionally, choose an icon.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**, then **Apply** to save the widget settings.
6. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-2.png', alt: 'Configure On polygon click action', caption: 'Add action dialog — set source to "On polygon click", enter name "Buildings details", select type "Navigate to new dashboard state", choose target state "buildings_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-3.png', alt: 'On polygon click action in list', caption: 'The action "Buildings details" appears in the list — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-4.png', alt: 'Dashboard ready for polygon click', caption: 'Dashboard in edit mode — the polygon is visible on the map — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-5.png', alt: 'Click inside polygon', caption: 'Dashboard in view mode — click anywhere inside the polygon to execute the configured action.' },
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-6.png', alt: 'Buildings details state', caption: 'The "Buildings details" state opens, showing contact info cards for Building A and Building B.' },
]} />

#### Tooltip tag action

Displays a tooltip when a marker is clicked. The tooltip can contain one or more clickable links, each tied to a named action via the `<link-act>` tag — this lets you offer multiple actions from a single marker. The `name` attribute must exactly match the action name (case-sensitive). For dynamic per-entity tooltip content, use <DocLink product={props.product} path="reference/widgets/map-widgets">tooltip function mode</DocLink> in the map widget's advanced settings.

<Steps>
1. Open the widget in edit mode, go to the **Actions** tab, and click **+**.
2. Select **Tooltip tag action** as the action source.
3. Enter an action name — this exact name (case-sensitive) will be used in the `<link-act>` tag. Optionally, choose an icon.
4. Select **Navigate to new dashboard state** as the action type, then choose the target state.
5. Click **Add**.
6. Go to the **Appearance** tab, enable **Show tooltip**, and add a `<link-act>` tag referencing the action name:

    ```text
    <link-act name='TooltipTag'>Navigate to the ${entityName}</link-act>
    ```

    where `TooltipTag` is the action name and the element text is the link label shown in the tooltip.

7. Click **Apply** to save the widget settings.
8. Click **Save** in the dashboard toolbar to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-1.png', alt: 'Configure Tooltip tag action', caption: 'Add action dialog — set source to "Tooltip tag action", enter name "TooltipTag", select type "Navigate to new dashboard state", choose target state "building_details", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-2.png', alt: 'Tooltip tag action in list', caption: 'The action "TooltipTag" appears in the list — navigate to the "Appearance" tab to configure the tooltip.' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-3.png', alt: 'Configure tooltip with link-act tag', caption: 'In the Appearance tab, enable "Show tooltip" and enter the link-act tag referencing action name "TooltipTag", then click "Apply".' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-4.png', alt: 'Dashboard ready for tooltip click', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-5.png', alt: 'Click marker to show tooltip', caption: 'Dashboard in view mode — click a marker to show the tooltip, then click the "Navigate to the Building A" link to execute the action.' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-6.png', alt: 'Building details state', caption: 'The "Building details" state opens with the contact info card for the selected building.' },
]} />

### Action options

#### Open right dashboard layout (mobile view)

<Aside type="note">
These settings will be useful to you if you are using the {[Products.PE, Products.PAAS, Products.PAAS_EU].includes(props.product) ? <a href="/docs/mobile/pe/" target="_blank"><b>ThingsBoard PE Mobile Application</b></a> : <a href="/docs/mobile/" target="_blank"><b>ThingsBoard Mobile Application</b></a>}. If you're not using it yet, {[Products.PE, Products.PAAS, Products.PAAS_EU].includes(props.product) ? <a href="/docs/mobile/pe/getting-started/"><b>give it a try</b></a> : <a href="/docs/mobile/getting-started/"><b>give it a try</b></a>}! Stay informed about all events in your IoT solution, even when you're on the go.
</Aside>

Often, users need to see updated information about an entity from one widget on another widget next to it. For example, we have a list of assets in the "Entity table" widget, and we want to see their details in the "Time series chart" widget next to it.
On the desktop, this is easy to set up by placing the two widgets side by side and selecting the action type "[Update current dashboard state](#update-current-dashboard-state)".
However, on the responsive screen of the {[Products.PE, Products.PAAS, Products.PAAS_EU].includes(props.product) ? <a href="/docs/mobile/pe/"><b>ThingsBoard PE Mobile Application</b></a> : <a href="/docs/mobile/"><b>ThingsBoard Mobile Application</b></a>} the widgets are automatically stacked one below the other. This means that you'll need to scroll down to view the relevant information on the second widget.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-1.png', alt: 'Desktop side-by-side layout', caption: 'Desktop dashboard with the "Entities table" (left) and "Time series chart" (right) shown side by side.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-2.png', alt: 'Widgets stacked on mobile', caption: 'On mobile, widgets are stacked — you must scroll down to see the "Time series chart" below the "Entities table".' },
]} />

<DocLink product={props.product} path="user-guide/dashboards">Layouts</DocLink> feature solves this issue. To configure layouts, you should:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-4.png', alt: 'Open layout settings', caption: 'In dashboard edit mode, click the "Layouts" button to manage layouts.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-5.png', alt: 'Enable right layout', caption: 'In the "Manage layouts" dialog, enable the "Divider" option.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-6.png', alt: 'Configure right layout', caption: 'The two-column layout (Left/Right) is configured — click "Save" to apply.' },
]} />

Now we need to add the "Time series chart" widget to the right layout:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-7.png', alt: 'Add widget to right layout', caption: 'In the right layout, click "Add new widget".' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-8.png', alt: 'Configure widget in right layout', caption: 'Widget bundle selection — select the "Charts" bundle.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-9.png', alt: 'Widget added to right layout', caption: 'Charts — select the "Time series chart" widget.' },
]} />

Configure the "Time series chart" widget to display data for the entity selected in the "Entities table":

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-10.png', alt: 'Open Add action window', caption: 'In the Time series chart configuration, set datasource to "Entity alias", enter "Chart temperature", and click "Create a new one!"' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-11.png', alt: 'Select On row click', caption: '"Add alias" dialog — set filter type to "Entity from dashboard state" and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-12.png', alt: 'Configure update state action', caption: 'Set the series label to "${entityName}", then click "Add" to finish adding the widget.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-13.png', alt: 'Save action and dashboard', caption: 'Dashboard in edit mode — resize the "Time series chart" widget in the right layout as needed.' },
]} />

Now add an "[Update current dashboard state](#update-current-dashboard-state)" action to the "Entities table" widget:

<Steps>
1. Open the "Entities table" widget in edit mode and navigate to the "Actions" tab.
2. Select "[On row click](#on-row-click)" as the action source and enter an action name.
3. Select "Update current dashboard state" as the action type.
4. Enable "Open right dashboard layout (mobile view)".
5. Click "Add", then "Save".
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-14.png', alt: 'Click entity row on desktop', caption: 'Add action dialog — set source to "On row click", enter name "Chart temperature", select type "Update current dashboard state", enable "Open right dashboard layout (mobile view)", and click "Add".' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-15.png', alt: 'Chart updated on desktop', caption: 'The action "Chart temperature" appears in the list — click "Save".' },
]} />

Apply the widget settings, save the dashboard, then test the action. On mobile, clicking an entity row navigates directly to the "Time series chart" in the right layout:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-16.png', alt: 'Apply widget settings', caption: 'The "Chart temperature" action is configured in the Actions section — click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-17.png', alt: 'Save dashboard', caption: 'Dashboard in edit mode — click "Save" to save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-18.png', alt: 'Click entity row on desktop', caption: 'Desktop view — click any entity row to update the "Time series chart" with that entity\'s data.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-19.png', alt: 'Navigate to right layout on mobile', caption: 'On mobile, clicking an entity row navigates directly to the "Time series chart" in the right layout.' },
]} />

#### Set entity from widget

The **Set entity from widget** checkbox passes the selected entity into the target dashboard state. This allows the target state to use it via the <DocLink product={props.product} path="user-guide/aliases">Entity from dashboard state</DocLink> alias. For example, clicking a row in an Entities table can push that device into the state so detail widgets on the next state display its data.

Sometimes you need to store more than one entity in the state. For example, you would like to navigate to customer list, then to devices of the customer and finally to specific device details. In such case, you may have three states: "Main", "Customer Devices" and "Device Details".
You may use two different state entity parameters to reference current customer (e.g. "currentCustomer") and current device (e.g. "currentDevice") on the "Device Details" state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/entityfromwidget.png', alt: 'Set entity from widget', caption: 'Add action dialog — enable "Set entity from widget" to pass the selected entity to the target state; use "State entity parameter name" when managing multiple state-to-state connections.' },
]} />
