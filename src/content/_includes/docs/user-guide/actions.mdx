import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import DocImage from '~/components/DocImage.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import { Steps, Aside } from '@astrojs/starlight/components';

Actions feature can be treated as navigation among <DocLink product={props.product} path="user-guide/dashboards">dashboard's states</DocLink> or between different <DocLink product={props.product} path="user-guide/dashboards">dashboards</DocLink>.
Actions allow quickly and easily configuring the transition to created state, transferring to other dashboards, or even updating the dashboard you are in.
Depending on the widget, the action sources differ. However, the type of action you are able to choose will be the same for all widgets.
Actions can be configured both when editing and creating the required widget.

In this guide, we will configure actions in the editing mode using the Entities table widget as an example. Please, <DocLink product={props.product} path="getting-started-guides/helloworld">learn here</DocLink> how to add an Entity table widget to your dashboard.

To fully understand how to use Actions, you have to add a State to your dashboard. <DocLink product={props.product} path="user-guide/dashboards">You can find information on how to do this here</DocLink>.

## Add action

Let's add your first action. In this step, we will outline only the general steps without going into the details of each setting. Examples of using each [type of action](#action-types) and [action source](#action-sources) will be covered later in the guide for better understanding.

Most widgets offer two configuration options: *basic* and *advanced*. Accordingly, the process of adding actions differs slightly between these modes. Let's explore both:

If you're using the *basic* widget configuration:

<Steps>
1. Enter the editing mode of the widget to which you want to add an action, scroll down and find the "Actions" menu item.
2. Click the "Add action" button (once you have created one or more actions, the "Add actions" button will change to the `+` icon).
   The "Actions" window will open. Right now, it's empty, but later it will display all created actions.
3. Click the 'plus' icon in the top right corner of the screen to open a new "Add action" window. Here you must configure a new action by entering a name, specifying the action source, and selecting the action type. Further action configuration will depend on the selected action type. Then, click "Add" to proceed.
4. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save".
5. Click "Apply" to save the widget settings.
6. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/add-action-2.png', alt: 'Enter widget edit mode', caption: 'Enter the editing mode of the widget.' },
  { src: '/src/assets/images/user-guide/actions/add-action-3.png', alt: 'Click Add action button', caption: 'Click the "Add action" button.' },
  { src: '/src/assets/images/user-guide/actions/add-action-4.png', alt: 'Open Add action window', caption: 'Click the "plus" icon to open the "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/add-action-5.png', alt: 'Configure the new action', caption: 'Configure the new action: enter a name, select action source and type.' },
  { src: '/src/assets/images/user-guide/actions/add-action-6.png', alt: 'Review configured action', caption: 'Review the configured action and click "Save".' },
  { src: '/src/assets/images/user-guide/actions/add-action-7.png', alt: 'Apply widget settings', caption: 'Click "Apply" to save the widget settings.' },
  { src: '/src/assets/images/user-guide/actions/add-action-8.png', alt: 'Save dashboard', caption: 'Save the dashboard.' },
]} />

If you're using the *advanced* widget settings:

<Steps>
1. Enter the widget editing mode, and navigate to the "Actions" tab. Currently, it is empty, but later all created actions will be displayed here.
2. Click the "plus" icon in the top right corner of the screen to open a new "Add action" window. Here you must configure a new action by entering a name, specifying the action source, and selecting the action type. Further action configuration will depend on the selected action type. Then, click "Add" to proceed.
3. Now, you can see the configured action, so you can double-check the action source, icon, and action type. Click "Save".
4. Click "Apply" to save the widget settings.
5. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/add-action-9.png', alt: 'Navigate to Actions tab', caption: 'Navigate to the "Actions" tab in advanced widget settings.' },
  { src: '/src/assets/images/user-guide/actions/add-action-10.png', alt: 'Add action in advanced mode', caption: 'Click the "plus" icon to add a new action.' },
  { src: '/src/assets/images/user-guide/actions/add-action-11.png', alt: 'Review and save action', caption: 'Review the configured action and click "Save".' },
  { src: '/src/assets/images/user-guide/actions/add-action-12.png', alt: 'Save dashboard', caption: 'Click "Apply" and save the dashboard.' },
]} />

Now, you can use the action. This can involve clicking on a separate button, the widget title, an individual row of entity, etc.

## Action types

The types of action define the specific action that will be taken.
There are seven action types that are applicable to all widgets:
- **Navigate to new dashboard state** - transfers you to the specified state within the current dashboard;
- **Update current dashboard state** - updates the dashboard that you are currently on;
- **Navigate to other dashboard** - transfers you to a specified dashboard;
- **Custom action** - allows manual configuration of a function for an individual action on your widget;
- **Custom action (with HTML template)** - allows manual entry of a function within an existing HTML template;
- **Mobile action** - allows the use of various mobile device functions such as taking photos, scanning QR codes, getting device location, making phone calls and so on;
- **Open URL** - allows you to go to any resource represented by a URL.

There is also a special action type available exclusively for <DocLink product={props.product} path="user-guide/widgets/map-widgets">Map widgets</DocLink>:
- **Place map item** — allows you to create and place a new entity directly on the map widget in just a few clicks.

Let's consider each of these types of actions separately.

### Navigate to new dashboard state

States are so-called levels that allow you to navigate between different devices, assets, and widget objects to see the information you need in more detail.
When choosing the _Navigate to new dashboard state_ action type, you will be transferred to the previously created state of your choice.

We assume that you have already added <DocLink product={props.product} path="user-guide/dashboards">a new state</DocLink> to the dashboard and familiarized yourself with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. As an example for this manual, select "[Action cell button](#action-cell-button)" action source.
3. Input a name for the action and select an icon that will represent a button. With this icon, action will be performed.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-2.png', alt: 'Select action cell button source', caption: 'Select "Action cell button" action source.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-3.png', alt: 'Configure navigate action', caption: 'Select "Navigate to new dashboard state" and choose the target state.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-4.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-5.png', alt: 'Review and save', caption: 'Review the action and click "Save", then "Apply".' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-6.png', alt: 'Save dashboard', caption: 'Save the dashboard.' },
]} />

After saving your changes, you'll see an action icon next to each entity. Click on any of them. You will be transferred to the state specified in the action.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-7.png', alt: 'Action icon next to entities', caption: 'Click the action icon next to any entity.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-8.png', alt: 'Navigated to new state', caption: 'You are transferred to the specified state.' },
]} />

#### Open new dashboard state in a separate dialog or in a popup window

There are times when it doesn't make sense to move to a separate dashboard state to view the details of a widget, and you just want to open it on the same dashboard page.
For these situations, there is a _Dashboard state display option_ feature that allows opening another <DocLink product={props.product} path="user-guide/dashboards">state</DocLink> on the same page in a separate dialog box or in a popup window.

In the action settings, select the desired dashboard state display option and make additional window settings such as show/hide dashboard toolbar, popup window width and height, etc.

As an example, consider displaying the new dashboard state in a separate dialog window:

<Steps>
1. Select an "Open in separate dialog" option in the "Dashboard state display option" field.
2. By default, the checkbox "Hide dashboard toolbar in dialog" is ticked. If you need to see a toolbar in the dialog window, you should uncheck it. The toolbar displays entities, <DocLink product={props.product} path="user-guide/dashboards" bold={false}>time window</DocLink>, dashboard export button, and expand to a fullscreen button.
3. It is optional to adjust a dialog width and height in percents. Width is relative to viewport height, and height is relative to width correspondingly.
4. When the action has been configured, save all changes.
</Steps>

To perform an action and open a dialog window with the new state, click an action button next to any entity.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-9.png', alt: 'Configure separate dialog', caption: 'Configure the action to open in a separate dialog.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-new-dashboard-state-10.png', alt: 'Dialog window with new state', caption: 'Click the action button to open the dialog window with the new state.' },
]} />

### Update current dashboard state

This action type allows updating a dashboard that you are currently on.
While using a dashboard, you can view detailed information about a specific device/asset in real-time.
The most common use of this action type is through Chart widgets where you can see the details more accurately.

For this example, in addition to the "Entities table" widget, you need to add another widget to your dashboard - "Timeseries Line Chart". As a data source, specify the <DocLink product={props.product} path="user-guide/ui/aliases">asset type "Entity from dashboard state"</DocLink>:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-1.png', alt: 'Add Timeseries Line Chart widget', caption: 'Add a "Timeseries Line Chart" widget to the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-2.png', alt: 'Configure data source', caption: 'Set "Entity from dashboard state" as the data source.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-3.png', alt: 'Widget added to dashboard', caption: 'The widget is added to the dashboard.' },
]} />

Now let's add an action with the type "Update current dashboard state" for the "Entities table" widget.
We assume that you are already familiar with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. As an example for this manual, select "[Action cell button](#action-cell-button)" action source.
3. Input a name for the action and select an icon that will represent a button. With this icon, action will be performed.
4. Select an "Update current dashboard state" action type from the "Action" drop-down menu.
5. Click the "Add" button at the bottom of the "Add action" window.
6. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
7. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-4.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-5.png', alt: 'Select action cell button', caption: 'Select "Action cell button" action source.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-6.png', alt: 'Select Update current dashboard state', caption: 'Select "Update current dashboard state" action type.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-7.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-8.png', alt: 'Review and save', caption: 'Review the action and click "Save", then "Apply".' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-9.png', alt: 'Save dashboard', caption: 'Save the dashboard.' },
]} />

After saving changes, you will see icons opposite the entity names in the "Entities table" widget. Clicking on any of these icons will update the entity details, and they will be shown on the "Timeseries Line Chart" widget on the current dashboard.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-10.png', alt: 'Action icons in table', caption: 'Action icons appear next to entity names.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-11.png', alt: 'Click action icon', caption: 'Click the action icon for an entity.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-12.png', alt: 'Chart updated', caption: 'The chart widget updates with selected entity data.' },
  { src: '/src/assets/images/user-guide/actions/update-current-dashboard-state-13.png', alt: 'Another entity selected', caption: 'Select another entity to update the chart.' },
]} />

### Navigate to other dashboard

This type of action transfers you to a previously created dashboard of your choice.

We assume that you are already familiar with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. As an example for this manual, select "[Widget header button](#widget-header-button)" action source.
3. Input a name for the action and select an icon that will represent a button. With this icon, action will be performed.
4. Select a "Navigate to other dashboard" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a dashboard you'd like to be transitioned to. Note that you can also navigate to an existing state of the selected dashboard.
6. When the desired dashboard has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

After saving the changes, you can see an icon in the upper right part of the widget. Click this icon. You will be transferred to the previously selected dashboard (or chosen state in that dashboard).

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-2.png', alt: 'Select widget header button', caption: 'Select "Widget header button" action source.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-3.png', alt: 'Select Navigate to other dashboard', caption: 'Select "Navigate to other dashboard" and choose the target dashboard.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-4.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-5.png', alt: 'Review and save', caption: 'Review the action and click "Save", then "Apply".' },
  { src: '/src/assets/images/user-guide/actions/navigate-to-other-dashboard-6.png', alt: 'Navigate to other dashboard', caption: 'Click the action icon to navigate to the selected dashboard.' },
]} />

### Custom action

A Custom action allows manually configuring a function that can be used to add an individual action to your widget (for example deleting listed devices/assets).
To configure a custom action with a function (with an example of a device deletion):

We assume that you are already familiar with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. As an example for this manual, select "[Action cell button](#action-cell-button)" action source.
3. Input a name for the action and select an icon that will represent an action button. With this icon, action will be performed.
4. Select a "Custom action" action type from the "Action" drop-down menu.
5. After choosing a Custom action type, a field for inputting a function will appear.
6. Enter your custom function there. An example in this manual is an action that adds the ability to delete devices right from the table (you will find an example of this function below).
</Steps>

An example of the function for a device deletion:

```js maxLines=20 collapsible
let $injector = widgetContext.$scope.$injector;
let dialogs = $injector.get(widgetContext.servicesMap.get('dialogs'));
let deviceService = $injector.get(widgetContext.servicesMap.get('deviceService'));

openDeleteDeviceDialog();

function openDeleteDeviceDialog() {
    let title = "Are you sure you want to delete the device " + entityName + "?";
    let content = "Be careful, after the confirmation, the device and all related data will become unrecoverable!";
    dialogs.confirm(title, content, 'Cancel', 'Delete').subscribe(
        function(result) {
            if (result) {
                deleteDevice();
            }
        }
    );
}

function deleteDevice() {
    deviceService.deleteDevice(entityId.id).subscribe(
        function() {
            widgetContext.updateAliases();
        }
    );
}
```

<Steps>
1. After input function, click the "Add" button at the bottom of the "Add action" window.
2. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
3. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-2.png', alt: 'Select custom action type', caption: 'Select "Custom action" type and enter the function.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-3.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-4.png', alt: 'Review and save', caption: 'Review the action and click "Save", then "Apply".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-5.png', alt: 'Save dashboard', caption: 'Save the dashboard.' },
]} />

After saving the changes, you will see an action button next to the entity names in the "Entities Table" widget.
Clicking on one of them will perform the action of deleting the corresponding object.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-6.png', alt: 'Action button in table', caption: 'Click the action button next to an entity to delete it.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-7.png', alt: 'Delete confirmation dialog', caption: 'Confirm deletion in the dialog.' },
]} />

### Custom action (with HTML template)

A Custom action with an HTML template allows manually entering a function in an existing HTML template (for example creating dialog windows with an opportunity to create or edit listed devices/assets).
After choosing a Custom action type (with HTML template), four tabs appear for setting up the action:

- "_Resources_" tab is used to specify external JavaScript/CSS resources used by the widget;
- "_CSS_" tab contains custom action specific CSS style definitions;
- "_HTML_" tab contains custom action HTML code;
- "_JavaScript_" tab contains JS code of your custom action.

In this example, we will add two action buttons. In the upper right corner of the widget there will be located action button for adding new entities. The second action will be responsible for the buttons next to the entity names for editing these entities.

Let's start with the action button for adding new entities. We assume that you have already completed the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. As an example for this manual, select "[Widget header button](#widget-header-button)" action source.
3. Input a name for the action and select an icon that will represent an action button. With this icon, action will be performed.
4. Select a "Custom action (with HTML template)" action type from the "Action" drop-down menu.
5. In the tabs that appear, select "_JavaScript_" tab. Enter a JavaScript function for your custom action. In this example, we use a function that adds the ability to create a new entity (you will find an example of this function below).
</Steps>

Example of a JavaScript function that adds the ability to add a device or an asset:

```js maxLines=20 collapsible
let $injector = widgetContext.$scope.$injector;
let customDialog = $injector.get(widgetContext.servicesMap.get('customDialog'));
let assetService = $injector.get(widgetContext.servicesMap.get('assetService'));
let deviceService = $injector.get(widgetContext.servicesMap.get('deviceService'));
let attributeService = $injector.get(widgetContext.servicesMap.get('attributeService'));
let entityRelationService = $injector.get(widgetContext.servicesMap.get('entityRelationService'));

openAddEntityDialog();

function openAddEntityDialog() {
customDialog.customDialog(htmlTemplate, AddEntityDialogController).subscribe();
}

function AddEntityDialogController(instance) {
let vm = instance;

    vm.allowedEntityTypes = ['ASSET', 'DEVICE'];
    vm.entitySearchDirection = {
        from: "FROM",
        to: "TO"
    }

    vm.addEntityFormGroup = vm.fb.group({
     entityName: ['', [vm.validators.required]],
     entityType: ['DEVICE'],
     entityLabel: [null],
     type: ['', [vm.validators.required]],
     attributes: vm.fb.group({
         latitude: [null],
         longitude: [null],
         address: [null],
         owner: [null],
         number: [null, [vm.validators.pattern(/^-?[0-9]+$/)]],
         booleanValue: [null]
     }),
     relations: vm.fb.array([])
    });

    vm.cancel = function() {
        vm.dialogRef.close(null);
    };

    vm.relations = function() {
        return vm.addEntityFormGroup.get('relations');
    };

    vm.addRelation = function() {
        vm.relations().push(vm.fb.group({
         relatedEntity: [null, [vm.validators.required]],
         relationType: [null, [vm.validators.required]],
         direction: [null, [vm.validators.required]]
        }));
    };

    vm.removeRelation = function(index) {
        vm.relations().removeAt(index);
        vm.relations().markAsDirty();
    };

    vm.save = function() {
        vm.addEntityFormGroup.markAsPristine();
        saveEntityObservable().subscribe(
            function (entity) {
                widgetContext.rxjs.forkJoin([
                    saveAttributes(entity.id),
                    saveRelations(entity.id)
                ]).subscribe(
                    function () {
                        widgetContext.updateAliases();
                        vm.dialogRef.close(null);
                    }
                );
            }
        );
    };

    function saveEntityObservable() {
        const formValues = vm.addEntityFormGroup.value;
        let entity = {
            name: formValues.entityName,
            type: formValues.type,
            label: formValues.entityLabel
        };
        if (formValues.entityType == 'ASSET') {
            return assetService.saveAsset(entity);
        } else if (formValues.entityType == 'DEVICE') {
            return deviceService.saveDevice(entity);
        }
    }

    function saveAttributes(entityId) {
        let attributes = vm.addEntityFormGroup.get('attributes').value;
        let attributesArray = [];
        for (let key in attributes) {
            if(attributes[key] !== null) {
                attributesArray.push({key: key, value: attributes[key]});
            }
        }
        if (attributesArray.length > 0) {
            return attributeService.saveEntityAttributes(entityId, "SERVER_SCOPE", attributesArray);
        }
        return widgetContext.rxjs.of([]);
    }

    function saveRelations(entityId) {
        let relations = vm.addEntityFormGroup.get('relations').value;
        let tasks = [];
        for(let i=0; i < relations.length; i++) {
            let relation = {
                type: relations[i].relationType,
                typeGroup: 'COMMON'
            };
            if (relations[i].direction == 'FROM') {
                relation.to = relations[i].relatedEntity;
                relation.from = entityId;
            } else {
                relation.to = entityId;
                relation.from = relations[i].relatedEntity;
            }
            tasks.push(entityRelationService.saveRelation(relation));
        }
        if (tasks.length > 0) {
            return widgetContext.rxjs.forkJoin(tasks);
        }
        return widgetContext.rxjs.of([]);
    }
}
```

<Steps>
1. In "_HTML_" tab, enter an HTML code for your custom action (you will find an example of this function below).
</Steps>

Example of HTML code that adds the ability to add a device or an asset:

```html maxLines=20 collapsible
<form #addEntityForm="ngForm" [formGroup]="addEntityFormGroup"
      (ngSubmit)="save()" class="add-entity-form">
    <mat-toolbar fxLayout="row" color="primary">
        <h2>Add entity</h2>
        <span fxFlex></span>
        <button mat-icon-button (click)="cancel()" type="button">
            <mat-icon class="material-icons">close</mat-icon>
        </button>
    </mat-toolbar>
    <mat-progress-bar color="warn" mode="indeterminate" *ngIf="isLoading$ | async">
    </mat-progress-bar>
    <div style="height: 4px;" *ngIf="!(isLoading$ | async)"></div>
    <div mat-dialog-content fxLayout="column">
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Name</mat-label>
                <input matInput formControlName="entityName" required>
                <mat-error *ngIf="addEntityFormGroup.get('entityName').hasError('required')">
                    Entity name is required.
                </mat-error>
            </mat-form-field>
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Label</mat-label>
                <input matInput formControlName="entityLabel" >
            </mat-form-field>
        </div>
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <tb-entity-type-select
                    class="mat-block"
                    formControlName="entityType"
                    [showLabel]="true"
                    [allowedEntityTypes]="allowedEntityTypes"
            ></tb-entity-type-select>
            <tb-entity-subtype-autocomplete
                    fxFlex *ngIf="addEntityFormGroup.get('entityType').value == 'ASSET'"
                    class="mat-block"
                    formControlName="type"
                    [required]="true"
                    [entityType]="'ASSET'"
            ></tb-entity-subtype-autocomplete>
            <tb-entity-subtype-autocomplete
                    fxFlex *ngIf="addEntityFormGroup.get('entityType').value != 'ASSET'"
                    class="mat-block"
                    formControlName="type"
                    [required]="true"
                    [entityType]="'DEVICE'"
            ></tb-entity-subtype-autocomplete>
        </div>
        <div formGroupName="attributes" fxLayout="column">
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Latitude</mat-label>
                    <input type="number" step="any" matInput formControlName="latitude">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Longitude</mat-label>
                    <input type="number" step="any" matInput formControlName="longitude">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Address</mat-label>
                    <input matInput formControlName="address">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Owner</mat-label>
                    <input matInput formControlName="owner">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Integer Value</mat-label>
                    <input type="number" step="1" matInput formControlName="number">
                    <mat-error *ngIf="addEntityFormGroup.get('attributes.number').hasError('pattern')">
                        Invalid integer value.
                    </mat-error>
                </mat-form-field>
                <div class="boolean-value-input" fxLayout="column" fxLayoutAlign="center start" fxFlex>
                    <label class="checkbox-label">Boolean Value</label>
                    <mat-checkbox formControlName="booleanValue" style="margin-bottom: 40px;">
                        {{ (addEntityFormGroup.get('attributes.booleanValue').value ? "value.true" : "value.false") | translate }}
                    </mat-checkbox>
                </div>
            </div>
        </div>
        <div class="relations-list">
            <div class="mat-body-1" style="padding-bottom: 10px; color: rgba(0,0,0,0.57);">Relations</div>
            <div class="body" [fxShow]="relations().length">
                <div class="row" fxLayout="row" fxLayoutAlign="start center" formArrayName="relations" *ngFor="let relation of relations().controls; let i = index;">
                    <div [formGroupName]="i" class="mat-elevation-z2" fxFlex fxLayout="row" style="padding: 5px 0 5px 5px;">
                        <div fxFlex fxLayout="column">
                            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                                <mat-form-field class="mat-block" style="min-width: 100px;">
                                    <mat-label>Direction</mat-label>
                                    <mat-select formControlName="direction" name="direction">
                                        <mat-option *ngFor="let direction of entitySearchDirection | keyvalue" [value]="direction.value">
                                            {{ ("relation.search-direction." + direction.value) | translate}}
                                        </mat-option>
                                    </mat-select>
                                    <mat-error *ngIf="relation.get('direction').hasError('required')">
                                        Relation direction is required.
                                    </mat-error>
                                </mat-form-field>
                                <tb-relation-type-autocomplete
                                        fxFlex class="mat-block"
                                        formControlName="relationType"
                                        [required]="true">
                                </tb-relation-type-autocomplete>
                            </div>
                            <div fxLayout="row" fxLayout.xs="column">
                                <tb-entity-select
                                        fxFlex class="mat-block"
                                        [required]="true"
                                        formControlName="relatedEntity">
                                </tb-entity-select>
                            </div>
                        </div>
                        <div fxLayout="column" fxLayoutAlign="center center">
                            <button mat-icon-button color="primary"
                                    aria-label="Remove"
                                    type="button"
                                    (click)="removeRelation(i)"
                                    matTooltip="Remove relation"
                                    matTooltipPosition="above">
                                <mat-icon>close</mat-icon>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <div>
                <button mat-raised-button color="primary"
                        type="button"
                        (click)="addRelation()"
                        matTooltip="Add Relation"
                        matTooltipPosition="above">
                    Add
                </button>
            </div>
        </div>
    </div>
    <div mat-dialog-actions fxLayout="row" fxLayoutAlign="end center">
        <button mat-button color="primary"
                type="button"
                [disabled]="(isLoading$ | async)"
                (click)="cancel()" cdkFocusInitial>
            Cancel
        </button>
        <button mat-button mat-raised-button color="primary"
                type="submit"
                [disabled]="(isLoading$ | async) || addEntityForm.invalid || !addEntityForm.dirty">
            Create
        </button>
    </div>
</form>
```

<Steps>
1. After input functions, click the "Add" button at the bottom of the "Add action" window.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-1.png', alt: 'Configure custom action with HTML template', caption: 'Select "Custom action (with HTML template)" and enter JavaScript function.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-2.png', alt: 'Enter HTML code', caption: 'Enter the HTML code in the "HTML" tab.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-3.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
]} />

Now let's add an action button to edit the entities.

<Steps>
1. Click the "plus" icon in the top right corner of the screen again to open a new "Add action" window.
2. As an example for this manual, select "[Action cell button](#action-cell-button)" action source.
3. Input a name for the action and select an icon that will represent an action button. With this icon, action will be performed.
4. Select a "Custom action (with HTML template)" action type from the "Action" drop-down menu.
5. In the tabs that appear, select "JavaScript" tab. Enter a JavaScript function for your custom action. In this example, we use a function that adds the ability to edit an entity (you will find an example of this function below).
</Steps>

Example of a JavaScript function that adds a possibility to edit a device or an asset:

```js maxLines=20 collapsible
let $injector = widgetContext.$scope.$injector;
let customDialog = $injector.get(widgetContext.servicesMap.get('customDialog'));
let entityService = $injector.get(widgetContext.servicesMap.get('entityService'));
let assetService = $injector.get(widgetContext.servicesMap.get('assetService'));
let deviceService = $injector.get(widgetContext.servicesMap.get('deviceService'));
let attributeService = $injector.get(widgetContext.servicesMap.get('attributeService'));
let entityRelationService = $injector.get(widgetContext.servicesMap.get('entityRelationService'));

openEditEntityDialog();

function openEditEntityDialog() {
customDialog.customDialog(htmlTemplate, EditEntityDialogController).subscribe();
}

function EditEntityDialogController(instance) {
let vm = instance;

    vm.entityName = entityName;
    vm.entityType = entityId.entityType;
    vm.entitySearchDirection = {
        from: "FROM",
        to: "TO"
    };
    vm.attributes = {};
    vm.oldRelationsData = [];
    vm.relationsToDelete = [];
    vm.entity = {};

    vm.editEntityFormGroup = vm.fb.group({
        entityName: ['', [vm.validators.required]],
        entityType: [null],
        entityLabel: [null],
        type: ['', [vm.validators.required]],
        attributes: vm.fb.group({
            latitude: [null],
            longitude: [null],
            address: [null],
            owner: [null],
            number: [null, [vm.validators.pattern(/^-?[0-9]+$/)]],
            booleanValue: [false]
        }),
        oldRelations: vm.fb.array([]),
        relations: vm.fb.array([])
    });

    getEntityInfo();

    vm.cancel = function() {
        vm.dialogRef.close(null);
    };

    vm.relations = function() {
        return vm.editEntityFormGroup.get('relations');
    };

    vm.oldRelations = function() {
        return vm.editEntityFormGroup.get('oldRelations');
    };

    vm.addRelation = function() {
        vm.relations().push(vm.fb.group({
            relatedEntity: [null, [vm.validators.required]],
            relationType: [null, [vm.validators.required]],
            direction: [null, [vm.validators.required]]
        }));
    };

    function addOldRelation() {
        vm.oldRelations().push(vm.fb.group({
            relatedEntity: [{value: null, disabled: true}, [vm.validators.required]],
            relationType: [{value: null, disabled: true}, [vm.validators.required]],
            direction: [{value: null, disabled: true}, [vm.validators.required]]
        }));
    }

    vm.removeRelation = function(index) {
        vm.relations().removeAt(index);
        vm.relations().markAsDirty();
    };

    vm.removeOldRelation = function(index) {
        vm.oldRelations().removeAt(index);
        vm.relationsToDelete.push(vm.oldRelationsData[index]);
        vm.oldRelations().markAsDirty();
    };

    vm.save = function() {
        vm.editEntityFormGroup.markAsPristine();
        widgetContext.rxjs.forkJoin([
            saveAttributes(entityId),
            saveRelations(entityId),
            saveEntity()
        ]).subscribe(
            function () {
                widgetContext.updateAliases();
                vm.dialogRef.close(null);
            }
        );
    };

    function getEntityAttributes(attributes) {
        for (var i = 0; i < attributes.length; i++) {
            vm.attributes[attributes[i].key] = attributes[i].value;
        }
    }

    function getEntityRelations(relations) {
        let relationsFrom = relations[0];
        let relationsTo = relations[1];
        for (let i=0; i < relationsFrom.length; i++) {
            let relation = {
                direction: 'FROM',
                relationType: relationsFrom[i].type,
                relatedEntity: relationsFrom[i].to
            };
            vm.oldRelationsData.push(relation);
            addOldRelation();
        }
        for (let i=0; i < relationsTo.length; i++) {
            let relation = {
                direction: 'TO',
                relationType: relationsTo[i].type,
                relatedEntity: relationsTo[i].from
            };
            vm.oldRelationsData.push(relation);
            addOldRelation();
        }
    }

    function getEntityInfo() {
        widgetContext.rxjs.forkJoin([
            entityRelationService.findInfoByFrom(entityId),
            entityRelationService.findInfoByTo(entityId),
            attributeService.getEntityAttributes(entityId, 'SERVER_SCOPE'),
            entityService.getEntity(entityId.entityType, entityId.id)
        ]).subscribe(
            function (data) {
                getEntityRelations(data.slice(0,2));
                getEntityAttributes(data[2]);
                vm.entity = data[3];
                vm.editEntityFormGroup.patchValue({
                    entityName: vm.entity.name,
                    entityType: vm.entityType,
                    entityLabel: vm.entity.label,
                    type: vm.entity.type,
                    attributes: vm.attributes,
                    oldRelations: vm.oldRelationsData
                }, {emitEvent: false});
            }
        );
    }

    function saveEntity() {
        const formValues = vm.editEntityFormGroup.value;
        if (vm.entity.label !== formValues.entityLabel){
            vm.entity.label = formValues.entityLabel;
            if (formValues.entityType == 'ASSET') {
                return assetService.saveAsset(vm.entity);
            } else if (formValues.entityType == 'DEVICE') {
                return deviceService.saveDevice(vm.entity);
            }
        }
        return widgetContext.rxjs.of([]);
    }

    function saveAttributes(entityId) {
        let attributes = vm.editEntityFormGroup.get('attributes').value;
        let attributesArray = [];
        for (let key in attributes) {
            if (attributes[key] !== vm.attributes[key]) {
                attributesArray.push({key: key, value: attributes[key]});
            }
        }
        if (attributesArray.length > 0) {
            return attributeService.saveEntityAttributes(entityId, "SERVER_SCOPE", attributesArray);
        }
        return widgetContext.rxjs.of([]);
    }

    function saveRelations(entityId) {
        let relations = vm.editEntityFormGroup.get('relations').value;
        let tasks = [];
        for(let i=0; i < relations.length; i++) {
            let relation = {
                type: relations[i].relationType,
                typeGroup: 'COMMON'
            };
            if (relations[i].direction == 'FROM') {
                relation.to = relations[i].relatedEntity;
                relation.from = entityId;
            } else {
                relation.to = entityId;
                relation.from = relations[i].relatedEntity;
            }
            tasks.push(entityRelationService.saveRelation(relation));
        }
        for (let i=0; i < vm.relationsToDelete.length; i++) {
            let relation = {
                type: vm.relationsToDelete[i].relationType
            };
            if (vm.relationsToDelete[i].direction == 'FROM') {
                relation.to = vm.relationsToDelete[i].relatedEntity;
                relation.from = entityId;
            } else {
                relation.to = entityId;
                relation.from = vm.relationsToDelete[i].relatedEntity;
            }
            tasks.push(entityRelationService.deleteRelation(relation.from, relation.type, relation.to));
        }
        if (tasks.length > 0) {
            return widgetContext.rxjs.forkJoin(tasks);
        }
        return widgetContext.rxjs.of([]);
    }
}
```

<Steps>
1. In "**HTML**" tab, enter an HTML code for your custom action (you will find an example of this function below).
</Steps>

Example of HTML code that adds a possibility to edit a device or an asset:

```html maxLines=20 collapsible
<form #editEntityForm="ngForm" [formGroup]="editEntityFormGroup"
      (ngSubmit)="save()"  class="edit-entity-form">
    <mat-toolbar fxLayout="row" color="primary">
        <h2>Edit {{entityType.toLowerCase()}} {{entityName}}</h2>
        <span fxFlex></span>
        <button mat-icon-button (click)="cancel()" type="button">
            <mat-icon class="material-icons">close</mat-icon>
        </button>
    </mat-toolbar>
    <mat-progress-bar color="warn" mode="indeterminate" *ngIf="isLoading$ | async">
    </mat-progress-bar>
    <div style="height: 4px;" *ngIf="!(isLoading$ | async)"></div>
    <div mat-dialog-content fxLayout="column">
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Name</mat-label>
                <input matInput formControlName="entityName" required readonly="">
            </mat-form-field>
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Label</mat-label>
                <input matInput formControlName="entityLabel">
            </mat-form-field>
        </div>
        <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Entity Type</mat-label>
                <input matInput formControlName="entityType" readonly>
            </mat-form-field>
            <mat-form-field fxFlex class="mat-block">
                <mat-label>Type</mat-label>
                <input matInput formControlName="type" readonly>
            </mat-form-field>
        </div>
        <div formGroupName="attributes" fxLayout="column">
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Latitude</mat-label>
                    <input type="number" step="any" matInput formControlName="latitude">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Longitude</mat-label>
                    <input type="number" step="any" matInput formControlName="longitude">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Address</mat-label>
                    <input matInput formControlName="address">
                </mat-form-field>
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Owner</mat-label>
                    <input matInput formControlName="owner">
                </mat-form-field>
            </div>
            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                <mat-form-field fxFlex class="mat-block">
                    <mat-label>Integer Value</mat-label>
                    <input type="number" step="1" matInput formControlName="number">
                    <mat-error *ngIf="editEntityFormGroup.get('attributes.number').hasError('pattern')">
                        Invalid integer value.
                    </mat-error>
                </mat-form-field>
                <div class="boolean-value-input" fxLayout="column" fxLayoutAlign="center start" fxFlex>
                    <label class="checkbox-label">Boolean Value</label>
                    <mat-checkbox formControlName="booleanValue" style="margin-bottom: 40px;">
                        {{ (editEntityFormGroup.get('attributes.booleanValue').value ? "value.true" : "value.false") | translate }}
                    </mat-checkbox>
                </div>
            </div>
        </div>
        <div class="relations-list old-relations">
            <div class="mat-body-1" style="padding-bottom: 10px; color: rgba(0,0,0,0.57);">Relations</div>
            <div class="body" [fxShow]="oldRelations().length">
                <div class="row" fxLayout="row" fxLayoutAlign="start center" formArrayName="oldRelations"
                     *ngFor="let relation of oldRelations().controls; let i = index;">
                    <div [formGroupName]="i" class="mat-elevation-z2" fxFlex fxLayout="row" style="padding: 5px 0 5px 5px;">
                        <div fxFlex fxLayout="column">
                            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                                <mat-form-field class="mat-block" style="min-width: 100px;">
                                    <mat-label>Direction</mat-label>
                                    <mat-select formControlName="direction" name="direction">
                                        <mat-option *ngFor="let direction of entitySearchDirection | keyvalue" [value]="direction.value">
                                            {{ ("relation.search-direction." + direction.value) | translate}}
                                        </mat-option>
                                    </mat-select>
                                    <mat-error *ngIf="relation.get('direction').hasError('required')">
                                        Relation direction is required.
                                    </mat-error>
                                </mat-form-field>
                                <tb-relation-type-autocomplete
                                        fxFlex class="mat-block"
                                        formControlName="relationType"
                                        required="true">
                                </tb-relation-type-autocomplete>
                            </div>
                            <div fxLayout="row" fxLayout.xs="column">
                                <tb-entity-select
                                        fxFlex class="mat-block"
                                        required="true"
                                        formControlName="relatedEntity">
                                </tb-entity-select>
                            </div>
                        </div>
                        <div fxLayout="column" fxLayoutAlign="center center">
                            <button mat-icon-button color="primary"
                                    aria-label="Remove"
                                    type="button"
                                    (click)="removeOldRelation(i)"
                                    matTooltip="Remove relation"
                                    matTooltipPosition="above">
                                <mat-icon>close</mat-icon>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="relations-list">
            <div class="mat-body-1" style="padding-bottom: 10px; color: rgba(0,0,0,0.57);">New Relations</div>
            <div class="body" [fxShow]="relations().length">
                <div class="row" fxLayout="row" fxLayoutAlign="start center" formArrayName="relations" *ngFor="let relation of relations().controls; let i = index;">
                    <div [formGroupName]="i" class="mat-elevation-z2" fxFlex fxLayout="row" style="padding: 5px 0 5px 5px;">
                        <div fxFlex fxLayout="column">
                            <div fxLayout="row" fxLayoutGap="8px" fxLayout.xs="column"  fxLayoutGap.xs="0">
                                <mat-form-field class="mat-block" style="min-width: 100px;">
                                    <mat-label>Direction</mat-label>
                                    <mat-select formControlName="direction" name="direction">
                                        <mat-option *ngFor="let direction of entitySearchDirection | keyvalue" [value]="direction.value">
                                            {{ ("relation.search-direction." + direction.value) | translate}}
                                        </mat-option>
                                    </mat-select>
                                    <mat-error *ngIf="relation.get('direction').hasError('required')">
                                        Relation direction is required.
                                    </mat-error>
                                </mat-form-field>
                                <tb-relation-type-autocomplete
                                        fxFlex class="mat-block"
                                        formControlName="relationType"
                                        [required]="true">
                                </tb-relation-type-autocomplete>
                            </div>
                            <div fxLayout="row" fxLayout.xs="column">
                                <tb-entity-select
                                        fxFlex class="mat-block"
                                        [required]="true"
                                        formControlName="relatedEntity">
                                </tb-entity-select>
                            </div>
                        </div>
                        <div fxLayout="column" fxLayoutAlign="center center">
                            <button mat-icon-button color="primary"
                                    aria-label="Remove"
                                    type="button"
                                    (click)="removeRelation(i)"
                                    matTooltip="Remove relation"
                                    matTooltipPosition="above">
                                <mat-icon>close</mat-icon>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <div>
                <button mat-raised-button color="primary"
                        type="button"
                        (click)="addRelation()"
                        matTooltip="Add Relation"
                        matTooltipPosition="above">
                    Add
                </button>
            </div>
        </div>
    </div>
    <div mat-dialog-actions fxLayout="row" fxLayoutAlign="end center">
        <button mat-button color="primary"
                type="button"
                [disabled]="(isLoading$ | async)"
                (click)="cancel()" cdkFocusInitial>
            Cancel
        </button>
        <button mat-button mat-raised-button color="primary"
                type="submit"
                [disabled]="(isLoading$ | async) || editEntityForm.invalid || !editEntityForm.dirty">
            Save
        </button>
    </div>
</form>
```

<Steps>
1. After input functions, click the "Add" button at the bottom of the "Add action" window.
2. Now in the "Actions" window, you can see the configured actions, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
3. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-4.png', alt: 'Add edit entity action', caption: 'Add a new "Action cell button" action for editing entities.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-5.png', alt: 'Configure edit action JavaScript', caption: 'Enter the JavaScript function for editing entities.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-6.png', alt: 'Configure edit action HTML', caption: 'Enter the HTML code for the edit dialog.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-7.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-8.png', alt: 'Review both actions', caption: 'Review both configured actions and click "Save", then "Apply".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-9.png', alt: 'Save dashboard', caption: 'Save the dashboard.' },
]} />

Perform the actions of adding a new device by clicking the "Add device" action button at the top of the widget.
In the "Add entity" window that opens, select the type of new entity - "Device" and enter its name. Enter the required data and click "Create".

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-10.png', alt: 'Click Add device button', caption: 'Click the "Add device" action button at the top of the widget.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-11.png', alt: 'Fill in entity details', caption: 'Select entity type, enter name and details.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-12.png', alt: 'Click Create', caption: 'Click "Create" to add the new entity.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-13.png', alt: 'New entity in table', caption: 'The new entity appears in the table.' },
]} />

Click on the action button next to the name of the device you want to edit. After clicking the action button, the device editing window will open. Make the necessary changes and click "Save".

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-14.png', alt: 'Click edit action button', caption: 'Click the edit action button next to an entity.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-15.png', alt: 'Edit entity dialog', caption: 'The edit entity dialog opens.' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-16.png', alt: 'Make changes and save', caption: 'Make changes and click "Save".' },
  { src: '/src/assets/images/user-guide/actions/custom-action-with-html-template-17.png', alt: 'Entity updated', caption: 'The entity is updated in the table.' },
]} />

### Mobile action

Mobile action is explained in mobile application configuration. See {props.product === Products.PE || props.product === Products.PAAS || props.product === Products.PAAS_EU ? <DocLink product={props.product} path="mobile/mobile-actions">ThingsBoard PE Mobile Application</DocLink> : <DocLink product={props.product} path="mobile/mobile-actions">ThingsBoard Mobile Application</DocLink>} for details.

### Open URL

This type of action will transfer you to the resource represented by the URL. This resource can be an HTML page, a document, an image, internal ThingsBoard page (for example, Alarms), etc.

We assume that you are already familiar with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. As an example for this manual, select "[Widget header button](#widget-header-button)" action source.
3. Input a name for the action and select an icon that will represent a button. With this icon, action will be performed.
4. Select an "Open URL" action type from the "Action" drop-down menu.
5. After selecting an action type, "URL" menu appears. Insert the URL link to the page you want to navigate to when the action button is clicked.
6. Optionally, enable the option to open the URL page in a new browser tab.
7. Click the "Add" button at the bottom of the "Add action" window.
8. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
9. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/open-url-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/open-url-2.png', alt: 'Configure Open URL action', caption: 'Select "Open URL" action type and enter the URL.' },
  { src: '/src/assets/images/user-guide/actions/open-url-3.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/open-url-4.png', alt: 'Review and save', caption: 'Review the action and click "Save", then "Apply".' },
]} />

After saving the changes, you can see an icon in the upper right part of the widget. Click this icon. You will be transferred to the URL specified in the action. In our case, it's the "Working with ThingsBoard widgets" documentation page.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/open-url-5.png', alt: 'Action icon in widget header', caption: 'Click the action icon in the widget header.' },
  { src: '/src/assets/images/user-guide/actions/open-url-6.png', alt: 'Navigated to URL', caption: 'You are transferred to the specified URL.' },
]} />

#### Place map item

The **Place map item** action type allows users to create new entities and immediately place them on the map widget.

As an example, let's add a button that allows users to place charging stations on the map:

To start, and to ensure that newly created entities are instantly displayed on the map, you need to configure the data source.

<Steps>
1. In the "**Overlays**" section, under the "**Marker**" tab, create a new entity alias as the data source. Name it "**EV stations**".
2. Since we'll be creating entities of the type "**EV station**", set the filter type to "**Asset type**" and specify the type as "**EV station**".
3. Apply changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/data-source-for-action-1.png', alt: 'Configure data source alias', caption: 'Create a new entity alias "EV stations" as the data source.' },
  { src: '/src/assets/images/user-guide/actions/data-source-for-action-2.png', alt: 'Apply data source changes', caption: 'Set filter type to "Asset type" and apply changes.' },
]} />

Now let's move on to configuring the action:

<Steps>
1. Scroll down to the "**Actions**" section and click the "**Add action**" button.
2. A new window will open, displaying all your created actions. Click the "plus" icon button in the top-right corner to add one.
3. Select the action source as "**Widget header button**", enter a name for the button, and choose its type.
4. Optionally, change the icon that will appear next to the button or hide it entirely. You can also set a custom color for the button.
5. From the dropdown menu, select the type of action to be performed. In our case, it's "**Place map item**".
6. Next, choose the **type of map item** to be placed — we'll be placing a "**Marker**".
7. The **custom action function** field already contains a default function that opens a dialog for creating a device or an asset — exactly what we need.
8. In the "**Action**" section, choose "**Place map item**" from the dropdown menu. Select the type of map item to be placed — in our case, it's "**Marker**". The Custom action function field contains a default function that opens a dialog to create a device or an asset — exactly what we need.
9. Then click "**Add**".
10. Apply all changes and save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-1.png', alt: 'Open Actions section', caption: 'Scroll to the "Actions" section and click "Add action".' },
  { src: '/src/assets/images/user-guide/actions/action-2.png', alt: 'Click plus icon', caption: 'Click the "plus" icon to add a new action.' },
  { src: '/src/assets/images/user-guide/actions/action-3.png', alt: 'Configure action source', caption: 'Select "Widget header button" and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/action-4.png', alt: 'Select Place map item', caption: 'Select "Place map item" action type and choose "Marker".' },
  { src: '/src/assets/images/user-guide/actions/action-5.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/action-6.png', alt: 'Apply and save', caption: 'Apply all changes and save the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/action-7.png', alt: 'Dashboard saved', caption: 'The dashboard is saved with the new action.' },
]} />

A new action button labeled "**Add EV station**" has now appeared at the top of the map. Let's use it.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-place-marker-1.png', alt: 'Click Add EV station button', caption: 'Click the "Add EV station" action button on the map.' },
  { src: '/src/assets/images/user-guide/actions/action-place-marker-2.png', alt: 'Place marker on map', caption: 'Click on the map to place the marker at the desired location.' },
  { src: '/src/assets/images/user-guide/actions/action-place-marker-3.png', alt: 'Create entity dialog', caption: 'Fill in the entity details in the dialog and click "Create".' },
]} />

Go to the "**Assets**" page — there you'll find your newly created asset, "**EV station 1**".

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-created-asset-1.png', alt: 'New asset on Assets page', caption: 'The newly created "EV station 1" asset appears on the Assets page.' },
]} />

## Action sources

Action sources are specific actions that need to be performed to achieve a goal (like clicking a widget header button, double-clicking a row, or a map's marker). They vary across different widgets.
Action sources will be explained separately for each widget type, separately using an example of the most commonly used type of action "[Navigate to new dashboard state](#navigate-to-new-dashboard-state)".

Next, we will demonstrate how to utilize each action source using the "Entities table" widget as an example.
If you're unfamiliar with adding an Entity table widget to your dashboard, you can <DocLink product={props.product} path="getting-started-guides/helloworld">learn how to do so here</DocLink>.

### Action cell button

The "Action cell button" action adds an action button to each individual entity in the widget, such as deleting devices/assets or editing them.
This action button is often used in the "Entity table" widget. In the example of this widget, let's consider the use of "Action cell button".
Clicking on the action icon of a selected entity will transfer us to another state with detailed information about that entity.

We assume that you have already added <DocLink product={props.product} path="user-guide/dashboards">a new state</DocLink> to the dashboard and familiarized yourself with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "Action cell button" as the action source.
3. Input a name for the action and select an icon that will represent a button. With this icon, action will be performed.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-cell-button-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-2.png', alt: 'Configure action cell button', caption: 'Select "Action cell button" source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-3.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

As you can see, there is an action cell button next to each entity name. By clicking this button, the corresponding action will be executed, namely transitioning to the chosen state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/action-cell-button-4.png', alt: 'Action cell buttons in table', caption: 'Action cell buttons appear next to each entity name.' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-5.png', alt: 'Click action cell button', caption: 'Click an action cell button to execute the action.' },
  { src: '/src/assets/images/user-guide/actions/action-cell-button-6.png', alt: 'Navigated to state', caption: 'You are transitioned to the chosen state.' },
]} />

### On cell click

*Only in Entities table, Alarms table, Device admin table, and Asset admin table widgets.*

The "On cell click" action adds a function for each cell in the specified table column.

Let's explore how this action works using the "Entities table" widget as an example. It displays a list of devices and their latest temperature and humidity readings.
We will configure this widget so that clicking on a cell in a specific column will open a popup with detailed telemetry information for the selected device.

For this, we will need to add an action for each column of the table.

We assume that you have already added two new <DocLink product={props.product} path="user-guide/dashboards">states</DocLink> to the dashboard and familiarized yourself with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

Let's add the first action for the "temperature" column:

<Steps>
1. Select the "On cell click" as the action source.
2. From the "Column index" drop-down menu, select the "temperature" column to which this action will be applied.
3. Enter a name for the action, and optionally, choose an icon.
4. For the action type, select "Navigate to new dashboard state" from the drop-down menu.
5. Specify the "Target dashboard state" which will display detailed temperature data for the selected device.
6. Select the "Open in popover" option in the "Dashboard state display options" field.
7. Specify the placement of the popover.
8. Optionally, specify the width and height of the popover.
9. Click "Add" to add the action.
</Steps>

We have configured an action for the "temperature" column.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-cell-click-1.png', alt: 'Select On cell click source', caption: 'Select "On cell click" action source.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-2-1.png', alt: 'Select temperature column', caption: 'Select the "temperature" column from the "Column index" dropdown.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-2-2.png', alt: 'Configure action for temperature', caption: 'Configure the action type and target state.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-3.png', alt: 'Configure popover settings', caption: 'Select "Open in popover" and configure placement.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-4.png', alt: 'Click Add', caption: 'Click "Add" to save the temperature action.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-5.png', alt: 'Temperature action configured', caption: 'The temperature action is configured.' },
]} />

Now, we need to add another action for the "humidity" column:

<Steps>
1. Click the "plus" icon in the top right corner of the "Actions" window to start adding a new action.
2. Repeat the steps as above but select the "humidity" column this time from the "Column index" drop-down menu.
3. Specify the "Target dashboard state" which will display detailed humidity data for the selected device.
4. Select the "Open in popover" option in the "Dashboard state display options" field.
5. Specify the placement of the popover.
6. Optionally, specify the width and height of the popover.
7. Click "Add" to add the action.
8. After configuring the actions, click "Save" and then "Apply" to save the widget settings.
9. Save your entire dashboard configuration by clicking "Save" located in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-cell-click-6-1.png', alt: 'Add humidity action', caption: 'Add a new action for the "humidity" column.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-6-2.png', alt: 'Configure humidity action', caption: 'Configure the action for the humidity column.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-7.png', alt: 'Configure popover for humidity', caption: 'Select "Open in popover" and configure placement.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-8.png', alt: 'Click Add', caption: 'Click "Add" to save the humidity action.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-9.png', alt: 'Both actions configured', caption: 'Both actions are configured. Click "Save" and "Apply".' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-10.png', alt: 'Save dashboard', caption: 'Save the dashboard.' },
]} />

Now, when you click on a cell in the "temperature" column for any device, an action will be performed: a popup window will appear with detailed information about the temperature of the selected device.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-cell-click-11.png', alt: 'Click temperature cell', caption: 'Click on a cell in the "temperature" column.' },
  { src: '/src/assets/images/user-guide/actions/on-cell-click-12.png', alt: 'Temperature popup', caption: 'A popup appears with detailed temperature information.' },
]} />

Similarly, clicking on a cell in the "humidity" column will appear with detailed information about the humidity of the selected device.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-cell-click-13.png', alt: 'Humidity popup', caption: 'Click on a cell in the "humidity" column to see humidity details.' },
]} />

### Widget header button

The "Widget header button" action adds an action button in the header of the widget. Clicking this button executes an action. This button is responsible for the whole widget, not for separate entities.
The widget header button is the most commonly used action source, and it can be found in all widgets. To configure the _Widget header button_ action source, you should follow these steps:

We assume that you have already added <DocLink product={props.product} path="user-guide/dashboards">a new state</DocLink> to the dashboard and familiarized yourself with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "Widget header button" as the action source.
3. Input a name for the action and select an icon that will represent a button. With this icon, action will be performed.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/widget-header-button-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-2.png', alt: 'Select Widget header button', caption: 'Select "Widget header button" action source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-3.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

As you see now, an action button has appeared in the header of the widget. After you click it, action will be performed, namely, the transition to the chosen state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/widget-header-button-4.png', alt: 'Widget header action button', caption: 'An action button has appeared in the widget header.' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-5.png', alt: 'Click widget header button', caption: 'Click the action button in the widget header.' },
  { src: '/src/assets/images/user-guide/actions/widget-header-button-6.png', alt: 'Navigated to state', caption: 'You are transitioned to the chosen state.' },
]} />

### On row click

The "On row click" action triggers an action when a row in the widget is clicked. This action button is often used in the "Entity table" widget. Let's illustrate the use of the "On row click" action with the aforementioned widget as an example. Clicking on a row of the selected entity in the widget will navigate us to another state with detailed information about that entity.

We assume that you have already added <DocLink product={props.product} path="user-guide/dashboards">a new state</DocLink> to the dashboard and familiarized yourself with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "On row click" as the action source.
3. Input a name for the action. Optionally, select an icon.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-click-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-2.png', alt: 'Select On row click', caption: 'Select "On row click" action source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-3.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

Click on any entity row to perform an action, namely, to navigate to the selected state where detailed information about the chosen device is displayed.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-click-4.png', alt: 'Click entity row', caption: 'Click on any entity row in the widget.' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-5.png', alt: 'Navigated to state', caption: 'You are navigated to the state with device details.' },
  { src: '/src/assets/images/user-guide/actions/on-row-click-6.png', alt: 'Device details state', caption: 'Detailed information about the chosen device is displayed.' },
]} />

### On row double click

The "On row double click" action triggers an action when a row in the widget is double-clicked. Let's explore its use with the "Entities table" widget, where this action is often used.
Double-clicking on a row of the selected entity in the widget will navigate us to another state with detailed information about that entity.

We assume that you have already added <DocLink product={props.product} path="user-guide/dashboards">a new state</DocLink> to the dashboard and familiarized yourself with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "On row double click" as the action source.
3. Input a name for the action. Optionally, select an icon.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-2.png', alt: 'Select On row double click', caption: 'Select "On row double click" action source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-3.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

Double-click on an entity row to perform an action, namely, navigate to the selected state. In our example, clicking a node will navigate you to a state displaying the details of the selected node.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-4.png', alt: 'Double-click entity row', caption: 'Double-click on an entity row in the widget.' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-5.png', alt: 'Navigated to state', caption: 'You are navigated to the selected state.' },
  { src: '/src/assets/images/user-guide/actions/on-row-double-click-6.png', alt: 'Device details', caption: 'The details of the selected node are displayed.' },
]} />

### On node selected

*Only in Entities Hierarchy widget.*

In this example, we'll add an "Entity hierarchy" widget to our dashboard. Entities Hierarchy widget displays the hierarchy of entities based on their relations.
For more information about entities and relations, you can <DocLink product={props.product} path="user-guide/entities-and-relations">read here</DocLink>. Clicking on any entity (node) in the Entities hierarchy will perform the configured action.

We assume that you have already added <DocLink product={props.product} path="user-guide/dashboards">a new state</DocLink> to the dashboard. Let's start setting up the action:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-node-selected-1.png', alt: 'Add Entity hierarchy widget', caption: 'Add an "Entity hierarchy" widget to the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-2.png', alt: 'Configure widget data source', caption: 'Configure the widget data source.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-3.png', alt: 'Open Add action window', caption: 'Open the "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-4.png', alt: 'Select On node selected', caption: 'Select "On node selected" action source.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-5.png', alt: 'Configure action', caption: 'Configure the action type and target state.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-6.png', alt: 'Click Add', caption: 'Click "Add" to save the action.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-7.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

To perform an action you should click any of the nodes in the Entities hierarchy. In our example, clicking a node will navigate to a state that displays the details of the selected node.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-node-selected-8.png', alt: 'Click node in hierarchy', caption: 'Click any node in the Entities hierarchy.' },
  { src: '/src/assets/images/user-guide/actions/on-node-selected-9.png', alt: 'Node details state', caption: 'You are navigated to a state displaying the node details.' },
]} />

### On HTML element click

<table style={{width:'auto'}}><thead><tr><td style={{textAlign:'center'}}><strong><em>Only for HTML widgets</em></strong></td></tr></thead></table>

**On HTML element click** allows you to trigger an action (navigate to a state, open a link, execute a custom action, etc.) when a user clicks on an element in the HTML layout of the widget.

**How it works**

When configuring **On HTML element click**, the `id` attribute is the link between the **HTML code** of the widget and the **action** defined in the widget's **Actions** tab.

You can assign the `id` to any HTML element (e.g., `<div>`, `<span>`, `<p>`, `<h1>`, `<button>`). Typically, the `id` is applied to a container element (like `<p>` or `<div>`), so the entire block becomes clickable.

```html
<p id="details" class="blue-box">
    Current temperature: <span>22.5 °C</span>
</p>
```

**Tips**
- Works **only with HTML widgets**; for tables, charts, and others use their specific action sources ([On row click](#on-row-click), [On cell click](#on-cell-click), [On node selected](#on-node-selected), etc.).
- `ID` must match **Name** exactly (case-sensitive).
- For more advanced scenarios (dialogs, forms), use [Custom action with HTML template](#custom-action-with-html-template) / *widgetContext.dialogs* / *widgetContext.customDialog* in widgets that support it.

**Configuration step by step**

<Steps>
1. Enter **widget edit mode** -> go to the **Actions** tab -> click **+** (**Add action**).
2. In Action source, select **On HTML element click**.
3. Set a **Name** (this will also serve as the **ID** for the HTML element).
4. In **Action**, choose the action type (e.g., [Navigate to new dashboard state](#navigate-to-new-dashboard-state)) and specify the target state.
5. Save the action (**Add**).
6. Go to the **Appearance** tab -> **HTML** section, and add an element with an `id` that **exactly matches** the action's Name.
7. Apply changes (**Apply**) and **save the dashboard**.
</Steps>

After saving, ThingsBoard "listens" for clicks on the element with an id that matches the Name of the created action for that widget. If found, the chosen action type (navigate, update state, custom action, etc.) is executed.

**Example: Markdown/HTML Card widget -- Click the blue area**

**Preparation:**
1. Add a <DocLink product={props.product} path="user-guide/widgets/cards/markdown-html-card">Markdown/HTML Card</DocLink> widget to your dashboard using the HTML and CSS provided below.
2. <DocLink product={props.product} path="user-guide/dashboards">Create a new dashboard state</DocLink> with device **temperature details**.

**Alternative:** You can <a href="/resources/smart_device_details.json" download="smart_device_details.json">download a pre-configured dashboard</a> with the Markdown/HTML Card widget and the details state already set up, then <DocLink product={props.product} path="user-guide/dashboards">import</DocLink> it into your ThingsBoard instance.

<Aside type="note">
**Important:** After importing, update the **target entity** and **data key** in the widget's datasource.
</Aside>

**Add the Markdown/HTML Card**

Markdown/HTML pattern:

```js
const entity = data[0];
const color = entity.temperature > 25 ? "red" : entity.temperature > 20 ? 'green' : 'blue'
const entityName = `### Temperature value card\n - Current entity: <span >${entity.entityName}</span>\n `
const temp = `- Current temperature: <span style="color:${color};">${entity.temperature.toFixed(1)} °C</span>\n `
return entityName + temp;
```

Markdown/HTML CSS:

```css maxLines=20 collapsible
.office-card {
  box-sizing: border-box;
  padding: 16px;
  margin: 6px 0;
  text-align: center;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, "Noto Sans";
}

.office-card h3 {
  margin: 0 0 12px;
  font-size: 24px;
}

.blue-box {
  background: #2196F31A;
  border: 2px solid #2196F3;
  border-radius: 10px;
  padding: 10px;
  margin: 8px 0;
  font-weight: 700;
}

.green-box {
  background: #4CAF501A;
  border: 2px solid #4CAF50;
  border-radius: 10px;
  padding: 10px;
  margin: 6px 0;
  font-weight: 700;
}

.temp-value,
.hum-value {
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}
```

**Add the action**

<Steps>
1. Enter **widget edit mode** -> go to the **Actions** tab -> click **+** (**Add action**).
2. Choose **Action source**: **On HTML element click**.
3. Enter **details** as the **action name**.
4. In **Action**, select **Navigate to new dashboard state**.
5. In **Target dashboard state**, select the state you <DocLink product={props.product} path="user-guide/dashboards">created earlier</DocLink>.
6. Click **Add** to save the action.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-1.png', alt: 'Configure On HTML element click action', caption: 'Configure the "On HTML element click" action with name "details".' },
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-2.png', alt: 'Select target state', caption: 'Select the target dashboard state and click "Add".' },
]} />

**Add id to the HTML**

<Steps>
1. Go to the **Appearance** tab -> **HTML** section.
2. Add an element with the `id=details` to the `<p class="blue-box">` line. Replace it with:

   ```text
   <p id="details" class="blue-box">
   ```

   This makes the entire blue-box block clickable and tied to the **details** action.

3. Apply changes (**Apply**) and **save the dashboard**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-3.png', alt: 'Add id to HTML element', caption: 'Add id="details" to the blue-box paragraph element.' },
]} />

**Result**

Clicking the **blue block with the current temperature** will execute the configured action and navigate to your target state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-html-element-click-4.png', alt: 'Click blue block to navigate', caption: 'Click the blue block to navigate to the target state.' },
]} />

### Map widget action sources

Map widget has unique action sources that need to be considered separately.

Let's start by adding a map widget, namely OpenStreetMap widget. We have a separate guide on <DocLink product={props.product} path="user-guide/ui/trip-animation-widget">how to create and configure a map widget</DocLink>. Please, familiarize yourself with it first.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/map-widget-action-sources-1.png', alt: 'Map widget overview', caption: 'OpenStreetMap widget added to the dashboard.' },
]} />

Now it's time to add an action.
All further explanations concerning actions assume that you have already added <DocLink product={props.product} path="user-guide/dashboards">a new state</DocLink> to the dashboard and familiarized yourself with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

#### On circle click

Circle is a plane figure, boundary points of which are always the same distance away from a fixed central point. We use circle which is based on coordinates that are specified within the device we use.
Learn how to add a circle on the Map widget, by reading <DocLink product={props.product} path="user-guide/ui/trip-animation-widget">here</DocLink>.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-circle-click-1.png', alt: 'Circle on map widget', caption: 'A circle displayed on the map widget.' },
]} />

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "On circle click" as the action source.
3. Input a name for the action. Optionally, select an icon.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-circle-click-2.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/on-circle-click-3.png', alt: 'Select On circle click', caption: 'Select "On circle click" action source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/on-circle-click-4.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

After clicking on the circle on the map, action will be performed, namely, the transition to the specified state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-circle-click-5.png', alt: 'Click circle on map', caption: 'Click on the circle on the map.' },
  { src: '/src/assets/images/user-guide/actions/on-circle-click-6.png', alt: 'Navigated to state', caption: 'You are transitioned to the specified state.' },
]} />

#### On marker click

The action will be executed by clicking on the red entity marker on the map.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "On marker click" as the action source.
3. Input a name for the action. Optionally, select an icon.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-marker-click-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/on-marker-click-2.png', alt: 'Select On marker click', caption: 'Select "On marker click" action source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/on-marker-click-3.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

Clicking on the marker on the map will take you to the specified state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-marker-click-4.png', alt: 'Click marker on map', caption: 'Click on the marker on the map.' },
  { src: '/src/assets/images/user-guide/actions/on-marker-click-5.png', alt: 'Navigated to state', caption: 'You are taken to the specified state.' },
]} />

#### On polygon click

Polygon is a plane figure that's described by a finite number of dots. We use polygon which is based on coordinates specified within the device we use, but you can use any other entity.
You may mark your assets and any other entities with a polygon option.
Learn how to add a polygon on the Map widget, by reading <DocLink product={props.product} path="user-guide/ui/trip-animation-widget">here</DocLink>.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-1.png', alt: 'Polygon on map widget', caption: 'A polygon displayed on the map widget.' },
]} />

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "On polygon click" as the action source.
3. Input a name for the action. Optionally, select an icon.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-2.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-3.png', alt: 'Select On polygon click', caption: 'Select "On polygon click" action source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-4.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

To execute an action click anywhere inside the polygon.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-5.png', alt: 'Click inside polygon', caption: 'Click anywhere inside the polygon on the map.' },
  { src: '/src/assets/images/user-guide/actions/on-polygon-click-6.png', alt: 'Navigated to state', caption: 'The action is executed and you are navigated to the specified state.' },
]} />

#### Tooltip tag action

You can configure the map widget settings so that when you click on a marker, a tooltip appears. The tooltip can contain a link that will execute the configured action.
Please note, in this manual explains only the basic usage of the tooltip tag action source. It is possible to configure several links for various devices/assets that return different values.
Use <DocLink product={props.product} path="user-guide/ui/trip-animation-widget">the tooltip function</DocLink> in the advanced mode of the map widget to accomplish this.

<Steps>
1. Enter edit widget mode and open "Add action" window.
2. Select "Tooltip tag action" as the action source.
3. Input a name for the action. Optionally, select an icon.
4. Select a "Navigate to new dashboard state" action type from the "Action" drop-down menu.
5. After choosing an action type, the "Target dashboard state" drop-down menu appears. Select a previously <DocLink product={props.product} path="user-guide/dashboards">created state</DocLink> you'd like to be transitioned to.
6. When the desired state has been selected, click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type.
8. Now navigate to the "Appearance" tab of the widget.
9. Scroll down to the Tooltip configuration and toggle the "Show tooltip" checkbox.
10. In the Tooltip line find link-act name and input an ID of the action which is its name in a format:

    ```text
    <link-act name='TooltipTag'>Navigate to the ${entityName}</link-act>
    ```

    where _TooltipTag_ is an action name and _Navigate to the Building A_ is the text that will be shown as a link on a tooltip.

11. Click "Apply" to save the widget settings.
12. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-1.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-2.png', alt: 'Select Tooltip tag action', caption: 'Select "Tooltip tag action" source and configure the action.' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-3.png', alt: 'Configure tooltip settings', caption: 'Enable "Show tooltip" and enter the link-act tag.' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-4.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

Click on the marker on the map to display the tooltip. To perform the action, click on the link text at the bottom of the tooltip.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-5.png', alt: 'Click marker to show tooltip', caption: 'Click on the marker to display the tooltip.' },
  { src: '/src/assets/images/user-guide/actions/tooltip-tag-action-6.png', alt: 'Click tooltip link', caption: 'Click the link in the tooltip to perform the action.' },
]} />

## Special actions settings

### Open right dashboard layout (mobile view)

<Aside type="note">
These settings will be useful to you if you are using the {props.product === Products.PE || props.product === Products.PAAS || props.product === Products.PAAS_EU ? <DocLink product={props.product} path="mobile">ThingsBoard PE Mobile Application</DocLink> : <DocLink product={props.product} path="mobile">ThingsBoard Mobile Application</DocLink>}. If you're not using it yet, {props.product === Products.PE || props.product === Products.PAAS || props.product === Products.PAAS_EU ? <DocLink product={props.product} path="mobile/getting-started">give it a try</DocLink> : <DocLink product={props.product} path="mobile/getting-started">give it a try</DocLink>}! Stay informed about all events in your IoT solution, even when you're on the go.
</Aside>

Often, users need to see updated information about an entity from one widget on another widget next to it. For example, we have a list of assets in the "Entity table" widget, and we want to see their details in the "Time series chart" widget next to it.
On the desktop, this is easy to set up by placing the two widgets side by side and selecting the action type "[Update current dashboard state](#update-current-dashboard-state)".
However, on the responsive screen of the {props.product === Products.PE || props.product === Products.PAAS || props.product === Products.PAAS_EU ? <DocLink product={props.product} path="mobile">ThingsBoard PE Mobile Application</DocLink> : <DocLink product={props.product} path="mobile">ThingsBoard Mobile Application</DocLink>} the widgets are automatically stacked one below the other. This means that you'll need to scroll down to view the relevant information on the second widget.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-1.png', alt: 'Widgets stacked on mobile', caption: 'On mobile, widgets are stacked and you need to scroll to see the second widget.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-2.png', alt: 'Scroll to see details', caption: 'Scrolling down to view the details widget.' },
]} />

<DocLink product={props.product} path="user-guide/dashboards">Layouts</DocLink> feature solves this issue. To configure layouts, you should:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-4.png', alt: 'Open layout settings', caption: 'Open the layout settings for the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-5.png', alt: 'Enable right layout', caption: 'Enable the right layout for the dashboard.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-6.png', alt: 'Configure right layout', caption: 'Configure the right layout settings.' },
]} />

Now we need to add the "Time series chart" widget to the right layout:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-7.png', alt: 'Add widget to right layout', caption: 'Add the "Time series chart" widget to the right layout.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-8.png', alt: 'Configure widget in right layout', caption: 'Configure the widget in the right layout.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-9.png', alt: 'Widget added to right layout', caption: 'The widget is added to the right layout.' },
]} />

Now let's add an action button with the "Update current dashboard state" type for each entity of the "Entities table" widget.
We assume that you are already familiar with the "[Add action](#add-action)" step. Therefore, we will proceed directly to the action configuration step.

<Steps>
1. Enter the editing mode of the "Time series chart" widget and open the "Add action" window.
2. As an example for this manual, select "[On row click](#on-row-click)" action source.
3. Input a name for the action and select an icon that will represent a button. With this icon, action will be performed.
4. Select an "Update current dashboard state" action type from the "Action" drop-down menu.
5. Turn on the "Open right dashboard layout (mobile view)" option.
6. Click the "Add" button at the bottom of the "Add action" window.
7. Now in the "Actions" window, you can see the configured action, allowing you to double-check the action source, icon, and action type. Click "Save", and then click "Apply" to save the widget settings.
8. Save the dashboard by clicking "Save" in the upper right corner of the dashboard page.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-10.png', alt: 'Open Add action window', caption: 'Enter edit widget mode and open "Add action" window.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-11.png', alt: 'Select On row click', caption: 'Select "On row click" action source.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-12.png', alt: 'Configure update state action', caption: 'Select "Update current dashboard state" and enable "Open right dashboard layout".' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-13.png', alt: 'Save action and dashboard', caption: 'Save the action, apply widget settings, and save the dashboard.' },
]} />

Click the row of any entity in the "Entity Table" widget to perform an action, namely, update the details about the selected entity. This entity's data will be displayed in the "Time series chart" widget on the current dashboard.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-14.png', alt: 'Click entity row on desktop', caption: 'Click on an entity row in the "Entity Table" widget.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-15.png', alt: 'Chart updated on desktop', caption: 'The "Time series chart" widget updates with the selected entity data.' },
]} />

Now open this dashboard in your {props.product === Products.PE || props.product === Products.PAAS || props.product === Products.PAAS_EU ? <DocLink product={props.product} path="mobile">ThingsBoard PE Mobile Application</DocLink> : <DocLink product={props.product} path="mobile">ThingsBoard Mobile Application</DocLink>}. In the "Entity table" widget click a row of any entity. You will be moved directly to the "Time series chart" widget where you can see the details of the selected entity of the main widget.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/mob-layout-16.png', alt: 'Dashboard on mobile', caption: 'Open the dashboard in the mobile application.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-17.png', alt: 'Click entity row on mobile', caption: 'Click a row of any entity in the "Entity table" widget.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-18.png', alt: 'Mobile navigates to right layout', caption: 'You are moved directly to the "Time series chart" widget.' },
  { src: '/src/assets/images/user-guide/actions/mob-layout-19.png', alt: 'Entity details on mobile', caption: 'The details of the selected entity are displayed.' },
]} />

### Set entity from widget

_Set entity from widget_ checkbox is responsible for adding specific entity from the widget to the state. This allows you to use the entity in the target dashboard state by creating the <DocLink product={props.product} path="user-guide/ui/aliases">Entity from dashboard state</DocLink> or other aliases. For example, if you have a list of devices in the table widget and would like to display specific device details when you click on the table row.

Sometimes you need to store more than one entity in the state. For example, you would like to navigate to customer list, then to devices of the customer and finally to specific device details. In such case, you may have three states: "Main", "Customer Devices" and "Device Details".
You may use two different state entity parameters to reference current customer (e.g. "currentCustomer") and current device (e.g. "currentDevice") on the "Device Details" state.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/actions/entityfromwidget.png', alt: 'Set entity from widget', caption: 'Use the "Set entity from widget" option to pass entity context to the target state.' },
]} />
