import DocLink from '@components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import DocImage from '~/components/DocImage.astro';
import Banner from '~/components/Banner.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import Tabs from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';

<Banner variant="peFeature" product={props.product} path="user-guide/integrations/azure-event-hub" />

Azure Event Hub Integration connects ThingsBoard to the built-in Event Hub-compatible endpoint of Azure IoT Hub. ThingsBoard subscribes as a consumer group, reads device-to-cloud messages, and converts them into devices and telemetry via an uplink converter. For downlink, ThingsBoard sends cloud-to-device messages back to Azure IoT Hub devices using the Azure IoT Hub API.

<DocImage src="/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-integration.svg" alt="Azure Event Hub integration architecture diagram" />

## Create IoT Hub in Azure

### Create the IoT Hub resource

<Steps>
1. Sign in to the <a href="https://portal.azure.com" target="_blank" rel="noopener noreferrer">Azure portal</a> and click **Create a resource**.
2. Search for **IoT Hub**, select it from the list, and click **Create**.
3. On the **Basics** tab, click **Create new** to create a new resource group. Enter a resource group name and an IoT hub name.
4. Click **Review + create**, review the configuration, then click **Create**.
5. Wait for the deployment to complete, then click **Go to resource**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/create-a-resource.png', alt: 'Azure portal home page with Create a resource tile highlighted', caption: 'Click "Create a resource" on the Azure portal home page' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/select-iot-hub.png', alt: 'Azure Marketplace New page — IoT Hub typed in the search box with IoT Hub result highlighted', caption: 'Type "IoT Hub" in the search box and select IoT Hub from the results' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/create-iot-hub.png', alt: 'IoT Hub marketplace page with Create button highlighted', caption: 'Click Create on the IoT Hub marketplace page' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/basics-iot-hub.png', alt: 'IoT Hub Basics form with resource group TB-322 and hub name thingsboard-integration filled in, Review + create button highlighted', caption: 'Set the resource group and IoT hub name, then click Review + create' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/create-basics-iot-hub.png', alt: 'IoT Hub Review + create page summarizing Subscription, Resource group, Region, and IoT hub name, with Create button', caption: 'Review the configuration and click Create' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/deployment-is-in-progress.png', alt: 'Azure deployment overview page showing Deployment is in progress with the IoT Hub resource listed', caption: 'Wait for the deployment to complete' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/go-to-resource.png', alt: 'Azure deployment overview page showing Your deployment is complete with Go to resource button highlighted', caption: 'Click "Go to resource" when the deployment finishes' },
]} />

### Create a device in the IoT Hub

<Steps>
1. In your IoT Hub resource, open **IoT devices** from the left menu.
2. Click **+ New**.
3. Enter a **Device ID** (e.g. `TB-D-01`), leave **Auto-generate keys** checked, and click **Save**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/iot-devices.png', alt: 'IoT Hub resource overview with IoT devices selected in the left menu', caption: 'Open IoT devices from the left menu' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/iot-devices-new.png', alt: 'IoT devices page with + New button highlighted and no devices in the list', caption: 'Click + New to register a device' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/save-device-id.png', alt: 'Create a device form with Device ID set to TB-D-01, Auto-generate keys checked, and Save button highlighted', caption: 'Enter the Device ID (e.g. TB-D-01), leave Auto-generate keys checked, and click Save' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/your-devices.png', alt: 'IoT devices list showing TB-D-01 with Enabled status and Cloud to Device Message Count of 0', caption: 'Device TB-D-01 appears in the list with Enabled status' },
]} />

## ThingsBoard integration setup

### Create an uplink converter

The uplink converter transforms incoming Azure Event Hub messages into the ThingsBoard format. Azure Event Hub uses a <DocLink product={props.product} path="user-guide/integrations/uplink-data-converter">generic uplink converter</DocLink>.

**Sample payload sent from the device:**

```json
{
  "devName": "TB-D-01",
  "msg": {
    "temp": 42,
    "humidity": 77
  }
}
```

The decoder reads `devName` to set the device name and extracts `temp` and `humidity` from the nested `msg` object as telemetry.

<Aside type="tip" title="TBEL">
  We recommend using <DocLink product={props.product} path="user-guide/tbel">TBEL</DocLink> for converter scripts — it executes much more efficiently than JavaScript inside ThingsBoard.
</Aside>

<Steps>
  <ol start="1">
    <li>Go to <strong>Integrations center &#8702; Data converters</strong>.</li>
    <li>Click <strong>+ Add data converter &#8702; Create new converter</strong>.</li>
    <li>Set <strong>Converter type</strong> to <strong>Uplink</strong> and enter a name.</li>
    <li>Paste the decoder function below and click <strong>Add</strong>.</li>
  </ol>
</Steps>

**The decoder function used in this tutorial:**

<Tabs>
<TabItem label="TBEL (Recommended)">
```js maxLines=15 collapsible
var data = decodeToJson(payload);
var deviceName = data.devName;
var deviceType = 'azure-event-hub-device';

var result = {
    deviceName: deviceName,
    deviceType: deviceType,
    telemetry: {
        temp: data.msg.temp,
        humidity: data.msg.humidity
    }
};

return result;
```
</TabItem>
<TabItem label="JavaScript">
```js maxLines=20 collapsible
var data = decodeToJson(payload);
var deviceName = data.devName;
var deviceType = 'azure-event-hub-device';

var result = {
    deviceName: deviceName,
    deviceType: deviceType,
    telemetry: {
        temp: data.msg.temp,
        humidity: data.msg.humidity
    }
};

function decodeToString(payload) {
    return String.fromCharCode.apply(String, payload);
}

function decodeToJson(payload) {
    var str = decodeToString(payload);
    var data = JSON.parse(str);
    return data;
}

return result;
```
</TabItem>
</Tabs>

**To adapt this converter to your device:**

- **Different device name field** — replace `data.devName` with whichever field carries the device identifier in your payload.
- **Different telemetry fields** — replace `temp` and `humidity` with your actual sensor fields; add or remove entries as needed.
- **Flat payload** — if your payload is not nested (no `msg` object), access fields directly: `data.temperature`, `data.battery`, etc.
- **Additional attributes** — add an `attributes: {}` block next to `telemetry` to store non-time-series data such as firmware version or hardware revision.

### Create the integration

<Steps>
  <ol start="1">
    <li>Go to <strong>Integrations center &#8702; Integrations</strong> and click <strong>+ Add integration</strong>.</li>
    <li>
      <span><strong>Basic settings:</strong></span>
      <ul>
        <li>Set <strong>Integration type</strong> to <strong>Azure Event Hub</strong> and enter a name.</li>
        <li>Click <strong>Next</strong>.</li>
      </ul>
    </li>
    <li>
      <span><strong>Uplink data converter:</strong></span>
      <ul>
        <li><strong>Select existing</strong> — choose the <a href="#create-an-uplink-converter">previously created</a> <code>Azure Event Hub Uplink Converter</code> from the list.</li>
        <li>Click <strong>Next</strong>.</li>
      </ul>
    </li>
    <li>
      <span><strong>Downlink data converter:</strong></span>
      <ul>
        <li>Click <strong>Skip</strong> — the downlink converter can be added later.</li>
      </ul>
    </li>
    <li>
      <span><strong>Connection settings</strong> — fill in the following fields (see subsections below for where to find each value):</span>
      <ul>
        <li><strong>Connection string</strong> — the <a href="#find-the-event-hub-compatible-endpoint">Event Hub-compatible endpoint</a> from your IoT Hub.</li>
        <li><strong>Storage connection string</strong> — the <a href="#find-the-storage-connection-string-and-container-name">connection string</a> from your Azure Storage account. Used for checkpointing.</li>
        <li><strong>Container name</strong> — the name of the Azure Blob Storage <a href="#find-the-storage-connection-string-and-container-name">container</a> used to store checkpoints.</li>
        <li>Optionally enable <strong>Persistent checkpoints</strong> to resume from the last processed event after a restart.</li>
      </ul>
    </li>
    <li>Click <strong>Check connection</strong> to verify, then click <strong>Add</strong>.</li>
  </ol>
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-integration-setup-1.png', alt: 'Add integration wizard — Basic settings step with Integration type set to Azure Event Hub and name entered', caption: 'Set Integration type to Azure Event Hub, enter a name, and click Next' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-integration-setup-2.png', alt: 'Add integration wizard — Uplink data converter step with Create new tab active and default TBEL decoder function pre-filled', caption: 'Select an existing uplink converter or click Create new — the default decoder is pre-filled; click Next' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-integration-setup-3.png', alt: 'Add integration wizard — Downlink data converter step showing Skip button highlighted', caption: 'Skip the downlink converter for now — it can be added later' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-integration-setup-4.png', alt: 'Add integration wizard — Connection step with Connection String, Persistent checkpoints enabled, Storage connection string, and Container name filled in, Check connection and Add buttons visible', caption: 'Enter the Connection String, Storage connection string, and Container name; click Check connection, then Add' },
]} />

#### Find the Event Hub-compatible endpoint

<Steps>
1. In your IoT Hub resource, go to **Built-in endpoints**.
2. Under **Event Hub-compatible endpoint**, copy the full connection string and paste it into the **Connection String** field in ThingsBoard.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/built-in-endpoints.png', alt: 'IoT Hub resource with Built-in endpoints highlighted in the left menu under Settings', caption: 'Open Built-in endpoints in your IoT Hub (Settings section of the left menu)' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/event-hub-compatible-endpoint.png', alt: 'Built-in endpoints page showing the Event Hub-compatible endpoint connection string with copy button', caption: 'Copy the full Event Hub-compatible endpoint connection string' },
]} />

#### Find the storage connection string and container name

ThingsBoard uses Azure Blob Storage to store consumer checkpoints — offsets that record which Event Hub messages have already been processed. This prevents duplicate processing when the integration restarts.

<h5>Container name</h5>

<Steps>
1. In the Azure portal, navigate to **Storage accounts** and open your storage account.
2. Go to **Data storage &#8702; Containers** and note the container name you want to use for checkpoints.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/container-1.png', alt: 'Azure portal home page with Storage accounts highlighted in Azure services and the storage account iothubstorage12345 selected from the Recent resources list', caption: 'Navigate to Storage accounts from the Azure portal home page and open your storage account' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/container-2.png', alt: 'Storage account Containers page with Data storage → Containers selected in the left menu and eventhubcontainer highlighted in the list', caption: 'Open Containers under Data storage and note the container name (e.g. eventhubcontainer)' },
]} />

<h5>Connection string</h5>

Go to **Security + networking &#8702; Access keys** and copy the **Connection string** for key1.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/storage-connection-string-1.png', alt: 'Storage account Access keys page with Access keys selected under Security + networking and the Connection string for key1 highlighted', caption: 'Go to Access keys under Security + networking and copy the Connection string for key1' },
]} />

## Test uplink

Use the Rule Engine to send a test message to Azure IoT Hub. Azure routes it to the built-in Event Hub endpoint, and ThingsBoard reads it back through the integration.

### Configure the rule chain

<Steps>
1. Open **Rule Chains** and open the **Root Rule Chain** in edit mode.
2. Add a **Generator** node. Configure it with a name, set the **Originator** to **Single entity &#8702; Integration &#8702; Azure Event Hub integration**, and paste the generate function below.
</Steps>

```js title="Generator function"
var msg = {
    devName: "TB-D-01",
    msg: {
        temp: 42,
        humidity: 77
    }
};
var metadata = { data: 40 };
var msgType = "POST_TELEMETRY_REQUEST";

return { msg: msg, metadata: metadata, msgType: msgType };
```

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/test-azure-event-hub-integration-1.png', alt: 'Root Rule Chain edit mode — Add rule node: generator dialog with name, Single entity originator set to Azure Event Hub integration, and generate function pasted in TBEL', caption: 'Add a Generator node: enter a name, set the originator to the Azure Event Hub integration, and paste the generate function; click Add' },
]} />

<Steps>
  <ol start="3">
    <li>
      Add an <strong>Azure IoT Hub</strong> action node with the following settings:
      <ul>
        <li><strong>Topic</strong> — <code>devices/TB-D-01/messages/events/</code></li>
        <li><strong>Hostname</strong> — your IoT Hub hostname (e.g. <code>thingsboard-integration.azure-devices.net</code>)</li>
        <li><strong>Device ID</strong> — <code>TB-D-01</code></li>
        <li><strong>Credentials</strong> — <strong>Shared Access Signature</strong>; <strong>SAS Key</strong> — the <a href="#find-the-device-primary-key">Primary key</a></li>
      </ul>
    </li>
    <li>Connect the <strong>Generator</strong> node to the <strong>Azure IoT Hub</strong> node using the <strong>Success</strong> relation.</li>
    <li>Click <strong>Apply changes</strong> to save the rule chain.</li>
  </ol>
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/test-azure-event-hub-integration-2.png', alt: 'Root Rule Chain — Add rule node: azure iot hub dialog with Name Azure IoT Sender, Topic devices/TB-D-01/messages/events/, Hostname thingsboard-integration.azure-devices.net, Device ID TB-D-01, and SAS Key field', caption: 'Add an Azure IoT Hub node: enter a name, set Topic to devices/TB-D-01/messages/events/, set Hostname and Device ID, and paste the Primary key as SAS Key; click Add' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/test-azure-event-hub-integration-3.png', alt: 'Root Rule Chain — Add link dialog with Success label; canvas shows generator node (Azure Event Hub test generator) connected to azure iot hub node (Azure IoT Sender)', caption: 'Connect the Generator node to the Azure IoT Hub node: drag from its right circle to the left circle of the Azure IoT Hub node, set the relation to Success, and click Apply changes' },
]} />

#### Find the device Primary key

<Steps>
  1. In your IoT Hub resource, open **IoT devices** and click on device `TB-D-01`.
  2. Copy the **Primary key** value — use it as the **SAS Key** in the next step.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/your-devices.png', alt: 'Azure IoT Hub IoT devices page for thingsboard-integration showing device TB-D-01 with Enabled status', caption: 'Open IoT devices in your IoT Hub — click on device TB-D-01 to open its details' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/primary-key.png', alt: 'Azure IoT Hub device TB-D-01 details page showing Primary Key and Secondary Key fields with copy buttons', caption: 'Copy the Primary key — use it as the SAS Key in the Azure IoT Hub rule node' },
]} />

### Verify uplink

Go to **Integrations center &#8702; Integrations**, open the **Azure Event Hub integration**, and check the **Events** tab — **Uplink** events with status **OK** confirm that data is flowing from Azure to ThingsBoard.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/test-azure-event-hub-integration-4.png', alt: 'ThingsBoard Integrations page with Azure Event Hub integration selected and Events tab showing an Uplink event with status OK', caption: 'Open the Azure Event Hub integration and navigate to the Events tab — an Uplink event with status OK confirms data is flowing from Azure to ThingsBoard' },
]} />

Go to **Entities &#8702; Devices** — a device named `TB-D-01` is automatically created by the integration. Open it and check the **Latest telemetry** tab — you should see `temp = 42` and `humidity = 77`.

## Configure and test downlink

To send commands from ThingsBoard to Azure IoT Hub devices, three things must be in place: a downlink converter attached to the integration with the Event Hub-compatible name configured, and a Rule Chain node that forwards attribute changes to the integration.

### Create a downlink converter

The downlink converter encodes ThingsBoard messages into the format required by the Azure IoT Hub cloud-to-device messaging API. For the full encoder reference, see <DocLink product={props.product} path="user-guide/integrations/downlink-data-converter">Downlink data converter</DocLink>.

**Expected output structure:**
```json
{
  "contentType": "JSON",
  "data": "{\"down-att\": \"scm:55\"}",
  "metadata": {
    "deviceId": "TB-D-01"
  }
}
```

The result object must follow this format:
- `contentType` — how data is encoded: `TEXT`, `JSON`, or `BINARY`
- `data` — the serialized payload sent to the device
- `metadata.deviceId` — the Azure IoT Hub Device ID of the target device

**The encoder function used in this tutorial:**

<Tabs>
<TabItem label="TBEL (Recommended)">
```js maxLines=15 collapsible
var result = {
    contentType: "JSON",
    data: JSON.stringify(msg),
    metadata: {
        deviceId: metadata.deviceName
    }
};
return result;
```
</TabItem>
<TabItem label="JavaScript">
```js maxLines=15 collapsible
var result = {
    contentType: "JSON",
    data: JSON.stringify(msg),
    metadata: {
        deviceId: metadata.deviceName
    }
};
return result;
```
</TabItem>
</Tabs>

The encoder serializes the full ThingsBoard message (`msg`) as JSON. `metadata.deviceName` is automatically populated from the device context when the downlink is triggered from the device's rule chain, and maps to the Azure IoT Hub Device ID.

**To adapt this converter to your device:**

- **Specific fields only** — replace `JSON.stringify(msg)` with a selective object to send only the relevant command fields.
- **Fixed device target** — replace `metadata.deviceName` with a hardcoded Device ID if the downlink always targets the same device.
- **Binary payload** — set `contentType` to `BINARY` and encode `data` accordingly.

### Add the downlink converter to the integration

<Steps>
  <ol>
    <li>Go to <strong>Integrations center &#8702; Integrations</strong> and open the <strong>Azure Event Hub integration</strong>.</li>
    <li>Click <strong>Toggle edit mode</strong>.</li>
    <li>In the <strong>Downlink data converter</strong> field, type a name and click <strong>Create new converter</strong>.</li>
    <li>Paste the encoder function from the <a href="#downlink-converter">section above</a> in the editor and click <strong>Add</strong>.</li>
    <li>Scroll down to <strong>Advanced settings</strong> and enter the <a href="#find-the-event-hub-compatible-name">Event Hub-compatible name</a> in the <strong>IoT Hub Name</strong> field.</li>
    <li>Click <strong>Apply changes</strong> to update the integration.</li>
  </ol>
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/adding-azure-event-hub-downlink-converter-1.png', alt: 'Azure Event Hub integration details panel showing the Downlink data converter field empty and the pencil edit icon highlighted', caption: 'Open the Azure Event Hub integration — the Downlink data converter field is empty; click the pencil icon to enter editing mode' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/adding-azure-event-hub-downlink-converter-2.png', alt: 'Integration in editing mode with a name typed in the Downlink data converter field and the Create new converter prompt visible', caption: 'Type a name for the downlink converter and click Create new converter' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/adding-azure-event-hub-downlink-converter-3.png', alt: 'Add data converter dialog with Downlink type, name Azure-Eventhub-Downlink, and TBEL editor showing the encoder function with deviceId set to TB-D-01', caption: 'Paste the encoder function in the TBEL editor and click Add' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/adding-azure-event-hub-downlink-converter-4.png', alt: 'Integration Advanced settings panel showing IoT Hub name field set to thingsboard-integration with Apply changes button highlighted', caption: 'In Advanced settings, enter the Event Hub-compatible name in the IoT Hub name field and click Apply changes' },
]} />

### Find the Event Hub-compatible name

<Steps>
  1. Open the <a href="https://portal.azure.com/" target="_blank">Azure portal</a>. In your **IoT Hub** resource, go to **Built-in endpoints**.
  2. Copy the **Event Hub-compatible name** — you will need it in the integration's Advanced settings.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/event-hub-compatible-name.png', alt: 'IoT Hub Built-in endpoints page showing the Event Hub-compatible name field with value thingsboard-integration', caption: 'Copy the Event Hub-compatible name from IoT Hub Built-in endpoints — you will need it in the integration\'s Advanced settings' },
]} />

### Configure the rule chain for downlinks

<Steps>
1. Open **Rule Chains &#8702; Root Rule Chain** and click the edit icon.
2. Drag the **Integration Downlink** action node onto the canvas. Enter a name, select the **Azure Event Hub integration**, and click **Add**.
3. Connect the **Message Type Switch** node to the **Integration Downlink** node using the **Post attributes** and **Attributes Updated** relations. These relations fire whenever a shared attribute is created or changed, triggering the downlink.
4. Click **Apply changes** to save the rule chain.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-configure-rule-chain-1.png', alt: 'Root Rule Chain in edit mode — node panel open with Integration Downlink node highlighted and tooltip: Pushes downlink message to selected integration', caption: 'Open the Root Rule Chain in edit mode — find Integration Downlink in the node panel and drag it onto the canvas' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-configure-rule-chain-2.png', alt: 'Add rule node: integration downlink dialog with name Azure Event Hub integration and Azure Event Hub integration selected in the Integration field', caption: 'Enter a name, select the Azure Event Hub integration, and click Add' },
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-configure-rule-chain-3.png', alt: 'Root Rule Chain canvas showing the Integration Downlink node connected via Post attributes / Attributes Updated relation with Apply changes button highlighted', caption: 'Connect the Integration Downlink node via Post attributes and Attributes Updated relations, then click Apply changes' },
]} />

### Trigger a downlink via shared attribute

<Steps>
  1. Go to **Entities &#8702; Devices**, open device **TB-D-01**, and click the **Attributes** tab.
  2. Switch to **Shared attributes** and click **+** to add a new attribute.
  3. Enter the key (e.g. `powerState`) and value (e.g. `on`), then click **Add**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-event-hub-downlink-verified-1.png', alt: 'ThingsBoard device TB-D-01 Shared attributes tab with Add attribute dialog showing key powerState and value on', caption: 'Open TB-D-01, switch to Shared attributes, and add attribute powerState = on' },
]} />

Adding the attribute fires the **Attributes Updated** relation in the rule chain, which routes the message to the Integration Downlink node and sends a cloud-to-device message to Azure IoT Hub device `TB-D-01`.

### Verify the downlink

Go to **Integrations center &#8702; Integrations**, open the **Azure Event Hub integration**, and check the **Events** tab for **Downlink** events with status **OK**. The message payload contains `deviceId` and the attribute key-value pair sent to the device.

To confirm delivery on the Azure side, open your IoT Hub in the Azure portal, navigate to **IoT devices**, select device `TB-D-01`, and verify that the **Cloud to Device Message Count** has increased to 1.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/azure-event-hub/azure-cloud-to-device-message-count.png', alt: 'Azure IoT Hub IoT devices page showing TB-D-01 with Cloud to Device Message Count equal to 1', caption: 'Azure IoT Hub confirms delivery — TB-D-01 shows Cloud to Device Message Count = 1' },
]} />

## See also

- <DocLink product={props.product} path="user-guide/integrations">Integration Overview</DocLink>
- <DocLink product={props.product} path="user-guide/integrations#uplink-data-converter">Uplink Converters</DocLink>
- <DocLink product={props.product} path="user-guide/integrations#downlink-data-converter">Downlink Converters</DocLink>
- <DocLink product={props.product} path="user-guide/rule-engine">Rule Engine</DocLink>
- <DocLink product={props.product} path="user-guide/tbel">ThingsBoard Expression Language (TBEL)</DocLink>
