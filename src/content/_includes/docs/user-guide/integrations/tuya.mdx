import DocLink from '@components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import DocImage from '~/components/DocImage.astro';
import Banner from '~/components/Banner.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import Tabs from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';

<Banner variant="peFeature" product={props.product} path="user-guide/integrations/tuya" />

<a href="https://iot.tuya.com/" target="_blank" rel="noopener noreferrer">Tuya</a> is an IoT platform that enables device manufacturers and solution providers to connect, manage, and control smart devices through the Tuya Cloud.
The ThingsBoard Tuya integration subscribes to the Tuya Message Queue Service using your project credentials and receives device status reports in real time.
It automatically creates ThingsBoard devices from the incoming data and, in the reverse direction, forwards RPC commands from ThingsBoard to Tuya devices via the Tuya Device Control API.

This guide walks through an end-to-end integration using a Tuya Smart Plug. A <a href="https://developer.tuya.com/en/docs/iot/manage-virtual-devices?id=Ka4725tiyfhg0" target="_blank">virtual Tuya device</a> can also be used for testing.

## Prerequisites

- A ThingsBoard PE account with permission to create integrations.
- A <a href="https://iot.tuya.com/" target="_blank" rel="noopener noreferrer">Tuya IoT Platform</a> account.
- At least one Tuya device (physical or virtual).
- The **Smart Life** or **Tuya Smart** app installed and signed in on your mobile device.

## Register the Smart Plug in the Tuya app

<Steps>
1. Open the **Smart Life** (or **Tuya Smart**) app. Tap **Add Device** on the home screen.
2. Browse to the **Electrical** category and tap **Socket (Wi-Fi)**.
3. Follow the on-screen reset instructions: power off the device for 10 seconds, then power it back on.
4. Press and hold the **RESET** button for 5 seconds.
5. Confirm the indicator light is blinking rapidly, then tap **Next**.
6. Wait for the app to discover and add the device. Tap **Done** when the success screen appears.
7. Open the device card and tap the power button to verify the plug responds to commands.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-1.png', alt: 'Smart Life app home screen showing no devices and an Add Device button', caption: 'Open the Smart Life app — tap Add Device on the home screen' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-2.png', alt: 'Smart Life app Add Device screen — device category list with Socket (Wi-Fi) highlighted under Electrical', caption: 'Select the Electrical category and tap Socket (Wi-Fi)' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-3.png', alt: 'Smart Life app reset instructions step 1 — power the device off for 10 seconds', caption: 'Reset step 1 — power the device off, wait 10 seconds, then power it back on' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-4.png', alt: 'Smart Life app reset instructions step 2 — press and hold the RESET button for 5 seconds', caption: 'Reset step 2 — press and hold the RESET button for 5 seconds' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-5.png', alt: 'Smart Life app reset instructions step 3 — confirm the indicator light is blinking', caption: 'Reset step 3 — confirm the indicator light is blinking rapidly, then tap Next' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-6.png', alt: 'Smart Life app — 1 device added successfully screen with Done button', caption: 'Device added successfully — tap Done' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-7.png', alt: 'Smart Life app Smart Plug control screen — power button shown, device is off', caption: 'Smart Plug control screen — the device is registered and currently off' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-add-device-8.png', alt: 'Smart Life app Smart Plug control screen — power button highlighted, device is on', caption: 'Tap the power button to turn the plug on and verify it responds' },
]} />

## Configure Tuya cloud

### Create a cloud project

<Steps>
1. In the <a href="https://www.tuya.com/" target="_blank" rel="noopener noreferrer">Tuya IoT Platform</a>, go to **Cloud &#8702; Project Management** and click **Create Cloud Project**.
2. Fill in the required fields in the pop-up window and click **Create**.
3. In the **Authorize API Services** window, complete the settings and click **Authorize**.
4. Note the **Access ID/Client ID** and **Access Secret/Client Secret** — you will need them when creating the ThingsBoard integration.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-create-cloud-project-1.png', alt: 'Tuya IoT Platform — Cloud Project Management page with Create Cloud Project button', caption: 'Go to Cloud → Project Management and click Create Cloud Project' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-create-cloud-project-2.png', alt: 'Create Cloud Project dialog — Project Name, Industry, Development Method, and Data Center fields', caption: 'Fill in the project name and required fields, then click Create' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-create-cloud-project-3.png', alt: 'Configuration Wizard — Authorize API Services dialog showing available and selected API services', caption: 'Review the API services and click Authorize' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-create-cloud-project-4.png', alt: 'Tuya cloud project Overview page showing Access ID/Client ID and Access Secret/Client Secret credentials', caption: 'Copy the Access ID and Access Secret — you will need them when creating the integration' },
]} />

### Enable the message service

The Tuya Message Service delivers real-time device status reports to the ThingsBoard integration via AMQP.

<Steps>
1. In your cloud project, go to the **Message Service** tab and toggle **Message Service** on.
2. In the pop-up, configure **Message Service Type** and **Message encryption algorithm**, then click **Ok**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-enable-message-service-1.png', alt: 'Tuya cloud project — Message Service tab with Message Service toggle in disabled state', caption: 'Open the Message Service tab in your cloud project and toggle Message Service on' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-enable-message-service-2.png', alt: 'Configure Message Service dialog — Message Queue type selected, Message Build-Up Alert, AES-GCM encryption algorithm', caption: 'Select Message Service Type and encryption algorithm, then click OK' },
]} />

### Link your Tuya app account

You need to link your devices to this project using your Smart Life app account.

<Steps>
1. Go to **Cloud &#8702; Project Management**, select your project, then navigate to **Devices &#8702; Link App Account**.
2. Click **Add App Account** and select **Tuya App Account Authorization**.
3. Scan the QR code with the Smart Life app to authorize.
4. In the pop-up, select **Automatic Link** and click **Ok**.
</Steps>

Your devices now appear under the **All Devices** tab of the project.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-link-your-tuya-app-account-1.png', alt: 'Tuya cloud project — Devices → Link App Account tab with Add App Account dropdown showing Tuya App Account Authorization option', caption: 'Go to Devices → Link App Account, click Add App Account, and select Tuya App Account Authorization' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-link-your-tuya-app-account-2.png', alt: 'Scan QR Code dialog — QR code displayed with instruction to scan using SmartLife App', caption: 'Scan the QR code with the Smart Life app (Me → Scan)' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-link-your-tuya-app-account-3.png', alt: 'Link App Account dialog — Device Linking Method with Automatic Link selected', caption: 'Select Automatic Link and click OK' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-link-your-tuya-app-account-4.png', alt: 'Tuya cloud project — Link App Account tab showing linked Tuya account with Automatic Link method', caption: 'The app account is linked — your devices will appear under the All Devices tab' },
]} />

### Create a messaging rule

Enable a messaging rule so the project receives device status uplinks via `statusReport`.

<Steps>
1. On the **Message Service** tab, click **Create Messaging Rules**.
2. Click **Add Message Filtering Rule**.
3. Select **statusReport** from the **BizCode** list and click **Release Rule**.
4. Enable the rule.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-enable-rules-environment-1.png', alt: 'Tuya IoT Platform Message Service tab showing Production Environment and Test Environment tabs', caption: 'Open the Message Service tab in your cloud project and click Create Messaging Rules' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-enable-rules-environment-2.png', alt: 'Tuya cloud Edit Messaging Rules page — Add Message Filtering Rule button highlighted', caption: 'Click Add Message Filtering Rule' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-enable-rules-environment-3.png', alt: 'Tuya cloud messaging rule configuration — statusReport selected in BizCode field, Release Rule button', caption: 'Select statusReport and click Release Rule' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-enable-rules-environment-5.png', alt: 'Tuya cloud Message Service tab — Enable Messaging Service notification with Messaging Rules button highlighted', caption: 'Enable the messaging rule — click Messaging Rules and activate the rule you just created' },
]} />

## ThingsBoard integration setup

### Create an uplink converter

The uplink converter decodes the Tuya `statusReport` payload. Tuya uses a <DocLink product={props.product} path="user-guide/integrations/uplink-data-converter">generic uplink converter</DocLink>. It reads `code`/`value` pairs from the `status` array, applies unit scaling (`cur_voltage` and `cur_power` ÷ 10; `cur_current` ÷ 100), and maps the result to ThingsBoard telemetry. The device name comes from the `devId` field (or `bizData.devId` for some message types).

<Aside type="tip" title="TBEL">
  We recommend using <DocLink product={props.product} path="user-guide/tbel">TBEL</DocLink> for converter scripts — it executes much more efficiently than JavaScript inside ThingsBoard.
</Aside>

Download the uplink converter file:

<Tabs>
  <TabItem label="TBEL (Recommended)">
    <a href="/resources/docs/integrations/tuya/tuya_tbel_uplink_converter.json" download="tuya_tbel_uplink_converter.json"><strong>tuya_tbel_uplink_converter.json</strong></a>
  </TabItem>
  <TabItem label="JavaScript">
    <a href="/resources/docs/integrations/tuya/tuya_javascript_uplink_converter.json" download="tuya_javascript_uplink_converter.json"><strong>tuya_javascript_uplink_converter.json</strong></a>
  </TabItem>
</Tabs>

**The decoder function used in this tutorial:**

<Tabs>
<TabItem label="TBEL (Recommended)">
```js maxLines=25 collapsible
/** Decoder **/

// decode payload to JSON
var data = decodeToJson(payload);

var deviceName = data.?bizData.?devId != null ? data.?bizData.?devId : data.?devId;
var deviceType = 'Tuya device';

var telemetry = [];
if (data.status != null) {
    for (var i = 0; i < data.status.length; i++) {
        var res = {};
        var code = data.status[i].code;
        var value = data.status[i].value;
        if (code == "cur_voltage" || code == "cur_power") {
            value = data.status[i].value / 10;
        } else if (code == "cur_current") {
            value = data.status[i].value / 100;
        }
        res[code] = value;
        telemetry.push(res);
    }
} else {
    telemetry = data;
}

var result = {
    deviceName: deviceName,
    deviceType: deviceType,
    attributes: {},
    telemetry: telemetry
};

/** Helper functions 'decodeToString' and 'decodeToJson' are already built-in **/

return result;
```
</TabItem>
<TabItem label="JavaScript">
```js maxLines=35 collapsible
/** Decoder **/

// decode payload to JSON
var data = decodeToJson(payload);

var deviceName = data.devId;
var deviceType = 'Tuya device';

var telemetry = [];
if (data.status != null) {
    for (var i = 0; i < data.status.length; i++) {
        var res = {};
        var code = data.status[i].code;
        var value = data.status[i].value;
        if (code == "cur_voltage" || code == "cur_power") {
            value = data.status[i].value / 10;
        } else if (code == "cur_current") {
            value = data.status[i].value / 100;
        }
        res[code] = value;
        telemetry.push(res);
    }
} else {
    telemetry = data;
}

var result = {
   deviceName: deviceName,
   deviceType: deviceType,
   attributes: {},
   telemetry: telemetry
};

/** Helper functions **/

function decodeToString(payload) {
   return String.fromCharCode.apply(String, payload);
}

function decodeToJson(payload) {
   var str = decodeToString(payload);
   var data = JSON.parse(str);
   return data;
}

return result;
```
</TabItem>
</Tabs>

<Steps>
1. Go to **Integrations center &#8702; Data converters**.
2. Click **+ Add data converter &#8702; Import converter**.
3. Drag and drop the downloaded JSON file into the **Import converter** window and click **Import**.
</Steps>

### Create a downlink converter

The downlink converter encodes ThingsBoard RPC messages into the Tuya Device Control API format. For the full encoder reference, see <DocLink product={props.product} path="user-guide/integrations/downlink-data-converter">Downlink data converter</DocLink>. It maps `msg.method` to the Tuya command `code` and `msg.params` to the command `value`, with automatic string-to-boolean conversion for on/off commands. The target device is identified via `metadata.deviceName`.

Download the downlink converter file:

<Tabs>
  <TabItem label="TBEL (Recommended)">
    <a href="/resources/docs/integrations/tuya/tuya_tbel_downlink_converter.json" download="tuya_tbel_downlink_converter.json"><strong>tuya_tbel_downlink_converter.json</strong></a>
  </TabItem>
  <TabItem label="JavaScript">
    <a href="/resources/docs/integrations/tuya/tuya_javascript_downlink_converter.json" download="tuya_javascript_downlink_converter.json"><strong>tuya_javascript_downlink_converter.json</strong></a>
  </TabItem>
</Tabs>

**The encoder function used in this tutorial:**

<Tabs>
<TabItem label="TBEL (Recommended)">
```js maxLines=20 collapsible
/** Encoder **/

var command = {};
command["code"] = msg.method;
if (msg.params == "false" || msg.params == "true") {
    command["value"] = Boolean.valueOf(msg.params);
} else {
    command["value"] = msg.params;
}

var result = {
    contentType: "JSON",
    data: JSON.stringify(command),
    metadata: {
        deviceId: metadata.deviceName
    }
};

return result;
```
</TabItem>
<TabItem label="JavaScript">
```js maxLines=20 collapsible
/** Encoder **/

const command = {
    code: msg.method,
    value:
        msg.params === "false" || msg.params === "true"
            ? msg.params === "true"
            : msg.params
};
const result = {
    contentType: "JSON",
    data: JSON.stringify(command),
    metadata: {
        deviceId: metadata.deviceName
    }
};
return result;
```
</TabItem>
</Tabs>

<Steps>
1. Go to **Integrations center &#8702; Data converters**.
2. Click **+ Add data converter &#8702; Import converter**.
3. Drag and drop the downloaded JSON file into the **Import converter** window and click **Import**.
</Steps>

### Create the Tuya integration

<Steps>
1. Go to <strong>Integrations center &#8702; Integrations</strong> and click <strong>+ Add integration</strong>.
2. <strong>Basic settings:</strong>
    - Set <strong>Integration type</strong> to <strong>Tuya</strong>.
    - <strong>Enable integration</strong> and <strong>Allow create devices or assets</strong> are on by default.
    - Click <strong>Next</strong>.
3. **Uplink data converter:**
    - Click <strong>Select existing</strong> and choose the <a href="#create-an-uplink-converter">previously created</a> <code>Tuya Uplink Converter</code>.
    - Click **Next**.
4. **Downlink data converter:**
    - Click <strong>Select existing</strong> and choose the <a href="#create-a-downlink-converter">previously created</a> <code>Tuya Downlink Converter</code>. 
    - Click **Next**.
5. **Connection settings:**
   - **Region** — select the region matching your Tuya account.
   - **Environment** — select **PROD** for a real physical device, or **TEST** for a virtual device.
   - **Access Id** and **Access Key** — paste the <a href="#create-a-cloud-project"><strong>Access ID/Client ID</strong> and <strong>Access Secret/Client Secret</strong></a> from your Tuya cloud project.
6. Click **Add** to save the integration.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-integration-setup-1.png', alt: 'Add integration wizard — Basic settings: Integration type Tuya, name Tuya integration, Enable integration and Allow create devices or assets toggled on', caption: 'Set Integration type to Tuya, enter a name, and click Next' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-integration-setup-2.png', alt: 'Add integration wizard — Uplink data converter step: Select existing active with Tuya Uplink Converter selected', caption: 'Click Select existing, choose Tuya Uplink Converter, and click Next' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-integration-setup-3.png', alt: 'Add integration wizard — Downlink data converter step: Select existing active with Tuya Downlink Converter selected', caption: 'Click Select existing, choose Tuya Downlink Converter, and click Next' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-integration-setup-4.png', alt: 'Add integration wizard — Connection step: Region Europe, Environment PROD, Access Id and Access Key fields filled', caption: 'Select Region and Environment, paste Access Id and Access Key from your Tuya project, then click Add' },
]} />

## Configure the Root Rule Chain

To forward RPC commands from ThingsBoard to Tuya devices, add an **Integration Downlink** node to the Root Rule Chain and connect it to the **RPC Request to Device** relation.

<Steps>
1. Go to **Rule chains** and open the **Root Rule Chain** in edit mode.
2. Find the **Integration Downlink** node in the node panel, drag it onto the canvas, enter a name (e.g. `Tuya integration`), select the Tuya integration, and click **Add**.
3. Connect the **Message Type Switch** node to the **Integration Downlink** node using the **RPC Request to Device** relation.
4. Click **Apply changes** to save the rule chain.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-configure-rule-chain-1.png', alt: 'ThingsBoard Root Rule Chain editor — Integration Downlink node being dragged from the node panel onto the canvas', caption: 'Drag the Integration Downlink node from the panel onto the canvas' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-configure-rule-chain-2.png', alt: 'ThingsBoard Rule chain editor — Add rule node dialog for Integration Downlink with name Tuya integration and Integration field set to Tuya integration', caption: 'Enter a name, select the Tuya integration, and click Add' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-configure-rule-chain-3.png', alt: 'ThingsBoard Root Rule Chain — Message Type Switch connected to Integration Downlink node via RPC Request to Device relation, Apply changes button visible', caption: 'Connect Message Type Switch → RPC Request to Device → Integration Downlink, then click Apply changes' },
]} />

## Verify the integration

Disconnect your Smart Plug from power and reconnect it. The device sends an uplink `statusReport` message to Tuya, which forwards it to ThingsBoard through the integration. Go to **Entities &#8702; Devices** — a device named after the Tuya `devId` is automatically created with the latest telemetry.

## Tuya Smart Plug dashboard

Import the ready-made dashboard to visualize Smart Plug telemetry and send RPC commands.

<Steps>
1. Download the dashboard: <a href="/resources/docs/integrations/tuya/tuya_smart_plug_dashboard.json" download="tuya_smart_plug_dashboard.json"><strong>tuya_smart_plug_dashboard.json</strong></a>
2. Go to **Dashboards** and click **+ &#8702; Import dashboard**.
3. Drop the downloaded JSON file into the import window and click **Import**.
4. Open the **Tuya Smart Plug** dashboard and enter **Edit** mode.
5. Click **Entity Aliases**, set the `smartPlug` alias to your provisioned device, and click **Save**.
6. Update the time series data keys in each widget to match the `code` values reported by your device if the widgets show no data.
</Steps>

<Aside type="note">
  Data point codes (`cur_voltage`, `cur_power`, `cur_current`, `switch_1`, etc.) differ between Tuya device models. Check the **Functions** tab of your device in the Tuya IoT Platform to see the exact codes it reports.
</Aside>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-dashboard-1.png', alt: 'ThingsBoard Import dashboard dialog — drop JSON file to import Tuya Smart Plug dashboard', caption: 'Go to Dashboards, click + → Import dashboard, and drop the downloaded JSON file into the import window' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-alias-1.png', alt: 'ThingsBoard dashboard edit mode — Edit alias dialog with smartPlug alias and device selector', caption: 'In Edit mode, open Entity Aliases and set the smartPlug alias to your provisioned device' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-edit-timeseries-data-keys.png', alt: 'ThingsBoard widget editor — time series data keys for Power History widget with voltage, current and power fields', caption: 'If widgets show no data, update the time series keys to match the data point codes reported by your device' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-dashboard-2.png', alt: 'Tuya Smart Plug dashboard — status indicator active, Power History chart, and voltage gauge showing 231V', caption: 'Smart Plug dashboard with live telemetry — voltage, power, and current data flowing from the device' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-dashboard-3.png', alt: 'Tuya Smart Plug dashboard — plug is on with green status indicator; On/Off switch highlighted', caption: 'Smart Plug is on — click the On/Off switch widget to send a turn-off RPC command to the device' },
  { src: '/src/assets/images/user-guide/integrations/tuya/tuya-dashboard-4.png', alt: 'Tuya Smart Plug dashboard — plug is off, power consumption drops to zero on the chart', caption: 'Smart Plug turned off — power consumption drops to zero, confirming the RPC command was delivered' },
]} />

## See also

- <DocLink product={props.product} path="user-guide/integrations">Integration Overview</DocLink>
- <DocLink product={props.product} path="user-guide/integrations/uplink-data-converter">Uplink Converters</DocLink>
- <DocLink product={props.product} path="user-guide/integrations/downlink-data-converter">Downlink Converters</DocLink>
- <DocLink product={props.product} path="user-guide/rule-engine-2-0/re-getting-started">Rule Engine</DocLink>
- <DocLink product={props.product} path="user-guide/tbel">ThingsBoard Expression Language (TBEL)</DocLink>
