import DocLink from '@components/DocLink.astro';
import Banner from '~/components/Banner.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import Tabs from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';

<Banner variant="peFeature" product={props.product} path="user-guide/integrations/pub-sub" />

**Google Cloud Pub/Sub** is a fully managed publish/subscribe messaging service. The ThingsBoard Pub/Sub integration pulls messages from a GCP pull subscription, decodes them via an uplink converter, and stores telemetry and attributes in ThingsBoard.

## Configure Google Cloud Pub/Sub

Before creating the integration in ThingsBoard, prepare the following in the <a href="https://console.cloud.google.com" target="_blank" rel="noopener noreferrer">Google Cloud Console</a>.

### Enable the Pub/Sub API

<Steps>
1. Open your GCP project and navigate to **APIs & Services &#8702; Library**.
2. Search for **Cloud Pub/Sub API** and click **Enable** if not already enabled.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/enable-pub-sub-api-1.png',
    alt: 'GCP APIs & Services Library — search results showing Cloud Pub/Sub API',
    caption: 'Navigate to APIs & Services → Library and search for "Cloud Pub/Sub API".'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/enable-pub-sub-api-2.png',
    alt: 'Cloud Pub/Sub API page with Enable button',
    caption: 'Click Enable to activate the Cloud Pub/Sub API for your project.'
  },
]} />

### Create a topic and subscription

<Steps>
1. Go to **Pub/Sub &#8702; Topics** and click **Create topic**.
2. Enter a **Topic ID** (e.g. `tb-uplink`) and click **Create**.
3. Open the new topic and click **Create subscription** in the **Subscriptions** tab.
4. Enter a **Subscription ID** (e.g. `tb-uplink-sub`), keep **Delivery type** set to **Pull**, and click **Create**.
</Steps>

<Aside type="note">
  ThingsBoard uses pull subscriptions to read messages. Push subscriptions are not supported by the integration.
</Aside>

<ImageGallery images={[
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-topic-and-subscription-1.png',
    alt: 'GCP Pub/Sub Topics list page with Create topic button',
    caption: 'Go to Pub/Sub → Topics and click Create topic.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-topic-and-subscription-2.png',
    alt: 'Create topic form with Topic ID field set to tb-uplink',
    caption: 'Enter a Topic ID (e.g. tb-uplink) and click Create.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-topic-and-subscription-3.png',
    alt: 'Topic tb-uplink details page showing the Subscriptions tab with Create subscription button',
    caption: 'Open the new topic and click Create subscription in the Subscriptions tab.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-topic-and-subscription-4.png',
    alt: 'Create subscription form — Subscription ID tb-uplink-sub, Delivery type Pull selected',
    caption: 'Enter a Subscription ID (e.g. tb-uplink-sub) and keep Delivery type set to Pull.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-topic-and-subscription-5.png',
    alt: 'Create subscription form — message retention and acknowledgement deadline settings',
    caption: 'Adjust message retention and acknowledgement deadline if needed, then click Create.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-topic-and-subscription-6.png',
    alt: 'Topic Subscriptions tab listing tb-uplink-sub with Pull delivery type',
    caption: 'The subscription tb-uplink-sub appears in the topic\'s Subscriptions tab.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-topic-and-subscription-7.png',
    alt: 'Subscription tb-uplink-sub details page showing topic, delivery type, and status',
    caption: 'Subscription details — note the Subscription ID and the attached topic; both are required for the ThingsBoard integration.'
  },
]} />

### Create a service account

ThingsBoard authenticates to GCP using a service account key. The account needs at least the **Pub/Sub Subscriber** role to receive uplink messages.

<Steps>
1. Go to **IAM & Admin &#8702; Service accounts** and click **Create service account**.
2. Enter a name (e.g. `thingsboard-pubsub`) and click **Create and continue**.
3. Assign the role **Pub/Sub Subscriber** and click **Done**.
4. Open the created service account, go to the **Keys** tab, and click **Add key &#8702; Create new key**.
5. Select **JSON** and click **Create**. Save the downloaded key file securely.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-1.png',
    alt: 'GCP IAM & Admin Service accounts page with Create service account button',
    caption: 'Go to IAM & Admin → Service accounts and click Create service account.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-2.png',
    alt: 'Create service account — step 1: Service account name set to thingsboard-pubsub',
    caption: 'Enter a service account name (e.g. thingsboard-pubsub) and click Create and continue.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-3.png',
    alt: 'Create service account — step 2: Grant this service account access, Pub/Sub Subscriber role selected',
    caption: 'Assign the Pub/Sub Subscriber role and click Done.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-4.png',
    alt: 'Service accounts list showing thingsboard-pubsub account',
    caption: 'The service account appears in the list. Click its name to open the details page.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-5.png',
    alt: 'Service account details page — Keys tab with Add key button',
    caption: 'Open the Keys tab and click Add key → Create new key.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-6.png',
    alt: 'Create private key dialog — JSON key type selected',
    caption: 'Select JSON as the key type and click Create.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-7.png',
    alt: 'Private key saved to computer confirmation dialog',
    caption: 'The JSON key file is downloaded automatically. Store it securely — you will upload it to ThingsBoard.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/create-service-account-8.png',
    alt: 'Service account Keys tab showing the newly created JSON key with its ID and creation date',
    caption: 'The key appears in the Keys tab. It is used to authenticate ThingsBoard to GCP Pub/Sub.'
  },
]} />

## ThingsBoard integration setup

### Create an uplink converter

The uplink converter decodes incoming Pub/Sub messages and maps them to the ThingsBoard data model. Pub/Sub uses a <DocLink product={props.product} path="user-guide/integrations/uplink-data-converter" target="_blank">generic uplink converter</DocLink>.

The decoder function receives:
- `payload` — the raw Pub/Sub message data as a UTF-8 byte array
- `metadata` — `integrationName` and any key-value pairs configured in the integration's Metadata settings

<Aside type="tip">
  Use <DocLink product={props.product} path="user-guide/tbel">TBEL</DocLink> for converter scripts — it executes significantly faster than plain JavaScript inside ThingsBoard.
</Aside>

**Sample payload:**

```json
{
  "deviceName": "Sensor A1",
  "deviceType": "thermostat",
  "temperature": 23.5,
  "humidity": 60
}
```

**The decoder function used in this tutorial:**

<Tabs>
  <TabItem label="TBEL (Recommended)">
    ```js maxLines=15 collapsible
    // Decode an uplink message from a buffer
    // payload - array of bytes
    // metadata - key/value object

    /** Decoder **/

    // decode payload to JSON
    var data = decodeToJson(payload);

    // --- Device name and type ---
    var deviceName = data.deviceName != null ? data.deviceName : 'Unknown Device';
    var deviceType = data.deviceType != null ? data.deviceType : 'default';
    // var customerName = 'Customer C';
    // var groupName = 'thermostat devices';

    // use assetName and assetType instead of deviceName and deviceType
    // to automatically create assets instead of devices.
    // var assetName = 'Asset A';
    // var assetType = 'building';

    // --- Timestamp parsing ---
    var timestamp = -1;
    if (data.ts != null) {
      timestamp = data.ts;
    } else if (data.timestamp != null) {
      timestamp = new Date(data.timestamp).getTime();
    }
    if (timestamp == -1) {
      timestamp = Date.now();
    }

    // --- Telemetry and attributes ---
    var telemetry = {};
    var attributes = {
      model: 'Model A',
      serialNumber: 'SN111',
      integrationName: metadata['integrationName'],
    };

    // Keys to exclude from telemetry (already used or non-telemetry fields)
    var excludeFromTelemetryList = ["deviceName", "deviceType", "ts", "timestamp"];

    // Parse all remaining JSON fields as telemetry
    telemetry.putAll(toFlatMap(data, excludeFromTelemetryList, true));

    // Result object with device attributes/telemetry data
    var result = {
      deviceName: deviceName,
      deviceType: deviceType,
      // customerName: customerName,
      // groupName: groupName,
      // assetName: assetName,
      // assetType: assetType,
      attributes: attributes,
      telemetry: {
        ts: timestamp,
        values: telemetry
      }
    };

    /** Helper functions 'decodeToString', 'decodeToJson' and 'toFlatMap' are already built-in **/

    return result;
    ```
  </TabItem>
  <TabItem label="JavaScript">
    ```js maxLines=15 collapsible
    // Decode an uplink message from a buffer
    // payload - array of bytes
    // metadata - key/value object

    /** Decoder **/

    // decode payload to JSON
    var data = decodeToJson(payload);

    // --- Device name and type ---
    var deviceName = data.deviceName != null ? data.deviceName : 'Unknown Device';
    var deviceType = data.deviceType != null ? data.deviceType : 'default';
    // var customerName = 'Customer C';
    // var groupName = 'thermostat devices';

    // use assetName and assetType instead of deviceName and deviceType
    // to automatically create assets instead of devices.
    // var assetName = 'Asset A';
    // var assetType = 'building';

    // --- Timestamp parsing ---
    var timestamp = -1;
    if (data.ts != null) {
      timestamp = data.ts;
    } else if (data.timestamp != null) {
      timestamp = new Date(data.timestamp).getTime();
    }
    if (timestamp == -1) {
      timestamp = Date.now();
    }

    // --- Telemetry and attributes ---
    var telemetry = {};
    var attributes = {
      model: 'Model A',
      serialNumber: 'SN111',
      integrationName: metadata['integrationName'],
    };

    // Keys to exclude from telemetry (already used or non-telemetry fields)
    var excludeFromTelemetryList = ["deviceName", "deviceType", "ts", "timestamp"];

    // Parse all remaining JSON fields as telemetry
    toFlatMap(data, telemetry, excludeFromTelemetryList);

    // Result object with device attributes/telemetry data
    var result = {
      deviceName: deviceName,
      deviceType: deviceType,
      // customerName: customerName,
      // groupName: groupName,
      // assetName: assetName,
      // assetType: assetType,
      attributes: attributes,
      telemetry: {
        ts: timestamp,
        values: telemetry
      }
    };

    /** Helper functions **/

    function decodeToString(payload) {
      return String.fromCharCode.apply(String, payload);
    }

    function decodeToJson(payload) {
      var str = decodeToString(payload);
      return JSON.parse(str);
    }

    function toFlatMap(obj, result, excludeList, prefix) {
      prefix = prefix || '';
      for (var key in obj) {
        if (excludeList.indexOf(key) !== -1) continue;
        var value = obj[key];
        var fullKey = prefix ? prefix + '.' + key : key;
        if (typeof value === 'object' && value !== null && !Array.isArray(value)) {
          toFlatMap(value, result, excludeList, fullKey);
        } else {
          result[fullKey] = value;
        }
      }
    }

    return result;
    ```
  </TabItem>
</Tabs>

**To adapt this converter to your device:**

- **Different device name / type fields** — replace `data.deviceName` and `data.deviceType` with the field names your device actually sends (e.g. `data.id`, `data.sensorType`).
- **Timestamp** — if your payload includes a Unix millisecond timestamp, name the field `ts`; for an ISO 8601 string, name it `timestamp`. If neither is present, the converter falls back to the server receive time automatically.
- **Telemetry fields** — all JSON fields not listed in `excludeFromTelemetryList` are mapped to telemetry automatically via `toFlatMap`. To exclude additional non-telemetry fields (e.g. a firmware version you want as an attribute), add their names to the exclude list.
- **Static attributes** — replace `model` and `serialNumber` in the `attributes` object with the actual device properties from your payload or hard-coded values; remove the entries if not needed.
- **Assets instead of devices** — uncomment `assetName` / `assetType` and comment out `deviceName` / `deviceType` to provision assets instead of devices.
- **Customer or group assignment** — uncomment `customerName` or `groupName` and set the appropriate values to assign the device to a customer or device group.

### Create the integration

<Steps>
1. Go to **Integrations center &#8702; Integrations** and click **+ Add integration**.
2. **Basic settings:**
    - Set **Integration type** to **Pub/Sub**.
    - **Enable integration** and **Allow create devices or assets** are on by default.
    - Click **Next**.
3. **Uplink data converter:**
    - Click **Create new**, enter a name, paste the decoder function from <a href="#create-an-uplink-converter">above</a>, and click **Add**.
    - Alternatively, click **Select existing** to reuse a previously created converter.
    - Click **Next**.
4. **Connection:**
    - **Project ID** — your GCP project ID (e.g. `my-gcp-project`).
    - **Subscription ID** — the pull subscription name (e.g. `tb-uplink-sub`).
    - **Service account key** — upload the JSON key file via **Browse file**, or click **Use Secret storage** to reference a stored secret.
    - See [Connection settings](#connection-settings) for a full description of each parameter.
    - Click **Add** to save the integration.
</Steps>

<Aside type="note">
  Enabling **Debug mode** stores all integration events in the database. Starting from ThingsBoard 3.9, full debug events are retained only for the first 15 minutes after the integration is created — afterward, only error events are stored.
</Aside>

<ImageGallery images={[
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-create-integration-1.png',
    alt: 'Add integration wizard — Basic settings: Integration type set to Pub/Sub, name Pub/Sub integration',
    caption: 'Basic settings — set Integration type to Pub/Sub, enter a name, and click Next.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-create-integration-2.png',
    alt: 'Add integration wizard — Uplink data converter: Select existing selected, Pub/Sub Uplink Converter chosen',
    caption: 'Uplink data converter — click Select existing, choose the converter from the list, and click Next.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-create-integration-3.png',
    alt: 'Add integration wizard — Connection step: Project ID igneous-aleph-393110, Subscription ID tb-uplink-sub, service account key file uploaded',
    caption: 'Connection — enter your Project ID, Subscription ID, and upload the service account key JSON file, then click Add.'
  },
]} />

#### Connection settings

<h5>Project ID</h5>

Your Google Cloud project ID. Found in the project selector dropdown in the GCP Console or on the **Project info** card on the dashboard.

<h5>Subscription ID</h5>

The pull subscription name on the uplink topic (e.g. `tb-uplink-sub`). The subscription must exist in GCP before the integration is created. ThingsBoard reads messages through this subscription — it is already bound to the topic in GCP.

<h5>Service account key</h5>

The GCP service account key JSON file. The account must have at least `roles/pubsub.subscriber` on the uplink subscription. Upload the file using **Browse file**, or store it in ThingsBoard Secret storage and reference it via **Use Secret storage**.

<h5>Execute remotely</h5>

When enabled, ThingsBoard generates an **Integration key** and **Integration secret** that allow the integration to run as a separate process outside the ThingsBoard cluster — useful when access to GCP is restricted to a specific network. See <DocLink product={props.product} path="user-guide/integrations/remote" target="_blank">Remote Integration</DocLink>.

<h5>Advanced settings</h5>

| Parameter | Description |
|-----------|-------------|
| **Description** | Optional text description for the integration. |
| **Metadata** | Key-value pairs injected into every uplink message as `integrationMetadata` in the converter script. |

<Aside type="note">
  In addition to any custom metadata, ThingsBoard automatically injects `pubSubMsgId` — the unique message ID assigned by GCP Pub/Sub — into every uplink event. This can be used for deduplication or message tracing.
</Aside>

## Send a test uplink

Publish a test message to the uplink topic using the `gcloud` CLI or the GCP Console.

<Tabs>
<TabItem label="gcloud CLI">
```bash
gcloud pubsub topics publish tb-uplink \
  --message='{"deviceName":"Sensor A1","deviceType":"thermostat","temperature":23.5,"humidity":60}' \
  --project=my-gcp-project
```
</TabItem>
<TabItem label="GCP Console">
<Steps>
1. Go to **Pub/Sub &#8702; Topics** and open the **tb-uplink** topic.
2. Click the **Messages** tab, then click **Publish message**.
3. Paste the JSON payload into the **Message body** field and click **Publish**.
</Steps>

```
{
  "deviceName":"Sensor A1",
  "deviceType":"thermostat",
  "temperature":23.5,
  "humidity":60
}
```
</TabItem>
</Tabs>

### Verify in ThingsBoard

**Check device telemetry** — go to **Entities &#8702; Devices**. The device **Sensor A1** is automatically provisioned on the first message. Open it and check the **Latest telemetry** tab — `temperature` and `humidity` should reflect the published values. The **Attributes** tab shows the client attributes set by the converter: `integrationName`, `model`, and `serialNumber`.

<ImageGallery images={[
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-verify-device-telemetry.png',
    alt: 'ThingsBoard Devices list with Sensor A1; Latest telemetry panel showing humidity: 60 and temperature: 23.5',
    caption: 'Entities → Devices: device Sensor A1 provisioned automatically. Latest telemetry shows humidity = 60 and temperature = 23.5.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-verify-device-attributes.png',
    alt: 'Device Sensor A1 Attributes tab — Client attributes: integrationName = Pub/Sub integration, model = Model A, serialNumber = SN111',
    caption: 'Attributes tab — client attributes set by the converter: integrationName, model, and serialNumber.'
  },
]} />

**Integration events** — go to **Integrations center &#8702; Integrations**, open the **Pub/Sub integration**, and check the **Events** tab. Click **…** in the **Message** column to inspect the raw payload pulled from the subscription.

<ImageGallery images={[
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-verify-integration-events.png',
    alt: 'Integration Events tab with Message dialog showing raw JSON: deviceName, deviceType, temperature, humidity',
    caption: 'Integration Events tab — click "…" in the Message column to inspect the raw payload received from the Pub/Sub subscription.'
  },
]} />

**Converter events** — go to **Integrations center &#8702; Data converters**, click the uplink converter, and open its **Events** tab. Click **…** in the respective column to inspect each field:
- **In** — the raw payload passed to the converter.
- **Out** — the decoded result: `deviceName`, `deviceType`, `attributes` (model, serialNumber, integrationName), and `telemetry` (temperature, humidity).
- **Metadata** — `integrationName` and `pubSubMsgId` (the unique message ID assigned by GCP Pub/Sub).

<ImageGallery images={[
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-verify-converter-events.png',
    alt: 'Data converters Events tab — Pub/Sub Uplink Converter with one Uplink event listed',
    caption: 'Integrations center → Data converters: open the uplink converter and navigate to the Events tab.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-verify-converter-in.png',
    alt: 'Converter event In dialog — raw JSON payload: deviceName Sensor A1, deviceType thermostat, temperature 23.5, humidity 60',
    caption: '"In" — the raw JSON payload received from Pub/Sub and passed to the decoder.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-verify-converter-out.png',
    alt: 'Converter event Out dialog — decoded result with deviceName, deviceType, attributes (model, serialNumber, integrationName), telemetry (temperature, humidity)',
    caption: '"Out" — the decoded converter output: deviceName, deviceType, attributes, and telemetry values.'
  },
  {
    src: '/src/assets/images/user-guide/integrations/pub-sub/pub-sub-verify-converter-metadata.png',
    alt: 'Converter event Metadata dialog — integrationName: Pub/Sub integration, pubSubMsgId: 14153284610310003',
    caption: '"Metadata" — integrationName set by the integration, and pubSubMsgId assigned by GCP Pub/Sub for this message.'
  },
]} />

## Publish to GCP Pub/Sub from ThingsBoard

The Pub/Sub integration is uplink-only — it does not have a dedicated downlink converter. To publish messages from ThingsBoard back to a GCP Pub/Sub topic, use the <DocLink product={props.product} path="reference/rule-engine/nodes/external/gcp-pubsub" target="_blank">GCP Pub/Sub Rule Node</DocLink> in the rule engine. Configure it with your Project ID, target topic name, and service account key.

{/*
## Configure downlink

To send messages from ThingsBoard back to GCP, create a dedicated downlink topic, add a downlink converter, configure the integration's advanced settings, and wire the Root Rule Chain to forward attribute updates.

### Create a downlink topic

In the GCP Console, go to **Pub/Sub &#8702; Topics** and create a topic for outgoing commands (e.g. `tb-downlink`). No subscription is needed on this topic unless another GCP service consumes the messages.

### Create the downlink converter

The downlink converter encodes Rule Engine messages into Pub/Sub payloads. The output must include:
- `contentType` — encoding format: `TEXT`, `JSON`, or `BINARY`
- `data` — the payload body to publish to the downlink topic
- `metadata` — optional Pub/Sub message attributes passed as key-value pairs

<Tabs>
  <TabItem label="TBEL (Recommended)">
    ```js maxLines=12 collapsible
    var result = {
    contentType: 'JSON',
    data: JSON.stringify(msg),
    metadata: {
    deviceName: metadata.deviceName
  }
  };

    return result;
    ```
  </TabItem>
  <TabItem label="JavaScript">
    ```js maxLines=12 collapsible
    var result = {
    contentType: 'JSON',
    data: JSON.stringify(msg),
    metadata: {
    deviceName: metadata.deviceName
  }
  };

    return result;
    ```
  </TabItem>
</Tabs>

<Steps>
  1. Go to **Integrations center &#8702; Data converters** and click **+ Add data converter**.
  2. Select **Downlink**, enter a name, paste the encoder function above, and click **Add**.
</Steps>

### Attach the downlink converter and configure the downlink topic

<Steps>
  1. Go to **Integrations center &#8702; Integrations** and open the **GCP Pub/Sub integration**.
  2. Click the edit (pencil) icon.
  3. In the **Downlink data converter** field, select the downlink converter you just created.
  4. Expand **Advanced settings** and enter the **Downlink topic name** (e.g. `tb-downlink`).
  5. Click **Apply changes**.
</Steps>

### Update the Root Rule Chain

Add an **Integration Downlink** node to forward attribute updates to the Pub/Sub downlink topic.

<Steps>
  1. Go to **Rule chains** and open the **Root Rule Chain**. Click the edit icon.
  2. Find **Integration Downlink** in the node panel and drag it onto the canvas.
  3. In the **Add rule node** dialog, enter a name (e.g. `Pub/Sub downlink`), select **GCP Pub/Sub integration**, and click **Add**.
  4. Connect the **Message Type Switch** node to the new node via the **Post attributes** and **Attributes Updated** relations.
  5. Click **Apply changes**.
</Steps>

### Test the downlink

Trigger a downlink by adding a shared attribute to device `Sensor A1`:

<Steps>
  1. Go to **Entities &#8702; Devices** and open `Sensor A1`.
  2. Navigate to the **Attributes** tab, switch to **Shared attributes**, and click **+**.
  3. Enter key `firmware` and value `1.0.1`, then click **Add**.
</Steps>

Adding the attribute triggers the rule chain and forwards the message to the Pub/Sub integration. Go to **Integrations center &#8702; Integrations**, open the integration, and check the **Events** tab — a **Downlink** event appears with the payload sent to the downlink topic.

To verify the message arrived in GCP, create a temporary subscription on `tb-downlink` and pull from it using the CLI:

```bash
gcloud pubsub subscriptions pull tb-downlink-sub \
  --project=my-gcp-project \
  --auto-ack \
  --limit=5
```

## Troubleshooting

<h5>Integration status is not Active</h5>

<Steps>
  1. Confirm the **Project ID** and **Subscription ID** exactly match the values in the GCP Console.
  2. Verify the **Service account key** JSON is complete and belongs to an active service account.
  3. Check that the service account has `roles/pubsub.subscriber` on the subscription.
  4. Ensure the **Cloud Pub/Sub API** is enabled in your GCP project.
</Steps>

<h5>Messages are not appearing in ThingsBoard</h5>

<Steps>
  1. Check the integration **Events** tab for errors.
  2. Confirm the pull subscription is attached to the correct topic and has no expiration policy that deactivated it.
  3. Use the **Converter Events** tab to inspect the raw **In** payload — verify the uplink converter handles the actual message format.
</Steps>

<h5>Downlink messages are not published to GCP</h5>

<Steps>
  1. Confirm the **Downlink topic name** is set in the integration's Advanced settings.
  2. Verify the service account has `roles/pubsub.publisher` on the downlink topic.
  3. Check that the **Integration Downlink** rule node is correctly connected in the Root Rule Chain.
</Steps>
*/}

## Troubleshooting

<h5>Integration status is not Active</h5>

<Steps>
1. Confirm the **Project ID** and **Subscription ID** exactly match the values in the GCP Console.
2. Verify the **Service account key** JSON is complete and belongs to an active service account.
3. Check that the service account has `roles/pubsub.subscriber` on the subscription.
4. Ensure the **Cloud Pub/Sub API** is enabled in your GCP project.
</Steps>

<h5>Messages are not appearing in ThingsBoard</h5>

<Steps>
1. Check the integration **Events** tab for errors.
2. Confirm the pull subscription is attached to the correct topic and has no expiration policy that deactivated it.
3. Use the **Converter Events** tab to inspect the raw **In** payload — verify the uplink converter handles the actual message format.
</Steps>

## See also

- <DocLink product={props.product} path="user-guide/integrations">Integration Overview</DocLink>
- <DocLink product={props.product} path="user-guide/integrations/integration-types">Integration types</DocLink>
- <DocLink product={props.product} path="user-guide/integrations/uplink-data-converter">Uplink data converter</DocLink>
- <DocLink product={props.product} path="user-guide/integrations/remote">Remote Integration</DocLink>
- <DocLink product={props.product} path="reference/rule-engine/nodes/external/gcp-pubsub">GCP Pub/Sub Rule Node</DocLink>
- <DocLink product={props.product} path="user-guide/rule-engine">Rule Engine</DocLink>
- <DocLink product={props.product} path="user-guide/tbel">ThingsBoard Expression Language (TBEL)</DocLink>
