import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import { Steps, Aside } from '@astrojs/starlight/components';
import YouTubeVideo from '~/components/YouTubeVideo.astro';

SCADA dashboards provide a specialized layout for building interactive supervisory control interfaces. Available since ThingsBoard 3.8.0, the SCADA layout places widgets on a fixed column grid — symbols hold their exact positions regardless of viewport size, making it ideal for process diagrams, factory floor plans, and energy system schematics.

## Video Tutorial

Watch this video to learn how to set up a SCADA system in ThingsBoard to monitor and control an industrial water tank system.

<YouTubeVideo
  videoId="kBMcsg7YEcQ"
  title="ThingsBoard SCADA dashboards overview"
/>

## SCADA Dashboard Layout

Unlike the <DocLink product={props.product} path="user-guide/layouts" bold={true} target="_blank">default layout</DocLink>, where widgets reflow and resize with the viewport, the SCADA layout fixes widget positions on a column grid:

- Widget positions and sizes are **fixed** — they do not adjust when the viewport changes.
- The canvas scrolls horizontally when the viewport is narrower than the configured minimum width.
- A configurable **margin** defines the space (in pixels) between the dashboard edge and the canvas.

## Create SCADA Dashboard

<Steps>
  <ol>
    <li>From the sidebar, navigate to <strong>Dashboards</strong>.</li>
    <li>Click the <strong>+ Add dashboard &#8702; Create new dashboard</strong> in the top right corner.</li>
    <li>Enter a dashboard <strong>title</strong> (all other settings are optional).</li>
    {props.product !== Products.CE && <><li>If needed: In <strong>Owner and groups</strong>, change the dashboard owner or add it to a group.</li></>}
    <li>Click <strong>Add</strong> — the dashboard opens automatically in edit mode.</li>
    <li>Click <strong>Layouts</strong> in the toolbar.</li>
    <li>In the <strong>Manage layouts</strong> dialog, change the layout type to <strong>SCADA</strong> and click <strong>Save</strong>.</li>
  </ol>
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/add-scada-dashboard-1.png', alt: 'Create dashboard dialog', caption: 'Go to Dashboards, click "+", select "Create new dashboard", enter a title, and click "Add".' },
  { src: '/src/assets/images/user-guide/scada/add-scada-dashboard-2.png', alt: 'Dashboard in edit mode', caption: 'New dashboard opens in edit mode — click "Layouts" in the toolbar.' },
  { src: '/src/assets/images/user-guide/scada/add-scada-dashboard-3.png', alt: 'Manage layouts dialog', caption: 'In the "Manage layouts" dialog, change the layout type to "SCADA" and click "Save".' },
]} />

### Layout Settings

To access layout settings, click **Layouts** in the edit mode toolbar, then click the **gear** icon next to the layout row in the **Manage layouts** dialog.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/layout-settings-1.png', alt: 'Gear icon in Manage layouts dialog', caption: 'In the "Manage layouts" dialog, click the gear icon next to the layout row to open layout settings.' },
  { src: '/src/assets/images/user-guide/scada/layout-settings-2.png', alt: 'Layout settings dialog', caption: 'The "Layout settings" dialog — configure columns count, minimum width, background color, and image.' },
]} />

#### Columns Count

Set the column count for the SCADA grid. Values must be **multiples of 24** (24 to 1008). Higher values give finer placement granularity.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-columns-count-1.png', alt: 'Columns count dropdown', caption: 'Columns count dropdown — values are multiples of 24 (24, 48, 72, … up to 1008).' },
  { src: '/src/assets/images/user-guide/scada/scada-columns-count-2.png', alt: 'Canvas with 24 columns', caption: 'SCADA canvas with 24 columns.' },
  { src: '/src/assets/images/user-guide/scada/scada-columns-count-3.png', alt: 'Canvas with 48 columns', caption: 'SCADA canvas with 48 columns — finer grid for more precise widget placement.' },
]} />

#### Minimum Layout Width

Set the minimum canvas width in **columns**. If the viewport is narrower, a horizontal scrollbar appears.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-minimum-layout-width-1.png', alt: 'Minimum layout width field', caption: 'Minimum layout width — set the minimum canvas width in columns.' },
  { src: '/src/assets/images/user-guide/scada/scada-minimum-layout-width-2.png', alt: 'Canvas with minimum width applied', caption: 'Canvas with minimum layout width set to 24 columns — the layout does not shrink below this.' },
]} />

#### Background Color

Set a custom background color for the SCADA canvas.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-background-color-1.png', alt: 'Color picker open', caption: 'Click the color swatch next to "Background color" to open the color picker; choose color and opacity, then click "Select".' },
  { src: '/src/assets/images/user-guide/scada/scada-background-color-2.png', alt: 'Canvas with custom background', caption: 'SCADA canvas with a custom background color applied.' },
]} />

#### Background Image

Upload a background image and choose a **Background size mode** — **Fit width**, **Fit height**, **Cover**, **Contain**, or **Original size**. The image typically represents the process diagram on which SCADA symbols are overlaid.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-background-image-1.png', alt: 'Background image section', caption: 'In the Background image section, click "Browse from gallery" to select or upload an image.' },
  { src: '/src/assets/images/user-guide/scada/scada-background-image-2.png', alt: 'Image gallery', caption: 'Image gallery — select an existing image or click "Upload image" to add a new one.' },
  { src: '/src/assets/images/user-guide/scada/scada-background-image-3.png', alt: 'Background size mode dropdown', caption: 'After selecting an image, choose the Background size mode (Fit width, Fit height, Cover, Contain, Original size).' },
  { src: '/src/assets/images/user-guide/scada/scada-background-image-4.png', alt: 'Canvas with background image', caption: 'Background image applied to the SCADA canvas.' },
]} />

### Multi-state Dashboards

A dashboard can combine regular <DocLink product={props.product} path="user-guide/dashboards/#states" bold={true} target="_blank">dashboard states</DocLink> with SCADA states, enabling navigation from a SCADA overview to a detail view — or from a symbol to a popup widget.

You can download and import a <a href="/resources/multi-state-scada-dashboard.json" download="multi-state-scada-dashboard.json"><strong>sample multi-state SCADA dashboard</strong></a> to explore this feature.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/multi-state-dashboards-1.png', alt: 'Main SCADA state', caption: 'Main SCADA state — two tanks and a pump with live telemetry.' },
  { src: '/src/assets/images/user-guide/scada/multi-state-dashboards-2.png', alt: 'Popup chart on symbol click', caption: 'Clicking a SCADA symbol opens a popup widget — here a flow rate chart triggered by clicking the flowmeter.' },
  { src: '/src/assets/images/user-guide/scada/multi-state-dashboards-3.png', alt: 'Target tank sub-state', caption: 'Clicking the right tank navigates to the "Target tank" state — the tank symbol with a live level chart.' },
  { src: '/src/assets/images/user-guide/scada/multi-state-dashboards-4.png', alt: 'Pump sub-state', caption: 'Clicking the pump navigates to the "Pump" state — the pump symbol with a state history chart.' },
]} />

## SCADA Symbol

A SCADA symbol is an <a href="https://en.wikipedia.org/wiki/SVG" target="_blank"><strong>SVG</strong></a> element that represents a piece of equipment — a valve, pump, tank, or any process component. Symbols include built-in behavior tags that connect them to device data.

ThingsBoard provides 100+ SCADA symbols out of the box, with plans to expand to 500+. You can also create <DocLink product={props.product} path="user-guide/scada-symbol-dev" target="_blank">custom SCADA symbols</DocLink>.

Starting with ThingsBoard 3.9, **high-performance SCADA symbols** are available — streamlined graphical elements designed for large-scale dashboards:

- **Simplified design** — clean, standardized visuals that reduce rendering overhead.
- **Color coding** — state is communicated through color changes.
- **Dynamic changes** — visual state updates reflect live process data in real time.
- **Standardization** — uniform appearance across all diagrams.
- **Compatibility** — designed for use across different devices and screen sizes.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/high-performance-scada-symbols-1.png', alt: 'High-performance symbols dashboard', caption: 'Swimming pool SCADA dashboard using high-performance symbols — simplified design with color-coded state labels and inline mini-charts.' },
  { src: '/src/assets/images/user-guide/scada/high-performance-scada-symbols-2.png', alt: 'Traditional symbols dashboard', caption: 'The same dashboard with traditional SCADA symbols — detailed 3D-style SVG graphics for all equipment.' },
]} />

## SCADA Symbol Widget

The **SCADA symbol** widget renders an SVG-based symbol on the dashboard and connects it to a target entity for live data.

<Steps>
1. In dashboard edit mode, click **+ Add widget** in the toolbar.
2. In the widget library, find and open a SCADA symbols bundle (e.g., **Traditional SCADA fluid system**).
3. Choose a symbol (e.g., <DocLink product={props.product} path="reference/widgets/scada/traditional-fluid-system/horizontal-wheel-valve/" target="_blank">Horizontal wheel valve</DocLink>).
4. Configure:
    - **[The target device](#target-entity)** — the device or entity the symbol will visualize.
    - **[SCADA symbol](#scada-symbol-1)** — the SVG symbol from the library to render.
    - **[Behavior](#behavior)** — how the symbol reads and writes data.
    - **[Appearance](#appearance)** — symbol-specific visual properties such as state colors and rotation.
    - **[Card appearance](#card-appearance)** — background, border radius, padding, and visible card buttons.
5. Click **Add**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/add-scada-symbol-widget-1.png', alt: 'Add widget button', caption: 'Click "+ Add widget" in the toolbar, or click the "Add new widget" placeholder on the canvas.' },
  { src: '/src/assets/images/user-guide/scada/add-scada-symbol-widget-2.png', alt: 'Widget library bundles', caption: 'In the widget library, find and open a SCADA symbols bundle (e.g., "Traditional SCADA fluid system").' },
  { src: '/src/assets/images/user-guide/scada/add-scada-symbol-widget-3.png', alt: 'Symbol selection', caption: 'Select a SCADA symbol from the bundle (e.g., "Horizontal wheel valve").' },
]} />

### Target Entity

Select the entity the SCADA symbol will visualize — either a specific device or an <DocLink product={props.product} path="user-guide/aliases" bold={true} target="_blank">entity alias</DocLink> resolving to a single entity.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-selected-target-device-1.png', alt: 'Target entity selection', caption: 'Set the target device or entity alias — select a specific device or an alias pointing to a single entity.' },
]} />

### SCADA Symbol

Select the SVG symbol from the library to render in this widget. The selected symbol determines which behavior parameters are available.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-symbol-device-1.png', alt: 'SCADA symbol selection', caption: 'The SCADA symbol field — the selected symbol determines which behavior parameters are available.' },
]} />

### Behavior

Behavior settings define how the symbol reads and writes data. Each behavior parameter has an **action type** that determines the data source or target. The set of parameters is specific to the selected symbol and defined by its author.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-behavior-settings-1.png', alt: 'Behavior parameters list', caption: 'Behavior parameters for the Horizontal wheel valve — Opened, Open, and Close — each with a configurable action type.' },
]} />

**Action types for retrieving a value:**

| Action type | Description |
|-------------|-------------|
| **Do nothing** | Uses a constant value defined in the widget |
| **Execute RPC** | Sends an RPC request to the device and uses the response |
| **Get attribute** | Subscribes to an entity attribute |
| **Get time series** | Subscribes to the latest time-series value |
| **Get alarm status** | Subscribes to the entity's alarm status |
| **Get dashboard state** | Uses the name of the current dashboard state |

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-action-do-nothing-1.png', alt: 'Do nothing action', caption: 'Do nothing — uses a constant True/False value defined in the widget.' },
  { src: '/src/assets/images/user-guide/scada/scada-action-execute-rpc-1.png', alt: 'Execute RPC action', caption: 'Execute RPC — sends an RPC command to the device and uses the response as the parameter value.' },
  { src: '/src/assets/images/user-guide/scada/scada-action-get-attribute-1.png', alt: 'Get attribute action', caption: 'Get attribute — subscribes to a target entity attribute (scope, key, and optional result converter).' },
  { src: '/src/assets/images/user-guide/scada/scada-action-get-time-series-1.png', alt: 'Get time series action', caption: 'Get time series — subscribes to a time-series key on the target entity.' },
  { src: '/src/assets/images/user-guide/scada/scada-action-get-alarm-status-1.png', alt: 'Get alarm status action', caption: 'Get alarm status — subscribes to alarm status, filtered by severity and alarm type.' },
  { src: '/src/assets/images/user-guide/scada/scada-action-get-dashboard-state-1.png', alt: 'Get dashboard state action', caption: 'Get dashboard state — uses the name of the current dashboard state as the parameter value.' },
]} />

**Action types for writing to the target entity:**

| Action type | Description |
|-------------|-------------|
| **Execute RPC** | Sends an RPC command to the target device |
| **Set attribute** | Writes a value to an attribute on the target entity |
| **Add time series** | Pushes a new time-series data point to the target entity |

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-action-execute-rpc-to-target-device-1.png', alt: 'Execute RPC to device', caption: 'Execute RPC — sends an RPC command with the configured method and parameters to the target device.' },
  { src: '/src/assets/images/user-guide/scada/scada-action-set-attribute-1.png', alt: 'Set attribute action', caption: 'Set attribute — writes a value to the specified attribute (scope, key, and constant or function value).' },
  { src: '/src/assets/images/user-guide/scada/scada-action-add-time-series-1.png', alt: 'Add time series action', caption: 'Add time series — pushes a new data point with the configured key and value to the target entity.' },
]} />

**Widget action (On click)** — defines what happens when a user clicks the SCADA symbol. See <DocLink product={props.product} path="user-guide/actions" bold={true} target="_blank">widget actions</DocLink> for details.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-action-left-motor-pump-1.png', alt: 'On click action', caption: 'The "On click" behavior parameter defines the widget action triggered when a user clicks the symbol.' },
]} />

**Example:** <DocLink product={props.product} path="reference/widgets/scada/traditional-fluid-system/horizontal-wheel-valve/" target="_blank">Horizontal wheel valve</DocLink> — all three behavior parameters use the shared attribute `open` on the target device: **Opened** reads it to render the valve state; **Open** writes `true` when the user opens the valve; **Close** writes `false` when the user closes it.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/example-of-parameters-scada-symbol-1.png', alt: 'Opened parameter', caption: '"Opened" parameter: Get attribute — reads the shared attribute "open" to render the valve as open or closed.' },
  { src: '/src/assets/images/user-guide/scada/example-of-parameters-scada-symbol-2.png', alt: 'Open parameter', caption: '"Open" parameter: Set attribute — writes "open = true" to the target entity when the user opens the valve.' },
  { src: '/src/assets/images/user-guide/scada/example-of-parameters-scada-symbol-3.png', alt: 'Close parameter', caption: '"Close" parameter: Set attribute — writes "open = false" to the target entity when the user closes the valve.' },
]} />

### Appearance

Appearance settings include generic parameters (title, card icon) and symbol-specific visual properties defined by the symbol's author — such as state colors and rotation angles.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-appearance-1.png', alt: 'Appearance settings', caption: 'Symbol-specific appearance settings — state colors (Opened/Closed) and rotation angles for the Horizontal wheel valve.' },
]} />

### Card Appearance

Card appearance settings are common to all SCADA symbol widgets: card background (transparent by default), visible card buttons, border radius, and padding.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/scada/scada-card-appearance-1.png', alt: 'Card appearance settings', caption: 'Card appearance — background, visible card buttons (e.g., Fullscreen), border radius, and padding.' },
]} />

---

## What You Can Do with SCADA

| Capability | Description | Guide |
|---|---|---|
| Configure the layout | Set column count, minimum width, background color, and background image for the SCADA canvas | <DocLink product={props.product} path="user-guide/layouts" target="_blank">Layouts</DocLink> |
| Create custom SCADA symbols | Design your own SVG-based symbols with built-in behavior tags for use in SCADA dashboards | <DocLink product={props.product} path="user-guide/scada-symbol-dev" target="_blank">Custom SCADA symbols</DocLink> |
| Add SCADA symbol widgets | Place SVG-based equipment symbols on the canvas and connect them to live device data via behavior parameters | <DocLink product={props.product} path="user-guide/scada/#scada-symbol-widget" target="_blank">Add SCADA symbol widget</DocLink> |
| Build multi-state SCADA dashboards | Combine SCADA states with regular dashboard states to navigate from an overview to detail views or popup widgets | <DocLink product={props.product} path="user-guide/dashboards/#states" target="_blank">Dashboard states</DocLink> |
| Trigger widget actions on click | Configure what happens when a user clicks a SCADA symbol — navigate to a state, open a popup, or send an RPC command | <DocLink product={props.product} path="user-guide/actions" target="_blank">Widget actions</DocLink> |
