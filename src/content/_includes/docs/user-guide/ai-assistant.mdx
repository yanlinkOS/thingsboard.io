import { Aside } from '@astrojs/starlight/components';
import DocLink from '@components/DocLink.astro';

The **AI Assistant** is built into the ThingsBoard UI. It edits **calculated fields** and **alarm rules** in natural language, and generates ready-to-use **dashboards** for any device or asset in one click.

<Aside type="note">
  Full documentation for the AI Assistant is on the way. In the meantime, see <DocLink product={props.product} path='user-guide/cli'>ThingsBoard CLI</DocLink> and <DocLink product={props.product} path='user-guide/ai-solution-creator'>AI Solution Creator</DocLink> for related AI-driven workflows.
</Aside>
