import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';

Widgets support automatic data conversion and unit switching based on the selected unit system. This is useful in global IoT solutions where users across different regions use different measurement standards — metric (°C, km, kg), imperial (°F, miles, lbs), or hybrid.

**Key benefits:**
- Automatic conversion of numerical values into the appropriate units directly within the widget — no additional data processing required.
- Flexible widget configuration that eliminates the need to duplicate widgets or create separate dashboards for users with different regional unit standards.
- Support for a wide range of measurement units, allowing users to view values in the units most familiar to them.
- Per-user setting — the same dashboard automatically displays the correct units for each user based on their profile setting.

All widgets designed to display telemetry data include a unit conversion option. Unit conversion is supported for telemetry data, Y-axis scales, and threshold settings.

<Aside type="note">
  If a widget contains multiple telemetry keys, unit conversion must be configured individually for each key.
</Aside>

## Enable unit conversion

Unit conversion is configured in the **Unit settings** dialog, accessible by clicking the **Units** field for a telemetry key, Y-axis, or threshold in the widget settings.

<Steps>
1. Specify the **source unit** — the unit of the incoming telemetry value sent by the device. This must match the actual device data; an incorrect source unit will produce wrong converted values.
2. Toggle the **Enable unit conversion** switch. The system automatically suggests commonly used target units for each unit system (e.g., °C for Metric, °F for Imperial, °C for Hybrid). You can override these by selecting different units from the dropdown.
3. Click **Apply** to save the changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/units/unit-conversion-configuring-1.png', alt: 'Unit settings dialog in widget configuration', caption: 'The Unit settings dialog — opened by clicking the Units field for a telemetry key, Y-axis, or threshold in the widget settings.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-configuring-2.png', alt: 'Specify source unit', caption: 'Specify the source unit — the unit of the incoming telemetry value received from the server.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-configuring-3.png', alt: 'Enable unit conversion and set target units', caption: 'Enable unit conversion and specify target units for each unit system. Click Apply to save.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-configuring-4.png', alt: 'Unit conversion active icon', caption: 'Once unit conversion is enabled, an icon appears in the Unit field indicating the feature is active.' },
]} />

<Aside type="tip">
  All available units are grouped by category (e.g., temperature, length, mass). You can only select target units from the same category as the source unit. If you do not specify a unit for a particular unit system, that system falls back to the source unit.
</Aside>

## Range color settings

Range color settings use the **original telemetry value** received from the server in its source units — before any unit conversion is applied.

For example, if your device sends a temperature of 22 °C and unit conversion is enabled, the widget may display 71 °F. However, the range logic still uses the original 22 °C to determine the color. A green range from 18 to 24 °C will show green regardless of whether the display reads 22 °C or 71 °F.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/units/range-color-settings-1.png', alt: 'Range color settings use original source units', caption: 'Range color settings use the original telemetry value in source units — before any unit conversion is applied.' },
]} />

## Changing the unit system

<Steps>
1. Click the **three-dot icon** in the top-right corner of the screen and select **Account**.
2. In the Profile settings, choose the desired unit system from the dropdown list:
   - **Auto** — automatically determines the unit system based on the browser locale or regional settings.
   - **Metric** — the most widely used system globally, based on meter (m), kilogram (kg), second (s), etc.
   - **Imperial** — commonly used in the USA, based on inches (in), feet (ft), miles (mi), pounds (lb), gallons (gal), etc.
   - **Hybrid** — a mixed system typically used in the UK. Blends metric and imperial units depending on context — miles (mi) for distance, Celsius (°C) for temperature, kilograms (kg) or stones (st) for body weight.
3. Click **Save**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/units/changing-unit-system-1.png', alt: 'Open Account settings', caption: 'Click the three-dot icon in the top-right corner and select Account.' },
  { src: '/src/assets/images/user-guide/units/changing-unit-system-2.png', alt: 'Select unit system', caption: 'In the Profile settings, choose the desired unit system from the Unit system dropdown and click "Save".' },
]} />

## Example

This walkthrough configures unit conversion on a Temperature & Humidity dashboard. The dashboard displays temperature in degrees Celsius (°C) and humidity in percentage (%). Unit conversion is applied to the temperature telemetry key, the Y-axis of the temperature chart, and the temperature threshold. Humidity remains unchanged since its unit (%) does not depend on the unit system.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-1.png', alt: 'Temperature and humidity dashboard', caption: 'Temperature & Humidity dashboard used in this example.' },
]} />

<h3>Step 1: Edit the Chart widget</h3>

Switch to edit mode for the "Temperature and Humidity history" chart widget. Configure unit conversion for the "temperature" telemetry key, the Y-axis, and the threshold.

<Steps>
1. Click on the **Units** row in the "temperature" key field. The source unit is already set to Celsius (°C).
2. Toggle on **Enable unit conversion** and specify the target units for each unit system (e.g., °F for Imperial, °C for Metric and Hybrid). Click **Apply**.
3. Repeat the same steps for the Y-axis — enable unit conversion and specify target units. Click **Apply**.
4. For the threshold, also enable unit conversion and specify target units. Apply all changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-2.png', alt: 'Open chart widget unit settings', caption: 'Switch to edit mode for the chart widget. Click on the Units row in the temperature key field.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-3.png', alt: 'Enable unit conversion for telemetry key', caption: 'Enable unit conversion and specify target units for each unit system. Click Apply.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-4.png', alt: 'Configure Y-axis unit conversion', caption: 'Enable unit conversion for the Y-axis and specify target units. Click Apply.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-5.png', alt: 'Configure threshold unit conversion', caption: 'Enable unit conversion for the threshold and specify target units. Apply all changes.' },
]} />

<h3>Step 2: Edit the Temperature Card widget</h3>

Switch to edit mode for the "Temperature" card widget. In the **Unit settings** dialog, enable unit conversion and specify the appropriate units for each unit system. Click **Apply** and save the dashboard.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-6.png', alt: 'Open temperature card widget', caption: 'Switch to edit mode for the Temperature card widget.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-7.png', alt: 'Configure card widget unit conversion', caption: 'Enable unit conversion and specify units for each unit system. Apply changes and save the dashboard.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-8.png', alt: 'Dashboard with Metric unit system', caption: 'Metric unit system: temperature is displayed in Celsius (°C). The threshold (27 °C) is shown in its original source units.' },
]} />

<h3>Step 3: Change the unit system</h3>

To see the unit conversion in action, change your unit system to Imperial. Go to your **Account** settings, find the **Unit system** dropdown, and select **Imperial**.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-9.png', alt: 'Open Account settings', caption: 'Click the three-dot icon in the top-right corner and select Account.' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-10.png', alt: 'Select Imperial unit system', caption: 'Select Imperial from the Unit system dropdown and click "Save".' },
  { src: '/src/assets/images/user-guide/units/unit-conversion-example-11.png', alt: 'Dashboard in Imperial system', caption: 'Temperature values, Y-axis labels, and threshold are now converted from Celsius to Fahrenheit.' },
]} />
