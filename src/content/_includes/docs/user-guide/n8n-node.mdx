import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import ImageGallery from '~/components/ImageGallery.astro';

[n8n](https://n8n.io/) is a workflow automation platform that lets you connect ThingsBoard to hundreds of other services.

The [ThingsBoard n8n Node](https://github.com/thingsboard/thingsboard-n8n-node) gives you direct access to your IoT entities, telemetry, and alarms from n8n workflows:

- **Manage IoT devices, assets, and customers** directly from n8n workflows.
- **Access and manipulate telemetry data** in real-time with attribute and time-series operations.
- **Monitor alarms** and create automated responses based on alarm severity and type.
- **Navigate entity relationships** to understand your IoT infrastructure topology.
- **Build AI-powered IoT automation** using n8n AI Agents with natural language commands.

## Quick start

Here's what you can build in 5 minutes — a real-world data pipeline that exports IoT telemetry to cloud storage.

**Scenario**: Every day at midnight, automatically export the last 24 hours of device telemetry to AWS S3 as JSON files for data warehousing, analytics, or compliance archival.

```plaintext
Schedule (Daily at 00:00 UTC)
    ↓
Code node (calculate last 24h time range in MS)
    ↓
ThingsBoard: Get device by name
    ↓
ThingsBoard: Get timeseries data
    ↓
Convert to File (JSON format)
    ↓
Upload to AWS S3
```

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/quick-example.png', alt: 'Complete n8n workflow', caption: 'The complete workflow: Schedule trigger → Code → Get device → Get timeseries → Convert to file → AWS S3 upload.' },
  { src: '/src/assets/images/user-guide/n8n-node/quick-example-result.png', alt: 'Workflow execution result', caption: 'Successful execution: telemetry data exported to S3 as a JSON file.' },
]} />

This is a production-ready pattern for IoT data archival, analytics pipelines, and compliance logging. See [Example 2](#example-2-daily-telemetry-export-to-aws-s3) for the full step-by-step walkthrough.

## When to use n8n with ThingsBoard

ThingsBoard's Rule Engine handles IoT-specific automation. When you need to connect to external systems that ThingsBoard doesn't integrate with natively, n8n fills the gap.

| Pattern | Examples |
|---|---|
| **Cloud archival** | AWS S3, Google Cloud Storage, Azure Blob |
| **Database export** | PostgreSQL, Snowflake, ClickHouse |
| **Ticketing** | Jira, ServiceNow |
| **CRM/ERP sync** | Salesforce, HubSpot |
| **AI chat** | Non-technical users querying data via AI agents |

## Requirements

- **ThingsBoard instance** — self-hosted or [ThingsBoard Cloud](https://thingsboard.cloud/signup).
- **Authentication credentials** — API key or username and password (deprecated) with appropriate permissions. See <DocLink product={props.product} path="user-guide/security/api-keys">API Keys</DocLink> for details on generating API keys.
- **n8n instance** — local installation, Docker-based, or [n8n Cloud](https://n8n.io/) (the ThingsBoard node is verified and available).

## Installation

Make sure n8n is already running. See the [official n8n docs](https://docs.n8n.io/) if you need to install it first.

### Install the ThingsBoard node

<Aside type="note">
  Community nodes require `N8N_COMMUNITY_PACKAGES_ENABLED=true` in your n8n environment. Most installations have this enabled by default. If you cannot access the Community Nodes section in Settings, set this variable before starting n8n.
</Aside>

<Steps>
1. Open n8n in your browser.
2. Click the **three dots** (bottom-left) → **Settings** → **Community Nodes**.
3. Click **Install a community node**.
4. Enter: `@thingsboard/n8n-nodes-thingsboard` and click **Install**.
5. Refresh your browser.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/n8n-gui-installation-1.png', alt: 'n8n Settings menu', caption: 'Open n8n, click the three dots (bottom-left), and select Settings.' },
  { src: '/src/assets/images/user-guide/n8n-node/n8n-gui-installation-2.png', alt: 'Community Nodes tab', caption: 'Go to Community Nodes and click Install a community node.' },
  { src: '/src/assets/images/user-guide/n8n-node/n8n-gui-installation-3.png', alt: 'Install package dialog', caption: 'Enter @thingsboard/n8n-nodes-thingsboard and click Install.' },
  { src: '/src/assets/images/user-guide/n8n-node/n8n-gui-installation-4.png', alt: 'Installation complete', caption: 'Wait for installation to complete, then refresh the browser.' },
]} />

### Configure AI agent support (optional)

Skip this if you are only using the node in regular workflows (not with AI agents).

To let AI agents use ThingsBoard as a tool, add this environment variable before starting n8n:

```text
N8N_COMMUNITY_PACKAGES_ALLOW_TOOL_USAGE=true
```

- **Docker**: Add to your `docker-compose.yml` environment section.
- **npm/local**: `N8N_COMMUNITY_PACKAGES_ALLOW_TOOL_USAGE=true n8n start`

### Configure credentials

Connect n8n to your ThingsBoard instance. There are two authentication options:

#### Option A: API Key

<Steps>
1. Click **+** (top-left) → **Credentials**.
2. Search for **ThingsBoard API Key** → **Continue**.
3. Fill in the required fields:
   - **Base URL**: Your instance URL (e.g., `https://thingsboard.cloud`).
   - **Connect using**: API Key.
   - **API Key**: Generated from your ThingsBoard instance. See <DocLink product={props.product} path="user-guide/security/api-keys">API Keys</DocLink> for details.
4. Click **Save**.
</Steps>

#### Option B: Username/Password (Deprecated)

<Steps>
1. Click **+** (top-left) → **Credentials**.
2. Search for **ThingsBoard Username/Password** → **Continue**.
3. Fill in the required fields:
   - **Base URL**: Your instance URL (e.g., `https://thingsboard.cloud`).
   - **Connect using**: Username/Password.
   - **Username**: Your email.
   - **Password**: Your password.
4. Click **Save**.
</Steps>

The credentials are encrypted and stored securely by n8n. You can reuse the same credentials across multiple ThingsBoard nodes in different workflows.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/credentials-configuration-1.png', alt: 'Add credentials', caption: 'Click + and select Credentials from the dropdown.' },
  { src: '/src/assets/images/user-guide/n8n-node/credentials-configuration-2.png', alt: 'Search ThingsBoard API', caption: 'Search for ThingsBoard API to connect to and click Continue.' },
  { src: '/src/assets/images/user-guide/n8n-node/credentials-configuration-3.png', alt: 'Fill credentials', caption: 'Fill in the Base URL — your ThingsBoard instance URL (without a trailing slash), and the API Key — generated from your ThingsBoard instance. Click Save to store the credentials.' },
  { src: '/src/assets/images/user-guide/n8n-node/credentials-configuration-4.png', alt: 'Credentials saved', caption: 'The credentials are saved and the connection is tested successfully. You can reuse the same credentials across multiple ThingsBoard nodes in different workflows.' },
]} />

### Verify installation

<Steps>
1. Open n8n in your browser.
2. Click **Create Workflow** (top-right).
3. Click the **+** button on the workflow canvas.
4. Type "ThingsBoard" in the search field.
5. Verify the ThingsBoard node appears with available actions.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/installation-verification-1.png', alt: 'Create workflow', caption: 'Click Create Workflow in the top-right corner.' },
  { src: '/src/assets/images/user-guide/n8n-node/installation-verification-2.png', alt: 'Add node button', caption: 'Click the + button on the workflow canvas.' },
  { src: '/src/assets/images/user-guide/n8n-node/installation-verification-3.png', alt: 'Search ThingsBoard', caption: 'Type ThingsBoard in the search field — the node should appear.' },
  { src: '/src/assets/images/user-guide/n8n-node/installation-verification-4.png', alt: 'Available actions', caption: 'A list of available ThingsBoard actions is displayed.' },
]} />

<Aside type="note">
  The number of operations may differ between Community Edition and Professional Edition.
</Aside>

If you don't see the node: verify `N8N_COMMUNITY_PACKAGES_ENABLED=true` is set, check that npm installation completed without errors, restart n8n, and check logs.

## How to use the node

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/thingsboard-n8n-node.png', alt: 'ThingsBoard n8n node', caption: 'ThingsBoard Node (regular workflow) and ThingsBoard Tool (AI Agent).' },
]} />

### Regular workflow node

Drag the ThingsBoard node onto your canvas and configure the operation (get devices, save telemetry, create alarms, etc.).

Best for: scheduled tasks (daily reports), webhooks (external triggers), multi-step workflows (get device → fetch telemetry → send to Slack).

### AI agent tool

Give an AI agent access to ThingsBoard operations. Users ask questions in plain English instead of building workflows.

Best for: non-technical users querying entities/data, support teams troubleshooting.

Requires [AI agent configuration](#configure-ai-agent-support-optional).

## Common workflow patterns

| Pattern | Flow |
|---|---|
| **Archive telemetry** | Schedule (daily) → Code (calc time range) → Get timeseries → Convert to file → S3/GCS upload |
| **Create tickets from alarms** | Webhook (alarm) → Check severity → Create Jira/ServiceNow ticket |
| **Customer onboarding** | CRM webhook (new customer) → Create devices and dashboard → Assign to customer |
| **AI-powered queries** | Chat → AI Agent → ThingsBoard operations → Natural language response |

## Usage examples

### Example 1: AI agent

**Scenario**: Ask questions in plain text to check entities, query data, or update configurations.

<Steps>
1. Create a new workflow with an **AI Agent** node. Choose your AI model (Gemini, Anthropic, ChatGPT, etc.) and configure memory settings.
2. Select **ThingsBoard** from the Tools list. Add **Get devices** and **Get timeseries** operations.
3. For tools with required fields, the AI model determines parameter values from conversation context automatically.
4. Open the chat interface and write your natural language query.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-1-1-0.png', alt: 'AI Agent workflow', caption: 'Create a workflow with AI Agent node and choose your AI model.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-1-1-1.png', alt: 'ThingsBoard tools', caption: 'Select ThingsBoard tools: Get devices, Get timeseries.' },
  { src: '/src/assets/images/user-guide/n8n-node/ai-agent-define-parameter.png', alt: 'AI auto-populates parameters', caption: 'The AI model intelligently determines parameter values from context.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-1-2.png', alt: 'Chat interface', caption: 'Open the chat and write your query in natural language.' },
]} />

**How it works**:

1. User asks: *"Which freezers in the warehouse are showing temperatures above -10°C?"*
2. AI Agent automatically calls `Get devices` to find freezer devices, then `Get timeseries` for temperature readings, filters results, and responds in plain language.
3. No code or API knowledge needed.

**Example queries:**
- *"Show me all devices that haven't sent data in the last 24 hours"*
- *"What's the current temperature of Freezer 3?"*
- *"Which sensors are in Building A?"*
- *"Get me the last week of humidity data for all warehouse sensors"*

---

### Example 2: Daily telemetry export to AWS S3

**Scenario**: Every night, export device telemetry to S3 for data warehousing, enriched with metadata and ready for analytics tools.

**Why use n8n instead of ThingsBoard's native export:**
- **Multi-destination** — same data to S3 + Snowflake + email report in one workflow.
- **Data transformation** — enrich telemetry with business context (customer names, locations from CRM).
- **Custom formats** — convert to Parquet, Avro, or CSV with specific schemas.
- **Integration chains** — after S3 upload, trigger AWS Lambda, update tracking database, send Slack notification.

#### Building the workflow

**Step 1: Add Schedule Trigger**

Click **Add first step** → **Schedule Trigger**. Set trigger to run every day at 00:00 (midnight UTC).

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-2-1.png', alt: 'Create workflow with schedule', caption: 'Create a new workflow and select Schedule Trigger.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-2-2.png', alt: 'Schedule configuration', caption: 'Configure to run every day at 00:00 UTC.' },
]} />

**Step 2: Add Code node for time range**

ThingsBoard's API requires timestamps in milliseconds. Add a Code node:

```javascript
const DAY_MS = 24 * 60 * 60 * 1000;
const endTsMs = new Date($input.first().json.timestamp).getTime();
const startTsMs = endTsMs - DAY_MS;
return [{ startTsMs, endTsMs }];
```

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-2-3.png', alt: 'Add Code node', caption: 'Add a Code node after the Schedule Trigger.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-2-4.png', alt: 'Code node configured', caption: 'Paste the time calculation code and execute to test.' },
]} />

**Step 3: Get device by name**

Click **+** → **ThingsBoard** → **Get a device by name**. Enter your device name (e.g., "Temperature Sensor 1").

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-2-5.png', alt: 'Add ThingsBoard node', caption: 'Add ThingsBoard node and select Get a device by name.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-2-6.png', alt: 'Device retrieved', caption: 'Enter the device name and execute to retrieve the device object.' },
]} />

**Step 4: Get timeseries data**

Click **+** → **ThingsBoard** → **Get timeseries**. Drag values from the INPUT panel:
- **Entity ID** and **Entity Type**: From "Get a device by name" output.
- **Start/End timestamps**: From the Code node output.
- **Keys**: Telemetry keys you want (e.g., "temperature,humidity").

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-2-7.png', alt: 'Add Get timeseries node', caption: 'Add ThingsBoard Get timeseries node.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-2-8.png', alt: 'Timeseries configured', caption: 'Configure with device ID, entity type, and time range from previous nodes.' },
]} />

**Step 5: Upload to AWS S3**

Click **+** → **AWS S3**. Configure: Operation = Upload, Bucket Name, File Name with datestamp expression (e.g., `telemetry-{{ $now.format('yyyy-MM-dd') }}.json`), and AWS credentials.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-2-10.png', alt: 'AWS S3 node', caption: 'Add AWS S3 node with upload configuration.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-2-11.png', alt: 'Upload successful', caption: 'Telemetry data successfully uploaded to S3.' },
]} />

**Result**: A complete automated pipeline that runs every night, fetches 24 hours of telemetry, and uploads to S3 with predictable naming (`telemetry-2024-12-23.json`).

---

### Example 3: Webhook-triggered device query

**Scenario**: A customer portal has a "Get Device Status" button. When clicked, it fetches real-time telemetry for any device — no hardcoded IDs.

This pattern enables: customer portals (each user sees their own devices), chatbots ("check temperature in Room 405"), mobile apps (query any device by name).

#### Building the workflow

**Step 1: Set up the trigger**

Use a manual trigger to simulate a webhook. Click **Add first step** → **Trigger manually**, then configure with test JSON:

```json
{
  "deviceName": "Refrigerator"
}
```

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-3-1-1.png', alt: 'Add manual trigger', caption: 'Click Add first step and select Trigger manually.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-1-2.png', alt: 'Open trigger config', caption: 'Click on the trigger node to open configuration.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-1-3.png', alt: 'Edit trigger input', caption: 'Click the pencil icon (top-right) to edit input.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-1-4.png', alt: 'Paste JSON', caption: 'Paste the JSON and save changes.' },
]} />

**Step 2: Find the device**

Click **+** → **ThingsBoard** → **Get device by name**. Drag `deviceName` from the INPUT panel into the Name field — this creates a dynamic link.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-3-2-1.png', alt: 'Add node', caption: 'Click + next to the trigger node.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-2-2.png', alt: 'Select ThingsBoard', caption: 'Find and select ThingsBoard.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-2-3.png', alt: 'Get device by name', caption: 'Choose Get a device by name.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-2-4.png', alt: 'Drag deviceName', caption: 'Drag deviceName from the INPUT panel into the Name field.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-2-5.png', alt: 'Execute step', caption: 'Click Execute step to test.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-2-6.png', alt: 'Device object returned', caption: 'Returns the full device object including ID, type, and metadata.' },
]} />

**Step 3: Discover telemetry keys**

Click **+** → **ThingsBoard** → **Get timeseries keys**. Drag Entity ID and Entity Type from the INPUT panel.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-3-3-1.png', alt: 'Add Get timeseries keys', caption: 'Add ThingsBoard Get timeseries keys node.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-3-2.png', alt: 'Keys discovered', caption: 'Entity ID and Entity Type are set dynamically. Execute to discover available keys.' },
]} />

**Step 4: Fetch telemetry data**

Click **+** → **ThingsBoard** → **Get timeseries**. Configure:
- Entity ID and Entity Type from Step 2.
- Keys from Step 3 — join as comma-separated: `{{ $json.timeseriesKeys.join(',') }}`.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-3-4-1.png', alt: 'Add Get timeseries', caption: 'Add ThingsBoard Get timeseries action.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-4-2.png', alt: 'Telemetry fetched', caption: 'Configure with device ID, entity type, and keys. Execute to fetch data.' },
]} />

**Result**: A completely reusable workflow. Change the input from "Refrigerator" to "Freezer 3" and it works automatically — no hardcoded IDs, no changes needed.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/n8n-node/example-3-final-1.png', alt: 'Complete workflow step 1', caption: 'Accepts a device name and finds the corresponding device.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-final-2.png', alt: 'Complete workflow step 2', caption: 'Discovers available telemetry keys.' },
  { src: '/src/assets/images/user-guide/n8n-node/example-3-final-3.png', alt: 'Complete workflow step 3', caption: 'Retrieves telemetry values for the specified time window.' },
]} />

**Next steps**: Replace the manual trigger with a Webhook to connect your customer portal, add Slack or Email output, use a Schedule trigger for monitoring, or connect to an AI Agent for chat-based queries.

## Links

- **npm package**: [@thingsboard/n8n-nodes-thingsboard](https://www.npmjs.com/package/@thingsboard/n8n-nodes-thingsboard)
- **GitHub**: [thingsboard/thingsboard-n8n-node](https://github.com/thingsboard/thingsboard-n8n-node)
- **n8n docs**: [docs.n8n.io](https://docs.n8n.io/)
- **ThingsBoard REST API**: <DocLink product={props.product} path="TODO">REST API Reference</DocLink>
