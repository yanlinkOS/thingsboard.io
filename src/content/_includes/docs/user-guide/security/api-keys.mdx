import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import ImageGallery from '~/components/ImageGallery.astro';
import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';

API keys provide a simpler alternative to password-based authentication for the ThingsBoard REST API.
Unlike JWT tokens, which require a login request and expire periodically, API keys are long-lived credentials that remain valid until they expire or are manually revoked.

| Feature | Description |
|---|---|
| **No login required** | API keys work immediately without exchanging a username and password. |
| **Long-lived** | They stay valid until the expiration date you set. |
| **Permission inheritance** | The key inherits the same permissions as the user it was created for. |
| **Easy management** | Enable, disable, or delete a key at any time. |
| **Simple integration** | Ideal for third-party apps, scripts, and MCP servers where you want to avoid complex authentication code. |

## Creating API Keys

API keys can be created for **your own account** or for **other platform users**, depending on your permissions.

### Creating an API Key for Your Account

<Steps>
1. In the top-right corner, click the **three-dot** menu and select **Account**.
2. Navigate to the **Security** tab.
3. In the **API keys** section, click the **Manage** button.
4. Click the **+ Generate** button.
5. Enter a **description** for the API key (e.g., *Production server*, *Testing environment*).
6. Select the expiration period.
7. Click **Generate**.
</Steps>

<Aside type="caution">
  Copy and save the generated API key immediately — it will not be displayed again.
</Aside>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-our-account-1.png', alt: 'Open account menu', caption: 'Click the three-dot menu and select Account.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-our-account-2.png', alt: 'Security tab', caption: 'Navigate to the Security tab.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-our-account-3.png', alt: 'Manage API keys', caption: 'Click Manage in the API keys section.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-our-account-4.png', alt: 'Generate API key', caption: 'Click + Generate, enter a description, and set the expiration.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-our-account-5.png', alt: 'API key generated', caption: 'Copy the generated API key — it will not be shown again.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-our-account-6.png', alt: 'API key in list', caption: 'The new API key appears in the list.' },
]} />

### Creating an API Key for Another User

A system administrator can create API keys for users of any tenant; a tenant administrator can create them for their customer users.

<Steps>
1. Navigate to the **Customers** or **Users** section.
2. Click the desired user to open their **details**.
3. Go to the **API keys** tab.
4. Click the **+ Generate** button.
5. Enter a **description** for the API key.
6. Select the expiration period.
7. Click **Generate**.
</Steps>

<Aside type="note">
  The API key inherits all permissions of the user for whom it was created.
</Aside>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-another-user-1.png', alt: 'Open user details', caption: 'Navigate to the Users section and click on a user.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-another-user-2.png', alt: 'API keys tab', caption: 'Go to the API keys tab.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-another-user-3.png', alt: 'Generate key for user', caption: 'Click + Generate, enter a description, and set the expiration.' },
  { src: '/src/assets/images/user-guide/security/api-keys/creating-api-key-for-another-user-4.png', alt: 'Key generated for user', caption: 'The API key is created for the selected user.' },
]} />

## Using API Keys

### In API Requests

Include the API key in the `X-Authorization` header with the `ApiKey` prefix:

```text
X-Authorization: ApiKey $YOUR_API_KEY_VALUE
```

Example:

```bash
curl -X GET --header 'Accept: application/json' \
            --header 'X-Authorization: ApiKey $YOUR_API_KEY_VALUE' \
            'https://$THINGSBOARD_HOST_NAME/api/auth/user'
```

Replace `$YOUR_API_KEY_VALUE` with your API key and `$THINGSBOARD_HOST_NAME` with your ThingsBoard hostname.

<Aside type="caution">
  Always use HTTPS in production environments. HTTP transmits the API key unencrypted, making it vulnerable to interception.
</Aside>

### In Swagger UI

When using <a href="https://swagger.io/" target="_blank" rel="noopener noreferrer">Swagger UI</a>, you can authenticate with an API key:

<Steps>
1. Open **Swagger UI**.
2. Click the **Authorize** button.
3. In the **API key form (apiKey)** section, enter your API key value with the `ApiKey` prefix:
   ```text
   ApiKey YOUR_API_KEY_VALUE
   ```
4. Click **Authorize**.
</Steps>

<Aside type="note">
  If a request includes both a JWT token and an API key, the JWT takes precedence.
</Aside>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/security/api-keys/authentication-in-swagger-ui-1.png', alt: 'Swagger UI authorize button', caption: 'Click the Authorize button in Swagger UI.' },
  { src: '/src/assets/images/user-guide/security/api-keys/swagger-ui-2.png', alt: 'Enter API key in Swagger UI', caption: 'Enter your API key with the ApiKey prefix and click Authorize.' },
]} />

## Managing API Keys

Each API key in the table displays:

| Column | Description |
|---|---|
| **Created time** | When the API key was generated. |
| **Description** | The description assigned to the key. |
| **Status** | Whether the key is active or disabled. |
| **Expiration time** | The date and time when the key will expire. |

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/security/api-keys/managing-api-keys-1.png', alt: 'API keys table', caption: 'API keys table showing status, description, and expiration.' },
]} />

Available actions for each API key:

- **Enable / disable** — use the toggle to temporarily disable or re-enable a key.
- **Delete** — click the delete icon to permanently remove a key. This action cannot be undone.
- **Edit description** — click the edit icon to update the key's description.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/security/api-keys/managing-api-keys-2.png', alt: 'API key actions', caption: 'Enable, disable, edit, or delete API keys.' },
]} />
