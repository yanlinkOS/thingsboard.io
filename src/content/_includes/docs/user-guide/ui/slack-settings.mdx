import DocLink from '@components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import {Steps, Aside} from '@astrojs/starlight/components';
import {Products} from '~/models/site.models';

Integrating Slack with ThingsBoard lets users receive <DocLink product={props.product === Products.EDGE_PE ? Products.PE : props.product === Products.EDGE ? Products.CE : props.product} path="user-guide/notifications" target="_blank">notifications</DocLink> in Slack channels or direct messages based on notification rules — for example, device status changes or detected issues.

<Aside type="note">
  You can send Slack notifications to users from **your Slack workspace only**.
</Aside>

## Create a Slack app and get the API token

You can follow the <a href="https://docs.slack.dev/app-management/quickstart-app-settings/" target="_blank" rel="noopener noreferrer">official Slack guide</a> or complete the steps below.

<Steps>
  1. Go to <a href="https://api.slack.com/apps" target="_blank" rel="noopener noreferrer">api.slack.com/apps</a> and click **Create an App**.
  2. In the **Create an app** dialog, select **From a manifest**.
  3. Select your workspace from the dropdown and click **Next**.
  4. Review the manifest and click **Next**.
  5. Review the app summary and click **Create**.
  6. On the app configuration page, navigate to **OAuth & Permissions** in the left sidebar.
  7. Scroll to the **Scopes** section. Under **Bot Token Scopes**, click **Add an OAuth Scope** and add all scopes listed in the table below.
  8. Scroll to the top of the **OAuth & Permissions** page and click **Install to my workspace**.
  9. Review the requested permissions and click **Allow**.
  10. Copy the **Bot User OAuth Token** under **OAuth Tokens**. This is the Slack API token you will use in ThingsBoard.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-1.png',
    alt: 'Slack API quickstart guide',
    caption: 'Slack API quickstart guide — official reference for creating a Slack app and obtaining a Bot User OAuth Token.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-2.png',
    alt: 'Create an App dialog — select From a manifest',
    caption: 'Click <strong>Create an App</strong>, then select <strong>From a manifest</strong> in the dialog.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-3.png',
    alt: 'Pick a workspace — Step 1 of 3',
    caption: 'Select your workspace from the dropdown and click <strong>Next</strong>.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-4.png',
    alt: 'Create app from manifest — Step 2 of 3',
    caption: 'Review the manifest JSON and click <strong>Next</strong>.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-5.png',
    alt: 'Review summary and create your app — Step 3 of 3',
    caption: 'Review the app summary and click <strong>Create</strong>.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-6.png',
    alt: 'App configuration — OAuth & Permissions in the left sidebar',
    caption: 'Navigate to <strong>OAuth &amp; Permissions</strong> in the left sidebar.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-7.png',
    alt: 'Bot Token Scopes section — click Add an OAuth Scope',
    caption: 'Under <strong>Bot Token Scopes</strong>, click <strong>Add an OAuth Scope</strong> and add each required scope.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-8.png',
    alt: 'All required Bot Token Scopes added',
    caption: 'All required Bot Token Scopes added.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-9.png',
    alt: 'Install to my workspace button',
    caption: 'Scroll to the top of the page and click <strong>Install to my workspace</strong>.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-10.png',
    alt: 'Authorization page — Allow the app to access Slack',
    caption: 'Review the requested permissions and click <strong>Allow</strong>.',
  },
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-11.png',
    alt: 'Bot User OAuth Token ready to copy',
    caption: 'Copy the <strong>Bot User OAuth Token</strong> — this is your Slack API token for ThingsBoard.',
  },
]}/>

### Required OAuth scopes

| Scope | Description |
|-------|-------------|
| `channels:join` | Join public channels in a workspace |
| `channels:read` | View basic information about public channels in a workspace |
| `chat:write` | Send messages as the app |
| `chat:write.customize` | Send messages as the app with a customized username and avatar |
| `groups:read` | View basic information about private channels the app has been added to |
| `im:read` | View basic information about direct messages the app has been added to |
| `mpim:read` | View basic information about group direct messages the app has been added to |
| `mpim:write.topic` | Set the description in group direct messages |
| `usergroups:read` | View user groups in a workspace |
| `users:read` | View people in a workspace |

## Configure Slack settings in ThingsBoard

{props.product !== Products.CE ? (
  <p>Both system administrators and tenant administrators can configure Slack settings. Tenant-level settings override the
  system defaults.</p>
) : (
  <p>A system administrator configures Slack settings for the entire platform.</p>
)}

<Steps>
  1. Navigate to **Settings** and open the **Notifications** tab.
  2. Paste the **Bot User OAuth Token** into the **Slack api token** field and click **Save**.
</Steps>

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/user-guide/ui/slack/slack-settings-in-thingsboard-1.png',
    alt: 'ThingsBoard Notifications settings — Slack API token field',
    caption: 'Paste the <strong>Bot User OAuth Token</strong> into the <strong>Slack API token</strong> field and click <strong>Save</strong>.',
  },
]}/>

Once configured, ThingsBoard will deliver Slack notifications according to your <DocLink product={props.product === Products.EDGE_PE ? Products.PE : props.product === Products.EDGE ? Products.CE : props.product} path="user-guide/notifications" target="_blank">notification rules</DocLink>.

## Troubleshooting

### Token expired or revoked

If the Slack API token expires or is revoked, notification deliveries will fail. You can see delivery failures in the **Sent** tab of the <DocLink product={props.product === Products.EDGE_PE ? Products.PE : props.product === Products.EDGE ? Products.CE : props.product} path="user-guide/notifications" target="_blank">Notification center</DocLink>. To resolve, generate a new token from the Slack app's **OAuth & Permissions** page and update the settings in ThingsBoard.

### Invalid token

**Error:** `Slack API error: {"ok":false,"warning":null,"error":"invalid_auth","needed":null,...}`

ThingsBoard cannot authenticate to the Slack API. The problem is with the token, not the channel or bot configuration.

Common causes:

- Token copied with extra spaces or characters
- Token expired or revoked
- User token (`xoxp-`) used instead of a Bot token (`xoxb-`)

<Steps>
1. Go to <a href="https://api.slack.com/apps" target="_blank" rel="noopener noreferrer">api.slack.com/apps</a>, open your app, and navigate to **OAuth & Permissions**.
2. Copy the **Bot User OAuth Token**. It starts with `xoxb-`.
3. Paste the token into ThingsBoard with no leading or trailing spaces and no quotes.
4. If you recently added or changed scopes, click **Reinstall to Workspace** first to generate a fresh token.
</Steps>

### App not added to the channel

**Error:** `Slack API error: app needs to be added to the channel`

ThingsBoard is trying to send a message to a Slack channel, but the bot does not have access to that channel.

To add the bot to the channel:

<Steps>
1. In Slack, open the channel you want the bot to post to.
2. Type `/invite @<bot_name>` in the message box and press **Enter**, or go to **Channel > Settings > Integrations > Add apps** and select your app.
</Steps>

Also verify:

- The channel ID or name configured in ThingsBoard is correct.
- The bot has the required scopes (`chat:write`, `channels:join`, etc.) — see [Required OAuth scopes](#required-oauth-scopes).
- The app is installed in the workspace.

## Example

The <DocLink product={props.product === Products.EDGE_PE ? Products.PE : props.product === Products.EDGE ? Products.CE : props.product} path="recipes/send-slack-alarm" bold={false} target="_blank">Send Slack Message on Alarm</DocLink> recipe walks through the full setup: configuring a Slack recipient, creating an alarm notification template, and setting up a rule that sends a message when an alarm is created.
