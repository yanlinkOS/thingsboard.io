import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import ImageGallery from '~/components/ImageGallery.astro';

The mail server settings configure the outgoing SMTP server ThingsBoard uses to send emails — including <DocLink product={props.product === Products.EDGE_PE ? Products.PE : props.product === Products.EDGE ? Products.CE : props.product} path="user-guide/notifications">notifications</DocLink>, user activation emails, password recovery messages, and scheduled reports.

{props.product === Products.CE ? (
  <p>The system administrator defines global mail server settings that apply to all tenants.</p>
) : (
  <>
    <p>Mail server settings can be configured at two levels:</p>
    <ul>
      <li><strong>System administrator</strong> — defines global SMTP settings that apply to all tenants unless overridden.</li>
      <li><strong>Tenant administrator</strong> — can override global settings with a custom SMTP configuration (e.g., a company-specific SMTP server).</li>
    </ul>
    <p>If a tenant does not configure their own mail server, the system administrator's settings are used.</p>
  </>
)}

<Aside type="tip">
  Tenant administrators can also send emails from the <DocLink product={props.product} path="reference/rule-engine/nodes/external/send-email" bold={false} target="_blank">Send Email</DocLink> rule node, which can use the system SMTP settings or its own custom SMTP configuration.
</Aside>

## Configuration

<Steps>
1. Navigate to **Settings** and open the **Mail server** tab.{props.product !== Products.CE && <> If you are a tenant administrator, uncheck <strong>Use System Mail Server Settings</strong> to configure your own.</>}
2. Fill in the **Mail From** field (the sender address that appears in outgoing emails).
3. Select an **SMTP provider** (SendGrid, Google, Office 365, or Custom).
4. Configure connection settings:
   - **SMTP protocol** (SMTP or SMTPS)
   - **SMTP host** and **port**
   - **Connection timeout**
   - Optionally enable **TLS** and **Proxy**
5. Configure **Authentication** — either **Basic** (username + password) or **OAuth 2.0** (available for Google and Office 365).
6. Click **Send test email** to verify the configuration.
7. Click **Save**.
</Steps>

## Provider examples

Since ThingsBoard 3.5.2, built-in templates are available for SendGrid, Gmail, and Office 365. These auto-fill connection settings (host, port, TLS). For other providers, select **Custom**.

### SendGrid

**1. Set up SendGrid**

<Steps>
1. Create a [SendGrid](https://sendgrid.com/) account. On the welcome page, click **Start**.
2. Go to **Email API** > **Integration Guide** and choose **SMTP Relay**.
3. Create an API key: enter a name and click **Generate**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/sendgrid-welcome.png', alt: 'SendGrid welcome page', caption: 'Click Start on the SendGrid welcome page' },
  { src: '/src/assets/images/user-guide/ui/mail/sendgrid-smtp-relay.png', alt: 'SendGrid SMTP Relay setup', caption: 'Choose SMTP Relay in the Integration Guide' },
  { src: '/src/assets/images/user-guide/ui/mail/sendgrid-token.png', alt: 'SendGrid API key generation', caption: 'Create and copy the API key' },
]} />

**2. Configure ThingsBoard**

In ThingsBoard, go to **Settings** > **Mail server**. Select SMTP provider **SendGrid**. Enter username `apikey` and paste the generated API key as the password. Click **Send test email** to verify, then **Save**.

### Gmail with basic authentication

**1. Generate a Google app password**

<Steps>
1. Go to your [Google Account Security](https://myaccount.google.com/security) page and select **2-Step Verification**.
2. At the bottom of the page, select **App passwords**.
3. Select **Other** app from the dropdown.
4. Enter an app name and click **Generate**.
5. Copy the generated app password.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/gmail-generate-an-app-password-1.png', alt: 'Google Security — 2-Step Verification', caption: 'Navigate to 2-Step Verification in your Google Account' },
  { src: '/src/assets/images/user-guide/ui/mail/gmail-generate-an-app-password-2.png', alt: 'App passwords option', caption: 'Select App passwords at the bottom of the page' },
  { src: '/src/assets/images/user-guide/ui/mail/gmail-generate-an-app-password-3.png', alt: 'Select Other app', caption: 'Select Other in the app dropdown' },
  { src: '/src/assets/images/user-guide/ui/mail/gmail-generate-an-app-password-4.png', alt: 'Enter app name', caption: 'Enter an app name and click Generate' },
  { src: '/src/assets/images/user-guide/ui/mail/gmail-generate-an-app-password-5.png', alt: 'Copy app password', caption: 'Copy the generated app password' },
]} />

**2. Configure ThingsBoard**

In ThingsBoard, select SMTP provider **Google**. Enter your Gmail address as the username, keep authentication type **Basic**, and enter the app password. Click **Send test email** to verify, then **Save**.

### Gmail with OAuth 2.0

**1. Configure ThingsBoard (initial setup)**

In ThingsBoard, select SMTP provider **Google**, enter your Gmail address as the username, and toggle authentication to **OAuth2**. Copy the **Redirect URI template** — you will need it when creating Google credentials.

**2. Create a Google Cloud project**

<Steps>
1. Go to the [Google Developers Console](https://console.developers.google.com/projectcreate). Enter a project name and click **Create**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/gmail-with-oauth2-1-pe.png', alt: 'Create Google Cloud project', caption: 'Create a new project in the Google Developers Console' },
]} />

**3. Create OAuth credentials**

<Steps>
1. Select the project and navigate to **APIs & Services**.
2. Go to the **Credentials** tab, click **Create Credentials**, and select **OAuth client ID**.
3. Select application type **Web application**, enter a name, and in **Authorized redirect URIs** paste the redirect URI from step 1. Click **Create**.
4. Copy the **Client ID** and **Client secret**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/gmail-with-oauth2-2-pe.png', alt: 'Navigate to APIs & Services', caption: 'Open APIs & Services for your project' },
  { src: '/src/assets/images/user-guide/ui/mail/gmail-with-oauth2-3-pe.png', alt: 'Create OAuth client ID', caption: 'Create Credentials > OAuth client ID' },
  { src: '/src/assets/images/user-guide/ui/mail/gmail-with-oauth2-4-pe.png', alt: 'Configure OAuth client', caption: 'Set application type, name, and redirect URI' },
  { src: '/src/assets/images/user-guide/ui/mail/gmail-with-oauth2-5-pe.png', alt: 'Copy Client ID and secret', caption: 'Save the Client ID and Client secret' },
]} />

**4. Complete ThingsBoard setup**

Back in ThingsBoard, paste the **Client ID** and **Client secret**, then click **Save**. Click **Generate access token** — your browser will redirect to Google for authorization. After granting access, the tokens are saved automatically. Click **Send test email** to verify.

### Office 365 with OAuth 2.0

<Aside type="note">
  OAuth 2.0 is supported for Microsoft 365 accounts only — not for personal Outlook accounts (@outlook.com, @hotmail.com).
</Aside>

**1. Copy the user principal name**

Sign in to the [Azure portal](https://portal.azure.com/), open **Microsoft Entra ID**, select a user, and copy the **User Principal Name**.

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/azure-create-application-1.png', alt: 'Azure portal — Microsoft Entra ID', caption: 'Open Microsoft Entra ID in the Azure portal' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-copy-user-principal-name-1-pe.png', alt: 'Copy user principal name', caption: 'Select a user and copy the User Principal Name' },
]} />

**2. Configure ThingsBoard (initial setup)**

In ThingsBoard, select SMTP provider **Office 365**. Enter the user principal name as both **Mail From** and **Username**. Toggle authentication to **OAuth2**. Copy the **Redirect URI template**.

**3. Register an application in Azure**

<Steps>
1. In the Azure portal, go to **App registrations** and click **New registration**.
2. Enter a name, paste the **Redirect URI** (Web type) from step 2, and click **Register**.
3. On the **Overview** page, note the **Client ID** and **Directory (tenant) ID**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/azure-create-application-2.png', alt: 'App registrations — New registration', caption: 'Click New registration under App registrations' },
  { src: '/src/assets/images/user-guide/ui/mail/azure-create-application-3.png', alt: 'Register application', caption: 'Enter app name, paste the redirect URI, and register' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-4-pe.png', alt: 'Application overview', caption: 'Copy the Client ID and Directory (tenant) ID from the Overview page' },
]} />

**4. Create a client secret**

<Steps>
1. Go to **Certificates & secrets** > **Client secrets** and click **New client secret**.
2. Add a description, select an expiration, and click **Add**.
3. Copy the **secret value**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-5-pe.png', alt: 'New client secret', caption: 'Click New client secret under Certificates & secrets' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-6-pe.png', alt: 'Configure client secret', caption: 'Add description and expiration, then click Add' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-7-pe.png', alt: 'Copy secret value', caption: 'Copy the client secret value' },
]} />

**5. Add SMTP API permissions**

<Steps>
1. Go to **API permissions** and click **Add a permission**.
2. Select **Microsoft Graph** > **Delegated permissions**.
3. Search for and check **SMTP.Send**, then click **Add permissions**.
4. Click **Grant admin consent**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-api-permissions-1-pe.png', alt: 'Add a permission', caption: 'Click Add a permission in API permissions' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-api-permissions-2-pe.png', alt: 'Select Microsoft Graph', caption: 'Select Microsoft Graph as the API' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-api-permissions-3-pe.png', alt: 'Delegated permissions', caption: 'Select Delegated permissions' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-api-permissions-4-pe.png', alt: 'Add SMTP.Send', caption: 'Search for SMTP.Send and add the permission' },
  { src: '/src/assets/images/user-guide/ui/mail/microsoft-azure-with-oauth2-api-permissions-5-pe.png', alt: 'Permissions updated', caption: 'Permissions updated successfully' },
]} />

**6. Complete ThingsBoard setup**

Back in ThingsBoard, paste the **Client ID**, **Client secret**, and **Directory (tenant) ID**, then click **Save**. Click **Generate access token** — your browser will redirect to Microsoft for authorization. After granting access, the tokens are saved automatically. Click **Send test email** to verify.

**Office 365 troubleshooting:**

If you see authentication errors (e.g., `5.7.3`):

1. Ensure SMTP is enabled for the user: go to the [Microsoft 365 admin center](https://portal.office.com/adminportal/home#/users), select the user > **Mail** > **Manage email apps** > enable **Authenticated SMTP** (if already enabled, disable and re-enable).
2. In the Azure portal, ensure **Security defaults** are disabled.

<Aside type="note">
  Each time you change provider settings, the refresh and access tokens are dropped. You will need to generate a new access token.
</Aside>

## Example

The <DocLink product={props.product === Products.EDGE_PE ? Products.PE : props.product === Products.EDGE ? Products.CE : props.product} path="recipes/send-email-alarm" bold={false} target="_blank">Send Email on Alarm</DocLink> recipe walks through the full setup: configuring notification recipients, creating an alarm notification template with email delivery, and setting up a rule that sends an email when an alarm is created.
