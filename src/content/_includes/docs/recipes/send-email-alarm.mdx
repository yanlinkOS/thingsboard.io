import DocLink from '@components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import { Steps, Aside, Code } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

export const tbHost = (product) =>
  product === Products.PAAS ? 'thingsboard.cloud' :
  product === Products.PAAS_EU ? 'eu.thingsboard.cloud' :
  '$THINGSBOARD_HOST_NAME';

Configure ThingsBoard to automatically send email notifications when an alarm is created. This guide extends the <DocLink product={props.product} path="recipes/create-clear-alarms">Create and Clear Alarms</DocLink> recipe by using the Notification Center to deliver alarm-based email notifications.

## Use case

Assume your refrigeration equipment has a temperature sensor that periodically publishes telemetry to ThingsBoard. The normal operating range is **2 °C – 5 °C**. When a reading goes outside this range:

- An alarm is created by the alarm rules.
- An email notification is sent to the responsible users.

## Prerequisites

Before proceeding:

- Complete the <DocLink product={props.product} path="recipes/create-clear-alarms">Create and Clear Alarms</DocLink> recipe — the alarm rules configured there are required for this guide.
- Add at least one <DocLink product={props.product} path="user-guide/customers/#create-customer">Customer user</DocLink> who will receive the notifications.
- Configure your <DocLink product={props.product} path="user-guide/ui/mail-settings">mail server settings</DocLink> in ThingsBoard.

Review the following documentation for background:

- <DocLink product={props.product} path="user-guide/notifications">Notification Center</DocLink> — how recipients, templates, and rules work together.
- <DocLink product={props.product} path="user-guide/alarm-rules">Alarm rules</DocLink> — how alarms are created and cleared.

## Configure Notification Center

The Notification Center delivers notifications to users. It consists of three components:

- **Recipients** — define who receives notifications.
- **Templates** — define the notification content and delivery method.
- **Rules** — define the conditions under which notifications are sent.

### Step 1. Configure notification recipients

Create a recipient group targeting the users responsible for the refrigeration equipment.

<Steps>
1. Navigate to **Notification Center &#8702; Recipients**.
2. Click **+ Add recipients** in the top-right corner.
3. Configure the group:
   - **Name**: `Refrigeration operators`
   - **Type**: Platform users
   - **User filter**: Customer users
   - **Customer**: Customer A
4. Click **Add**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/recipes/send-email-alarm/send-email-add-recipients-1.png', alt: 'Add notification recipients group dialog with Name set to Refrigeration operators, Type to Platform users, User filter to Customer users, and Customer A selected', caption: 'Recipients group: Platform users filtered to Customer A, named "Refrigeration operators"' },
]} />

### Step 2. Create notification template

Create a template that defines the email subject and message body using alarm variables.

<Steps>
1. Navigate to **Notification Center &#8702; Templates**.
2. Click **+ Add template** in the top-right corner.
3. **Setup** — configure the template:
   - **Name**: `Device temperature notification`
   - **Type**: Alarm
   - **Delivery methods**: toggle **Email** on
4. Click **Next**.
5. **Compose** — expand the **Email** section and set:
   - **Subject**: `${alarmType} on ${alarmOriginatorName} device`
   - **Message**: `A ${alarmType} has been detected on the ${alarmOriginatorName} device: ${details.data} °C.`
6. Click **Add**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/recipes/send-email-alarm/send-email-add-template-1.png', alt: 'Add notification template dialog — Setup step with Name set to Device temperature notification, Type to Alarm, and Email delivery method toggled on', caption: 'Setup: name "Device temperature notification", type "Alarm", Email delivery method enabled' },
  { src: '/src/assets/images/recipes/send-email-alarm/send-email-add-template-2.png', alt: 'Add notification template dialog — Compose step showing the Email section expanded with Subject and Message fields filled in using alarm template variables', caption: 'Compose: email subject and message body configured with alarm template variables' },
]} />

Template variables used:

- `${alarmType}` — the alarm type name (e.g. *High temperature*).
- `${alarmOriginatorName}` — the name of the device that triggered the alarm.
- `${details.data}` — the reported telemetry value, populated by the alarm rule.

### Step 3. Create notification rule

Create a rule that links the alarm trigger to the template and recipients.

<Steps>
1. Navigate to **Notification Center &#8702; Rules**.
2. Click **+ Add rule** in the top-right corner.
3. **Basic settings** — configure the rule:
   - **Rule name**: `Unacceptable device temperature`
   - **Trigger**: Alarm
   - **Template**: Device temperature notification
   - **Escalation chain &#8702; First recipient**: Refrigeration operators
4. Click **Next**.
5. **Alarm trigger settings** — configure the filter:
   - **Alarm type list**: Any type
   - **Alarm severity list**: Any severity
   - **Notify on**: Alarm created
6. Click **Add**.
</Steps>

<ImageGallery images={[
  { src: '/src/assets/images/recipes/send-email-alarm/send-email-add-rule-1.png', alt: 'Add rule dialog — Basic settings step with Rule name set to Unacceptable device temperature, Trigger to Alarm, Template to Device temperature notification, and Refrigeration operators added as First recipient', caption: 'Basic settings: rule name, trigger, template, and recipient configured' },
  { src: '/src/assets/images/recipes/send-email-alarm/send-email-add-rule-2.png', alt: 'Add rule dialog — Alarm trigger settings step with Alarm type list set to Any type, Alarm severity list to Any severity, and Notify on set to Alarm created', caption: 'Alarm trigger settings: any alarm type and severity, notify on Alarm created' },
]} />

<Aside type="note">
  Email notifications are sent only when an alarm is **created**, not when an existing alarm is updated.
</Aside>

## Verification

Publish a temperature value that triggers the alarm (above 5 °C):

<Aside type="note">
  {(props.product === Products.PAAS || props.product === Products.PAAS_EU) ? (
    <>Replace <code>$ACCESS_TOKEN</code> with the device access token.</>
  ) : (
    <>Replace <code>$THINGSBOARD_HOST_NAME</code> with your ThingsBoard host (e.g. <code>localhost:8080</code>) and <code>$ACCESS_TOKEN</code> with the device access token.</>
  )}
</Aside>

<Code code={`curl -v -X POST \\
  --header "Content-Type: application/json" \\
  --data '{"temperature": 7}' \\
  http://${tbHost(props.product)}/api/v1/$ACCESS_TOKEN/telemetry`} lang="bash" />

A new alarm is created and the email notification is sent immediately to all users of Customer A. The email subject and body are populated from the template variables.

<ImageGallery images={[
  { src: '/src/assets/images/recipes/send-email-alarm/send-email-verification-1.png', alt: 'Gmail inbox showing an unread email from ThingsBoard with subject "High temperature on Thermostat device" and a preview of the message body', caption: 'Email delivered to the inbox — subject built from ${alarmType} and ${alarmOriginatorName}' },
  { src: '/src/assets/images/recipes/send-email-alarm/send-email-verification-2.png', alt: 'Gmail email opened showing subject "High temperature on Thermostat device" and body "A High temperature has been detected on the Thermostat device: 7 °C."', caption: 'Email body: alarm type, device name, and temperature value resolved from the template variables' },
]} />

## See also

- <DocLink product={props.product} path="user-guide/notifications">Notification Center</DocLink> — full configuration reference for recipients, templates, and rules
- <DocLink product={props.product} path="recipes/create-clear-alarms">Create and Clear Alarms</DocLink> — alarm rules this recipe builds upon
- <DocLink product={props.product} path="user-guide/alarm-rules">Alarm rules</DocLink> — full alarm rule configuration reference
