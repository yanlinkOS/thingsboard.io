import DocLink from '@components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import { Steps, Aside, Code } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

export const tbHost = (product) =>
  product === Products.PAAS ? 'thingsboard.cloud' :
  product === Products.PAAS_EU ? 'eu.thingsboard.cloud' :
  '$THINGSBOARD_HOST_NAME';

Configure ThingsBoard to automatically send a Slack message when an alarm is created. This guide extends the <DocLink product={props.product} path="recipes/create-clear-alarms" bold={false} target="_blank">Create and Clear Alarms</DocLink> recipe by using the Notification Center to deliver alarm-based Slack notifications.

## Use case

Assume your refrigeration equipment has a temperature sensor that periodically publishes telemetry to ThingsBoard. The normal operating range is **2 °C** – **5 °C**. When a reading goes outside this range:

- An alarm is created by the alarm rules.
- A Slack message is sent to the responsible user.

## Prerequisites

Before proceeding:

- Complete the <DocLink product={props.product} path="recipes/create-clear-alarms" bold={false} target="_blank">Create and Clear Alarms</DocLink> recipe — the alarm rules configured there are required for this guide.
- Configure <DocLink product={props.product} path="user-guide/ui/slack-settings" bold={false} target="_blank">Slack settings</DocLink> in ThingsBoard with a valid Bot User OAuth Token.

Review the following documentation for background:

- <DocLink product={props.product} path="user-guide/notifications" bold={false} target="_blank">Notification Center</DocLink> — how recipients, templates, and rules work together.
- <DocLink product={props.product} path="user-guide/alarm-rules" bold={false} target="_blank">Alarm rules</DocLink> — how alarms are created and cleared.

## Configure Notification Center

The Notification Center delivers notifications to users. It consists of three components:

- **Recipients** — define who receives notifications.
- **Templates** — define the notification content and delivery method.
- **Rules** — define the conditions under which notifications are sent.

### Step 1. Configure notification recipients

Create a recipient that targets a specific Slack user via direct message.

<Steps>
1. Navigate to **Notification Center &#8702; Recipients**.
2. Click **Add recipients** (+) in the top-right corner.
3. Configure the recipient:
   - **Name**: `Direct message to Slack`
   - **Type**: Slack
   - **Slack channel type**: Direct message
   - **Conversation**: enter the target Slack user's name
4. Click **Add**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/recipes/send-slack-alarm/add-recipients.png',
    alt: 'Add notification recipients dialog with Type set to Slack, channel type to Direct message, and a user name entered in the Conversation field',
    caption: 'Recipient configured as a Slack direct message to a specific user.',
  },
]} />

<Aside type="note">
  To send to a Slack channel instead, select **Public channel** or **Private channel** as the channel type and enter the channel name. Make sure the bot is added to that channel.
</Aside>

### Step 2. Create notification template

Create a template that defines the Slack message body using alarm variables.

<Steps>
1. Navigate to **Notification Center &#8702; Templates**.
2. Click **Add template** (+) in the top-right corner.
3. **Setup** — configure the template:
   - **Name**: `Alarm notification to Slack`
   - **Type**: Alarm
   - **Delivery methods**: toggle **Slack** on
4. Click **Next**.
5. **Compose** — expand the **Slack** section and enter the message:
   `A ${alarmType} has been detected on the ${alarmOriginatorName} device: ${details.data} °C.`
6. Click **Add**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/recipes/send-slack-alarm/add-template-setup.png',
    alt: 'Add notification template — Setup step with Name set to Alarm notification to Slack, Type to Alarm, and Slack delivery method toggled on',
    caption: 'Setup: name, Alarm type, and Slack delivery method selected.',
  },
  {
    src: '/src/assets/images/recipes/send-slack-alarm/add-template-compose.png',
    alt: 'Add notification template — Compose step with the Slack message field filled in using alarm template variables',
    caption: 'Compose: Slack message body with alarm template variables.',
  },
]} />

Template variables used:

- `${alarmType}` — the alarm type name (e.g. *High temperature*).
- `${alarmOriginatorName}` — the name of the device that triggered the alarm.
- `${details.data}` — the reported telemetry value, populated by the alarm rule.

### Step 3. Create notification rule

Create a rule that links the alarm trigger to the template and recipient.

<Steps>
1. Navigate to **Notification Center &#8702; Rules**.
2. Click **Add rule** (+) in the top-right corner.
3. **Basic settings** — configure the rule:
   - **Rule name**: `New alarm to Slack`
   - **Trigger**: Alarm
   - **Template**: Alarm notification to Slack
   - **Escalation chain &#8702; First recipient**: Direct message to Slack
4. Click **Next**.
5. **Alarm trigger settings** — configure the filter:
   - **Alarm type list**: Any type
   - **Alarm severity list**: Any severity
   - **Notify on**: Alarm created
6. Click **Add**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/recipes/send-slack-alarm/add-rule-basic.png',
    alt: 'Add rule — Basic settings with Rule name set to New alarm to Slack, Trigger to Alarm, Template to Alarm notification to Slack, and Direct message to Slack as First recipient',
    caption: 'Basic settings: rule name, trigger, template, and recipient configured.',
  },
  {
    src: '/src/assets/images/recipes/send-slack-alarm/add-rule-trigger.png',
    alt: 'Add rule — Alarm trigger settings with Alarm type list set to Any type, Alarm severity list to Any severity, and Notify on set to Alarm created',
    caption: 'Alarm trigger settings: any alarm type and severity, notify on Alarm created.',
  },
]} />

<Aside type="note">
  Slack notifications are sent only when an alarm is **created**, not when an existing alarm is updated.
</Aside>

## Verification

Publish a temperature value that triggers the alarm (above 5 °C).  
Use the <DocLink product={props.product} path="user-guide/devices/#check-connectivity"  bold={false} target="_blank">Check Connectivity</DocLink> feature or the command below:

<Aside type="note">
  {(props.product === Products.PAAS || props.product === Products.PAAS_EU) ? (
    <>Replace <code>$ACCESS_TOKEN</code> with the device access token.</>
  ) : (
    <>Replace <code>$THINGSBOARD_HOST_NAME</code> with your ThingsBoard host (e.g. <code>localhost:8080</code>) and <code>$ACCESS_TOKEN</code> with the device access token.</>
  )}
</Aside>

<Code code={`curl -v -X POST \\
  --header "Content-Type: application/json" \\
  --data '{"temperature": 25}' \\
  http://${tbHost(props.product)}/api/v1/$ACCESS_TOKEN/telemetry`} lang="bash" />

<ImageGallery images={[
  {
    src: '/src/assets/images/recipes/send-slack-alarm/verification-curl.png',
    alt: 'Terminal showing a curl POST command sending temperature 25 to the ThingsBoard telemetry API',
    caption: 'Publishing a temperature of 25 °C — above the 5 °C threshold, this triggers the alarm.',
  },
]} />

A new alarm is created and the Slack message is sent to the configured recipient. The message body is populated from the template variables.

<ImageGallery images={[
  {
    src: '/src/assets/images/recipes/send-slack-alarm/verification-slack.png',
    alt: 'Slack direct message from the app bot showing "A High temperature has been detected on the Thermometer device: 25 °C."',
    caption: 'Slack direct message received — alarm type, device name, and temperature value resolved from the template.',
  },
]} />

## See also

- <DocLink product={props.product} path="user-guide/notifications">Notification Center</DocLink> — full configuration reference for recipients, templates, and rules
- <DocLink product={props.product} path="user-guide/ui/slack-settings">Slack settings</DocLink> — how to configure the Slack API token in ThingsBoard
- <DocLink product={props.product} path="recipes/create-clear-alarms">Create and Clear Alarms</DocLink> — alarm rules this recipe builds upon
- <DocLink product={props.product} path="user-guide/alarm-rules">Alarm rules</DocLink> — full alarm rule configuration reference
